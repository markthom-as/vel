# `0.5.2` Apple Client Handoff Pointer

Apple implementation remains out of scope for `v0.5.2`.

The authoritative handoff packet for the post-`0.5.2` Apple surface line is:

- [77-APPLE-HANDOFF.md](/home/jove/code/vel/.planning/milestones/v0.5.2-operator-surface-embodiment/77-APPLE-HANDOFF.md)

That packet updates the earlier `0.5.1` truth-only handoff with the embodied `0.5.2` surface model:

- `Now` as `Focus` / `Commitments` / `Calendar` / `Triage`
- `Threads` as bound-object-first with chronology second
- `System` as grouped sidebar/detail structural truth
- the same backend doctrine with platform-native expression rather than pixel parity
