# UI-V4-008 — Add attention-token system language and polish

Status: deferred
Priority: P2
Lane: D

## Goal

Add a coherent attention-token layer only after surface roles are stable.

## Scope

- define attention-token vocabulary
- align tokens with the IA and state hierarchy
- avoid decorative polish that lacks semantic meaning

## Acceptance criteria

- tokens reinforce the system's information hierarchy instead of adding visual noise
