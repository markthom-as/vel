---
phase: 11-agent-grounding-and-operator-relevant-data-tool-awareness
plan: 02
type: execute
wave: 1
depends_on:
  - 11-01
files_modified:
  - crates/veld/src/services/agent_grounding.rs
  - crates/veld/src/services/mod.rs
  - crates/veld/src/routes/agent_grounding.rs
  - crates/veld/src/routes/mod.rs
  - crates/veld/src/services/execution_context.rs
  - crates/veld/src/routes/execution.rs
  - crates/veld/src/app.rs
  - crates/veld/tests/agent_grounding.rs
  - docs/api/runtime.md
autonomous: true
requirements:
  - AGENT-CTX-01
  - AGENT-CTX-02
  - AGENT-TOOLS-01
  - AGENT-TOOLS-02
  - AGENT-REVIEW-01
  - AGENT-TRUST-01
must_haves:
  truths:
    - "An operator-authenticated inspect route returns the current agent grounding bundle and bounded capability summary from one backend-owned policy seam."
    - "Capability entries fail closed with explicit blocker reasons when SAFE MODE, review state, or missing grants prevent a mutation."
    - "The supervised execution export path includes the same grounding contract so repo-local agents receive real Vel state plus tool-awareness through the existing Phase 08 handoff flow."
  artifacts:
    - path: "crates/veld/src/services/agent_grounding.rs"
      provides: "Grounding assembly and capability summarization over existing persisted seams."
      contains: "build_agent_inspect"
    - path: "crates/veld/src/routes/agent_grounding.rs"
      provides: "Thin authenticated runtime inspect route."
      contains: "get_agent_inspect"
    - path: "crates/veld/src/services/execution_context.rs"
      provides: "Phase 08 artifact export widened with agent-grounding assets."
      contains: "agent-inspect.json"
    - path: "crates/veld/tests/agent_grounding.rs"
      provides: "Integration coverage for inspect payloads, blockers, and execution export wiring."
      contains: "agent_grounding"
  key_links:
    - from: "crates/veld/src/routes/agent_grounding.rs"
      to: "crates/veld/src/services/agent_grounding.rs"
      via: "thin route delegation"
      pattern: "build_agent_inspect"
    - from: "crates/veld/src/services/agent_grounding.rs"
      to: "crates/veld/src/services/now.rs"
      via: "grounding summary reuse"
      pattern: "get_now"
    - from: "crates/veld/src/services/agent_grounding.rs"
      to: "crates/veld/src/services/execution_routing.rs"
      via: "pending handoff review reuse"
      pattern: "list_execution_handoffs"
    - from: "crates/veld/src/services/execution_context.rs"
      to: "crates/veld/src/services/agent_grounding.rs"
      via: "execution export composition"
      pattern: "build_agent_inspect"
---

<objective>
Assemble the backend-owned grounding bundle, expose it through an authenticated runtime inspect route, and reuse it inside the existing supervised execution export path.

Purpose: Make real supervised agents aware of real Vel state and bounded affordances without widening chat-first behavior or inventing a second trust model.
Output: `agent_grounding` service, `/v1/agent/inspect`, execution-export grounding artifacts, integration tests, and truthful runtime docs.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-agent-grounding-and-operator-relevant-data-tool-awareness/11-CONTEXT.md
@.planning/phases/11-agent-grounding-and-operator-relevant-data-tool-awareness/11-RESEARCH.md
@.planning/phases/05-now-inbox-core-and-project-substrate/05-05-SUMMARY.md
@.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-05-SUMMARY.md
@.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-07-SUMMARY.md
@.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-04-SUMMARY.md
@crates/veld/src/services/now.rs
@crates/veld/src/services/operator_queue.rs
@crates/veld/src/services/execution_routing.rs
@crates/veld/src/services/execution_context.rs
@crates/veld/src/services/operator_settings.rs
@crates/veld/src/routes/execution.rs

<interfaces>
From crates/veld/src/services/now.rs:
```rust
pub struct NowOutput {
    pub action_items: Vec<ActionItem>,
    pub review_snapshot: ReviewSnapshot,
    pub pending_writebacks: Vec<WritebackOperationRecord>,
    pub conflicts: Vec<ConflictCaseRecord>,
    pub people: Vec<vel_core::PersonRecord>,
    pub reasons: Vec<String>,
    pub debug: NowDebugOutput,
}
```

From crates/veld/src/services/execution_routing.rs:
```rust
pub struct ExecutionHandoffRecordData {
    pub project_id: ProjectId,
    pub review_state: HandoffReviewState,
    pub handoff: ExecutionHandoff,
    pub routing: ExecutionRoutingDecisionData,
    pub decision_reason: Option<String>,
}
```

From crates/veld/src/services/execution_context.rs:
```rust
pub struct ExecutionArtifactPackData {
    pub project_id: String,
    pub project_slug: String,
    pub repo_root: String,
    pub output_dir: String,
    pub files: Vec<ExecutionArtifactFileData>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build the backend grounding service and authenticated inspect route</name>
  <files>crates/veld/src/services/agent_grounding.rs, crates/veld/src/services/mod.rs, crates/veld/src/routes/agent_grounding.rs, crates/veld/src/routes/mod.rs, crates/veld/src/app.rs, crates/veld/tests/agent_grounding.rs</files>
  <behavior>
    - Test 1: `GET /v1/agent/inspect` returns a typed pack containing `Now`, projects, people, open commitments, review obligations, and pending execution handoffs assembled from existing persisted seams.
    - Test 2: Mutation-oriented capability entries are marked unavailable with explicit blocker text when SAFE MODE is on, review state is pending, or no matching grant exists.
    - Test 3: The route stays operator-authenticated and unknown/unsupported affordances fail closed instead of returning permissive defaults.
  </behavior>
  <action>Create `services::agent_grounding` as the single backend policy seam for Phase 11. Assemble the inspect payload from existing sources only: `services::now::get_now`, storage-backed project and commitment queries, the shipped people registry, pending writebacks/conflicts, and pending execution handoffs from `services::execution_routing`. Derive operator-oriented capability groups such as read context, review queues, and bounded mutations from existing policy inputs like `runtime_writeback_enabled`, handoff review state, declared write scopes, and allowed tools. Add a thin `GET /v1/agent/inspect` route plus app wiring and integration coverage. Do not widen `/api/chat`, do not persist a second agent-state blob, and do not invent a new permission system outside the existing SAFE MODE + handoff + capability seams.</action>
  <verify>
    <automated>cargo test -p veld agent_grounding_inspect -- --nocapture</automated>
  </verify>
  <done>The runtime exposes one truthful inspect response over existing Vel state, capability blockers are explicit and fail closed, and the backend owns all summarization and policy decisions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reuse the inspect contract in supervised execution exports and document the route truthfully</name>
  <files>crates/veld/src/services/execution_context.rs, crates/veld/src/routes/execution.rs, crates/veld/tests/agent_grounding.rs, docs/api/runtime.md</files>
  <behavior>
    - Test 1: `preview_gsd_artifacts` and `export_gsd_artifacts` include a grounding artifact pair such as `agent-grounding.md` and `agent-inspect.json` under the existing bounded output directory.
    - Test 2: The exported grounding content comes from the same backend inspect service, not from a second renderer with different policy or field names.
    - Test 3: Runtime docs list the new inspect endpoint, auth class, and fail-closed capability semantics without claiming chat/tool execution behavior that is not yet shipped.
  </behavior>
  <action>Extend the Phase 08 execution export seam so approved repo-local handoffs receive the new grounding bundle and capability summary through the same bounded `.planning/vel` flow. Keep the existing output-dir safety checks intact and source the new artifact contents from `services::agent_grounding` so the runtime route and supervised execution pack cannot drift. Update `docs/api/runtime.md` to document the new `/v1/agent/inspect` surface and the extra export artifacts. Avoid introducing a chat-specific grounding path in this slice; the first agent consumer should be the already-shipped supervised execution lane.</action>
  <verify>
    <automated>cargo test -p veld execution_context -- --nocapture</automated>
  </verify>
  <done>Repo-local supervised execution exports now carry the same real Vel grounding bundle exposed by the runtime inspect route, and the runtime docs accurately describe the shipped Phase 11 backend boundary.</done>
</task>

</tasks>

<verification>
Run the focused `veld` integration tests for the inspect route and execution export path. Confirm `GET /v1/agent/inspect` is mounted under the operator-authenticated class, and confirm the execution artifact preview/export lists the new grounding files while preserving the existing repo-root safety checks.
</verification>

<success_criteria>
There is one backend-owned grounding service, one authenticated inspect route, and one supervised execution export path consuming the same contract. Agents launched through the Phase 08 workflow can see real Vel context and bounded affordances, and blocked actions explain why they are unavailable.
</success_criteria>

<output>
After completion, create `.planning/phases/11-agent-grounding-and-operator-relevant-data-tool-awareness/11-02-SUMMARY.md`
</output>
