---
phase: 11-agent-grounding-and-operator-relevant-data-tool-awareness
plan: 03
type: execute
wave: 2
depends_on:
  - 11-02
files_modified:
  - crates/vel-cli/src/client.rs
  - crates/vel-cli/src/commands/agent.rs
  - crates/vel-cli/src/commands/mod.rs
  - crates/vel-cli/src/main.rs
  - clients/web/src/types.ts
  - clients/web/src/data/agent-grounding.ts
  - clients/web/src/components/SettingsPage.tsx
  - clients/web/src/components/SettingsPage.test.tsx
  - clients/web/src/data/agent-grounding.test.ts
  - docs/user/daily-use.md
autonomous: true
requirements:
  - AGENT-TOOLS-01
  - AGENT-TOOLS-02
  - AGENT-REVIEW-01
  - AGENT-TRUST-01
must_haves:
  truths:
    - "The operator can inspect what an agent can currently see and do from both CLI and web without those surfaces re-deriving policy locally."
    - "Blocked mutations clearly name the narrow escalation path such as enabling SAFE MODE writeback or approving a pending handoff."
    - "The same inspect contract is rendered consistently across API, CLI, and web surfaces."
  artifacts:
    - path: "crates/vel-cli/src/commands/agent.rs"
      provides: "CLI inspect surface for agent grounding and capability status."
      contains: "run_inspect"
    - path: "clients/web/src/data/agent-grounding.ts"
      provides: "Typed web loader for the Phase 11 inspect route."
      contains: "loadAgentInspect"
    - path: "clients/web/src/components/SettingsPage.tsx"
      provides: "Operator-facing web rendering of grounding scope, review obligations, and capability blockers."
      contains: "Agent grounding"
    - path: "docs/user/daily-use.md"
      provides: "Operator workflow guidance for inspecting agent trust before supervised runs."
      contains: "vel agent inspect"
  key_links:
    - from: "crates/vel-cli/src/client.rs"
      to: "/v1/agent/inspect"
      via: "authenticated CLI client method"
      pattern: "agent/inspect"
    - from: "clients/web/src/data/agent-grounding.ts"
      to: "/v1/agent/inspect"
      via: "typed query loader"
      pattern: "loadAgentInspect"
    - from: "clients/web/src/components/SettingsPage.tsx"
      to: "clients/web/src/data/agent-grounding.ts"
      via: "backend-owned inspect rendering"
      pattern: "loadAgentInspect"
---

<objective>
Expose the shared Phase 11 inspect contract through thin CLI and web trust surfaces so the operator can see what agents can currently see and do, plus why.

Purpose: Close the trust loop across API, CLI, and web without moving policy or review logic out of Rust.
Output: `vel agent inspect`, typed web loader/decoder coverage, Settings rendering, and updated daily-use guidance.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-agent-grounding-and-operator-relevant-data-tool-awareness/11-CONTEXT.md
@.planning/phases/11-agent-grounding-and-operator-relevant-data-tool-awareness/11-RESEARCH.md
@.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-07-SUMMARY.md
@.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-04-SUMMARY.md
@crates/vel-cli/src/commands/review.rs
@crates/vel-cli/src/commands/exec.rs
@clients/web/src/data/operator.ts
@clients/web/src/components/SettingsPage.tsx
@docs/user/daily-use.md

<interfaces>
From crates/vel-cli/src/commands/exec.rs:
```rust
pub async fn run_review_handoffs(
    client: &ApiClient,
    project_id: Option<&str>,
    state: Option<&str>,
    json: bool,
) -> anyhow::Result<()>
```

From clients/web/src/data/operator.ts:
```ts
export interface OperatorReviewStatusData {
  writeback_enabled: boolean;
  pending_writebacks: WritebackOperationData[];
  open_conflicts: ConflictCaseData[];
  people_needing_review: PersonRecordData[];
  pending_execution_handoffs: ExecutionHandoffRecordData[];
}
```

From clients/web/src/components/SettingsPage.tsx:
```tsx
<h4 className="text-sm font-medium text-zinc-100">Execution handoff review</h4>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add a CLI inspect command over the shared Phase 11 route</name>
  <files>crates/vel-cli/src/client.rs, crates/vel-cli/src/commands/agent.rs, crates/vel-cli/src/commands/mod.rs, crates/vel-cli/src/main.rs, docs/user/daily-use.md</files>
  <behavior>
    - Test 1: `vel agent inspect` prints grounded sections, review obligations, and capability groups in operator language rather than raw internal tool names.
    - Test 2: `vel agent inspect --json` returns the raw API response for automation and verification.
    - Test 3: Blocked mutation entries display the narrow escalation path, such as enabling writeback or approving a handoff, without the CLI inventing policy locally.
  </behavior>
  <action>Add a thin CLI client method for `/v1/agent/inspect`, a new `vel agent inspect` command module, module export wiring in `crates/vel-cli/src/commands/mod.rs`, and command wiring in `main.rs`. Reuse the existing `vel review` and `vel exec` formatting style where it helps readability, but render the new inspect payload as a distinct trust surface: grounded context scope, pending review obligations, read/review/mutate capability groups, blockers, and escalation hints. Update `docs/user/daily-use.md` to show how operators inspect grounding before supervised execution. Keep the CLI as a renderer only; do not duplicate backend policy computations in Rust client code.</action>
  <verify>
    <automated>cargo test -p vel-cli agent_inspect -- --nocapture</automated>
  </verify>
  <done>The CLI exposes one truthful inspect command, JSON output stays automation-friendly, and the user docs tell operators how to inspect agent trust before launch/review work.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Render the same inspect contract in the web Settings trust surface</name>
  <files>clients/web/src/types.ts, clients/web/src/data/agent-grounding.ts, clients/web/src/components/SettingsPage.tsx, clients/web/src/components/SettingsPage.test.tsx, clients/web/src/data/agent-grounding.test.ts</files>
  <behavior>
    - Test 1: The web decoder accepts the new `AgentInspectData` shape and rejects malformed blocker/capability payloads.
    - Test 2: Settings renders grounding scope, review obligations, and capability groups from the runtime payload without computing availability client-side.
    - Test 3: The UI displays blocker reasons and escalation guidance when mutations are unavailable, and continues to respect existing review queue/safe-mode messaging.
  </behavior>
  <action>Add a dedicated web loader for `/v1/agent/inspect`, extend the shared decoder/types surface, and render the result inside `SettingsPage.tsx` near the existing runtime/review cards. The page should show what the agent can see, what it can review, what it could request to mutate, and why a blocked entry is blocked. Reuse the existing Settings trust posture language and pending-queue context instead of creating a new shell or navigation lane. Do not add frontend-owned policy branches for SAFE MODE, handoff review, or grant checks; render the backend response exactly.</action>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/types.test.ts src/data/agent-grounding.test.ts src/components/SettingsPage.test.tsx</automated>
  </verify>
  <done>The web shell shows the same backend-owned inspect contract the CLI sees, the decoder/tests prove the wire contract, and the operator can understand current grounding/capability state from Settings without policy drift.</done>
</task>

</tasks>

<verification>
Run the focused `vel-cli` and web tests for the new inspect surfaces. Manually confirm that CLI and Settings both show the same counts/blockers for SAFE MODE, pending writebacks/conflicts, and execution review when pointed at the same daemon state.
</verification>

<success_criteria>
Phase 11 trust visibility is shared across API, CLI, and web: one inspect route, one CLI command, one Settings rendering, and consistent blocker language telling the operator what an agent can currently see and do plus the narrow escalation path required to proceed.
</success_criteria>

<output>
After completion, create `.planning/phases/11-agent-grounding-and-operator-relevant-data-tool-awareness/11-03-SUMMARY.md`
</output>
