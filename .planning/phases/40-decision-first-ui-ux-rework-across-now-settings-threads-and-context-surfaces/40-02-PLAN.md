# 40-02 Plan

## Slice

Rework `Now` into the decision-first execution surface defined by the phase contract while repairing the highest-value broken primary interactions found in discovery.

## Work

- implement the dominant action strip / subordinate time context / decision-first task stack structure
- remove duplicate and dead-state `Now` presentation
- keep ask/capture/talk visible and primary
- repair broken/inert `Now` affordances on web and mobile where they block the intended action path
- preserve inline-first action and decomposition behavior

## Verification

- focused `NowView` tests
- targeted mobile/iPhone shell checks where `Now` behavior is represented
- review against discovery findings and phase context contract

## Exit

`Now` clearly answers what the operator should do now and the primary surface actions actually work.
