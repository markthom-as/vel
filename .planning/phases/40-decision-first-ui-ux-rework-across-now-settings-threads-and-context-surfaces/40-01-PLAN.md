# 40-01 Plan

## Slice

Define the strict `v0.2` MVP boundary and operator loop so later work stops re-deciding scope.

## Work

- document the fixed loop `overview -> commitments -> reflow -> threads -> review`
- define what each loop step must let the operator do
- write explicit in-scope and out-of-scope rules for adjacent work that must be deferred
- define the thread-escalation rule for genuinely multi-step work
- publish milestone-level acceptance criteria for the loop and non-goals that block drift

## Verification

- Phase 40 docs state one unambiguous MVP loop
- in-scope and out-of-scope rules match the approved milestone decisions
- thread escalation is defined as a bounded exception, not a parallel product model

## Exit

The MVP boundary is explicit enough that downstream phases can be rejected when they widen scope.
