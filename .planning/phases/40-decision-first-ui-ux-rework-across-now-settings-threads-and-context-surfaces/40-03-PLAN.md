# 40-03 Plan

## Slice

Rework `Settings`, `Threads`, and the context/right-panel model so each surface has one clear job and less internal-state leakage.

## Work

- restructure `Settings` around system profile, planning model, routines, and recovery model
- reframe `Threads` as open loops needing deeper thought with structured state
- reshape the context/right panel into `State / Why / Debug`
- repair broken/inert interaction paths discovered in these surfaces
- keep inline editing and continuity flows clear without creating shell-owned product logic

## Verification

- focused `SettingsPage` and `ThreadView` tests
- shell/right-panel tests as needed
- affordance review for action vs link behavior, inline editing, and disclosure

## Exit

`Settings` configures, `Threads` thinks, and the context panel explains without acting like a raw debug dump.
