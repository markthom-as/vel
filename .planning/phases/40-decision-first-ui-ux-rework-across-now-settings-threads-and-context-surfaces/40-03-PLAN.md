# 40-03 Plan

## Slice

Refine the MVP architecture, authority boundaries, and shell-consumption rules around the canonical contracts.

## Work

- update durable architecture docs to show where MVP authority lives
- define service, DTO, and client-shell responsibilities for each loop step
- document how overview, reflow, and threads connect without leaking shell-owned policy
- define review/apply boundaries, provenance expectations, and failure handling across surfaces
- repair stale authority pointers if docs still imply the superseded UI-only Phase 40

## Verification

- durable docs in `docs/` reflect the `v0.2` MVP architecture
- shell roles are clearly thin-client roles, not alternate policy owners
- reflow and thread continuation boundaries are coherent across backend and transport layers

## Exit

Architecture docs tell downstream implementers where behavior belongs and where it must not migrate.
