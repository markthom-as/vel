# 35-02 Plan

## Slice

Implement the backend-owned current-day/day-boundary seam and use it for `GET /v1/now` schedule selection.

## Work

- add the narrowest typed Rust seam for current-day derivation and sleep-relative boundary handling
- route `Now` current/next event selection and same-day task/commitment selection through that seam
- keep routine blocks out of next-event truth while preserving them for current-day shaping

## Verification

- targeted Rust tests for current-day derivation
- targeted Rust `GET /v1/now` tests for current/next event behavior across midnight

## Exit

Backend `Now` truth no longer depends on implicit midnight behavior.
