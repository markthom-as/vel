---
phase: 20-daily-use-usability-closure-and-unified-operator-entry
plan: 02
type: execute
wave: 2
depends_on:
  - 20-01
files_modified:
  - crates/vel-api-types/src/lib.rs
  - crates/veld/src/routes/chat.rs
  - crates/veld/src/services/chat/messages.rs
  - crates/veld/src/services/chat/assistant.rs
  - crates/veld/src/services/chat/tools.rs
  - crates/veld/tests/chat_assistant_entry.rs
  - docs/api/chat.md
autonomous: true
requirements:
  - ASSIST-01
  - ASSIST-02
  - ENTRY-01
  - THREADS-02
must_haves:
  truths:
    - "One authenticated backend-owned assistant entry seam can accept operator text from outside a preselected thread."
    - "Assistant entry persists through the existing conversation/message model and returns typed routing outcomes for `Inbox`, `Threads`, or inline/no-thread handling."
    - "Missing or unavailable model routing fails safely without making remote providers mandatory."
  artifacts:
    - path: "crates/vel-api-types/src/lib.rs"
      provides: "Typed request/response contract for the unified assistant entry seam."
      contains: "Assistant"
    - path: "crates/veld/src/services/chat/messages.rs"
      provides: "Conversation/message orchestration reused by the new entry route."
      contains: "create_message_response"
    - path: "crates/veld/tests/chat_assistant_entry.rs"
      provides: "Execution-backed contract tests for entry creation, continuity, and safe fallback."
      contains: "assistant"
  key_links:
    - from: "crates/veld/src/routes/chat.rs"
      to: "crates/veld/src/services/chat/messages.rs"
      via: "new assistant entry route delegates to existing conversation/message seam"
      pattern: "create_message"
    - from: "crates/veld/src/services/chat/assistant.rs"
      to: "crates/veld/src/services/chat/tools.rs"
      via: "grounded reply generation with bounded read-only tools"
      pattern: "chat_tool_specs"
---

<objective>
Add one backend-owned grounded assistant entry contract that launches from default daily surfaces while preserving conversation/thread continuity.

Purpose: Make assistant entry a real product seam instead of forcing clients to start inside `Threads` or invent their own capture/chat routing.
Output: typed `/api` assistant-entry contract, Rust service/route implementation over the existing chat stack, focused backend tests, and updated API authority.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/20-daily-use-usability-closure-and-unified-operator-entry/20-CONTEXT.md
@.planning/phases/20-daily-use-usability-closure-and-unified-operator-entry/20-RESEARCH.md
@.planning/phases/20-daily-use-usability-closure-and-unified-operator-entry/20-VALIDATION.md
@docs/api/chat.md
@crates/veld/src/services/chat/messages.rs
@crates/veld/src/services/chat/assistant.rs
@crates/veld/src/services/chat/tools.rs

<interfaces>
From `crates/veld/src/services/chat/messages.rs`:

```rust
pub(crate) struct ChatMessageCreateResult {
    pub user_message: ChatMessage,
    pub assistant_message: Option<ChatMessage>,
    pub assistant_error: Option<String>,
}

pub(crate) async fn create_message_response(
    state: &AppState,
    conversation_id: &str,
    payload: &ChatMessageCreateInput,
) -> Result<ChatMessageCreateResult, AppError>
```

From `crates/veld/src/services/chat/assistant.rs`:

```rust
pub(crate) async fn generate_assistant_reply(
    state: &AppState,
    conversation_id: &str,
    profile_id: &str,
    fallback_profile_id: Option<&str>,
    router: &Router,
) -> Result<Option<ChatMessage>, AppError>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define and test the unified assistant-entry transport contract</name>
  <files>crates/vel-api-types/src/lib.rs, crates/veld/tests/chat_assistant_entry.rs, docs/api/chat.md</files>
  <behavior>
    - Test 1: Posting assistant entry text without a conversation id returns a typed routing outcome describing whether the result belongs in `Inbox`, `Threads`, or inline/no-thread handling.
    - Test 2: Posting with an existing conversation id appends to that conversation instead of inventing a parallel archive.
    - Test 3: Quick capture-like input can land in `Inbox` without forcing a thread, while interactive follow-up returns continuity data for `Threads`.
    - Test 4: When no chat model is configured or the primary route fails, the response still persists the user message and returns a safe assistant error/fallback state plus a typed route outcome.
  </behavior>
  <action>Define the new authenticated `/api` assistant-entry request/response DTOs first and add focused backend tests around routing outcome, capture-versus-thread continuity, reuse, and safe no-model behavior. The contract should accept operator text plus optional conversation/thread continuity hints and return typed route metadata the web can consume directly: `inbox`, `threads`, or inline/no-thread handling, plus any conversation/message/thread-route data needed for continuity. Keep routing backend-owned and grounded in existing conversation/action ownership; do not add mutation affordances, write-capable tools, Apple-specific fields, or daily-loop assistant semantics.</action>
  <verify>
    <automated>cargo test -p veld chat_assistant_entry -- --nocapture</automated>
  </verify>
  <done>The new assistant-entry contract is explicit, tested, documents backend-owned route outcomes, and is safe for web wiring without local routing heuristics.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the backend-owned assistant-entry route over the current grounded chat seam</name>
  <files>crates/veld/src/routes/chat.rs, crates/veld/src/services/chat/messages.rs, crates/veld/src/services/chat/assistant.rs, crates/veld/src/services/chat/tools.rs, docs/api/chat.md</files>
  <action>Implement the route and service orchestration so assistant entry persists through existing conversations/messages, delegates reply generation to the current grounded Rust service, and returns typed route outcomes the shell can trust: quick capture to `Inbox`, interactive continuity to `Threads`, or inline/no-thread handling when appropriate. Preserve optional configured fallback routing and fail closed when the model path is unavailable. If tool or metadata updates are needed, keep them read-only and bounded to current Vel state. Do not move routing logic into the web shell and do not create a second assistant archive or thread taxonomy.</action>
  <verify>
    <automated>cargo test -p veld chat_assistant_entry -- --nocapture && cargo test -p veld chat::tools -- --nocapture</automated>
  </verify>
  <done>The backend exposes one grounded assistant-entry seam that clients can call from `Now` or `Threads`, with route outcomes for `Inbox` versus `Threads` remaining Rust-owned alongside continuity, tool use, and safe fallback.</done>
</task>

</tasks>

<verification>
Exercise the new route through the focused backend tests first. Then inspect `docs/api/chat.md` to confirm the contract is described as a current Phase 20 capability and still states that tools are read-only and remote routing is optional.
</verification>

<success_criteria>
Clients have one backend-owned assistant entry they can call without preselecting a thread, route outcomes for `Inbox` versus `Threads` are typed and backend-owned, and all durable continuity still lives in the existing conversation/thread model with remote routing remaining optional instead of required.
</success_criteria>

<output>
After completion, create `.planning/phases/20-daily-use-usability-closure-and-unified-operator-entry/20-02-SUMMARY.md`
</output>
