---
phase: 20-daily-use-usability-closure-and-unified-operator-entry
plan: 03
type: execute
wave: 3
depends_on:
  - 20-02
files_modified:
  - clients/web/src/types.ts
  - clients/web/src/data/chat.ts
  - clients/web/src/components/NowView.tsx
  - clients/web/src/components/NowView.test.tsx
  - clients/web/src/components/ThreadView.tsx
  - clients/web/src/components/ThreadView.test.tsx
  - clients/web/src/components/MessageComposer.tsx
  - clients/web/src/components/MessageComposer.test.tsx
autonomous: true
requirements:
  - USABLE-01
  - NOW-UX-01
  - ENTRY-01
  - ASSIST-01
  - THREADS-02
must_haves:
  truths:
    - "The default web daily-use path can start assistant entry from `Now` without forcing the operator into `Threads` first."
    - "The same grounded entry behavior is reused in `Threads`, so asking, capture-like input, and continuity do not fork into separate client rules."
    - "When assistant entry creates or continues a conversation, the shell lands in canonical `Threads` continuity using backend-returned data."
  artifacts:
    - path: "clients/web/src/types.ts"
      provides: "Typed decoder coverage for the assistant-entry contract."
      contains: "decode"
    - path: "clients/web/src/data/chat.ts"
      provides: "Thin web helper for the new backend-owned assistant entry seam."
      contains: "assistant"
    - path: "clients/web/src/components/NowView.tsx"
      provides: "Primary assistant-first entry embodiment on the urgent-first default surface."
      contains: "Now"
  key_links:
    - from: "clients/web/src/components/NowView.tsx"
      to: "clients/web/src/data/chat.ts"
      via: "assistant entry submission from the default daily-use surface"
      pattern: "assistant"
    - from: "clients/web/src/components/MessageComposer.tsx"
      to: "clients/web/src/data/chat.ts"
      via: "shared send path between thread and now entry"
      pattern: "send"
---

<objective>
Embody the new assistant-entry seam in the web shell so `Now` becomes the practical default operator entry while `Threads` continues to own continuity.

Purpose: Deliver the actual daily-use product value of the backend seam without letting the shell invent its own capture/chat policy.
Output: typed web helper/decoder coverage, `Now` assistant-entry UI, and shared thread/composer wiring over the same backend contract.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/20-daily-use-usability-closure-and-unified-operator-entry/20-CONTEXT.md
@.planning/phases/20-daily-use-usability-closure-and-unified-operator-entry/20-RESEARCH.md
@.planning/phases/20-daily-use-usability-closure-and-unified-operator-entry/20-VALIDATION.md
@clients/web/src/types.ts
@clients/web/src/data/chat.ts
@clients/web/src/components/NowView.tsx
@clients/web/src/components/ThreadView.tsx
@clients/web/src/components/MessageComposer.tsx

<interfaces>
From `clients/web/src/components/MessageComposer.tsx`:

```ts
interface MessageComposerProps {
  conversationId: string;
  onOptimisticSend?: (text: string) => string | undefined;
  onSent: (clientMessageId: string | undefined, userMessage: MessageData, assistantMessage?: MessageData | null) => void;
  onSendFailed?: (clientMessageId: string | undefined) => void;
}
```

From `clients/web/src/components/NowView.tsx`:

```ts
interface NowViewProps {
  onOpenSettings?: (target: { tab: 'integrations'; integrationId: SettingsIntegrationTarget }) => void;
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed web assistant-entry helpers and regression coverage</name>
  <files>clients/web/src/types.ts, clients/web/src/data/chat.ts, clients/web/src/components/NowView.test.tsx, clients/web/src/components/ThreadView.test.tsx, clients/web/src/components/MessageComposer.test.tsx</files>
  <behavior>
    - Test 1: `Now` can submit assistant entry and receive typed conversation continuity data.
    - Test 2: Thread composer uses the same helper/decoder path instead of keeping a separate send contract.
    - Test 3: When the backend returns an assistant error or no-model state, the UI preserves the user message and shows the bounded error without breaking continuity.
  </behavior>
  <action>Extend the web types and `data/chat.ts` with a thin helper for the new assistant-entry contract, then add focused tests describing `Now` launch, thread reuse, and safe fallback rendering. Reuse existing query/cache patterns; do not create local policy for capture-versus-chat decisions, conversation selection, or model fallback.</action>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/NowView.test.tsx src/components/ThreadView.test.tsx src/components/MessageComposer.test.tsx src/types.test.ts</automated>
  </verify>
  <done>The web has one typed assistant-entry contract with regression coverage for default launch, thread continuity, and safe fallback behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Make `Now` the primary assistant-first entry while keeping `Threads` as continuity</name>
  <files>clients/web/src/components/NowView.tsx, clients/web/src/components/ThreadView.tsx, clients/web/src/components/MessageComposer.tsx</files>
  <action>Add one clear assistant-first entry affordance to `Now` that submits through the shared assistant-entry helper and only routes into `Threads` when continuity is actually needed. Reuse the same helper path inside `ThreadView`/`MessageComposer` so typed and thread-local entry do not drift. Keep `Now` compact and urgent-first, preserve summary-first routing into Inbox/Threads, and avoid adding a shell-owned mode switch matrix; if an override is needed, keep it lightweight and subordinate to automatic routing.</action>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/NowView.test.tsx src/components/ThreadView.test.tsx src/components/MessageComposer.test.tsx</automated>
  </verify>
  <done>`Now` becomes the practical first entry surface for the assistant, while `Threads` remains the continuity/search destination rather than the mandatory starting screen.</done>
</task>

</tasks>

<verification>
Run the focused web tests and confirm the new `Now` flow still respects the Phase 17 compact-surface posture. Check that thread navigation is always based on backend continuity data instead of inferred client rules.
</verification>

<success_criteria>
The web shell exposes one assistant-first entry path centered on `Now`, reuses it in `Threads`, and preserves backend-owned continuity without collapsing `Now` into a full thread surface.
</success_criteria>

<output>
After completion, create `.planning/phases/20-daily-use-usability-closure-and-unified-operator-entry/20-03-SUMMARY.md`
</output>
