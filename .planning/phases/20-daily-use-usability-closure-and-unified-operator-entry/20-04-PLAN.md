---
phase: 20-daily-use-usability-closure-and-unified-operator-entry
plan: 04
type: execute
wave: 4
depends_on:
  - 20-03
files_modified:
  - clients/web/src/data/chat.ts
  - clients/web/src/components/InboxView.tsx
  - clients/web/src/components/InboxView.test.tsx
  - clients/web/src/components/ThreadView.tsx
  - clients/web/src/components/ThreadView.test.tsx
  - clients/web/src/components/SettingsPage.tsx
  - clients/web/src/components/SettingsPage.test.tsx
  - docs/api/chat.md
  - docs/user/daily-use.md
autonomous: true
requirements:
  - USABLE-02
  - INBOX-UX-01
  - THREADS-UX-01
  - SETTINGS-UX-01
must_haves:
  truths:
    - "Inbox remains a decision-first triage queue and does not become the assistant history surface."
    - "Threads are easier to use for continuity, filtering, and assistant escalation without inheriting Inbox pressure."
    - "Setup and trust guidance for assistant readiness stays summary-first and does not force advanced runtime detail into the default loop."
  artifacts:
    - path: "clients/web/src/components/InboxView.tsx"
      provides: "Tighter triage-first affordances and routing wording for Inbox."
      contains: "Triage"
    - path: "clients/web/src/components/ThreadView.tsx"
      provides: "Improved continuity/filter/search posture for Threads."
      contains: "Continuity"
    - path: "clients/web/src/components/SettingsPage.tsx"
      provides: "Summary-first assistant/setup readiness guidance."
      contains: "Advanced operator setup"
  key_links:
    - from: "clients/web/src/components/InboxView.tsx"
      to: "clients/web/src/components/ThreadView.tsx"
      via: "canonical open-thread escalation without collapsing surface roles"
      pattern: "Open thread"
    - from: "clients/web/src/components/SettingsPage.tsx"
      to: "docs/user/daily-use.md"
      via: "summary-first setup and trust guidance"
      pattern: "setup"
---

<objective>
Close the remaining daily-use friction across Inbox, Threads, and Settings without breaking the assistant-first surface taxonomy.

Purpose: Finish Phase 20 by making the default loop more usable in practice after the assistant-entry seam is live.
Output: sharper Inbox triage behavior, stronger Threads continuity/filtering affordances, summary-first assistant readiness/setup framing, and aligned docs/tests.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/20-daily-use-usability-closure-and-unified-operator-entry/20-CONTEXT.md
@.planning/phases/20-daily-use-usability-closure-and-unified-operator-entry/20-RESEARCH.md
@.planning/phases/20-daily-use-usability-closure-and-unified-operator-entry/20-VALIDATION.md
@docs/product/now-inbox-threads-boundaries.md
@docs/product/operator-surface-taxonomy.md
@docs/api/chat.md
@docs/user/daily-use.md
@clients/web/src/components/InboxView.tsx
@clients/web/src/components/ThreadView.tsx
@clients/web/src/components/SettingsPage.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten Inbox triage behavior and regression coverage</name>
  <files>clients/web/src/components/InboxView.tsx, clients/web/src/components/InboxView.test.tsx</files>
  <behavior>
    - Test 1: Inbox empty and populated states reinforce explicit triage, not thread history.
    - Test 2: `Open thread` remains the escalation path from an inbox item into continuity.
    - Test 3: Recent handled items or helper copy do not make Inbox read like a second archive.
  </behavior>
  <action>Refine Inbox wording and rendering so explicit triage remains dominant after assistant entry moves to `Now`. If recent-history or helper affordances are added, keep them visually demoted and clearly subordinate to active decisions. Do not surface longer conversation history, search, or assistant archive controls here.</action>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/InboxView.test.tsx</automated>
  </verify>
  <done>`Inbox` reads and behaves as the decision queue even after the assistant-first entry flow is added elsewhere.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Improve Threads continuity and filtering without inheriting triage pressure</name>
  <files>clients/web/src/data/chat.ts, clients/web/src/components/ThreadView.tsx, clients/web/src/components/ThreadView.test.tsx</files>
  <behavior>
    - Test 1: Threads expose clearer continuity/filter/search affordances over existing conversation and typed thread-route data.
    - Test 2: Assistant-entry landings from `Now` or `Inbox` resolve into the right conversation or filtered thread view.
    - Test 3: Threads still default to archive/continuity posture rather than active triage pressure.
  </behavior>
  <action>Use the existing conversation list and typed thread-route/filter seams to improve continuity/search ergonomics in `Threads`. Favor backend-returned conversation ids and typed filters over client guesses. If a lightweight local text filter is added for the visible thread list, keep it scoped to already-loaded persisted conversations and do not turn it into a second inbox or a new semantic retrieval engine.</action>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/ThreadView.test.tsx</automated>
  </verify>
  <done>`Threads` is easier to search, filter, and continue from assistant entry, while still feeling like continuity/history rather than active triage.</done>
</task>

<task type="auto">
  <name>Task 3: Reduce assistant/setup friction through summary-first Settings and doc alignment</name>
  <files>clients/web/src/components/SettingsPage.tsx, clients/web/src/components/SettingsPage.test.tsx, docs/api/chat.md, docs/user/daily-use.md</files>
  <action>Update Settings and nearby docs so assistant readiness, local-first fallback, and optional remote configuration are summarized clearly without dropping the operator into runtime detail first. Keep advanced/runtime controls one step away, explain that configured remote routing is optional, and point operators back to `Now`, `Inbox`, and `Threads` for normal daily use. Do not widen this slice into provider onboarding expansion or Apple voice parity.</action>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/SettingsPage.test.tsx && rg -n "optional|assistant|Now|Inbox|Threads|setup|runtime" docs/api/chat.md docs/user/daily-use.md clients/web/src/components/SettingsPage.tsx</automated>
  </verify>
  <done>The product communicates assistant readiness and setup clearly enough for repeated daily use without making advanced runtime detail the default operator path.</done>
</task>

</tasks>

<verification>
Run the focused Inbox, ThreadView, and Settings tests, then read the updated daily-use and chat docs together. The result should teach one default loop: enter through `Now`, triage in `Inbox`, continue/search in `Threads`, and visit `Settings` only for summary-guided setup or trust inspection.
</verification>

<success_criteria>
Phase 20 closes with a usable default loop: assistant-first entry from `Now`, explicit Inbox triage, continuity/search in Threads, and summary-first setup guidance that keeps advanced runtime complexity out of the main path.
</success_criteria>

<output>
After completion, create `.planning/phases/20-daily-use-usability-closure-and-unified-operator-entry/20-04-SUMMARY.md`
</output>
