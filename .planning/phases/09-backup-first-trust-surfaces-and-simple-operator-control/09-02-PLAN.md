---
phase: 09-backup-first-trust-surfaces-and-simple-operator-control
plan: 02
type: execute
wave: 2
depends_on:
  - 09-01
files_modified:
  - migrations/0043_phase9_backup_history.sql
  - crates/vel-storage/src/db.rs
  - crates/vel-storage/src/lib.rs
  - crates/vel-storage/src/repositories/mod.rs
  - crates/vel-storage/src/repositories/backup_runs_repo.rs
  - crates/veld/src/services/mod.rs
  - crates/veld/src/services/backup.rs
  - crates/veld/src/routes/backup.rs
  - crates/veld/src/routes/mod.rs
  - crates/veld/src/app.rs
  - crates/veld/tests/backup_flow.rs
autonomous: true
requirements:
  - BACKUP-01
  - CTRL-01
must_haves:
  truths:
    - "The runtime can create a real local backup pack instead of relying on manual shell-copy guidance."
    - "Backup creation uses a consistent SQLite snapshot strategy and records what was included and omitted."
    - "CLI and API backup flows stay bounded, inspectable, and explicit about destination and verification metadata."
  artifacts:
    - path: "crates/veld/src/services/backup.rs"
      provides: "typed backup snapshot and manifest orchestration"
      contains: "create_backup"
    - path: "crates/vel-storage/src/repositories/backup_runs_repo.rs"
      provides: "durable backup history and last-success status persistence"
      min_lines: 120
    - path: "crates/veld/tests/backup_flow.rs"
      provides: "focused integration coverage for snapshot, omission, and verification behavior"
      contains: "backup"
  key_links:
    - from: "crates/veld/src/services/backup.rs"
      to: "crates/vel-storage/src/repositories/backup_runs_repo.rs"
      via: "persisted backup history and manifest metadata"
      pattern: "backup_runs|last_success"
    - from: "crates/veld/src/services/backup.rs"
      to: "config/schemas/backup-manifest.schema.json"
      via: "manifest rendering against published contract"
      pattern: "BackupManifest"
---

<objective>
Turn backup from manual guidance into a real snapshot-backed trust workflow.

Purpose: create the smallest real backup subsystem that materially reduces loss anxiety: consistent DB snapshot, bounded artifact/config coverage, persisted history, and authenticated runtime inspection.
Output: backup history persistence, backup service, authenticated route family, and focused integration tests.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-backup-first-trust-surfaces-and-simple-operator-control/09-RESEARCH.md
@.planning/phases/09-backup-first-trust-surfaces-and-simple-operator-control/09-VALIDATION.md
@.planning/phases/09-backup-first-trust-surfaces-and-simple-operator-control/09-01-PLAN.md
@crates/veld/src/services/execution_context.rs
@crates/veld/src/services/doctor.rs
@crates/vel-storage/src/repositories/artifacts_repo.rs
@crates/vel-cli/src/commands/backup.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing integration coverage for backup snapshot creation, omission rules, and route-level verification</name>
  <files>crates/veld/tests/backup_flow.rs</files>
  <behavior>
    - Test 1: creating a backup pack writes a manifest, database snapshot, and bounded artifact/config entries under the requested output root.
    - Test 2: secret-bearing settings are omitted from the pack and listed explicitly in the manifest omission section.
    - Test 3: backup route verification fails closed when the manifest is missing, malformed, or points outside the backup root.
  </behavior>
  <action>Add focused failing coverage for the Phase 09 backend trust path before implementation. Use temporary directories and a seeded SQLite/artifact fixture, and assert against manifest contents rather than raw tarball bytes or shell-copy behavior. Keep restore and CLI UX out of scope here: tests should prove backend backup creation, inspection, and verification only.</action>
  <verify>
    <automated>cargo test -p veld backup_flow -- --nocapture</automated>
  </verify>
  <done>Phase 09 has red tests for real backend backup creation, omission rules, and inspect/verify behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement backup history persistence, snapshot service, and authenticated route family</name>
  <files>migrations/0043_phase9_backup_history.sql, crates/vel-storage/src/db.rs, crates/vel-storage/src/lib.rs, crates/vel-storage/src/repositories/mod.rs, crates/vel-storage/src/repositories/backup_runs_repo.rs, crates/veld/src/services/mod.rs, crates/veld/src/services/backup.rs, crates/veld/src/routes/backup.rs, crates/veld/src/routes/mod.rs, crates/veld/src/app.rs</files>
  <action>Create a typed backup service that snapshots the live SQLite database using a SQLite-safe snapshot strategy, copies only bounded artifact/config files into a plain backup root, renders the published manifest, and persists backup history plus last-success metadata. Expose authenticated create/inspect/verify routes for later CLI and UI consumers, but do not replace the current `vel backup` CLI yet. Keep writes bounded to an operator-selected root or default backup root, fail closed on out-of-scope paths, and keep secret migration out of this slice.</action>
  <verify>
    <automated>cargo test -p veld backup_flow -- --nocapture</automated>
  </verify>
  <done>The runtime can create and inspect a real, consistent, typed backup pack through authenticated backend seams without relying on manual shell copy instructions.</done>
</task>

</tasks>

<verification>
Run focused backend integration tests only. Phase 09 should prove backup creation and trust metadata first; CLI replacement and restore behavior remain explicitly out of scope for this slice.
</verification>

<success_criteria>
Vel can create a local backup pack with a consistent database snapshot, explicit omissions, and inspectable manifest metadata through backend-owned backup seams.
</success_criteria>

<output>
After completion, create `.planning/phases/09-backup-first-trust-surfaces-and-simple-operator-control/09-02-SUMMARY.md`
</output>
