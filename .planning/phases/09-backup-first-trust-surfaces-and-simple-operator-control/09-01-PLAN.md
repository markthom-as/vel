---
phase: 09-backup-first-trust-surfaces-and-simple-operator-control
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - .planning/REQUIREMENTS.md
  - config/README.md
  - config/contracts-manifest.json
  - config/schemas/backup-manifest.schema.json
  - config/examples/backup-manifest.example.json
  - docs/cognitive-agent-architecture/architecture/backup-and-operator-trust-contracts.md
  - docs/user/backup-and-restore.md
  - crates/vel-api-types/src/lib.rs
autonomous: true
requirements:
  - BACKUP-01
must_haves:
  truths:
    - "Phase 09 starts from one typed backup/export manifest and status vocabulary instead of ad hoc JSON or guidance-only copy text."
    - "The operator gets one documented backup contract that explains what a backup pack contains, what it omits, and how it should be inspected."
    - "Backup trust is framed around inspectable contents, explicit omissions, and bounded operator controls rather than restore automation."
  artifacts:
    - path: ".planning/REQUIREMENTS.md"
      provides: "ratified Phase 09 requirement definitions for BACKUP-01, BACKUP-02, CTRL-01, and CTRL-02"
      contains: "BACKUP-01"
    - path: "config/schemas/backup-manifest.schema.json"
      provides: "machine-readable backup manifest and verification contract"
      contains: "\"BackupManifest\""
    - path: "docs/user/backup-and-restore.md"
      provides: "operator-facing authority for backup creation, inspection, and manual recovery guidance"
      contains: "manual restore"
    - path: "crates/vel-api-types/src/lib.rs"
      provides: "transport DTOs for backup manifest, verification, and status surfaces"
      contains: "pub struct BackupManifestData"
  key_links:
    - from: "config/contracts-manifest.json"
      to: "config/schemas/backup-manifest.schema.json"
      via: "contracts manifest registration"
      pattern: "backup-manifest.schema.json"
    - from: "crates/vel-api-types/src/lib.rs"
      to: "config/schemas/backup-manifest.schema.json"
      via: "typed transport matches the published manifest contract"
      pattern: "BackupManifestData|BackupStatusData"
---

<objective>
Freeze the Phase 09 contract and requirement boundary before backup/runtime implementation widens.

Purpose: close the missing requirement definitions, publish the typed backup/export manifest and status vocabulary, and make the trust-first scope explicit before any snapshot or UI work begins.
Output: ratified Phase 09 requirements, backup manifest schema/example assets, matching transport/status DTO seams, and owner/operator docs.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-backup-first-trust-surfaces-and-simple-operator-control/09-RESEARCH.md
@.planning/phases/09-backup-first-trust-surfaces-and-simple-operator-control/09-VALIDATION.md
@config/README.md
@config/contracts-manifest.json
@docs/api/runtime.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Ratify Phase 09 requirement IDs and publish the backup manifest contract assets</name>
  <files>.planning/REQUIREMENTS.md, config/README.md, config/contracts-manifest.json, config/schemas/backup-manifest.schema.json, config/examples/backup-manifest.example.json, docs/cognitive-agent-architecture/architecture/backup-and-operator-trust-contracts.md, docs/user/backup-and-restore.md</files>
  <action>Add explicit `BACKUP-01`, `BACKUP-02`, `CTRL-01`, and `CTRL-02` definitions to `.planning/REQUIREMENTS.md` and make them match the roadmap/research stance: backup confidence first, restore automation intentionally smaller. Repair the canonical requirements ledger while doing so: add the new Phase 09 requirement section(s), extend traceability rows, update coverage totals, and refresh the `Last updated` metadata so `.planning/REQUIREMENTS.md` stays internally consistent after ratification. Create `config/schemas/backup-manifest.schema.json` and `config/examples/backup-manifest.example.json` covering backup ID, created timestamp, output root, database snapshot path, artifact coverage, config coverage, explicit omissions, secret omission flags, and verification/checksum summary. Register the new assets in `config/contracts-manifest.json`, document ownership in `config/README.md`, add an owner doc at `docs/cognitive-agent-architecture/architecture/backup-and-operator-trust-contracts.md`, and create `docs/user/backup-and-restore.md` that makes manual restore and inspection the shipped operator posture instead of automated recovery.</action>
  <verify>
    <automated>node scripts/verify-repo-truth.mjs && rg -n "BACKUP-01|BACKUP-02|CTRL-01|CTRL-02|v1 requirements: 32 total|Mapped to phases: 32|Last updated" .planning/REQUIREMENTS.md && node -e "const fs=require('fs'); JSON.parse(fs.readFileSync('config/schemas/backup-manifest.schema.json','utf8')); JSON.parse(fs.readFileSync('config/examples/backup-manifest.example.json','utf8'));" && rg -n "backup-manifest\\.schema\\.json|backup-manifest\\.example\\.json" config/contracts-manifest.json config/README.md && test -f docs/cognitive-agent-architecture/architecture/backup-and-operator-trust-contracts.md && rg -n "manual restore|backup inspect|backup verify" docs/user/backup-and-restore.md</automated>
  </verify>
  <done>Phase 09 has ratified requirement IDs and a published machine-readable backup manifest contract before service or UI work begins.</done>
</task>

<task type="auto">
  <name>Task 2: Add typed backup manifest and status DTO seams for later route, CLI, and UI work</name>
  <files>crates/vel-api-types/src/lib.rs</files>
  <action>Add transport DTOs for `BackupManifestData`, `BackupVerificationData`, `BackupCoverageData`, and a compact `BackupStatusData` that later routes, CLI commands, and UI surfaces can reuse. Do not touch settings, doctor, or operator control seams in this Wave 0 plan; Phase 09 should acknowledge the existing settings-baseline drift by isolating those touches to later implementation slices.</action>
  <verify>
    <automated>cargo test -p vel-api-types backup -- --nocapture</automated>
  </verify>
  <done>The repository has a typed transport vocabulary for backup manifest and trust status that later plans can implement against without widening into opaque JSON.</done>
</task>

</tasks>

<verification>
Start Phase 09 with true Wave 0 contract and requirement closure only. The focused verification must prove the new requirement IDs exist and the contract assets are discoverable before snapshot, settings, or UI work starts.
</verification>

<success_criteria>
Phase 09 begins from one documented, machine-readable, typed backup/export contract and explicit requirement coverage, with restore kept manual-first and secondary to backup confidence.
</success_criteria>

<output>
After completion, create `.planning/phases/09-backup-first-trust-surfaces-and-simple-operator-control/09-01-SUMMARY.md`
</output>
