---
phase: 09-backup-first-trust-surfaces-and-simple-operator-control
plan: 03
type: execute
wave: 3
depends_on:
  - 09-01
  - 09-02
files_modified:
  - crates/vel-api-types/src/lib.rs
  - crates/veld/src/services/doctor.rs
  - crates/veld/src/routes/doctor.rs
  - crates/veld/src/routes/diagnostics.rs
  - crates/veld/src/services/chat/settings.rs
  - crates/veld/src/services/operator_settings.rs
  - crates/vel-cli/src/commands/doctor.rs
  - crates/vel-cli/src/commands/review.rs
  - crates/vel-cli/src/client.rs
autonomous: true
requirements:
  - BACKUP-02
  - CTRL-02
must_haves:
  truths:
    - "The operator can see when the last successful backup happened, what it covered, and why it may be unsafe to trust."
    - "Backup trust status appears in existing backend and CLI control surfaces rather than a new generic admin plane."
  artifacts:
    - path: "crates/veld/src/services/doctor.rs"
      provides: "backup freshness and trust checks alongside existing runtime diagnostics"
      contains: "backup"
    - path: "crates/vel-cli/src/commands/doctor.rs"
      provides: "CLI projection of backup trust checks"
      contains: "backup"
  key_links:
    - from: "crates/veld/src/services/doctor.rs"
      to: "crates/vel-storage/src/repositories/backup_runs_repo.rs"
      via: "latest successful backup status and warnings"
      pattern: "backup"
    - from: "crates/veld/src/services/chat/settings.rs"
      to: "crates/veld/src/services/operator_settings.rs"
      via: "effective settings payload includes backup trust state"
      pattern: "backup"
---

<objective>
Project backup confidence into the existing operator trust surfaces.

Purpose: make backup/export confidence visible from the same backend and CLI places the operator already checks for runtime trust, without creating a broad control plane or pushing policy into the client.
Output: typed backup status in doctor/settings/diagnostics, CLI trust output, and backend/runtime payload closure for later web/doc projection.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-backup-first-trust-surfaces-and-simple-operator-control/09-RESEARCH.md
@.planning/phases/09-backup-first-trust-surfaces-and-simple-operator-control/09-VALIDATION.md
@.planning/phases/09-backup-first-trust-surfaces-and-simple-operator-control/09-02-PLAN.md
@crates/veld/src/services/doctor.rs
@crates/veld/src/routes/doctor.rs
@crates/veld/src/routes/diagnostics.rs
@crates/veld/src/services/chat/settings.rs
@crates/vel-cli/src/commands/doctor.rs
<interfaces>
From crates/veld/src/routes/doctor.rs:
```rust
pub async fn doctor(
    State(state): State<AppState>,
) -> Result<Json<ApiResponse<DoctorData>>, AppError>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend backup trust status through doctor, settings, diagnostics, and CLI review surfaces</name>
  <files>crates/vel-api-types/src/lib.rs, crates/veld/src/services/doctor.rs, crates/veld/src/routes/doctor.rs, crates/veld/src/routes/diagnostics.rs, crates/veld/src/services/chat/settings.rs, crates/veld/src/services/operator_settings.rs, crates/vel-cli/src/commands/doctor.rs, crates/vel-cli/src/commands/review.rs, crates/vel-cli/src/client.rs</files>
  <behavior>
    - Test 1: doctor reports `ok`, `warn`, or `fail` backup trust state based on last successful backup freshness and verification state.
    - Test 2: settings payload returns typed backup destination and last-backup summary without exposing secret settings.
    - Test 3: CLI doctor or review output prints backup freshness, omissions, and destination path from the same backend-owned status shape.
  </behavior>
  <action>Extend the existing doctor/settings/diagnostics seams with typed backup trust state, including last successful backup timestamp, destination root, covered paths summary, omission counts, and warnings for missing or stale backups. Keep the service authoritative and backend-owned; the CLI should project the same typed status instead of computing its own backup heuristics. If a `warn` state is needed for stale or missing verification, add it deliberately at the transport boundary rather than encoding it as a stringly-typed message-only convention.</action>
  <verify>
    <automated>cargo test -p veld doctor -- --nocapture && cargo test -p vel-cli doctor -- --nocapture</automated>
  </verify>
  <done>Existing CLI/API trust surfaces show backup freshness and safety state from one backend-owned typed source.</done>
</task>

<task type="auto">
  <name>Task 2: Add typed backup trust output to CLI review surfaces without creating a second policy source</name>
  <files>crates/vel-cli/src/commands/doctor.rs, crates/vel-cli/src/commands/review.rs, crates/vel-cli/src/client.rs</files>
  <action>Update the CLI transport calls and projections so backup trust status prints from the expanded backend doctor/settings/runtime payload. The CLI should show last backup status, freshness, destination, coverage/omission summary, and a direct hint that the dedicated create/inspect/verify command flow is completed in the next slice when trust is degraded. Keep the CLI as a renderer of backend-owned truth rather than a second source of backup heuristics.</action>
  <verify>
    <automated>cargo test -p veld doctor -- --nocapture && cargo test -p vel-cli doctor -- --nocapture</automated>
  </verify>
  <done>The CLI trust surfaces expose the same backup trust state and inspection workflow as the backend without inventing local heuristics.</done>
</task>

</tasks>

<verification>
Run focused backend and CLI trust-surface checks. This slice is complete when backup status is visible and consistent across the backend/operator surfaces that already own runtime trust.
</verification>

<success_criteria>
The operator can inspect backup freshness and omission/safety state from doctor, settings, diagnostics, and CLI without learning a new control plane or guessing whether backups are trustworthy.
</success_criteria>

<output>
After completion, create `.planning/phases/09-backup-first-trust-surfaces-and-simple-operator-control/09-03-SUMMARY.md`
</output>
