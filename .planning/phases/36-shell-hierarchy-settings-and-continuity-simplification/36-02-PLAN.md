# 36-02 Plan

## Slice

Restructure `Settings` into clearer summary-first categories with less clutter and less misleading sync/connect posture.

## Work

- group `Settings` into stronger categories
- demote top-level prose and misplaced recovery/runtime detail
- simplify integration management affordances
- keep docs/help globally reachable rather than buried

## Verification

- focused `SettingsPage` tests
- targeted wording/category checks

## Exit

`Settings` reads as a management surface instead of a dense mixed-purpose document.
