---
phase: 10-daily-loop-morning-overview-and-standup-commitment-engine
plan: 03
type: execute
wave: 2
depends_on:
  - 10-01
  - 10-02
files_modified:
  - crates/veld/src/services/daily_loop.rs
  - crates/veld/src/services/daily_loop_inputs.rs
  - crates/veld/tests/daily_loop_standup.rs
  - crates/vel-cli/src/client.rs
  - crates/vel-cli/src/commands/daily_loop.rs
  - crates/vel-cli/src/commands/morning.rs
  - crates/vel-cli/src/commands/mod.rs
  - crates/vel-cli/src/main.rs
autonomous: true
requirements:
  - STANDUP-01
  - STANDUP-02
  - STANDUP-03
  - SESSION-01
must_haves:
  truths:
    - "Standup can resume from Morning Overview or start directly from the CLI text shell."
    - "Standup compresses work into must/should/stretch and ends with one to three commitments, explicit deferrals, confirmed calendar state, and focus-block proposals."
    - "Standup enforces the three-commitment cap, reprompts once when no commitments are defined, and stays interruptible/resumable."
  artifacts:
    - path: "crates/veld/src/services/daily_loop.rs"
      provides: "standup orchestration, commitment extraction, and final outcome persistence"
      contains: "start_standup"
    - path: "crates/veld/tests/daily_loop_standup.rs"
      provides: "integration proof for direct start, reduction pressure, and commitment writes"
      contains: "standup"
    - path: "crates/vel-cli/src/commands/daily_loop.rs"
      provides: "text-capable morning/standup shell over the backend session-turn API"
      contains: "run_standup"
  key_links:
    - from: "crates/veld/src/services/daily_loop.rs"
      to: "crates/veld/src/routes/commitments.rs"
      via: "standup finalization reuses existing commitment persistence instead of inventing a parallel store"
      pattern: "commitment"
    - from: "crates/vel-cli/src/client.rs"
      to: "crates/veld/src/routes/daily_loop.rs"
      via: "CLI start/resume/turn calls the same backend authority as web and Apple"
      pattern: "daily-loop"
---

<objective>
Implement the bounded standup engine and make the CLI the strongest text fallback for the new daily loop.

Purpose: turn morning signals plus live runtime state into a short standup that forces reduction to one to three daily commitments while preserving explicit deferrals, focus protection, and resumability.
Output: standup backend behavior, commitment-writing closure, and CLI morning/standup commands backed by the shared daily-loop API.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-daily-loop-morning-overview-and-standup-commitment-engine/10-CONTEXT.md
@.planning/phases/10-daily-loop-morning-overview-and-standup-commitment-engine/10-RESEARCH.md
@.planning/phases/10-daily-loop-morning-overview-and-standup-commitment-engine/10-02-PLAN.md
@crates/veld/src/routes/commitments.rs
@crates/vel-cli/src/client.rs
@crates/vel-cli/src/commands/morning.rs
@crates/vel-cli/src/main.rs
@docs/user/daily-use.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing integration coverage for standup compression, cap enforcement, and final outcome writes</name>
  <files>crates/veld/tests/daily_loop_standup.rs</files>
  <behavior>
    - Test 1: standup can start directly or resume from an existing morning session and carries forward any morning intent signals.
    - Test 2: when more than three commitment candidates are proposed, standup forces reduction and persists only one to three final commitments.
    - Test 3: if no commitments are defined, standup reprompts once, keeps the total question budget at three or fewer prompts including that reprompt, and then exits cleanly with explicit deferrals/calendar confirmation/focus proposals plus terminal session state.
  </behavior>
  <action>Add focused red integration coverage for standup turns and finalization. Seed calendar events, Todoist commitments, and morning intent signals so the tests prove the must/should/stretch and reduction rules. Assert on persisted commitment rows plus typed session outcome fields for deferrals, calendar confirmation, and focus blocks, and include one silence/skip path so the capped prompt budget remains enforceable under passive use too.</action>
  <verify>
    <automated>cargo test -p veld daily_loop_standup -- --nocapture</automated>
  </verify>
  <done>Standup has failing proof for direct start, compression, cap enforcement, reprompt behavior, and typed outcome persistence.</done>
</task>

<task type="auto">
  <name>Task 2: Implement standup orchestration and commitment-writing closure in the backend</name>
  <files>crates/veld/src/services/daily_loop.rs, crates/veld/src/services/daily_loop_inputs.rs</files>
  <action>Extend the daily-loop service to support standup starts and turns on the same session protocol. Implement the exact locked flow: calendar reconciliation, must/should/stretch compression, commitment extraction, constraint shaping, and opportunistic metadata enrichment. Finalization must write one to three commitments through the existing commitment seam with a clear Phase 10 source/kind, persist explicit deferred tasks plus calendar/focus outputs in the session outcome, enforce the three-question ceiling even when the no-commitment reprompt is used, and remain skip-safe/resumable at every turn. Keep heuristics simple and inspectable. Do not create a generalized planner or new first-class focus-block tables in this phase.</action>
  <verify>
    <automated>cargo test -p veld daily_loop_standup -- --nocapture</automated>
  </verify>
  <done>The backend can drive standup end-to-end, enforce the commitment cap, and persist the final daily outcome through typed backend-owned records.</done>
</task>

<task type="auto">
  <name>Task 3: Replace the old CLI morning brief path with text-backed daily-loop commands</name>
  <files>crates/vel-cli/src/client.rs, crates/vel-cli/src/commands/daily_loop.rs, crates/vel-cli/src/commands/morning.rs, crates/vel-cli/src/commands/mod.rs, crates/vel-cli/src/main.rs</files>
  <action>Add daily-loop API methods to the CLI client, keep `vel morning` as the Phase 10 manual-start text entry point, and add a new `vel standup` command for direct-start or resume. Structure the text shell as one-turn-per-invocation (`start`, `resume`, `reply`, `skip`, `cancel`, `--json`) so the backend owns state and question flow. Remove CLI dependence on the legacy `/v1/context/morning` route for the daily loop, but keep the output calm, brief, and explainable rather than conversationally heavy.</action>
  <verify>
    <automated>cargo test -p vel-cli daily_loop -- --nocapture</automated>
  </verify>
  <done>The CLI becomes the strongest text-capable Phase 10 shell, and both morning and standup go through the shared backend daily-loop authority.</done>
</task>

</tasks>

<verification>
Standup is only closed when automated tests prove the reduction rules and the CLI verifies that text-mode start/resume/skip paths use the new backend session-turn API instead of the old morning context brief.
</verification>

<success_criteria>
Operators can finish a bounded Phase 10 standup through the backend and CLI with one to three durable commitments, explicit deferrals, and resumable state.
</success_criteria>

<output>
After completion, create `.planning/phases/10-daily-loop-morning-overview-and-standup-commitment-engine/10-03-SUMMARY.md`
</output>
