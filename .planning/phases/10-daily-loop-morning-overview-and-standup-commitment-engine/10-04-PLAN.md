---
phase: 10-daily-loop-morning-overview-and-standup-commitment-engine
plan: 04
type: execute
wave: 3
depends_on:
  - 10-02
  - 10-03
files_modified:
  - clients/web/src/types.ts
  - clients/web/src/types.test.ts
  - clients/web/src/data/context.ts
  - clients/web/src/components/NowView.tsx
  - clients/web/src/components/NowView.test.tsx
autonomous: true
requirements:
  - SESSION-01
  - MORNING-01
  - STANDUP-01
must_haves:
  truths:
    - "The web Now surface can start or resume the daily loop without owning daily-loop policy."
    - "Web rendering shows backend prompts, partial state, and final outcomes instead of recomputing friction, buckets, or commitments locally."
    - "Session completion refreshes Now and commitment state so the operator sees the new daily commitments immediately."
  artifacts:
    - path: "clients/web/src/types.ts"
      provides: "typed daily-loop DTO decoders for the web boundary"
      contains: "DailyLoopSession"
    - path: "clients/web/src/data/context.ts"
      provides: "web loaders and mutations for active/start/turn daily-loop API calls"
      contains: "dailyLoop"
    - path: "clients/web/src/components/NowView.tsx"
      provides: "thin Now-shell rendering for daily-loop start, resume, prompt, and outcome state"
      contains: "Start morning"
  key_links:
    - from: "clients/web/src/data/context.ts"
      to: "crates/veld/src/routes/daily_loop.rs"
      via: "typed start/active/turn API calls"
      pattern: "daily-loop"
    - from: "clients/web/src/components/NowView.tsx"
      to: "clients/web/src/data/context.ts"
      via: "Now shell reads backend session state and mutation helpers"
      pattern: "load.*daily|start.*daily|turn.*daily"
---

<objective>
Expose the Phase 10 daily loop through the existing web Now shell without moving orchestration into React.

Purpose: make the web surface a practical start/resume view for Morning Overview and Standup while preserving backend ownership of ranking, reduction, and durable session state.
Output: typed web decoders/data helpers and a compact Now-surface panel for start/resume/prompt/outcome rendering.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-daily-loop-morning-overview-and-standup-commitment-engine/10-CONTEXT.md
@.planning/phases/10-daily-loop-morning-overview-and-standup-commitment-engine/10-02-PLAN.md
@.planning/phases/10-daily-loop-morning-overview-and-standup-commitment-engine/10-03-PLAN.md
@clients/web/src/components/NowView.tsx
@clients/web/src/data/context.ts
@clients/web/src/types.ts
@.planning/phases/05-now-inbox-core-and-project-substrate/05-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed daily-loop web decoders and data helpers</name>
  <files>clients/web/src/types.ts, clients/web/src/types.test.ts, clients/web/src/data/context.ts</files>
  <action>Extend the web transport boundary with explicit decoders for daily-loop session records, prompts, morning signals, standup outcomes, and turn/start requests. Add `context.ts` helpers for active-session lookup, session start, and session turn submission, using the same cache invalidation discipline already used for `Now` and sync/bootstrap. Do not cast unknown JSON or derive missing fields in the browser.</action>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/types.test.ts</automated>
  </verify>
  <done>The web layer can call and decode the Phase 10 daily-loop routes with explicit typed contracts.</done>
</task>

<task type="auto">
  <name>Task 2: Add a compact daily-loop panel to the web Now surface</name>
  <files>clients/web/src/components/NowView.tsx, clients/web/src/components/NowView.test.tsx</files>
  <action>Update `NowView` to surface a compact Phase 10 panel that can start morning, resume an active morning or standup session, submit a brief response, skip safely, and render the final standup outcome. Keep the component as a thin shell: it should display backend summary/friction/questions/outcome data and trigger backend mutations only. Do not compute local friction ranking, must/should/stretch buckets, or commitment selection in React. On completion, invalidate `Now` and commitments so the updated action stack and new daily commitments appear immediately.</action>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/NowView.test.tsx src/types.test.ts</automated>
  </verify>
  <done>The web Now view can start or resume the daily loop and render backend-owned state without introducing client policy.</done>
</task>

</tasks>

<verification>
The web slice is complete when the typed decoder tests and `NowView` tests prove the browser is only a transport/render shell over the backend daily-loop API.
</verification>

<success_criteria>
The operator can use the existing Now page to start or resume Morning Overview and Standup, and the UI reflects backend state immediately after completion.
</success_criteria>

<output>
After completion, create `.planning/phases/10-daily-loop-morning-overview-and-standup-commitment-engine/10-04-SUMMARY.md`
</output>
