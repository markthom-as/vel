---
phase: 10-daily-loop-morning-overview-and-standup-commitment-engine
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - migrations/0045_phase10_daily_sessions.sql
  - crates/vel-core/src/daily_loop.rs
  - crates/vel-core/src/lib.rs
  - crates/vel-api-types/src/lib.rs
  - crates/vel-storage/src/repositories/daily_sessions_repo.rs
  - crates/vel-storage/src/repositories/mod.rs
  - crates/vel-storage/src/lib.rs
  - crates/vel-storage/src/db.rs
autonomous: true
requirements:
  - SESSION-01
  - MORNING-01
must_haves:
  truths:
    - "Phase 10 starts from typed daily-loop session contracts instead of extending `current_context` or client-local state."
    - "Morning and standup sessions share one durable backend session record that can be resumed later from any thin shell."
    - "Manual start is explicit now, while the contract remains future-compatible with automatic start metadata."
  artifacts:
    - path: "crates/vel-core/src/daily_loop.rs"
      provides: "domain types for daily-loop phases, prompts, signals, and outcomes"
      contains: "DailyLoopSession"
    - path: "crates/vel-api-types/src/lib.rs"
      provides: "transport DTOs for start, active-session lookup, turn submission, and final outcome rendering"
      contains: "DailyLoopSessionData"
    - path: "crates/vel-storage/src/repositories/daily_sessions_repo.rs"
      provides: "typed persistence seam for active and completed daily sessions"
      contains: "daily_sessions"
    - path: "migrations/0045_phase10_daily_sessions.sql"
      provides: "SQLite schema for durable resumable daily-loop state"
      contains: "daily_sessions"
  key_links:
    - from: "crates/vel-api-types/src/lib.rs"
      to: "crates/vel-core/src/daily_loop.rs"
      via: "transport DTOs mirror the backend-owned daily-loop contract"
      pattern: "DailyLoop(Start|Turn|Outcome|Session)"
    - from: "crates/vel-storage/src/repositories/daily_sessions_repo.rs"
      to: "crates/vel-core/src/daily_loop.rs"
      via: "JSON serialization happens at the storage edge over typed structs"
      pattern: "DailyLoop(Session|Outcome|Prompt)"
---

<objective>
Publish the typed Phase 10 daily-loop contract and durable persistence seam before morning or standup behavior widens.

Purpose: keep policy, resumability, and inspectability in Rust backend layers while preventing this phase from deepening `current_context` or pushing state into web/Apple shells.
Output: domain/session types, transport DTOs, and a durable `daily_sessions` repository/migration that later plans can implement against.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/10-daily-loop-morning-overview-and-standup-commitment-engine/10-CONTEXT.md
@.planning/phases/10-daily-loop-morning-overview-and-standup-commitment-engine/10-RESEARCH.md
@crates/veld/src/routes/context.rs
@crates/veld/src/services/context_generation.rs
@crates/veld/src/services/apple_voice.rs
@crates/veld/src/services/now.rs
@crates/vel-storage/src/repositories/mod.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Publish typed daily-loop domain and transport contracts</name>
  <files>crates/vel-core/src/daily_loop.rs, crates/vel-core/src/lib.rs, crates/vel-api-types/src/lib.rs</files>
  <behavior>
    - Test 1: daily-loop DTOs round-trip morning and standup session phases, statuses, and prompt/outcome payloads without collapsing them into untyped JSON.
    - Test 2: morning intent signals are distinct from final commitment outcomes, so Morning Overview cannot accidentally persist commitments before Standup.
    - Test 3: session start metadata carries initiation source and surface so manual start now can coexist with a future automatic-start path.
  </behavior>
  <action>Create a dedicated `vel-core::daily_loop` module exporting the exact backend-owned Phase 10 vocabulary: session ID/newtypes, `DailyLoopPhase`, `DailyLoopStatus`, start source, shell surface, question budget, morning intent signals, standup buckets, deferred task records, focus-block proposals, and final outcome structs. Mirror those contracts in `vel-api-types` with explicit start/active/turn/outcome DTOs. Keep the contract calm and narrow: do not add reflection, analytics, or generic workflow-engine abstractions. Preserve the locked decisions in the types themselves: question budget max 3 per sub-flow, commitment cap max 3, morning carries signals only, and standup owns commitments.</action>
  <verify>
    <automated>cargo test -p vel-api-types daily_loop -- --nocapture</automated>
  </verify>
  <done>Phase 10 has explicit shared types for daily-loop state and transport, with morning signals and standup commitments intentionally separated.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add the durable daily-session migration and repository seam</name>
  <files>migrations/0045_phase10_daily_sessions.sql, crates/vel-storage/src/repositories/daily_sessions_repo.rs, crates/vel-storage/src/repositories/mod.rs, crates/vel-storage/src/lib.rs, crates/vel-storage/src/db.rs</files>
  <behavior>
    - Test 1: a daily session can be inserted, updated, and reloaded with typed snapshot/state/outcome payloads intact.
    - Test 2: active-session lookup returns the resumable session for a given date and phase without reading `current_context`.
    - Test 3: completion/cancellation writes terminal status and timestamps so abandoned sessions remain inspectable.
  </behavior>
  <action>Add `0045_phase10_daily_sessions.sql` with one durable `daily_sessions` table plus the smallest useful indexes for resumability, including active-by-date lookup. Implement `daily_sessions_repo.rs` and expose it through the storage facade with typed methods for create, get, get_active_for_date, update_state, and complete/cancel. Serialize typed structs only at the repository boundary; do not push raw JSON strings into services. Keep this slice persistence-only: no routes, no shell logic, and no `current_context` backfill.</action>
  <verify>
    <automated>cargo test -p vel-storage daily_sessions -- --nocapture</automated>
  </verify>
  <done>The repository can persist and resume a typed daily-loop session without abusing current-context or client-local state.</done>
</task>

</tasks>

<verification>
Wave 0 is complete only when the Phase 10 session contract exists in core, API, and storage with automated proof that resumable state survives round-trip persistence.
</verification>

<success_criteria>
Executors can build morning and standup behavior against one typed durable session seam instead of inventing parallel state in routes, web state, or Apple clients.
</success_criteria>

<output>
After completion, create `.planning/phases/10-daily-loop-morning-overview-and-standup-commitment-engine/10-01-SUMMARY.md`
</output>
