---
phase: 10-daily-loop-morning-overview-and-standup-commitment-engine
plan: 05
type: execute
wave: 3
depends_on:
  - 10-02
  - 10-03
files_modified:
  - crates/veld/src/services/apple_voice.rs
  - crates/veld/src/routes/apple.rs
  - clients/apple/VelAPI/Package.swift
  - clients/apple/VelAPI/Sources/VelAPI/Models.swift
  - clients/apple/VelAPI/Sources/VelAPI/VelClient.swift
  - clients/apple/VelAPI/Sources/VelAPI/OfflineStore.swift
  - clients/apple/VelAPI/Tests/VelAPITests/DailyLoopTests.swift
  - clients/apple/Apps/VeliOS/ContentView.swift
  - clients/apple/README.md
  - docs/api/runtime.md
  - docs/user/daily-use.md
autonomous: false
requirements:
  - VOICE-01
  - MORNING-01
  - STANDUP-01
must_haves:
  truths:
    - "Apple voice can start or resume the same backend-owned morning/standup session-turn flow used by text shells."
    - "Offline Apple behavior remains a cache/provenance shell and does not synthesize new daily-loop policy locally."
    - "Docs point operators to the Phase 10 daily-loop authority instead of the legacy morning brief when they want the bounded morning/standup loop."
  artifacts:
    - path: "crates/veld/src/services/apple_voice.rs"
      provides: "transcript-first Apple voice routing into the shared daily-loop authority"
      contains: "MorningBriefing"
    - path: "clients/apple/VelAPI/Sources/VelAPI/Models.swift"
      provides: "typed Apple transport models for daily-loop start, active, and turn responses"
      contains: "DailyLoop"
    - path: "clients/apple/Apps/VeliOS/ContentView.swift"
      provides: "voice-first iPhone shell over the backend daily-loop session API"
      contains: "morning"
    - path: "docs/api/runtime.md"
      provides: "runtime API authority for the new daily-loop routes"
      contains: "/v1/daily-loop"
    - path: "docs/user/daily-use.md"
      provides: "operator-facing repeated-use guidance for Phase 10 morning and standup commands"
      contains: "vel standup"
  key_links:
    - from: "crates/veld/src/services/apple_voice.rs"
      to: "crates/veld/src/services/daily_loop.rs"
      via: "Apple morning/standup intents delegate to the shared backend session engine after transcript capture"
      pattern: "DailyLoop|MorningBriefing|Standup"
    - from: "clients/apple/VelAPI/Sources/VelAPI/VelClient.swift"
      to: "crates/veld/src/routes/daily_loop.rs"
      via: "shared authenticated start/active/turn API methods"
      pattern: "daily-loop"
    - from: "clients/apple/Apps/VeliOS/ContentView.swift"
      to: "clients/apple/VelAPI/Sources/VelAPI/VelClient.swift"
      via: "voice shell reuses the backend daily-loop transport rather than local synthesis"
      pattern: "dailyLoop|resume"
---

<objective>
Connect the existing transcript-first Apple backend seam, the Apple client, and the owner docs to the new daily-loop authority without making Apple the source of truth.

Purpose: satisfy the voice-first requirement by extending the shipped Phase 07 Apple voice boundary so transcript-backed Apple intents enter the same backend-owned daily-loop session-turn API used by CLI and web.
Output: backend Apple-voice routing updates, typed Apple transport, iPhone voice-shell wiring, and runtime/user docs aligned to the new daily-loop routes and commands.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/10-daily-loop-morning-overview-and-standup-commitment-engine/10-CONTEXT.md
@.planning/phases/10-daily-loop-morning-overview-and-standup-commitment-engine/10-02-PLAN.md
@.planning/phases/10-daily-loop-morning-overview-and-standup-commitment-engine/10-03-PLAN.md
@crates/veld/src/routes/apple.rs
@crates/veld/src/services/apple_voice.rs
@clients/apple/README.md
@clients/apple/VelAPI/Package.swift
@clients/apple/VelAPI/Sources/VelAPI/Models.swift
@clients/apple/VelAPI/Sources/VelAPI/VelClient.swift
@clients/apple/Apps/VeliOS/ContentView.swift
@.planning/phases/07-apple-action-loops-and-behavioral-signal-ingestion/07-02-SUMMARY.md
@.planning/phases/07-apple-action-loops-and-behavioral-signal-ingestion/07-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add typed Apple daily-loop transport coverage in the shared VelAPI package</name>
  <files>clients/apple/VelAPI/Package.swift, clients/apple/VelAPI/Sources/VelAPI/Models.swift, clients/apple/VelAPI/Sources/VelAPI/VelClient.swift, clients/apple/VelAPI/Tests/VelAPITests/DailyLoopTests.swift</files>
  <behavior>
    - Test 1: Apple daily-loop request/response models decode and encode morning and standup session payloads without collapsing prompt or outcome fields.
    - Test 2: the shared client exposes authenticated start, active-session, and turn methods for the backend daily-loop route family.
  </behavior>
  <action>Add shared Swift models and client methods for the Phase 10 daily-loop API in `VelAPI`, declare the `DailyLoopTests` target in `clients/apple/VelAPI/Package.swift`, and keep package-level tests runnable on this Linux host. Reuse the existing operator-token/auth-header seam; do not add Apple-only policy flags or parallel daily-loop endpoints.</action>
  <verify>
    <automated>swift test --package-path clients/apple/VelAPI --filter DailyLoop</automated>
  </verify>
  <done>The shared Apple transport layer can speak the backend daily-loop API with automated package-level proof.</done>
</task>

<task type="auto">
  <name>Task 2: Extend the backend Apple voice seam, then wire the iPhone shell and docs to the daily-loop authority</name>
  <files>crates/veld/src/services/apple_voice.rs, crates/veld/src/routes/apple.rs, clients/apple/VelAPI/Sources/VelAPI/OfflineStore.swift, clients/apple/Apps/VeliOS/ContentView.swift, clients/apple/README.md, docs/api/runtime.md, docs/user/daily-use.md</files>
  <action>Update the existing transcript-first Apple voice backend so Morning Overview and Standup resume intents delegate to the shared daily-loop service after transcript capture instead of falling back to the generic explain path. Keep the route/auth shape aligned with the shipped Apple voice endpoint, and keep offline behavior bounded to cached active-session rendering plus provenance capture; Apple must not invent new commitments, friction callouts, or reduction logic locally. Update the Apple README, runtime API docs, and daily-use guide so operators see `vel morning`, `vel standup`, and `/v1/daily-loop/*` as the Phase 10 authority while the older `/v1/context/morning` endpoint remains a legacy context brief.</action>
  <verify>
    <automated>cargo test -p veld apple_voice -- --nocapture && make check-apple-swift && rg -n "/v1/daily-loop|vel standup|legacy context brief|resume" clients/apple/README.md docs/api/runtime.md docs/user/daily-use.md clients/apple/Apps/VeliOS/ContentView.swift clients/apple/VelAPI/Sources/VelAPI/OfflineStore.swift crates/veld/src/services/apple_voice.rs</automated>
  </verify>
  <done>The transcript-first Apple voice path and the owner docs point to the same backend daily-loop authority, and offline Apple behavior remains a thin cache/provenance shell.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify Apple voice start and resume behavior on a real simulator or device</name>
  <files>none</files>
  <action>Pause after the automated Apple transport and docs work lands, then confirm the actual SwiftUI shell behavior on macOS/Xcode because the Linux-host package checks do not compile or execute the app targets.</action>
  <what-built>Voice-first iPhone morning/standup start and resume over the backend daily-loop API</what-built>
  <how-to-verify>
    1. On a macOS host with Xcode, run the daemon with the Phase 10 backend enabled and launch the `VeliOS` app in a simulator or device.
    2. Use the Voice tab to request a morning briefing. Confirm the reply is the bounded morning session output, not the generic explain-why summary.
    3. Answer or skip through the morning prompts, then resume into standup and confirm the backend enforces the one-to-three commitment cap.
    4. Disconnect the daemon briefly and confirm the app only shows cached session state / provenance capture behavior instead of generating a new local standup.
  </how-to-verify>
  <verify>Human verification on simulator/device only; resume after the four-step check above.</verify>
  <done>Apple voice start/resume has been confirmed on a real simulator or device, or concrete defects are reported back for correction.</done>
  <resume-signal>Type "approved" or describe the Apple voice issues that still need correction.</resume-signal>
</task>

</tasks>

<verification>
Phase 10 voice closure requires both automated VelAPI coverage and one real iPhone simulator/device check, because Linux-host package builds do not compile the full SwiftUI app targets.
</verification>

<success_criteria>
Voice-first entry uses the same backend daily-loop authority as CLI/web, and the docs clearly distinguish that new loop from the older context brief surfaces.
</success_criteria>

<output>
After completion, create `.planning/phases/10-daily-loop-morning-overview-and-standup-commitment-engine/10-05-SUMMARY.md`
</output>
