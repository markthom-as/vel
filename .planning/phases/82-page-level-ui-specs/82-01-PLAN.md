# Phase 82 — Plan 01

## Goal

Produce concrete page-level UI specs and in-repo interactive prototypes for `Now`, `Threads`, and `System` that validate shell law, disclosure maps, and the bounded surface doctrine.

## Planned Work

1. Write page-specific spec documents for:
   - `Now`
   - `Threads`
   - `System`
2. Ensure each page spec applies the shared law from Phases 78–81:
   - bounded `Now`
   - object/context-first `Threads`
   - hybrid read-first/operational `System`
   - row-first component posture
   - docked action bar and persistent shell zones
3. Create HTML/CSS interactive prototypes in repo covering:
   - `Now` normal
   - `Now` degraded
   - `Threads` normal
   - `Threads` focused block
   - `System` integration issue
   - `System` control view
4. Capture banned patterns directly in the page specs so the implementation line has explicit “do not build this” constraints.
5. Verify prototype coverage and page-spec completeness.

## Acceptance Criteria

- page-level specs exist for `Now`, `Threads`, and `System`
- the prototypes are interactive HTML/CSS artifacts checked into the repo
- prototype coverage includes the six required scenarios
- surface disclosure maps are explicit
- banned patterns are named directly in the page specs
