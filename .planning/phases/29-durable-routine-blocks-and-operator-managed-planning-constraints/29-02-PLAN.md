# 29-02 Plan

## Slice

Persist operator-declared routine blocks and bounded planning constraints in backend/storage seams.

## Why this slice second

Once the contract exists, the next step is to make it durable in backend/storage layers before widening planning/runtime behavior.

## Outcomes

- typed persistence for routine blocks and bounded planning constraints
- backend-owned load/save seams for planning/runtime consumers
- no shell-owned storage or JSON blob drift

## Likely Touch Points

- `crates/vel-storage/src/`
- `crates/vel-core/src/`
- `crates/veld/src/services/`

## Verification

- focused storage/repository tests
- migration or persistence round-trip checks
- narrow service tests proving typed load/save behavior
