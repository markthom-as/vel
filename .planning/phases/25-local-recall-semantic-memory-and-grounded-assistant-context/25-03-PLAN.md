---
phase: 25-local-recall-semantic-memory-and-grounded-assistant-context
plan: 03
type: execute
depends_on:
  - 25-02-PLAN.md
files_modified:
  - crates/veld/src/services/chat/tools.rs
  - crates/veld/src/services/chat/assistant.rs
  - crates/veld/src/services/agent_grounding.rs
  - crates/veld/src/services/context_runs.rs
  - crates/vel-api-types/src/lib.rs
  - crates/veld/tests/chat_grounding.rs
  - clients/web/src/types.ts
autonomous: true
requirements:
  - GROUND-CTX-01
  - GROUND-CTX-02
must_haves:
  truths:
    - "Assistant turns can consume a stronger backend-owned recall/context seam instead of stitching together repeated raw tools."
    - "Shells remain consumers of typed backend context rather than owners of recall assembly logic."
---

<objective>
Assemble stronger backend-owned assistant context from recall results instead of ad hoc tool responses.
</objective>

<tasks>
- Add a bounded assistant recall/context assembly seam that reuses improved retrieval results and grounding posture.
- Preserve inspectable source references and explainability for the assembled context handed to assistant turns.
- Update the affected transport boundary and focused tests so backend-owned context is shared consistently.
</tasks>

<verification>
- cargo test -p veld --test chat_grounding -- --nocapture
- cargo test -p veld context_runs -- --nocapture
- npm --prefix clients/web test -- --run src/types.test.ts
</verification>

<output>
Create `25-03-SUMMARY.md`.
</output>
