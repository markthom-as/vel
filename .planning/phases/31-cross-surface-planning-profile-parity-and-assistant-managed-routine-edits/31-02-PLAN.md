# 31-02 Plan

## Slice

Ship CLI and Apple planning-profile inspection/parity over the canonical backend seam.

## Why this slice second

Once the shared contract is explicit, the next step is to remove the web-only shell split by exposing the same durable planning profile through the other real operator entry surfaces.

## Outcomes

- CLI inspection or management entry over the canonical planning profile
- Apple-readable planning-profile parity where appropriate for summary-first usage
- no shell-local profile state or duplicated planning semantics

## Likely Touch Points

- `crates/veld/src/routes/`
- `crates/veld/src/services/planning_profile.rs`
- `crates/vel-cli/src/commands/`
- `clients/apple/`
- `crates/vel-api-types/src/lib.rs`

## Verification

- focused route/service tests for planning-profile read behavior
- CLI command tests or command-backed snapshots
- Apple contract/build checks where DTOs or client surfaces change
