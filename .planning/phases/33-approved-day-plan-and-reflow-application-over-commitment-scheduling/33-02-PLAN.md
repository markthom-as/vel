# 33-02 Plan

## Slice

Implement backend application of bounded `day_plan` and `reflow` changes through canonical commitment scheduling mutation seams.

## Why this slice second

Once the lifecycle contract exists, Vel needs the real backend bridge that turns approved same-day planning changes into durable commitment scheduling state through one backend-owned path.

## Outcomes

- backend apply path for approved bounded same-day scheduling changes
- durable persistence of applied or failed outcomes into thread continuity
- explicit reuse of canonical commitment mutation logic instead of route- or shell-local scheduling writes

## Likely Touch Points

- `crates/vel-storage/src/repositories/`
- `crates/veld/src/services/day_plan.rs`
- `crates/veld/src/services/reflow.rs`
- `crates/veld/src/services/`
- `crates/veld/src/routes/`

## Verification

- focused Rust service and route tests covering apply success and failure paths
- regression coverage for same-day/bounded enforcement
- command-backed checks for proposal lifecycle persistence and commitment mutation results
