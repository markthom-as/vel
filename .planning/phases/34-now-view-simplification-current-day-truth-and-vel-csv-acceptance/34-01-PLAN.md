# 34-01 Plan

## Slice

Publish the compact `Now` contract, ranking rules, and `Vel.csv`-anchored acceptance baseline before UI changes widen.

## Work

- capture the locked operator decisions in Phase 34 artifacts
- make explicit which `Vel.csv` items count as direct Phase 34 acceptance pressure
- record the intended rendered order and removed-by-default sections so implementation can be judged against a stable target

## Verification

- planning consistency checks over roadmap, context, validation, and plan files

## Exit

Phase 34 has explicit execution criteria instead of a generic “clean up Now” direction.
