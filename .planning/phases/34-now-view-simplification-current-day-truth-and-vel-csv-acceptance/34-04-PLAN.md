# 34-04 Plan

## Slice

Close duplicate/sloppy `Now` presentation issues, broken actions, and verification against `Vel.csv` pressure items.

## Work

- remove duplicated sections and low-value default panels
- demote freshness/sync/debug posture out of the default surface
- align docs to the new `Now` truth where needed
- verify against the directly relevant `Vel.csv` items captured in 34-01

## Verification

- targeted web tests
- targeted Rust `Now` tests
- `rg` checks over docs if wording changes

## Exit

Phase 34 closes with a materially calmer, more trustworthy `Now` and explicit verification evidence.
