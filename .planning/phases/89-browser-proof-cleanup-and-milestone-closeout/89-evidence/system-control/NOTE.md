# Phase 89 Browser Proof — System Control View

## Command

`npm --prefix clients/web run proof:phase89:ui-proof`

## What Was Tested

Opened the `Control` section and verified dense-but-readable project detail.

## Expected Canonical Behavior

`Control` should present structural objects as readable rows with bounded detail, not playful cards or raw tables.

## Observed Result

The browser rendered `Project registry` with dense row selection, bounded detail, and the retained `Capabilities` browse path.

## Deviation

None.
