# Phase 01 Validation

- confirm the current local GSD install path and version
- confirm the repo-local commands and docs that depend on it
- confirm the concrete gaps between the current setup and `GSD 2`
- reject speculative migration claims without command-backed evidence
