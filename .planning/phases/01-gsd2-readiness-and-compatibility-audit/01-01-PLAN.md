# Plan 01-01: GSD 2 readiness and compatibility audit

## Goal

Determine whether this repo can safely migrate from the current local `get-shit-done` install to `GSD 2` without breaking practical workflow use.

## Scope

- inventory current local install paths, command entrypoints, and repo-specific assumptions
- map those assumptions against `GSD 2`
- document migration blockers, fallback needs, and rollback shape

## Planned Work

1. identify current repo and Codex workflow dependencies on `get-shit-done`
2. compare those dependencies against the available `GSD 2` surface
3. produce a concrete readiness verdict with cutover constraints

## Verification

- the repo-specific dependency inventory is written down
- the migration risks are concrete enough to drive Phase 02 without guesswork
