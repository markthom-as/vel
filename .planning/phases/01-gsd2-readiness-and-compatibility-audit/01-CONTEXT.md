# Phase 01 Context

`v0.5.8` starts after deferring `0.5.7` as future duplex work.

The immediate problem is workflow reliability:

- the repo currently uses the local `get-shit-done` v1 layout under `~/.codex/get-shit-done`
- `GSD 2` exists as a separate product shape
- the repo now uses reset-numbered active phases to keep `.planning/phases/` milestone-local

Phase 01 exists to replace assumptions with a concrete migration-readiness audit.
