---
phase: 05-now-inbox-core-and-project-substrate
plan: 06
type: execute
wave: 5
depends_on:
  - 05-02
  - 05-03
  - 05-05
files_modified:
  - clients/web/src/types.ts
  - clients/web/src/types.test.ts
  - clients/web/src/data/chat.ts
  - clients/web/src/data/chat.test.ts
  - clients/web/src/data/context.ts
  - clients/web/src/data/operator.ts
autonomous: true
requirements:
  - NOW-01
  - NOW-02
  - INBOX-01
  - INBOX-02
  - CONTINUITY-01
  - PROJ-03
must_haves:
  truths:
    - "The web data layer decodes typed Now, Inbox, project, linking, and review-snapshot payloads without unchecked casts."
    - "Inbox data mutations exist for `acknowledge`, `snooze`, and `dismiss`, and open-thread reuses the existing conversation route family."
    - "Project and linking requests are available to the UI without moving backend policy into the browser."
  artifacts:
    - path: "clients/web/src/types.ts"
      provides: "TypeScript DTOs and decoders for Phase 05 payloads"
      contains: "ReviewSnapshotData"
    - path: "clients/web/src/data/chat.ts"
      provides: "Inbox loaders, triage mutation functions, and open-thread route helper"
      contains: "acknowledgeInboxItem"
    - path: "clients/web/src/data/operator.ts"
      provides: "project and linking API loaders/mutators"
      contains: "issuePairingToken"
    - path: "clients/web/src/data/chat.test.ts"
      provides: "data-layer tests for inbox mutations and thread-route reuse"
      contains: "getInboxThreadPath"
  key_links:
    - from: "clients/web/src/types.ts"
      to: "clients/web/src/data/context.ts"
      via: "Now decode path uses named review snapshot and action item decoders"
      pattern: "decodeReviewSnapshotData|decodeActionItemData"
    - from: "clients/web/src/data/chat.ts"
      to: "/api/interventions/:id/acknowledge"
      via: "inbox mutation helper"
      pattern: "acknowledge"
    - from: "clients/web/src/data/chat.ts"
      to: "/api/conversations/:id"
      via: "open-thread helper reuses conversation route family"
      pattern: "getInboxThreadPath"
---

<objective>
Prepare the web boundary layer for Phase 05 by adding typed decoders plus project, linking, and Inbox triage data functions.

Purpose: Keep React thin by moving all Phase 05 contract decoding and mutation wiring into the web data layer before view work begins.
Output: Typed web DTOs/decoders, project/linking API helpers, inbox triage mutation helpers, and data-layer tests.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-now-inbox-core-and-project-substrate/05-CONTEXT.md
@clients/web/README.md
@clients/web/src/types.ts
@clients/web/src/types.test.ts
@clients/web/src/data/chat.ts
@clients/web/src/data/context.ts
@clients/web/src/data/operator.ts
@crates/vel-api-types/src/lib.rs
@crates/veld/src/routes/now.rs
@crates/veld/src/routes/chat.rs
@crates/veld/src/routes/linking.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Phase 05 TypeScript contracts and decoders</name>
  <files>clients/web/src/types.ts, clients/web/src/types.test.ts</files>
  <read_first>clients/web/src/types.ts
clients/web/src/types.test.ts
crates/vel-api-types/src/lib.rs
crates/veld/src/routes/now.rs
crates/veld/src/routes/chat.rs</read_first>
  <action>Add TypeScript interfaces and decoders for `ProjectRecordData`, `ProjectCreateRequestData`, `ProjectCreateResponseData`, `ActionEvidenceRefData`, `ActionItemData`, `ReviewSnapshotData`, `LinkedNodeData`, and the expanded `NowData`, `InboxItemData`, `ClusterBootstrapData`, and `SyncBootstrapData` shapes. `InboxItemData` must include `conversation_id`, `title`, `summary`, `project_id`, `project_label`, `available_actions`, and `evidence`. `NowData` must include `action_items` and `review_snapshot`. Extend `clients/web/src/types.test.ts` with explicit decode tests for the named review snapshot contract and the expanded inbox/project/linking payloads. Keep all decoders explicit in `clients/web/src/types.ts`; do not use `as unknown as` shortcuts or `any`.</action>
  <acceptance_criteria>- `rg -n "export interface ProjectRecordData|export interface ActionItemData|export interface ReviewSnapshotData|export interface LinkedNodeData" clients/web/src/types.ts` returns all four interfaces.
- `rg -n "export function decodeProjectRecordData|export function decodeActionItemData|export function decodeReviewSnapshotData|export function decodeLinkedNodeData" clients/web/src/types.ts` returns all four decoders.
- `rg -n "review_snapshot|conversation_id|available_actions" clients/web/src/types.test.ts` returns tests that exercise the expanded Phase 05 fields.</acceptance_criteria>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/types.test.ts</automated>
  </verify>
  <done>The web boundary can decode every new Phase 05 payload shape with named contracts instead of unchecked casts or opaque blobs.</done>
</task>

<task type="auto">
  <name>Task 2: Add project, linking, and Inbox triage data functions</name>
  <files>clients/web/src/data/chat.ts, clients/web/src/data/chat.test.ts, clients/web/src/data/context.ts, clients/web/src/data/operator.ts</files>
  <read_first>clients/web/src/data/chat.ts
clients/web/src/data/context.ts
clients/web/src/data/operator.ts
clients/web/src/data/chat.test.ts
clients/web/src/types.ts
crates/veld/src/routes/chat.rs
crates/veld/src/routes/linking.rs
crates/veld/src/routes/projects.rs</read_first>
  <action>Add loader/mutator functions for `loadProjects`, `createProject`, `issuePairingToken`, `redeemPairingToken`, and `loadLinkingStatus`. Extend the chat data layer with `acknowledgeInboxItem(interventionId: string)`, `snoozeInboxItem(interventionId: string, minutes: number)`, and `dismissInboxItem(interventionId: string)` that call `POST /api/interventions/:id/acknowledge`, `POST /api/interventions/:id/snooze`, and `POST /api/interventions/:id/dismiss`. Add `getInboxThreadPath(item: InboxItemData): string | null` that returns `/api/conversations/${item.conversation_id}` only when `item.available_actions` contains `open_thread` and `conversation_id` is present; otherwise return `null`. Update `clients/web/src/data/context.ts` to decode the expanded `NowData` and sync/bootstrap payloads. Create `clients/web/src/data/chat.test.ts` covering the three mutation helpers and `getInboxThreadPath` route reuse.</action>
  <acceptance_criteria>- `rg -n "loadProjects|createProject|issuePairingToken|redeemPairingToken|loadLinkingStatus" clients/web/src/data/operator.ts` returns all five API functions.
- `rg -n "acknowledgeInboxItem|snoozeInboxItem|dismissInboxItem|getInboxThreadPath" clients/web/src/data/chat.ts` returns all four helpers.
- `rg -n "/api/interventions/.*/acknowledge|/api/interventions/.*/snooze|/api/interventions/.*/dismiss|/api/conversations/" clients/web/src/data/chat.test.ts` returns tests for all three mutations plus conversation route reuse.
- `rg -n "review_snapshot|linked_nodes|projects|action_items" clients/web/src/data/context.ts` returns matches for the expanded payload decode path.</acceptance_criteria>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/types.test.ts src/data/chat.test.ts</automated>
  </verify>
  <done>The web data layer can call the new project/linking APIs, mutate Inbox triage state, and reuse the existing thread route family for open-thread actions.</done>
</task>

</tasks>

<verification>
Run the focused Vitest suites for types and web data helpers. Confirm the data layer now has explicit triage mutations and a deterministic thread-route helper.
</verification>

<success_criteria>
React can consume the shared Phase 05 backend contracts through typed decoders and narrow data helpers, with no browser-owned policy or ad hoc route construction.
</success_criteria>

<output>
After completion, create `.planning/phases/05-now-inbox-core-and-project-substrate/05-06-SUMMARY.md`
</output>
