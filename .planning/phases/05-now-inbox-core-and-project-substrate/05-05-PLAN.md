---
phase: 05-now-inbox-core-and-project-substrate
plan: 05
type: execute
wave: 4
depends_on:
  - 05-02
  - 05-03
files_modified:
  - crates/vel-storage/src/db.rs
  - crates/vel-storage/src/repositories/chat_repo.rs
  - crates/veld/src/services/mod.rs
  - crates/veld/src/services/operator_queue.rs
  - crates/veld/src/services/now.rs
  - crates/veld/src/services/chat/interventions.rs
  - crates/veld/src/services/chat/mapping.rs
  - crates/veld/src/services/chat/reads.rs
  - crates/veld/src/services/synthesis.rs
  - crates/veld/src/routes/now.rs
  - crates/veld/src/routes/chat.rs
  - crates/vel-cli/src/commands/review.rs
  - docs/user/daily-use.md
  - docs/api/chat.md
  - docs/api/runtime.md
autonomous: true
requirements:
  - NOW-01
  - NOW-02
  - INBOX-01
  - INBOX-02
  - ACTION-01
  - REVIEW-01
must_haves:
  truths:
    - "`Now` returns the top ranked action/intervention stack plus typed review counts from persisted evidence."
    - "`Inbox` returns triage rows with explicit `acknowledge`, `snooze`, `dismiss`, and `open_thread` actions backed by server-side state."
    - "Daily and weekly review outputs expose project-aware action counts and review candidates using the same backend-owned vocabulary."
  artifacts:
    - path: "crates/veld/src/services/operator_queue.rs"
      provides: "backend-owned action/intervention synthesis and ranking"
      min_lines: 140
    - path: "crates/veld/src/services/chat/interventions.rs"
      provides: "acknowledge, snooze, dismiss, and resolve intervention mutations"
      contains: "acknowledge_intervention"
    - path: "crates/veld/src/routes/chat.rs"
      provides: "Now/Inbox mutation and thread reuse routes"
      contains: "/api/interventions/:id/acknowledge"
    - path: "crates/vel-cli/src/commands/review.rs"
      provides: "review today/week output with action and project summaries"
      min_lines: 80
  key_links:
    - from: "crates/veld/src/services/operator_queue.rs"
      to: "crates/veld/src/services/now.rs"
      via: "Now projection uses ranked action items"
      pattern: "build_action_items"
    - from: "crates/veld/src/routes/chat.rs"
      to: "crates/veld/src/services/chat/interventions.rs"
      via: "Inbox triage mutations call intervention services"
      pattern: "acknowledge_intervention|snooze_intervention|dismiss_intervention"
    - from: "crates/vel-cli/src/commands/review.rs"
      to: "crates/veld/src/services/synthesis.rs"
      via: "review output references project-aware weekly synthesis candidates"
      pattern: "project_review_candidates"
---

<objective>
Turn `Now`, `Inbox`, and review flows into one backend-owned action/intervention system with explicit triage mutations.

Purpose: Preserve `Now` and `Inbox` as the primary operator loop while converging commitments, freshness, interventions, conflicts, degraded continuity, and review prompts under one explainable vocabulary.
Output: Ranked action synthesis service, typed `Now`/`Inbox` payloads, triage mutation routes, and project-aware daily/weekly review outputs.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-now-inbox-core-and-project-substrate/05-CONTEXT.md
@.planning/phases/05-now-inbox-core-and-project-substrate/05-UI-SPEC.md
@docs/user/daily-use.md
@crates/veld/src/services/now.rs
@crates/veld/src/services/chat/reads.rs
@crates/veld/src/services/chat/interventions.rs
@crates/veld/src/services/synthesis.rs
@crates/vel-cli/src/commands/review.rs

<interfaces>
From `crates/vel-core/src/operator_queue.rs`:
```rust
pub struct ActionItem {
    pub id: ActionItemId,
    pub surface: ActionSurface,
    pub kind: ActionKind,
    pub title: String,
    pub summary: String,
    pub project_id: Option<ProjectId>,
    pub state: ActionState,
    pub rank: i32,
    pub surfaced_at: OffsetDateTime,
    pub snoozed_until: Option<OffsetDateTime>,
    pub evidence: Vec<ActionEvidenceRef>,
}

pub struct ReviewSnapshot {
    pub open_action_count: usize,
    pub triage_count: usize,
    pub projects_needing_review: usize,
}
```

From `crates/vel-api-types/src/lib.rs`:
```rust
pub struct ReviewSnapshotData {
    pub open_action_count: i64,
    pub triage_count: i64,
    pub projects_needing_review: i64,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build a shared backend action/intervention ranking service</name>
  <files>crates/veld/src/services/mod.rs, crates/veld/src/services/operator_queue.rs</files>
  <read_first>crates/veld/src/services/mod.rs
crates/veld/src/services/operator_queue.rs
crates/veld/src/services/now.rs
crates/veld/src/services/chat/reads.rs
crates/veld/src/services/client_sync.rs
crates/vel-core/src/operator_queue.rs
crates/vel-storage/src/repositories/projects_repo.rs</read_first>
  <action>Create `crates/veld/src/services/operator_queue.rs` with a `build_action_items` function that reads open commitments, active interventions, freshness state, pending project provisions, and linked-node status. Rank items with concrete bands: freshness `error|disconnected|missing` items start at `90`, linking `pending|expired|revoked` items start at `85`, active inbox interventions start at `80`, blocked/conflict project items start at `75`, next commitments due within `24` hours start at `70`, weekly review candidates start at `60`, and everything else sorts below `50`. Every returned `ActionItem` must include at least one `ActionEvidenceRef` pointing to a persisted source such as `commitment`, `intervention`, `integration_status`, `linked_node`, or `project`. Export the module from `services/mod.rs`.</action>
  <acceptance_criteria>- `rg -n "build_action_items" crates/veld/src/services/operator_queue.rs` returns one match.
- `rg -n "90|85|80|75|70|60" crates/veld/src/services/operator_queue.rs` returns all six ranking bands.
- `rg -n "integration_status|linked_node|project|commitment|intervention" crates/veld/src/services/operator_queue.rs` returns all evidence source kinds.</acceptance_criteria>
  <verify>
    <automated>cargo test -p veld action_items -- --nocapture</automated>
  </verify>
  <done>The runtime has one backend-owned synthesis function for action/intervention items with stable ranks and explainable evidence.</done>
</task>

<task type="auto">
  <name>Task 2: Extend Now and Inbox with typed triage rows and explicit mutations</name>
  <files>crates/vel-storage/src/db.rs, crates/vel-storage/src/repositories/chat_repo.rs, crates/veld/src/services/now.rs, crates/veld/src/services/chat/interventions.rs, crates/veld/src/services/chat/mapping.rs, crates/veld/src/services/chat/reads.rs, crates/veld/src/routes/now.rs, crates/veld/src/routes/chat.rs</files>
  <read_first>crates/vel-storage/src/db.rs
crates/vel-storage/src/repositories/chat_repo.rs
crates/veld/src/services/now.rs
crates/veld/src/services/chat/interventions.rs
crates/veld/src/services/chat/mapping.rs
crates/veld/src/services/chat/reads.rs
crates/veld/src/routes/now.rs
crates/veld/src/routes/chat.rs
crates/vel-api-types/src/lib.rs
crates/vel-core/src/operator_queue.rs
crates/veld/src/services/operator_queue.rs</read_first>
  <action>Update `crates/veld/src/services/now.rs` so `NowOutput` includes `action_items: Vec<ActionItem>` limited to the top `5` ranked items and `review_snapshot: ReviewSnapshot` using exactly `open_action_count`, `triage_count`, and `projects_needing_review`. Update `crates/veld/src/routes/now.rs` to map those fields into `NowData` and `ReviewSnapshotData`. Update `crates/veld/src/services/chat/reads.rs` and `crates/veld/src/services/chat/mapping.rs` so `/api/inbox` returns triage-oriented records with `conversation_id`, `title`, `summary`, `project_id`, `project_label`, `available_actions`, and `evidence`, ordered by `rank DESC, surfaced_at DESC`. Populate `available_actions` with the exact strings `acknowledge`, `snooze`, `dismiss`, and `open_thread`; only include `open_thread` when a `conversation_id` is present. Add `acknowledge_intervention` to `crates/veld/src/services/chat/interventions.rs`, storage support in `crates/vel-storage/src/repositories/chat_repo.rs` and `crates/vel-storage/src/db.rs` to persist the `acknowledged` state, and a new route `POST /api/interventions/:id/acknowledge` in `crates/veld/src/routes/chat.rs`. Keep the existing `POST /api/interventions/:id/snooze` and `POST /api/interventions/:id/dismiss` routes for Inbox triage. Do not add an inbox-specific thread endpoint; instead, expose `conversation_id` so clients reuse the existing `/api/conversations/:id`, `/api/conversations/:id/messages`, and `/api/conversations/:id/interventions` route family.</action>
  <acceptance_criteria>- `rg -n "review_snapshot|action_items" crates/veld/src/services/now.rs crates/veld/src/routes/now.rs` returns matches in both files.
- `rg -n "acknowledge_intervention" crates/veld/src/services/chat/interventions.rs crates/veld/src/routes/chat.rs` returns matches in both files.
- `rg -n '"/api/interventions/:id/acknowledge"|"/api/interventions/:id/snooze"|"/api/interventions/:id/dismiss"' crates/veld/src/routes/chat.rs` returns all three route patterns.
- `rg -n "conversation_id|available_actions|open_thread|acknowledge|dismiss|snooze" crates/veld/src/services/chat/reads.rs crates/veld/src/services/chat/mapping.rs crates/vel-api-types/src/lib.rs` returns matches across the inbox path files.</acceptance_criteria>
  <verify>
    <automated>cargo test -p veld now -- --nocapture && cargo test -p veld inbox -- --nocapture && cargo test -p veld intervention -- --nocapture</automated>
  </verify>
  <done>`Now` exposes the ranked decision stack with typed review counts, and `Inbox` exposes triage rows plus explicit acknowledge/snooze/dismiss/open-thread actions backed by server-side state.</done>
</task>

<task type="auto">
  <name>Task 3: Make review and synthesis flows project-aware</name>
  <files>crates/veld/src/services/synthesis.rs, crates/vel-cli/src/commands/review.rs, docs/user/daily-use.md, docs/api/chat.md, docs/api/runtime.md</files>
  <read_first>crates/veld/src/services/synthesis.rs
crates/vel-cli/src/commands/review.rs
docs/user/daily-use.md
docs/api/chat.md
docs/api/runtime.md
crates/veld/src/services/operator_queue.rs
crates/vel-storage/src/repositories/projects_repo.rs</read_first>
  <action>Update `crates/veld/src/services/synthesis.rs` so project synthesis resolves against the typed `projects.slug` table first and only falls back to legacy `commitment.project` alias matches when no typed project exists. Update `crates/vel-cli/src/commands/review.rs` so `vel review today --json` emits `open_action_count`, `triage_count`, and `top_action_titles`, and `vel review week --json` emits `project_review_candidates` with `project_id`, `slug`, `family`, and `open_commitment_count`. Update `docs/user/daily-use.md` so the daily loop explicitly says `orient in Now`, `triage Inbox`, and `review Projects weekly`. Update `docs/api/chat.md` and `docs/api/runtime.md` so `Inbox` is documented as a triage queue rather than a message list, and note that `open_thread` reuses the existing conversation route family.</action>
  <acceptance_criteria>- `rg -n "project_review_candidates|top_action_titles|triage_count" crates/vel-cli/src/commands/review.rs` returns all three fields.
- `rg -n "get_project_by_slug|project_alias" crates/veld/src/services/synthesis.rs` returns at least one match.
- `rg -n "orient in Now|triage Inbox|review Projects weekly" docs/user/daily-use.md` returns all three phrases.
- `rg -n "triage queue|open_thread" docs/api/chat.md docs/api/runtime.md` returns matches in both docs.</acceptance_criteria>
  <verify>
    <automated>cargo test -p veld synthesis -- --nocapture && cargo test -p vel-cli review -- --nocapture</automated>
  </verify>
  <done>Daily and weekly review flows reinforce the same `Now`/`Inbox`/Projects model that the operator-facing surfaces use.</done>
</task>

</tasks>

<verification>
Run focused daemon and CLI review/synthesis tests, then grep the updated docs so `Inbox` and the daily loop are described with their new triage semantics and thread reuse path.
</verification>

<success_criteria>
`Now`, `Inbox`, and review flows all speak one backend-owned action/intervention vocabulary, and every surfaced item remains traceable to persisted evidence with explicit triage mutations.
</success_criteria>

<output>
After completion, create `.planning/phases/05-now-inbox-core-and-project-substrate/05-05-SUMMARY.md`
</output>
