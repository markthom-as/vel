---
phase: 05-now-inbox-core-and-project-substrate
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - migrations/0038_phase5_projects.sql
  - crates/vel-storage/src/db.rs
  - crates/vel-storage/src/lib.rs
  - crates/vel-storage/src/repositories/mod.rs
  - crates/vel-storage/src/repositories/projects_repo.rs
  - crates/veld/src/services/mod.rs
  - crates/veld/src/services/projects.rs
  - crates/veld/src/routes/mod.rs
  - crates/veld/src/routes/projects.rs
  - crates/veld/src/app.rs
  - docs/api/runtime.md
  - docs/user/surfaces.md
autonomous: true
requirements:
  - PROJ-01
  - PROJ-02
  - PROJ-03
  - FAMILY-01
must_haves:
  truths:
    - "The runtime persists typed project records instead of treating free-form commitment project strings as the long-term project substrate."
    - "Operators can create a local-first project with family, primary repo, and primary notes root without triggering upstream side effects."
    - "The runtime can list and inspect projects through authenticated API routes."
  artifacts:
    - path: "migrations/0038_phase5_projects.sql"
      provides: "projects and project_aliases tables"
      contains: "CREATE TABLE projects"
    - path: "crates/vel-storage/src/repositories/projects_repo.rs"
      provides: "project persistence and lookup operations"
      min_lines: 120
    - path: "crates/veld/src/services/projects.rs"
      provides: "local-first project creation and listing service"
      min_lines: 100
    - path: "crates/veld/src/routes/projects.rs"
      provides: "GET /v1/projects, GET /v1/projects/:id, POST /v1/projects, GET /v1/projects/families"
      exports: ["list_projects", "get_project", "create_project", "list_project_families"]
  key_links:
    - from: "migrations/0038_phase5_projects.sql"
      to: "crates/vel-storage/src/repositories/projects_repo.rs"
      via: "sqlx queries"
      pattern: "FROM projects"
    - from: "crates/veld/src/services/projects.rs"
      to: "crates/vel-storage/src/repositories/projects_repo.rs"
      via: "Storage facade project methods"
      pattern: "create_project|list_projects|get_project"
    - from: "crates/veld/src/app.rs"
      to: "crates/veld/src/routes/projects.rs"
      via: "authenticated route mounts"
      pattern: "/v1/projects"
---

<objective>
Implement the typed project substrate as persisted runtime state and expose a local-first project workspace API.

Purpose: Make projects real backend-owned records with stable IDs, families, roots, and pending upstream confirmations before any UI starts treating them as a working surface.
Output: Project migration, storage repository, service layer, authenticated routes, and truthful runtime/user docs for project creation and inspection.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-now-inbox-core-and-project-substrate/05-CONTEXT.md
@.planning/phases/05-now-inbox-core-and-project-substrate/05-RESEARCH.md
@crates/vel-core/src/commitment.rs
@crates/vel-storage/src/lib.rs
@crates/vel-storage/src/repositories/commitments_repo.rs
@crates/veld/src/routes/now.rs
@crates/veld/src/routes/synthesis.rs
@docs/user/surfaces.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist typed project records and legacy aliases in SQLite</name>
  <files>migrations/0038_phase5_projects.sql, crates/vel-storage/src/db.rs, crates/vel-storage/src/lib.rs, crates/vel-storage/src/repositories/mod.rs, crates/vel-storage/src/repositories/projects_repo.rs</files>
  <read_first>migrations/0037_semantic_memory.sql
crates/vel-storage/src/db.rs
crates/vel-storage/src/lib.rs
crates/vel-storage/src/repositories/mod.rs
crates/vel-storage/src/repositories/commitments_repo.rs
crates/vel-core/src/project.rs</read_first>
  <action>Create `migrations/0038_phase5_projects.sql` with a `projects` table and a `project_aliases` table. The `projects` table must include `id TEXT PRIMARY KEY`, `slug TEXT NOT NULL UNIQUE`, `name TEXT NOT NULL`, `family TEXT NOT NULL`, `status TEXT NOT NULL`, `primary_repo_path TEXT NOT NULL`, `primary_notes_root TEXT NOT NULL`, `secondary_repo_paths_json TEXT NOT NULL DEFAULT '[]'`, `secondary_notes_roots_json TEXT NOT NULL DEFAULT '[]'`, `upstream_ids_json TEXT NOT NULL DEFAULT '{}'`, `pending_provision_json TEXT NOT NULL DEFAULT '{}'`, `created_at INTEGER NOT NULL`, `updated_at INTEGER NOT NULL`, and `archived_at INTEGER`. The `project_aliases` table must include `alias TEXT PRIMARY KEY`, `project_id TEXT NOT NULL`, and `source TEXT NOT NULL`. Add `crates/vel-storage/src/repositories/projects_repo.rs` with `create_project`, `list_projects`, `get_project`, `get_project_by_slug`, `upsert_project_alias`, and `list_project_families` queries. Export the new repository methods through `db.rs` and `lib.rs` without importing `vel-api-types` into storage.</action>
  <acceptance_criteria>- `rg -n "CREATE TABLE projects" migrations/0038_phase5_projects.sql` returns one match.
- `rg -n "CREATE TABLE project_aliases" migrations/0038_phase5_projects.sql` returns one match.
- `rg -n "async fn create_project|async fn list_projects|async fn get_project_by_slug" crates/vel-storage/src/repositories/projects_repo.rs` returns matches for all three functions.
- `rg -n "pub async fn create_project|pub async fn list_projects|pub async fn get_project" crates/vel-storage/src/db.rs crates/vel-storage/src/lib.rs` returns facade exports.</acceptance_criteria>
  <verify>
    <automated>cargo test -p vel-storage projects_repo -- --nocapture</automated>
  </verify>
  <done>The database can persist typed projects and alias legacy project strings to stable project IDs.</done>
</task>

<task type="auto">
  <name>Task 2: Add authenticated project workspace routes and a local-first create flow</name>
  <files>crates/veld/src/services/mod.rs, crates/veld/src/services/projects.rs, crates/veld/src/routes/mod.rs, crates/veld/src/routes/projects.rs, crates/veld/src/app.rs</files>
  <read_first>crates/veld/src/services/mod.rs
crates/veld/src/routes/mod.rs
crates/veld/src/routes/now.rs
crates/veld/src/routes/synthesis.rs
crates/veld/src/app.rs
crates/vel-core/src/project.rs
crates/vel-api-types/src/lib.rs</read_first>
  <action>Create `crates/veld/src/services/projects.rs` with `list_projects`, `get_project`, `list_project_families`, and `create_project`. `create_project` must accept `name`, `slug`, `family`, `primary_repo_path`, `primary_notes_root`, `secondary_repo_paths`, `secondary_notes_roots`, and `pending_provision` from `ProjectCreateRequestData`; validate that `family` is one of `personal`, `creative`, or `work`; persist the project locally; and record pending upstream intent only in `pending_provision_json`. It must never create a repo, notes root, or external upstream record in this phase. Create `crates/veld/src/routes/projects.rs` exposing `GET /v1/projects`, `GET /v1/projects/:id`, `POST /v1/projects`, and `GET /v1/projects/families`, register the module in `routes/mod.rs`, and mount the routes in `crates/veld/src/app.rs` under the operator-authenticated class.</action>
  <acceptance_criteria>- `rg -n '"/v1/projects"' crates/veld/src/app.rs` returns at least one route mount.
- `rg -n '"/v1/projects/families"' crates/veld/src/app.rs` returns one route mount.
- `rg -n "pub async fn create_project|pub async fn list_project_families" crates/veld/src/routes/projects.rs` returns both handlers.
- `rg -n "never create a repo|never create.*notes root" crates/veld/src/services/projects.rs` is not required; instead `rg -n "pending_provision" crates/veld/src/services/projects.rs crates/veld/src/routes/projects.rs` must return matches in both files.</acceptance_criteria>
  <verify>
    <automated>cargo test -p veld project -- --nocapture</automated>
  </verify>
  <done>The runtime exposes typed project list/detail/create routes, and project creation remains strictly local-first with pending upstream confirmations only.</done>
</task>

<task type="auto">
  <name>Task 3: Document the implemented project workspace API and operator surface truth</name>
  <files>docs/api/runtime.md, docs/user/surfaces.md</files>
  <read_first>docs/api/runtime.md
docs/user/surfaces.md
crates/veld/src/routes/projects.rs
crates/veld/src/app.rs
.planning/phases/05-now-inbox-core-and-project-substrate/05-CONTEXT.md</read_first>
  <action>Update `docs/api/runtime.md` to list `GET /v1/projects`, `GET /v1/projects/:id`, `POST /v1/projects`, and `GET /v1/projects/families` under operator-authenticated routes, with explicit text that project creation is local-first and stores pending upstream confirmation only. Update `docs/user/surfaces.md` so the `Projects` section says it is now a supporting coordination surface backed by typed project records, not the home screen, and names the stable families `Personal`, `Creative`, and `Work`.</action>
  <acceptance_criteria>- `rg -n "GET /v1/projects|POST /v1/projects|GET /v1/projects/families" docs/api/runtime.md` returns all three route lines.
- `rg -n "local-first|pending upstream" docs/api/runtime.md` returns both phrases.
- `rg -n "Projects" docs/user/surfaces.md` returns a new surface section or updated heading.
- `rg -n "Personal|Creative|Work" docs/user/surfaces.md` returns all three family names.</acceptance_criteria>
  <verify>
    <automated>rg -n "GET /v1/projects|POST /v1/projects|Personal|Creative|Work" docs/api/runtime.md docs/user/surfaces.md</automated>
  </verify>
  <done>The runtime docs and user-facing surface docs accurately describe the newly implemented project substrate and keep `Projects` secondary to `Now` and `Inbox`.</done>
</task>

</tasks>

<verification>
Run the focused storage and daemon tests for project persistence and routes, then grep the updated docs so the route inventory and surface hierarchy are truthful.
</verification>

<success_criteria>
Operators can create and inspect typed projects through authenticated backend APIs, projects carry stable families and workspace roots, and upstream side effects remain explicitly deferred behind pending confirmations.
</success_criteria>

<output>
After completion, create `.planning/phases/05-now-inbox-core-and-project-substrate/05-02-SUMMARY.md`
</output>
