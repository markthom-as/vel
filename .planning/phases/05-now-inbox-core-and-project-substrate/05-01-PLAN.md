---
phase: 05-now-inbox-core-and-project-substrate
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/vel-core/src/lib.rs
  - crates/vel-core/src/project.rs
  - crates/vel-core/src/operator_queue.rs
  - crates/vel-core/src/linking.rs
  - crates/vel-api-types/src/lib.rs
  - config/contracts-manifest.json
  - config/README.md
  - config/schemas/project-workspace.schema.json
  - config/examples/project-workspace.example.json
  - config/schemas/operator-action-item.schema.json
  - config/examples/operator-action-item.example.json
  - docs/cognitive-agent-architecture/architecture/project-action-linking-contracts.md
autonomous: true
requirements:
  - ACTION-01
  - PROJ-01
  - PROJ-02
  - FAMILY-01
must_haves:
  truths:
    - "Project, Now, Inbox, sync bootstrap, and linking payloads can all be expressed with named Phase 05 fields instead of opaque JSON blobs."
    - "Review counts for the Now surface travel through a named `ReviewSnapshot` contract that backend and clients can decode directly."
    - "Phase 05 schema/example artifacts are checked in and listed in the contract manifest so later slices can verify the contract boundary before wiring UI."
  artifacts:
    - path: "crates/vel-core/src/project.rs"
      provides: "ProjectId, ProjectFamily, ProjectRecord, ProjectRootRef, ProjectProvisionRequest"
      min_lines: 80
    - path: "crates/vel-core/src/operator_queue.rs"
      provides: "ActionItem, ActionEvidenceRef, ActionSurface, ActionKind, ActionState, ReviewSnapshot"
      min_lines: 80
    - path: "crates/vel-api-types/src/lib.rs"
      provides: "Project/action/linking DTOs plus ReviewSnapshotData"
      contains: "pub struct ReviewSnapshotData"
    - path: "crates/vel-core/src/linking.rs"
      provides: "LinkScope, LinkStatus, PairingTokenRecord, LinkedNodeRecord"
      min_lines: 70
    - path: "config/schemas/project-workspace.schema.json"
      provides: "Machine-readable project workspace contract"
      contains: "\"ProjectFamily\""
    - path: "config/schemas/operator-action-item.schema.json"
      provides: "Machine-readable action/intervention contract"
      contains: "\"ActionKind\""
  key_links:
    - from: "crates/vel-core/src/lib.rs"
      to: "crates/vel-core/src/project.rs"
      via: "pub use re-export"
      pattern: "pub use project::"
    - from: "crates/vel-api-types/src/lib.rs"
      to: "crates/vel-core/src/operator_queue.rs"
      via: "DTO mapping boundary"
      pattern: "ActionItemData|ReviewSnapshotData"
    - from: "config/contracts-manifest.json"
      to: "config/schemas/project-workspace.schema.json"
      via: "contracts manifest entry"
      pattern: "project-workspace.schema.json"
---

<objective>
Freeze the Phase 05 contracts before persistence, services, or client shells widen around them.

Purpose: Stop the codebase from extending `commitment.project: Option<String>` and ad hoc inbox/linking payloads as the long-term boundary.
Output: Typed `vel-core` modules, matching `vel-api-types` DTOs, and contract docs/schema/example artifacts for projects, action items, typed review counts, and linking.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-now-inbox-core-and-project-substrate/05-CONTEXT.md
@.planning/phases/05-now-inbox-core-and-project-substrate/05-RESEARCH.md
@.planning/phases/04-autonomous-swarm-graph-rag-zero-trust-execution/04-01-SUMMARY.md
@crates/vel-core/src/commitment.rs
@crates/veld/src/services/now.rs
@crates/veld/src/services/client_sync.rs

<interfaces>
From `crates/vel-core/src/commitment.rs`:
```rust
pub struct Commitment {
    pub id: CommitmentId,
    pub text: String,
    pub source_type: String,
    pub source_id: Option<String>,
    pub status: CommitmentStatus,
    pub due_at: Option<OffsetDateTime>,
    pub project: Option<String>,
    pub commitment_kind: Option<String>,
    pub created_at: OffsetDateTime,
    pub resolved_at: Option<OffsetDateTime>,
    pub metadata_json: serde_json::Value,
}
```

From `crates/veld/src/services/client_sync.rs`:
```rust
pub struct SyncBootstrap {
    pub cluster: ClusterBootstrap,
    pub current_context: Option<CurrentContext>,
    pub nudges: Vec<vel_storage::NudgeRecord>,
    pub commitments: Vec<Commitment>,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish canonical Phase 05 domain contracts in vel-core</name>
  <files>crates/vel-core/src/lib.rs, crates/vel-core/src/project.rs, crates/vel-core/src/operator_queue.rs, crates/vel-core/src/linking.rs</files>
  <read_first>crates/vel-core/src/lib.rs
crates/vel-core/src/commitment.rs
crates/veld/src/services/now.rs
crates/veld/src/services/client_sync.rs
.planning/phases/05-now-inbox-core-and-project-substrate/05-CONTEXT.md</read_first>
  <action>Create `crates/vel-core/src/project.rs` with `ProjectId` using the `proj_` prefix, `ProjectFamily` with exactly `Personal`, `Creative`, and `Work`, `ProjectStatus` with exactly `active`, `paused`, and `archived`, `ProjectRootRef` with fields `path`, `label`, and `kind`, `ProjectProvisionRequest` with booleans `create_repo` and `create_notes_root`, and `ProjectRecord` with fields `id`, `slug`, `name`, `family`, `status`, `primary_repo`, `primary_notes_root`, `secondary_repos`, `secondary_notes_roots`, `upstream_ids`, `pending_provision`, `created_at`, `updated_at`, and `archived_at`. Create `crates/vel-core/src/operator_queue.rs` with `ActionItemId` using the `act_` prefix, `ActionSurface` with exactly `now` and `inbox`, `ActionKind` with exactly `next_step`, `intervention`, `review`, `freshness`, `blocked`, `conflict`, and `linking`, `ActionState` with exactly `active`, `acknowledged`, `resolved`, `dismissed`, and `snoozed`, `ActionEvidenceRef` with `source_kind`, `source_id`, `label`, and `detail`, `ActionItem` with `id`, `surface`, `kind`, `title`, `summary`, `project_id`, `state`, `rank`, `surfaced_at`, `snoozed_until`, and `evidence`, and `ReviewSnapshot` with `open_action_count`, `triage_count`, and `projects_needing_review`. Create `crates/vel-core/src/linking.rs` with `LinkScope` booleans `read_context`, `write_safe_actions`, and `execute_repo_tasks`, `LinkStatus` with exactly `pending`, `linked`, `revoked`, and `expired`, `PairingTokenRecord` with `token_id`, `token_code`, `issued_at`, `expires_at`, `issued_by_node_id`, and `scopes`, and `LinkedNodeRecord` with `node_id`, `node_display_name`, `status`, `scopes`, `linked_at`, `last_seen_at`, and `transport_hint`. Re-export all new modules from `crates/vel-core/src/lib.rs`.</action>
  <acceptance_criteria>- `rg -n "pub enum ProjectFamily" crates/vel-core/src/project.rs` returns one match.
- `rg -n "Personal|Creative|Work" crates/vel-core/src/project.rs` returns all three family variants.
- `rg -n "pub struct ActionItem" crates/vel-core/src/operator_queue.rs` returns one match.
- `rg -n "pub struct ReviewSnapshot" crates/vel-core/src/operator_queue.rs` returns one match.
- `rg -n "pub struct LinkedNodeRecord" crates/vel-core/src/linking.rs` returns one match.
- `rg -n "pub mod project|pub mod operator_queue|pub mod linking" crates/vel-core/src/lib.rs` returns all three module declarations.</acceptance_criteria>
  <verify>
    <automated>cargo test -p vel-core -- --nocapture</automated>
  </verify>
  <done>Core Phase 05 contracts compile, are re-exported from `vel-core`, and no downstream slice needs to invent project/action/linking domain types ad hoc.</done>
</task>

<task type="auto">
  <name>Task 2: Extend vel-api-types with Phase 05 project, action, and linking DTOs</name>
  <files>crates/vel-api-types/src/lib.rs</files>
  <read_first>crates/vel-api-types/src/lib.rs
crates/vel-core/src/project.rs
crates/vel-core/src/operator_queue.rs
crates/vel-core/src/linking.rs
crates/veld/src/routes/now.rs
crates/veld/src/routes/sync.rs</read_first>
  <action>Add transport DTOs for `ProjectFamilyData`, `ProjectRootRefData`, `ProjectRecordData`, `ProjectProvisionRequestData`, `ProjectCreateRequestData`, `ProjectCreateResponseData`, `ProjectListResponseData`, `ActionEvidenceRefData`, `ActionItemData`, `ReviewSnapshotData`, `LinkScopeData`, `PairingTokenData`, and `LinkedNodeData`. Extend `NowData` with `action_items: Vec<ActionItemData>` and `review_snapshot: ReviewSnapshotData`. Extend `InboxItemData` with `conversation_id`, `title`, `summary`, `project_id`, `project_label`, `available_actions`, and `evidence`. Extend `ClusterBootstrapData` and `SyncBootstrapData` with `linked_nodes: Vec<LinkedNodeData>`, `projects: Vec<ProjectRecordData>`, and `action_items: Vec<ActionItemData>`. Keep all new fields typed with `Vec`, `Option`, string enums, or named DTO structs; do not add `serde_json::Value`, raw JSON-string fields, or `as`-cast placeholders to carry Phase 05 data.</action>
  <acceptance_criteria>- `rg -n "pub struct ProjectRecordData" crates/vel-api-types/src/lib.rs` returns one match.
- `rg -n "pub struct ActionItemData" crates/vel-api-types/src/lib.rs` returns one match.
- `rg -n "pub struct ReviewSnapshotData" crates/vel-api-types/src/lib.rs` returns one match.
- `rg -n "pub struct LinkedNodeData" crates/vel-api-types/src/lib.rs` returns one match.
- `rg -n "pub action_items: Vec<ActionItemData>" crates/vel-api-types/src/lib.rs` returns matches for `NowData` and `SyncBootstrapData`.
- `rg -n "pub review_snapshot: ReviewSnapshotData" crates/vel-api-types/src/lib.rs` returns one match.
- `rg -n "pub linked_nodes: Vec<LinkedNodeData>" crates/vel-api-types/src/lib.rs` returns matches for `ClusterBootstrapData` or `SyncBootstrapData`.</acceptance_criteria>
  <verify>
    <automated>cargo test -p vel-api-types -- --nocapture</automated>
  </verify>
  <done>The wire contract can represent typed projects, surfaced actions, named review counts, pairing tokens, and linked-node state without opaque JSON extensions.</done>
</task>

<task type="auto">
  <name>Task 3: Check in schema, example, and owner docs for the new contracts</name>
  <files>config/contracts-manifest.json, config/README.md, config/schemas/project-workspace.schema.json, config/examples/project-workspace.example.json, config/schemas/operator-action-item.schema.json, config/examples/operator-action-item.example.json, docs/cognitive-agent-architecture/architecture/project-action-linking-contracts.md</files>
  <read_first>config/contracts-manifest.json
config/README.md
docs/cognitive-agent-architecture/architecture/
.planning/phases/04-autonomous-swarm-graph-rag-zero-trust-execution/04-01-SUMMARY.md
crates/vel-core/src/project.rs
crates/vel-core/src/operator_queue.rs
crates/vel-core/src/linking.rs</read_first>
  <action>Create `config/schemas/project-workspace.schema.json` and `config/examples/project-workspace.example.json` describing a project with `family`, `primary_repo`, `primary_notes_root`, `secondary_repos`, `secondary_notes_roots`, and `pending_provision`. Create `config/schemas/operator-action-item.schema.json` and `config/examples/operator-action-item.example.json` describing surfaced `now` and `inbox` items with `kind`, `rank`, `project_id`, `available_actions`, and `evidence`. Add both schema/example pairs to `config/contracts-manifest.json` and document them in `config/README.md`. Write `docs/cognitive-agent-architecture/architecture/project-action-linking-contracts.md` that names the Phase 05 owner modules, the stable family vocabulary `Personal/Creative/Work`, the action kinds, and the linking scopes `read_context`, `write_safe_actions`, and `execute_repo_tasks`.</action>
  <acceptance_criteria>- `rg -n "project-workspace.schema.json" config/contracts-manifest.json config/README.md` returns matches in both files.
- `rg -n "\"ProjectFamily\"" config/schemas/project-workspace.schema.json` returns one match.
- `rg -n "\"ActionKind\"" config/schemas/operator-action-item.schema.json` returns one match.
- `rg -n "read_context|write_safe_actions|execute_repo_tasks" docs/cognitive-agent-architecture/architecture/project-action-linking-contracts.md` returns all three scopes.</acceptance_criteria>
  <verify>
    <automated>node scripts/verify-repo-truth.mjs</automated>
  </verify>
  <done>Phase 05 contract artifacts are published on disk with a machine-readable schema, a checked-in example, and nearby owner documentation.</done>
</task>

</tasks>

<verification>
Run the crate tests for `vel-core` and `vel-api-types`, then run repo-truth verification so the new schema/example assets are discoverable from the contracts manifest.
</verification>

<success_criteria>
The repository has one typed vocabulary for projects, action/intervention surfacing, named review counts, and linking scopes; every downstream Phase 05 plan can build against those contracts without rediscovering names or payload shapes.
</success_criteria>

<output>
After completion, create `.planning/phases/05-now-inbox-core-and-project-substrate/05-01-SUMMARY.md`
</output>
