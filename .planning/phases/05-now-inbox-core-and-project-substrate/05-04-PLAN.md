---
phase: 05-now-inbox-core-and-project-substrate
plan: 04
type: execute
wave: 4
depends_on:
  - 05-03
files_modified:
  - crates/vel-cli/src/client.rs
  - crates/vel-cli/src/commands/mod.rs
  - crates/vel-cli/src/commands/node.rs
  - crates/vel-cli/src/main.rs
  - docs/api/runtime.md
autonomous: true
requirements:
  - CONTINUITY-02
must_haves:
  truths:
    - "Operators can issue, redeem, and inspect node links from the CLI without using the web shell."
    - "CLI status output shows current link state and granted scopes for each linked node."
    - "Runtime docs describe the shipped linking endpoints and the CLI fallback commands."
  artifacts:
    - path: "crates/vel-cli/src/commands/node.rs"
      provides: "vel node link issue|redeem and vel node status"
      min_lines: 100
    - path: "crates/vel-cli/src/client.rs"
      provides: "client methods for linking token issue, redeem, and status"
      contains: "linking/status"
    - path: "docs/api/runtime.md"
      provides: "documented linking routes and CLI fallback"
      contains: "vel node link issue"
  key_links:
    - from: "crates/vel-cli/src/commands/node.rs"
      to: "crates/vel-cli/src/client.rs"
      via: "CLI handler calls linking client methods"
      pattern: "issue_pairing_token|redeem_pairing_token|load_linking_status"
    - from: "crates/vel-cli/src/main.rs"
      to: "crates/vel-cli/src/commands/node.rs"
      via: "top-level Node command registration"
      pattern: "NodeCommand"
    - from: "docs/api/runtime.md"
      to: "crates/veld/src/routes/linking.rs"
      via: "documented runtime surface"
      pattern: "/v1/linking"
---

<objective>
Provide the CLI and docs fallback for guided node linking after the backend routes exist.

Purpose: Close the operator journey when the web shell is unavailable and make the shipped linking surface explicit in repo docs.
Output: `vel node` linking commands, matching CLI client methods, and updated runtime API documentation.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-now-inbox-core-and-project-substrate/05-CONTEXT.md
@docs/tickets/phase-2/012-tester-readiness-onboarding.md
@crates/vel-cli/src/client.rs
@crates/vel-cli/src/commands/sync.rs
@crates/veld/src/routes/linking.rs
@docs/api/runtime.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add `vel node` linking and status commands</name>
  <files>crates/vel-cli/src/client.rs, crates/vel-cli/src/commands/mod.rs, crates/vel-cli/src/commands/node.rs, crates/vel-cli/src/main.rs</files>
  <read_first>crates/vel-cli/src/client.rs
crates/vel-cli/src/commands/mod.rs
crates/vel-cli/src/commands/node.rs
crates/vel-cli/src/main.rs
crates/vel-cli/src/commands/sync.rs
crates/veld/src/routes/linking.rs</read_first>
  <action>Create `crates/vel-cli/src/commands/node.rs` and add a `Node` top-level command in `crates/vel-cli/src/main.rs` with subcommands `link issue`, `link redeem`, and `status`. `vel node link issue` must call `POST /v1/linking/tokens` with `--expires-seconds` and boolean flags `--read-context`, `--write-safe-actions`, and `--execute-repo-tasks`. `vel node link redeem` must call `POST /v1/linking/redeem` with `--node-id`, `--node-display-name`, and a required token code. `vel node status` must call `GET /v1/linking/status` and print each linked node, its `pending|linked|revoked|expired` status, and the granted scopes. Add matching client methods to `crates/vel-cli/src/client.rs` and export the command module from `crates/vel-cli/src/commands/mod.rs`.</action>
  <acceptance_criteria>- `rg -n "enum NodeCommand|struct NodeCommand" crates/vel-cli/src/main.rs crates/vel-cli/src/commands/node.rs` returns at least one match.
- `rg -n "link issue|link redeem|status" crates/vel-cli/src/commands/node.rs` returns all three command handlers.
- `rg -n "linking/tokens|linking/redeem|linking/status" crates/vel-cli/src/client.rs` returns client methods for all three endpoints.
- `rg -n "read_context|write_safe_actions|execute_repo_tasks" crates/vel-cli/src/commands/node.rs` returns all three scopes.</acceptance_criteria>
  <verify>
    <automated>cargo test -p vel-cli node -- --nocapture</automated>
  </verify>
  <done>The linking flow is operable and inspectable from the CLI even when the web shell is unavailable.</done>
</task>

<task type="auto">
  <name>Task 2: Document the shipped linking routes and CLI fallback</name>
  <files>docs/api/runtime.md</files>
  <read_first>docs/api/runtime.md
crates/veld/src/routes/linking.rs
crates/vel-cli/src/commands/node.rs
.planning/phases/05-now-inbox-core-and-project-substrate/05-CONTEXT.md</read_first>
  <action>Update `docs/api/runtime.md` so the implemented linking routes `POST /v1/linking/tokens`, `POST /v1/linking/redeem`, `GET /v1/linking/status`, and `POST /v1/linking/revoke/:node_id` are documented as shipped behavior. Add a CLI fallback section that includes the exact commands `vel node link issue`, `vel node link redeem`, and `vel node status`, and explicitly says the operator can inspect granted read/write/execute scopes from CLI output before trusting a link.</action>
  <acceptance_criteria>- `rg -n "POST /v1/linking/tokens|POST /v1/linking/redeem|GET /v1/linking/status|POST /v1/linking/revoke/:node_id" docs/api/runtime.md` returns all four route lines.
- `rg -n "vel node link issue|vel node link redeem|vel node status" docs/api/runtime.md` returns all three CLI commands.
- `rg -n "read/write/execute|granted scopes" docs/api/runtime.md` returns at least one trust-disclosure phrase.</acceptance_criteria>
  <verify>
    <automated>rg -n "vel node link issue|/v1/linking/status|granted scopes" docs/api/runtime.md</automated>
  </verify>
  <done>The runtime docs match the shipped linking routes and tell operators how to use the CLI fallback path.</done>
</task>

</tasks>

<verification>
Run focused CLI tests for the new `vel node` commands, then grep the runtime docs to confirm the route inventory and fallback commands are documented.
</verification>

<success_criteria>
Guided linking has an explicit non-web fallback path, and the repository docs describe the implemented linking surface truthfully.
</success_criteria>

<output>
After completion, create `.planning/phases/05-now-inbox-core-and-project-substrate/05-04-SUMMARY.md`
</output>
