---
phase: 15-incremental-core-migration-and-canonical-rust-service-seams
plan: 02
type: execute
wave: 1
depends_on:
  - 15-01
files_modified:
  - crates/vel-core/src/operator_queue.rs
  - crates/vel-storage/src/repositories/
  - crates/veld/src/services/
  - crates/veld/src/routes/now.rs
  - crates/vel-api-types/src/lib.rs
  - docs/product/operator-mode-policy.md
autonomous: true
requirements:
  - SERVICE-01
  - READMODEL-01
must_haves:
  truths:
    - "A backend-owned `check_in` seam exists without requiring full Phase 16 product logic."
    - "`check_in` semantics live in Rust layers, not in shell-specific code or route-local JSON."
    - "`Now` remains a consumer of the seam rather than the semantic owner."
    - "Escalation toward `Threads` is preserved as metadata or linkage, not hard-coded UI behavior."
---

<objective>
Introduce the first backend-owned `check_in` seam.

Purpose: Create the minimum core/storage/service/read-model path for `check_in` so context-repair behavior has a canonical Rust-owned landing zone before fuller product logic arrives in Phase 16.
Output: typed `check_in` domain and DTO support, persisted or queryable seam as needed, and one integration path through the existing `Now` read model.
</objective>
