---
phase: 08-coding-centric-supervised-execution-with-gsd-and-local-agents
plan: 06
type: execute
wave: 4
depends_on:
  - 08-02
  - 08-04
  - 08-05
files_modified:
  - crates/vel-agent-sdk/src/lib.rs
  - crates/veld/tests/agent_sdk.rs
  - crates/vel-cli/src/commands/exec.rs
  - crates/vel-cli/src/commands/connect.rs
  - docs/user/README.md
  - docs/user/coding-workflows.md
  - docs/api/runtime.md
  - docs/cognitive-agent-architecture/agents/coding-execution-contracts.md
  - clients/web/src/components/SettingsPage.tsx
autonomous: true
requirements:
  - GSD-01
  - GSD-02
  - HANDOFF-02
  - LOCAL-01
  - POLICY-01
must_haves:
  truths:
    - "The reference SDK and runtime docs describe the real shipped connect and handoff flow rather than historical stubs."
    - "Operators can follow one documented repo-local workflow from project execution context to GSD handoff to supervised runtime launch."
    - "Phase 8 closes with execution-backed samples and docs, not contract-only optimism."
  artifacts:
    - path: "docs/user/coding-workflows.md"
      provides: "operator-facing workflow for repo-local GSD artifacts, handoff review, and runtime launch"
      contains: "repo-local"
    - path: "crates/veld/tests/agent_sdk.rs"
      provides: "end-to-end SDK flow against the activated transport"
      contains: "sdk_flow"
    - path: "crates/vel-agent-sdk/src/lib.rs"
      provides: "reference SDK updated for the shipped transport and handoff closure"
      contains: "AgentSdkClient"
  key_links:
    - from: "crates/veld/tests/agent_sdk.rs"
      to: "crates/vel-agent-sdk/src/lib.rs"
      via: "reference SDK verified against live transport"
      pattern: "AgentSdkClient"
    - from: "docs/user/coding-workflows.md"
      to: "docs/api/runtime.md"
      via: "operator and runtime doc truth alignment"
      pattern: "connect|handoff|repo-local"
    - from: "crates/vel-cli/src/commands/exec.rs"
      to: "docs/user/coding-workflows.md"
      via: "documented CLI workflow"
      pattern: "exec"
---

<objective>
Close Phase 8 with SDK, repo-local GSD workflow docs, and execution-backed operator guidance.

Purpose: make the coding-execution path usable and reviewable by operators and external/local agent developers, not just technically present in the runtime.
Output: updated SDK/sample flow, docs for repo-local artifact and handoff usage, and final CLI/operator closure.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-RESEARCH.md
@.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-02-PLAN.md
@.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-04-PLAN.md
@.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-05-PLAN.md
@crates/vel-agent-sdk/src/lib.rs
@crates/veld/tests/agent_sdk.rs
@docs/cognitive-agent-architecture/agents/coding-execution-contracts.md

<interfaces>
From crates/veld/tests/agent_sdk.rs:
```rust
let sdk = AgentSdkClient::new(
    "node_sdk",
    "sdk_reference",
    "external_limb",
    "vel-agent-sdk-rust",
    "0.1.0",
);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update the reference SDK flow and end-to-end tests for the shipped transport</name>
  <files>crates/vel-agent-sdk/src/lib.rs, crates/veld/tests/agent_sdk.rs</files>
  <action>Extend the reference SDK and its integration coverage so it reflects the real shipped Phase 8 connect lifecycle, any new manifest/handoff inputs, and the explicit review/routing expectations from earlier slices. Keep the SDK focused on the stable contract and reference workflow rather than widening to provider-specific capabilities or hidden shortcuts.</action>
  <verify>
    <automated>cargo test -p veld agent_sdk -- --nocapture && cargo test -p vel-agent-sdk -- --nocapture</automated>
  </verify>
  <done>The reference SDK path exercises the live transport and remains aligned with the Phase 8 contract and review model.</done>
</task>

<task type="auto">
  <name>Task 2: Document and lightly surface the repo-local coding workflow end to end</name>
  <files>crates/vel-cli/src/commands/exec.rs, crates/vel-cli/src/commands/connect.rs, docs/user/README.md, docs/user/coding-workflows.md, docs/api/runtime.md, docs/cognitive-agent-architecture/agents/coding-execution-contracts.md, clients/web/src/components/SettingsPage.tsx</files>
  <action>Write operator-facing docs for the real Phase 8 path: project execution context, repo-local GSD sidecar export, explicit handoff review, local runtime launch, and guest-runtime caveats. Update runtime docs and architecture docs to match the shipped transport and policy model. Add only light operator-surface closure where it materially helps discoverability, such as linking execution status/help from existing settings surfaces, without turning this slice into a large UI redesign.</action>
  <verify>
    <automated>cargo test -p veld agent_sdk -- --nocapture && rg -n "repo-local|GSD|handoff|connect|writable|review" docs/user/README.md docs/user/coding-workflows.md docs/api/runtime.md docs/cognitive-agent-architecture/agents/coding-execution-contracts.md</automated>
  </verify>
  <done>Operators and SDK users can follow one documented, execution-backed workflow from project context to GSD handoff to supervised runtime execution.</done>
</task>

</tasks>

<verification>
Close the phase with end-to-end SDK validation and documentation truth checks so the shipped coding-execution workflow is usable, inspectable, and grounded in live commands.
</verification>

<success_criteria>
The reference SDK, operator docs, and light discoverability surfaces all align on one real repo-local supervised-execution workflow, and that workflow is backed by live transport/tests rather than historical stubs.
</success_criteria>

<output>
After completion, create `.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-06-SUMMARY.md`
</output>
