---
phase: 08-coding-centric-supervised-execution-with-gsd-and-local-agents
plan: 05
type: execute
wave: 3
depends_on:
  - 08-01
  - 08-03
files_modified:
  - crates/veld/src/services/mod.rs
  - crates/veld/src/services/sandbox.rs
  - crates/veld/src/services/wasm_guest_runtime.rs
  - crates/veld/src/services/connect_runtime.rs
  - crates/veld/tests/wasm_guest_runtime.rs
  - docs/cognitive-agent-architecture/agents/wasm-local-runtime.md
  - docs/api/runtime.md
autonomous: true
requirements:
  - EXEC-02
  - LOCAL-01
  - POLICY-01
must_haves:
  truths:
    - "Direct WASM guest execution reuses the same brokered capability, trace, and review boundaries as local runtimes."
    - "Guest modules cannot widen permissions, filesystem roots, or network scope after launch."
    - "Host-executor behavior remains a compatibility path, not a hidden bypass around guest-runtime policy."
  artifacts:
    - path: "crates/veld/src/services/wasm_guest_runtime.rs"
      provides: "direct guest-runtime execution and lifecycle mediation"
      min_lines: 220
    - path: "crates/veld/tests/wasm_guest_runtime.rs"
      provides: "integration coverage for guest approval and denial paths"
      contains: "wasm_guest"
    - path: "docs/cognitive-agent-architecture/agents/wasm-local-runtime.md"
      provides: "owner doc for guest-runtime policy and lifecycle"
      contains: "writable roots"
  key_links:
    - from: "crates/veld/src/services/wasm_guest_runtime.rs"
      to: "crates/veld/src/services/sandbox.rs"
      via: "shared policy and broker mediation"
      pattern: "SandboxCapabilityPolicy"
    - from: "crates/veld/src/services/wasm_guest_runtime.rs"
      to: "crates/veld/src/services/connect_runtime.rs"
      via: "connect lifecycle integration"
      pattern: "connect"
    - from: "docs/api/runtime.md"
      to: "docs/cognitive-agent-architecture/agents/wasm-local-runtime.md"
      via: "runtime/operator doc alignment"
      pattern: "wasm|guest|connect"
---

<objective>
Add the direct WASM guest runtime follow-on behind the same mediated connect boundary.

Purpose: close the re-scoped Phase 4 guest-runtime gap without creating a second capability or trace model.
Output: direct guest-runtime execution service, policy-enforced lifecycle integration, tests, and nearby docs.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-RESEARCH.md
@.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-03-PLAN.md
@docs/tickets/phase-4/010-wasm-agent-sandboxing.md
@crates/veld/src/services/sandbox.rs
@crates/veld/src/services/agent_protocol.rs
@crates/veld/src/services/connect_runtime.rs

<interfaces>
From crates/veld/src/services/sandbox.rs:
```rust
pub async fn execute_host_calls(
    state: &AppState,
    policy: &SandboxCapabilityPolicy,
    allowlist: &[CapabilityDescriptor],
    calls: &[SandboxHostCallEnvelope],
) -> Result<SandboxExecutionResult, AppError>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing guest-runtime tests for approved and denied execution paths</name>
  <files>crates/veld/tests/wasm_guest_runtime.rs</files>
  <action>Create failing integration tests covering: a guest module launched through connect with an approved bounded host call; denial of an out-of-scope capability request; and rejection of write-scope or network expansion beyond the manifest/policy. Assert trace/run linkage and terminal status on both approval and denial paths.</action>
  <verify>
    <automated>cargo test -p veld wasm_guest -- --nocapture</automated>
  </verify>
  <done>Phase 8 has red tests that lock the direct guest-runtime policy and lifecycle behavior before implementation lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement direct guest-runtime execution without bypassing broker or trace boundaries</name>
  <files>crates/veld/src/services/mod.rs, crates/veld/src/services/sandbox.rs, crates/veld/src/services/wasm_guest_runtime.rs, crates/veld/src/services/connect_runtime.rs, docs/cognitive-agent-architecture/agents/wasm-local-runtime.md, docs/api/runtime.md</files>
  <action>Create `crates/veld/src/services/wasm_guest_runtime.rs` as the direct guest-runtime seam, wire it into connect-runtime lifecycle handling, and ensure guest execution still flows through the same sandbox/broker mediation and trace/run events already used by host calls. Keep any host-executor fallback explicit and documented as compatibility behavior. Document guest-runtime policy, writable-root rules, and denial expectations close to the code and in runtime docs.</action>
  <verify>
    <automated>cargo test -p veld wasm_guest -- --nocapture && cargo test -p veld sandbox -- --nocapture</automated>
  </verify>
  <done>Vel can run a direct guest runtime under the same explicit policy and trace model as other supervised execution paths.</done>
</task>

</tasks>

<verification>
Run dedicated guest-runtime tests plus the existing sandbox checks so the direct guest path proves it does not bypass the mediated execution baseline.
</verification>

<success_criteria>
Direct guest-runtime execution is now available through the supervised connect boundary, but it remains deny-by-default, broker-mediated, and trace-linked rather than becoming a privileged escape hatch.
</success_criteria>

<output>
After completion, create `.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-05-SUMMARY.md`
</output>
