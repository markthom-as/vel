---
phase: 17-shell-embodiment-operator-mode-application-and-surface-simplification
plan: 03
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - clients/web/src/components/ProjectsView.tsx
  - clients/web/src/components/ProjectsView.test.tsx
  - clients/web/src/components/SettingsPage.tsx
  - clients/web/src/components/SettingsPage.test.tsx
  - clients/web/src/components/StatsView.tsx
  - clients/web/src/components/SuggestionsView.tsx
autonomous: true
requirements:
  - SHELL-MODE-02
  - TRUST-SUMMARY-01
must_haves:
  truths:
    - "`Projects` remains useful but no longer competes with daily-use triage for top-level attention."
    - "`Settings` expresses advanced operator setup and trust inspection before runtime internals."
    - "Trust and onboarding cues stay summary-first, with raw implementation detail progressively disclosed."
    - "Support/detail surfaces no longer leak as first-contact shell categories."
  artifacts:
    - path: clients/web/src/components/ProjectsView.tsx
      provides: secondary project drill-down embodiment
    - path: clients/web/src/components/SettingsPage.tsx
      provides: advanced/operator versus runtime/internal disclosure boundary
    - path: clients/web/src/components/StatsView.tsx
      provides: passive observability drill-down rather than peer navigation destination
  key_links:
    - from: clients/web/src/components/SettingsPage.tsx
      to: existing operator queries
      via: summary-first trust and onboarding guidance
      pattern: buildSettingsOnboardingGuide|buildBackupTrustProjection|buildOperatorReviewStatus
---

<objective>
Apply the approved secondary and advanced disclosure policy across the remaining web support surfaces.

Purpose: Prevent `Projects`, `Settings`, trust, and passive detail surfaces from diluting the daily-use shell while keeping them real, reachable, and aligned to existing typed backend state.
Output: one web embodiment slice for projects, settings, trust/setup, and passive detail views.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-shell-embodiment-operator-mode-application-and-surface-simplification/17-CONTEXT.md
@docs/product/operator-surface-taxonomy.md
@docs/product/operator-mode-policy.md
@docs/product/onboarding-and-trust-journeys.md
@clients/web/src/components/ProjectsView.tsx
@clients/web/src/components/SettingsPage.tsx
@clients/web/src/components/StatsView.tsx
@clients/web/src/components/SuggestionsView.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Reframe `Projects` as contextual drill-down, not daily-use center of gravity</name>
  <files>clients/web/src/components/ProjectsView.tsx, clients/web/src/components/ProjectsView.test.tsx</files>
  <behavior>
    - Test 1: project detail still exposes durable project identity and local roots
    - Test 2: the view reads as contextual support for work and project-owned actions rather than a rival inbox/dashboard
  </behavior>
  <action>Update `ProjectsView.tsx` to embody the approved secondary role for projects: keep project identity, durable roots, and project-owned action context visible, but remove any peer-destination framing that competes with `Inbox` or `Now`. Preserve typed project data and the bounded create-from-existing handoff instead of inventing unsupported mutation flows.</action>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/ProjectsView.test.tsx</automated>
  </verify>
  <done>`Projects` remains a real shell surface, but it now reads as context and drill-down rather than a co-equal daily-use destination.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply progressive disclosure to Settings, trust, and passive detail surfaces</name>
  <files>clients/web/src/components/SettingsPage.tsx, clients/web/src/components/SettingsPage.test.tsx, clients/web/src/components/StatsView.tsx, clients/web/src/components/SuggestionsView.tsx</files>
  <behavior>
    - Test 1: `Settings` foregrounds guided operator actions and summary trust/setup state ahead of deep runtime detail
    - Test 2: Stats and suggestion detail remain available without being taught as first-contact shell categories
  </behavior>
  <action>Refactor the web advanced-support lane so `SettingsPage.tsx` explicitly separates advanced operator workflows from internal/runtime inspection. Keep trust, onboarding, linking, and freshness summary-first, then progressively disclose deeper controls. Rework `StatsView.tsx` and `SuggestionsView.tsx` so they operate as drill-down/detail views from the embodied shell rather than peer daily-use destinations. Do not move logic out of backend-owned typed projections.</action>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/SettingsPage.test.tsx</automated>
  </verify>
  <done>The web shell now distinguishes daily use, advanced operator work, and passive/internal detail without losing access to existing typed surfaces.</done>
</task>

</tasks>

<verification>
Run the targeted Settings and Projects tests, then confirm the remaining support views are classified as contextual or advanced rather than peer daily-use destinations.
</verification>

<success_criteria>
- `Projects` remains secondary but useful.
- `Settings` is no longer treated as one undifferentiated surface.
- Trust, onboarding, and setup are summary-first in the embodied web shell.
</success_criteria>

<output>
After completion, create `.planning/phases/17-shell-embodiment-operator-mode-application-and-surface-simplification/17-03-SUMMARY.md`
</output>
