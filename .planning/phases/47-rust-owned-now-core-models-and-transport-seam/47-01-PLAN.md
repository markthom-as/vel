---
phase: 47-rust-owned-now-core-models-and-transport-seam
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - docs/cognitive-agent-architecture/architecture/now-surface-rust-core-contract.md
  - crates/vel-api-types/src/lib.rs
  - clients/web/src/types.ts
  - clients/web/src/types.test.ts
  - clients/apple/VelAPI/Sources/VelAPI/Models.swift
autonomous: true
requirements:
  - NOW-02
  - CORE-01
  - CORE-02
must_haves:
  truths:
    - "The canonical Now transport seam explicitly carries header, status-row, context-line, nudge-stack, task-lane, and docked-input routing DTOs."
    - "The shared DTO contract names bucket categories, intent taxonomy, and continuity markers in Rust-owned transport rather than shell-local labels."
    - "Web and Apple boundary types are updated in the same slice when the wire contract changes."
  artifacts:
    - path: "crates/vel-api-types/src/lib.rs"
      provides: "Canonical transport DTOs for the Rust-owned Now seam."
      contains: "Now"
    - path: "clients/web/src/types.ts"
      provides: "Typed web boundary for the canonical Now transport seam."
      contains: "Now"
    - path: "clients/apple/VelAPI/Sources/VelAPI/Models.swift"
      provides: "Apple transport boundary for the canonical Now seam."
      contains: "Now"
---

<objective>
Define the canonical shared Now DTO contract before deeper service assembly work.

Purpose: Give every client one portable transport vocabulary for header buckets, status row, one-liner, nudge bars, task subset, thread escalation markers, and docked-input routing.
Output: aligned Rust DTOs plus web and Apple boundary updates and decoder coverage.
</objective>

<context>
@.planning/phases/47-rust-owned-now-core-models-and-transport-seam/47-CONTEXT.md
@.planning/phases/46-canonical-now-contract-boundaries-and-milestone-lock/46-SUBSYSTEM-INVENTORY.md
@docs/product/now-surface-canonical-contract.md
@docs/cognitive-agent-architecture/architecture/now-surface-rust-core-contract.md
@crates/vel-api-types/src/lib.rs
@clients/web/src/types.ts
@clients/web/src/types.test.ts
@clients/apple/VelAPI/Sources/VelAPI/Models.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish the canonical Now DTO and cross-client boundary types</name>
  <files>docs/cognitive-agent-architecture/architecture/now-surface-rust-core-contract.md, crates/vel-api-types/src/lib.rs, clients/web/src/types.ts, clients/web/src/types.test.ts, clients/apple/VelAPI/Sources/VelAPI/Models.swift</files>
  <read_first>docs/cognitive-agent-architecture/architecture/now-surface-rust-core-contract.md
crates/vel-api-types/src/lib.rs
clients/web/src/types.ts
clients/web/src/types.test.ts
clients/apple/VelAPI/Sources/VelAPI/Models.swift</read_first>
  <action>Add explicit DTOs for the canonical `Now` contract: header bucket data, status-row data, context-line data, nudge/action-bar data, task-lane data, thread-escalation/open-target data, and docked-input routing/intent data. Keep the new seam Rust-owned and typed, align the architecture doc if naming or decomposition becomes clearer during the change, and update web plus Apple boundary models in the same slice. Add focused type tests in web for the new transport shape.</action>
  <acceptance_criteria>- `rg -n "Header|Bucket|StatusRow|ContextLine|Nudge|TaskLane|DockedInput|Intent" crates/vel-api-types/src/lib.rs` returns the new canonical transport types.
- `rg -n "Now.*header|Now.*status|Now.*nudge|Now.*task|Now.*dock" clients/web/src/types.ts clients/apple/VelAPI/Sources/VelAPI/Models.swift` returns aligned boundary types.
- `rg -n "header|status|nudge|task|dock" clients/web/src/types.test.ts` returns decoder coverage for the canonical seam.</acceptance_criteria>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/types.test.ts</automated>
  </verify>
  <done>The canonical Now DTO contract is explicit, shared, and consumed by web and Apple boundary code.</done>
</task>

</tasks>

<verification>
Confirm the Rust DTO layer now names the portable Now seam explicitly enough that later client phases can embody it without inventing product behavior locally.
</verification>

<success_criteria>
Phase 47 starts from one explicit shared transport contract for canonical Now behavior.
</success_criteria>

<output>
After completion, create `.planning/phases/47-rust-owned-now-core-models-and-transport-seam/47-01-SUMMARY.md`
</output>
