---
phase: 41-rust-owned-overview-commitment-flow-and-orientation-core
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - crates/veld/src/services/now.rs
  - crates/veld/src/routes/now.rs
  - crates/vel-api-types/src/lib.rs
  - clients/web/src/types.ts
  - clients/web/src/types.test.ts
autonomous: true
requirements:
  - MVP-01
  - ORIENT-01
must_haves:
  truths:
    - "Now/overview exposes one backend-owned current-day read model rather than scattered shell-derived state."
    - "The overview contract preserves action + timeline, dominant action, visible nudge, Why + state, and 1-3 suggestions."
    - "Web consumes the new overview fields through typed transport decoding."
  artifacts:
    - path: "crates/veld/src/services/now.rs"
      provides: "Canonical overview assembly over the current-day truth seam."
      contains: "overview"
    - path: "crates/vel-api-types/src/lib.rs"
      provides: "Transport DTO for the backend-owned overview block."
      contains: "Overview"
    - path: "clients/web/src/types.ts"
      provides: "Typed web decoder for the overview contract."
      contains: "overview"
---

<objective>
Implement the canonical backend-owned overview read model and current-day truth seam.

Purpose: Make `Now` the live embodiment of the Phase 40 overview contract instead of a legacy aggregate that shells reinterpret locally.
Output: Rust-owned overview fields in the `Now` service and transport DTOs, with web decoding aligned.
</objective>

<context>
@.planning/phases/41-rust-owned-overview-commitment-flow-and-orientation-core/41-CONTEXT.md
@docs/product/mvp-operator-loop.md
@docs/cognitive-agent-architecture/architecture/mvp-loop-contracts.md
@crates/veld/src/services/now.rs
@crates/veld/src/routes/now.rs
@crates/vel-api-types/src/lib.rs
@clients/web/src/types.ts
@clients/web/src/types.test.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add the canonical overview block to the backend `Now` seam and typed web transport</name>
  <files>crates/veld/src/services/now.rs, crates/veld/src/routes/now.rs, crates/vel-api-types/src/lib.rs, clients/web/src/types.ts, clients/web/src/types.test.ts</files>
  <read_first>crates/veld/src/services/now.rs
crates/veld/src/routes/now.rs
crates/vel-api-types/src/lib.rs
clients/web/src/types.ts
clients/web/src/types.test.ts
docs/product/mvp-operator-loop.md
docs/cognitive-agent-architecture/architecture/mvp-loop-contracts.md</read_first>
  <action>Add a backend-owned `overview` block to the `Now` service output and wire it through `routes/now.rs`, `vel-api-types`, and web type decoding. The overview contract must carry fields for the locked Phase 40 behavior: `action + timeline`, dominant action, compact timeline, visible nudge, `Why + state`, and a 1-3 suggestion fallback when no dominant action exists. Reuse existing `Now` inputs and ranked backend-owned state rather than inventing a new endpoint or shell-side synthesis. Add or update focused type/decoder tests in `clients/web/src/types.test.ts` for the new overview block.</action>
  <acceptance_criteria>- `rg -n "overview" crates/veld/src/services/now.rs crates/veld/src/routes/now.rs crates/vel-api-types/src/lib.rs clients/web/src/types.ts` returns matches in all four files.
- `rg -n "dominant|timeline|nudge|why|state|suggest" crates/vel-api-types/src/lib.rs clients/web/src/types.ts` returns overview-contract field names.
- `rg -n "overview" clients/web/src/types.test.ts` returns at least one decoder test covering the new block.</acceptance_criteria>
  <verify>
    <automated>cargo test -p veld now_service_output_maps_to_existing_now_dto_shape -- --nocapture && npm --prefix clients/web test -- --run src/types.test.ts</automated>
  </verify>
  <done>The backend owns one canonical overview block and web decodes it without local reinterpretation.</done>
</task>

</tasks>

<verification>
Confirm the new `overview` block is assembled in Rust, exposed through the existing `Now` route, and decoded in web without a second shell-owned overview model.
</verification>

<success_criteria>
Phase 41 starts with one canonical Rust-owned overview read model over the current-day truth seam.
</success_criteria>

<output>
After completion, create `.planning/phases/41-rust-owned-overview-commitment-flow-and-orientation-core/41-01-SUMMARY.md`
</output>
