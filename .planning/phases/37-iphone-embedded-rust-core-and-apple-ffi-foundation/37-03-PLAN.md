# 37-03 Plan

## Slice

Route the highest-value iPhone local flows through the embedded seam without forking product logic from the canonical Rust core.

## Work

- move a bounded set of high-frequency local flows onto the embedded seam
- preserve the same Rust-owned domain semantics across daemon-backed and embedded-capable paths
- keep Swift focused on shell embodiment, queue UX, and permissions

## Verification

- focused Apple validation over the embedded-capable slice
- regression checks that daemon-backed behavior still works when embedded is unavailable

## Exit

The first iPhone local flows use the embedded seam materially, but the product model is still one Rust-owned system.
