# Phase 89 Plan

## Objective

Close the `0.5.4` implementation line with browser proof, cleanup evidence, and honest milestone closeout.

## Scope

### In scope

- browser proofs for:
  - `Now` normal
  - `Now` degraded
  - `Threads` normal
  - `Threads` focused block
  - `System` integrations issue
  - `System` control view
- proof artifacts written into the phase evidence directory
- cleanup notes for retained temporary wrappers and seams
- milestone summary and verification updates

### Out of scope

- new UI implementation beyond what proof uncovers
- generalized screenshot-regression infrastructure

## Implementation Slice

1. Add a dedicated proof script for the six required browser flows.
2. Capture screenshots, DOM/summary JSON, and evidence notes for each flow.
3. Record the remaining compatibility wrappers and their removal path.
4. Close roadmap/state and milestone docs honestly against the delivered evidence.

## Verification

- run the Phase 89 proof script
- rerun focused web tests
- rerun production web build
