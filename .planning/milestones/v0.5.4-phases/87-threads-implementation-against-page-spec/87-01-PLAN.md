# Phase 87 Plan

## Objective

Rebuild `Threads` into the approved object/context-first continuity surface.

## Scope

### In scope

- supporting thread rail with sticky filters
- context-first thread header/shelf
- continuity stream that keeps chronology secondary to context
- shared review surface for provenance and traces
- run/config/system-like blocks visually distinct from generic messages

### Out of scope

- `System`-style config browsing
- full focus-mode routing for every block type
- broad provenance redesign beyond the shared review surface

## Implementation slice

1. Replace the old chat-first split with:
   - left supporting rail
   - context shelf
   - continuity stream
   - review/detail surface
2. Keep filters sticky and selection stable.
3. Preserve message rendering and provenance drawer integration.
4. Reframe bound-object/context copy so `Threads` reads as continuity, not detached chat.

## Verification

- targeted `ThreadView`, neighboring `Now`/`System`, and `MainPanel` tests
- production web build
