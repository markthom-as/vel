---
phase: 41-rust-owned-overview-commitment-flow-and-orientation-core
plan: 03
type: execute
wave: 2
depends_on:
  - 41-01
  - 41-02
files_modified:
  - crates/veld/src/services/now.rs
  - crates/veld/src/services/operator_queue.rs
  - crates/vel-api-types/src/lib.rs
  - clients/web/src/components/NowView.tsx
  - clients/web/src/components/NowView.test.tsx
autonomous: true
requirements:
  - ORIENT-01
  - ORIENT-02
  - ORIENT-03
must_haves:
  truths:
    - "Suggestions and nudges are grounded in persisted backend state and explainable from Rust-owned rules."
    - "Only one top nudge is visible by default in the overview embodiment."
    - "Orientation output emphasizes action and intervention over status clutter."
  artifacts:
    - path: "crates/veld/src/services/now.rs"
      provides: "Rust-owned orientation and nudge assembly."
      contains: "nudge"
    - path: "crates/veld/src/services/operator_queue.rs"
      provides: "Grounded ranking/support inputs for intervention pressure."
      contains: "ActionItem"
    - path: "clients/web/src/components/NowView.tsx"
      provides: "Web embodiment of the backend-owned one-nudge overview."
      contains: "overview"
---

<objective>
Normalize suggestions and nudges as explainable backend-owned orientation outputs.

Purpose: Turn intervention pressure and orientation into a Rust-owned lane that shells embody without local ranking or dashboard sprawl.
Output: backend-owned orientation output plus web embodiment/tests for the one-nudge overview posture.
</objective>

<context>
@.planning/phases/41-rust-owned-overview-commitment-flow-and-orientation-core/41-CONTEXT.md
@docs/product/mvp-operator-loop.md
@docs/cognitive-agent-architecture/architecture/mvp-loop-contracts.md
@crates/veld/src/services/now.rs
@crates/veld/src/services/operator_queue.rs
@crates/vel-api-types/src/lib.rs
@clients/web/src/components/NowView.tsx
@clients/web/src/components/NowView.test.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make orientation outputs backend-owned and keep the overview one-nudge by default</name>
  <files>crates/veld/src/services/now.rs, crates/veld/src/services/operator_queue.rs, crates/vel-api-types/src/lib.rs, clients/web/src/components/NowView.tsx, clients/web/src/components/NowView.test.tsx</files>
  <read_first>crates/veld/src/services/now.rs
crates/veld/src/services/operator_queue.rs
crates/vel-api-types/src/lib.rs
clients/web/src/components/NowView.tsx
clients/web/src/components/NowView.test.tsx
docs/product/mvp-operator-loop.md
docs/cognitive-agent-architecture/architecture/mvp-loop-contracts.md</read_first>
  <action>Normalize orientation output so the backend decides the dominant action, the visible nudge, and the explanation payloads, while web embodies that output without local ranking logic. Keep only one top nudge visible by default, surface `Why + state` as explicit backend-owned data, and adjust `NowView` so the overview emphasizes what to do next and what needs intervention rather than dashboard clutter. Add or update focused `NowView` tests for the one-nudge, suggestion-fallback, and explanation affordance behavior.</action>
  <acceptance_criteria>- `rg -n "nudge|suggest|why|state|overview" crates/veld/src/services/now.rs crates/vel-api-types/src/lib.rs` returns orientation-contract wiring.
- `rg -n "nudge|suggest|why|state" clients/web/src/components/NowView.tsx` returns the overview embodiment logic.
- `rg -n "nudge|suggest|why|state" clients/web/src/components/NowView.test.tsx` returns focused coverage for the new behavior.</acceptance_criteria>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/NowView.test.tsx</automated>
  </verify>
  <done>Orientation output is backend-owned, explainable, and embodied in web without local ranking drift.</done>
</task>

</tasks>

<verification>
Confirm the one-nudge default and suggestion fallback are decided in Rust-owned outputs and merely rendered by the web shell.
</verification>

<success_criteria>
Phase 41 makes orientation and intervention pressure a backend-owned lane instead of a shell-derived dashboard.
</success_criteria>

<output>
After completion, create `.planning/phases/41-rust-owned-overview-commitment-flow-and-orientation-core/41-03-SUMMARY.md`
</output>
