---
phase: 41-rust-owned-overview-commitment-flow-and-orientation-core
plan: 02
type: execute
wave: 1
depends_on:
  - 41-01
files_modified:
  - crates/veld/src/services/daily_loop.rs
  - crates/veld/src/services/check_in.rs
  - crates/veld/src/services/now.rs
  - crates/vel-api-types/src/lib.rs
  - clients/apple/VelAPI/Sources/VelAPI/Models.swift
autonomous: true
requirements:
  - MVP-02
must_haves:
  truths:
    - "Commitment flow and session continuity are backend-owned rather than reconstructed per shell."
    - "The inline action set remains accept/defer/choose/close across transport seams."
    - "Apple transport models consume the same bounded commitment/session semantics."
  artifacts:
    - path: "crates/veld/src/services/daily_loop.rs"
      provides: "Backend-owned commitment/session continuity."
      contains: "DailyLoop"
    - path: "crates/vel-api-types/src/lib.rs"
      provides: "Transport DTO updates for commitment/session continuity."
      contains: "DailyLoopSessionData"
    - path: "clients/apple/VelAPI/Sources/VelAPI/Models.swift"
      provides: "Apple transport model parity for the updated commitment/session contract."
      contains: "DailyLoopSessionData"
---

<objective>
Close the Rust-owned commitment flow and session continuity lane.

Purpose: Keep commitment decisions and active-session continuity in backend services and shared transport contracts rather than letting shells derive their own commitment state.
Output: tightened backend session/commitment behavior and Apple transport parity.
</objective>

<context>
@.planning/phases/41-rust-owned-overview-commitment-flow-and-orientation-core/41-CONTEXT.md
@docs/product/mvp-operator-loop.md
@docs/cognitive-agent-architecture/architecture/mvp-loop-contracts.md
@crates/veld/src/services/daily_loop.rs
@crates/veld/src/services/check_in.rs
@crates/veld/src/services/now.rs
@crates/vel-api-types/src/lib.rs
@clients/apple/VelAPI/Sources/VelAPI/Models.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Tighten backend commitment/session continuity and carry it through transport</name>
  <files>crates/veld/src/services/daily_loop.rs, crates/veld/src/services/check_in.rs, crates/veld/src/services/now.rs, crates/vel-api-types/src/lib.rs, clients/apple/VelAPI/Sources/VelAPI/Models.swift</files>
  <read_first>crates/veld/src/services/daily_loop.rs
crates/veld/src/services/check_in.rs
crates/veld/src/services/now.rs
crates/vel-api-types/src/lib.rs
clients/apple/VelAPI/Sources/VelAPI/Models.swift
docs/product/mvp-operator-loop.md
docs/cognitive-agent-architecture/architecture/mvp-loop-contracts.md</read_first>
  <action>Tighten the existing daily-loop and check-in seams so commitment/session continuity is explicitly backend-owned and transportable. Preserve the bounded inline commitment choices `accept / defer / choose / close`; ensure `Now` can surface the active commitment/session state without shell-local reconstruction; and update transport models so Apple consumes the same session/commitment semantics. Keep scope narrow: do not widen into broad thread/tool work or broad reflow implementation.</action>
  <acceptance_criteria>- `rg -n "accept|defer|choose|close" crates/veld/src/services/daily_loop.rs crates/veld/src/services/check_in.rs crates/vel-api-types/src/lib.rs` returns commitment-choice handling or transport vocabulary.
- `rg -n "DailyLoopSessionData|check_in|daily_loop" crates/vel-api-types/src/lib.rs clients/apple/VelAPI/Sources/VelAPI/Models.swift` returns matching transport seams.
- `rg -n "check_in|daily_loop" crates/veld/src/services/now.rs` returns the backend-owned continuity integration points.</acceptance_criteria>
  <verify>
    <automated>cargo test -p veld daily_loop -- --nocapture</automated>
  </verify>
  <done>Commitment flow and active-session continuity are backend-owned and shared through typed transport seams.</done>
</task>

</tasks>

<verification>
Confirm the same commitment/session continuity can be consumed from backend transport by multiple shells without shell-specific decision state.
</verification>

<success_criteria>
Phase 41 closes the bounded commitment lane over one backend-owned session and transport model.
</success_criteria>

<output>
After completion, create `.planning/phases/41-rust-owned-overview-commitment-flow-and-orientation-core/41-02-SUMMARY.md`
</output>
