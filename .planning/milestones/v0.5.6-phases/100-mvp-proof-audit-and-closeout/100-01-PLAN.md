# Phase 100 Plan

## Objective

Close `0.5.6` honestly by proving the single-node MVP works in the accepted environment and auditing remaining work directly against the locked feedback and phase packets.

## Proof Checklist

1. Core onboarding and gating proof
   Status: partially verified through automated web tests; manual Chrome proof still pending.
   Check:
   - onboarding nudge appears when Core setup is incomplete
   - Core settings can complete the minimum setup path
    - Core settings persist the operator's client location label
   - Core settings can auto-set client location from browser permission through the accepted `System` flow
   - composer/task bar is disabled before setup and enabled after setup
   - developer override behaves as intended

2. Provider and multimodal proof
   Status: blocked on live provider/runtime proof.
   Check:
   - local `llama.ccp` works through the supported MVP chat path
   - OpenAI works through API key and OAuth setup paths
   - file/image input works through the accepted thread/composer flow
   - voice input works
   - thread-level call mode works through STT plus TTS over the normal assistant flow

3. Google and Todoist proof
   Status: blocked on live integration proof.
   Check:
   - connect/reconnect/edit flows are truthful in `System`
   - Google read/write behavior is proven through the accepted MVP path
   - Todoist create/edit/complete/reopen/day-assignment truth is proven through the accepted MVP path
   - `Now` reflects current-day event/task truth after these changes

4. Surface proof
   Status: automated evidence gathered; manual Chrome proof still pending.
   Check:
   - `Now` is acceptable in desktop Chrome, including today-plus-overdue handling and drag-to-commit
   - `Threads` is acceptable in desktop Chrome, including last-thread/empty-state behavior and call-mode entry
   - `System` is acceptable in desktop Chrome, including Core settings, integrations, unavailable-services collapse, and developer-mode disclosure

5. Acceptance audit and deferred list
   Status: audit complete; milestone close remains blocked.
   Check:
   - audit directly against [00-FEEDBACK-TODO.md](/home/jove/code/vel/.planning/milestones/v0.5.6-single-node-mvp-polished-web-ui/00-FEEDBACK-TODO.md)
   - audit directly against the locked MVP must-pass flows
   - any unfinished work is moved into explicit deferred scope with a reason
   - milestone is closed only if the operator-defined MVP bar is actually met

## Verification

- desktop-Chrome manual QA is the primary closeout authority
- targeted automated checks remain supporting evidence, not substitute acceptance
- audit notes should distinguish complete, partial, blocked, and deferred work explicitly

## Success Criteria

1. The milestone has direct evidence for onboarding, providers, integrations, `Now`, `Threads`, and `System`.
2. The closeout decision is tied to the locked MVP bar rather than a vague “good enough” impression.
3. Any deferred work is explicit and bounded enough to seed the next milestone cleanly.
