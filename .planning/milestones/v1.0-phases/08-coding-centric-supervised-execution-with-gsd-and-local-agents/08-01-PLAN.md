---
phase: 08-coding-centric-supervised-execution-with-gsd-and-local-agents
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/vel-core/src/lib.rs
  - crates/vel-core/src/execution.rs
  - crates/vel-core/src/run.rs
  - crates/vel-api-types/src/lib.rs
  - crates/vel-protocol/src/lib.rs
  - config/contracts-manifest.json
  - config/schemas/project-execution-context.schema.json
  - config/examples/project-execution-context.example.json
  - config/schemas/execution-handoff.schema.json
  - config/examples/execution-handoff.example.json
  - config/schemas/local-agent-manifest.schema.json
  - config/examples/local-agent-manifest.example.json
  - docs/cognitive-agent-architecture/agents/coding-execution-contracts.md
  - docs/cognitive-agent-architecture/agents/handoffs.md
autonomous: true
requirements:
  - EXEC-01
  - GSD-01
  - HANDOFF-01
  - HANDOFF-02
  - LOCAL-01
  - POLICY-01
must_haves:
  truths:
    - "Phase 8 begins with one typed contract set for project execution context, execution handoffs, routing policy inputs, and local-agent manifests."
    - "Write scopes, review gates, token budget classes, and agent profiles are explicit fields in the contract, not implied behavior."
    - "Repo-local GSD integration starts from durable sidecar artifacts and typed handoff context rather than a bespoke planner transport."
  artifacts:
    - path: "crates/vel-core/src/execution.rs"
      provides: "canonical Phase 8 execution-context and routing-policy vocabulary"
      min_lines: 180
    - path: "crates/vel-api-types/src/lib.rs"
      provides: "transport DTOs for execution context, handoffs, and local-agent manifests"
      contains: "pub struct ProjectExecutionContextData"
    - path: "config/schemas/project-execution-context.schema.json"
      provides: "machine-readable project execution context contract"
      contains: "\"ProjectExecutionContext\""
    - path: "config/schemas/local-agent-manifest.schema.json"
      provides: "machine-readable local-agent manifest contract"
      contains: "\"LocalAgentManifest\""
  key_links:
    - from: "crates/vel-core/src/execution.rs"
      to: "crates/vel-api-types/src/lib.rs"
      via: "DTO mapping boundary"
      pattern: "ProjectExecutionContextData|ExecutionHandoffData|LocalAgentManifestData"
    - from: "crates/vel-core/src/run.rs"
      to: "docs/cognitive-agent-architecture/agents/handoffs.md"
      via: "trace-linked handoff contract alignment"
      pattern: "HandoffEnvelope|trace_id|write_scopes"
    - from: "config/contracts-manifest.json"
      to: "config/schemas/project-execution-context.schema.json"
      via: "contracts manifest entry"
      pattern: "project-execution-context.schema.json"
---

<objective>
Publish the typed Phase 8 execution contracts before implementation widens.

Purpose: lock down repo-aware execution context, explicit handoff semantics, local-agent manifest shape, and routing-policy inputs before connect transport or runtime code expands.
Output: canonical core types, matching transport DTOs, schemas/examples, and owner docs for the coding-execution boundary.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-RESEARCH.md
@docs/cognitive-agent-architecture/agents/handoffs.md
@docs/cognitive-agent-architecture/cognition/self-awareness-and-supervised-self-modification.md
@docs/tickets/phase-2/006-connect-launch-protocol.md
@docs/tickets/phase-4/010-wasm-agent-sandboxing.md
@docs/tickets/phase-4/014-swarm-execution-sdk.md
@crates/vel-core/src/project.rs
@crates/vel-core/src/run.rs
@crates/vel-protocol/src/lib.rs

<interfaces>
From crates/vel-core/src/project.rs:
```rust
pub struct ProjectRecord {
    pub primary_repo: ProjectRootRef,
    pub primary_notes_root: ProjectRootRef,
}
```

From crates/vel-core/src/run.rs:
```rust
pub struct HandoffEnvelope {
    pub objective: String,
    pub read_scopes: Vec<String>,
    pub write_scopes: Vec<String>,
    pub expected_output_schema: serde_json::Value,
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish canonical execution, routing, and handoff contracts in core/transport/protocol layers</name>
  <files>crates/vel-core/src/lib.rs, crates/vel-core/src/execution.rs, crates/vel-core/src/run.rs, crates/vel-api-types/src/lib.rs, crates/vel-protocol/src/lib.rs</files>
  <action>Create `crates/vel-core/src/execution.rs` with typed contracts for `ProjectExecutionContext`, `RepoWorktreeRef`, `ExecutionTaskKind`, `AgentProfile`, `TokenBudgetClass`, `ExecutionPolicyInput`, `ExecutionHandoff`, `ExecutionReviewGate`, and `LocalAgentManifest`. Extend the existing handoff contract only where Phase 8 needs stable repo-aware fields rather than inventing parallel envelope types. Add matching DTOs in `crates/vel-api-types/src/lib.rs` and any necessary protocol payload or manifest-link fields in `crates/vel-protocol/src/lib.rs`. Keep write scopes, review gates, and capability scopes explicit.</action>
  <verify>
    <automated>cargo test -p vel-core execution -- --nocapture && cargo test -p vel-api-types execution -- --nocapture && cargo test -p vel-protocol -- --nocapture</automated>
  </verify>
  <done>Vel has one typed vocabulary for project execution context, handoff semantics, routing-policy inputs, and local-agent manifests.</done>
</task>

<task type="auto">
  <name>Task 2: Add schema/example assets and owner docs for the new execution boundary</name>
  <files>config/contracts-manifest.json, config/schemas/project-execution-context.schema.json, config/examples/project-execution-context.example.json, config/schemas/execution-handoff.schema.json, config/examples/execution-handoff.example.json, config/schemas/local-agent-manifest.schema.json, config/examples/local-agent-manifest.example.json, docs/cognitive-agent-architecture/agents/coding-execution-contracts.md, docs/cognitive-agent-architecture/agents/handoffs.md</files>
  <action>Create schema/example assets for project execution context, execution handoff, and local-agent manifest contracts; register them in `config/contracts-manifest.json`; and write owner docs in `docs/cognitive-agent-architecture/agents/coding-execution-contracts.md`. Update `docs/cognitive-agent-architecture/agents/handoffs.md` so the repo-aware coding handoff rules explicitly cover project IDs, repo roots, writable roots, token budget classes, and review gates. State clearly that repo-local GSD sidecars are inputs to supervised execution, not blanket permission grants.</action>
  <verify>
    <automated>node scripts/verify-repo-truth.mjs && cargo test -p vel-config -- --nocapture</automated>
  </verify>
  <done>Every new Phase 8 contract has machine-readable assets and nearby ownership docs before runtime implementation begins.</done>
</task>

</tasks>

<verification>
Run focused contract/schema tests first so Phase 8 starts from one governed execution boundary that later plans can build on safely.
</verification>

<success_criteria>
The repository has one explicit, machine-readable, documented contract set for coding execution context, repo-aware handoffs, and local-agent manifests, and that contract set makes write scope and review gates non-optional.
</success_criteria>

<output>
After completion, create `.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-01-SUMMARY.md`
</output>
