---
phase: 08-coding-centric-supervised-execution-with-gsd-and-local-agents
plan: 03
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - crates/veld/src/app.rs
  - crates/veld/src/routes/connect.rs
  - crates/veld/src/routes/mod.rs
  - crates/veld/src/services/connect_runtime.rs
  - crates/veld/src/services/agent_protocol.rs
  - crates/veld/src/services/mod.rs
  - crates/veld/tests/connect_runtime.rs
  - crates/vel-cli/src/client.rs
  - crates/vel-cli/src/commands/connect.rs
  - docs/api/runtime.md
autonomous: true
requirements:
  - EXEC-02
  - LOCAL-01
  - POLICY-01
must_haves:
  truths:
    - "The live `/v1/connect` transport becomes operator-authenticated and lifecycle-complete rather than staying a deny-all reservation."
    - "Local coding runtimes launch from explicit manifests and capability allowlists with lease supervision, inspectability, and termination."
    - "Undefined routes, unsupported runtime kinds, and write-scope expansion requests fail closed."
  artifacts:
    - path: "crates/veld/src/routes/connect.rs"
      provides: "thin authenticated connect route handlers"
      contains: "launch_connect_runtime"
    - path: "crates/veld/src/services/connect_runtime.rs"
      provides: "supervised local runtime launch/list/terminate lifecycle"
      min_lines: 220
    - path: "crates/veld/tests/connect_runtime.rs"
      provides: "integration coverage for connect lifecycle and denial paths"
      contains: "connect_runtime"
  key_links:
    - from: "crates/veld/src/app.rs"
      to: "crates/veld/src/routes/connect.rs"
      via: "authenticated connect route mount"
      pattern: "/v1/connect|RouteExposureClass::OperatorAuthenticated"
    - from: "crates/veld/src/routes/connect.rs"
      to: "crates/veld/src/services/connect_runtime.rs"
      via: "thin route to lifecycle service"
      pattern: "connect_runtime"
    - from: "crates/vel-cli/src/commands/connect.rs"
      to: "crates/veld/src/routes/connect.rs"
      via: "CLI stubs replaced by live backend routes"
      pattern: "instances|launch|terminate"
---

<objective>
Activate authenticated connect transport and supervised local runtime lifecycle for coding-first work.

Purpose: close the re-scoped Phase 2 transport gap and give Vel a real local runtime launch/list/heartbeat/terminate loop before routing or guest-runtime work expands.
Output: live `/v1/connect` routes, supervised local-runtime service, CLI wiring, and lifecycle integration tests.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-RESEARCH.md
@.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-01-PLAN.md
@docs/tickets/phase-2/006-connect-launch-protocol.md
@crates/veld/src/services/agent_protocol.rs
@crates/vel-storage/src/repositories/connect_runs_repo.rs
@crates/vel-cli/src/commands/connect.rs
@crates/vel-cli/src/client.rs

<interfaces>
From crates/vel-cli/src/client.rs:
```rust
pub async fn list_connect_instances(&self) -> anyhow::Result<serde_json::Value>
pub async fn get_connect_instance(&self, id: &str) -> anyhow::Result<serde_json::Value>
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing integration coverage for connect launch/list/heartbeat/terminate and denial paths</name>
  <files>crates/veld/tests/connect_runtime.rs</files>
  <action>Create failing integration tests for: launching a local runtime with an approved manifest; heartbeats extending a lease; terminating a running instance; and rejecting unsupported runtime kinds or out-of-scope writable roots. Assert stable run/trace linkage and persisted connect-run state throughout the lifecycle.</action>
  <verify>
    <automated>cargo test -p veld connect -- --nocapture</automated>
  </verify>
  <done>Phase 8 has red tests for the real connect transport and supervised local-runtime lifecycle.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement authenticated `/v1/connect` routes and wire the CLI stubs into live behavior</name>
  <files>crates/veld/src/app.rs, crates/veld/src/routes/connect.rs, crates/veld/src/routes/mod.rs, crates/veld/src/services/connect_runtime.rs, crates/veld/src/services/agent_protocol.rs, crates/veld/src/services/mod.rs, crates/vel-cli/src/client.rs, crates/vel-cli/src/commands/connect.rs, docs/api/runtime.md</files>
  <action>Replace the current deny-all `/v1/connect` reservation with thin authenticated handlers for launch, list, inspect, heartbeat, and terminate behavior backed by a new `services::connect_runtime` orchestration boundary. Reuse `agent_protocol` and existing connect-run persistence where possible, but add explicit local-runtime manifest handling, lifecycle supervision, and failure-closed denial behavior for unsupported runtime kinds, unknown actions, or out-of-scope writable roots. Wire `vel connect` to the live routes and update runtime docs to describe the real transport and auth posture.</action>
  <verify>
    <automated>cargo test -p veld connect -- --nocapture && cargo test -p vel-cli connect -- --nocapture</automated>
  </verify>
  <done>Vel can launch, inspect, and supervise local coding runtimes through real authenticated connect routes and CLI commands.</done>
</task>

</tasks>

<verification>
Run the connect integration tests and CLI checks to prove that the re-scoped transport closure is now real, supervised, and fail-closed.
</verification>

<success_criteria>
Authenticated `/v1/connect` routes are live, local runtimes are supervised through persisted leases, and the CLI no longer points at an unimplemented stub transport.
</success_criteria>

<output>
After completion, create `.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-03-SUMMARY.md`
</output>
