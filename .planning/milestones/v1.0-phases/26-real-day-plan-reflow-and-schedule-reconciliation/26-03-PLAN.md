---
phase: 26-real-day-plan-reflow-and-schedule-reconciliation
plan: 03
type: execute
depends_on:
  - 26-02-PLAN.md
files_modified:
  - clients/web/src/components/NowView.tsx
  - clients/web/src/components/ThreadView.tsx
  - clients/web/src/components/SettingsPage.tsx
  - clients/web/src/types.ts
  - clients/web/src/components/*.test.tsx
autonomous: true
requirements:
  - REFLOW-REAL-02
  - SCHED-RECON-02
must_haves:
  truths:
    - "Shell surfaces consume typed reflow output and recovery posture instead of deriving schedule logic locally."
    - "Settings reflects real freshness and recovery posture as part of daily-use trust, not only generic assistant readiness."
---

<objective>
Embody the new reflow and recovery posture in `Now`, `Threads`, and `Settings` without breaking thin-shell boundaries.
</objective>

<tasks>
- Update `Now` to show compact remaining-day reflow output, moved/unscheduled summaries, and supervised next steps.
- Keep longer schedule shaping or disagreement in `Threads` continuity rather than expanding `Now` into a planner.
- Update `Settings` so recovery/freshness posture and contextual help are visible without dropping into raw runtime internals first.
</tasks>

<verification>
- npm --prefix clients/web test -- --run src/components/NowView.test.tsx src/components/ThreadView.test.tsx src/components/SettingsPage.test.tsx src/types.test.ts
</verification>

<output>
Create `26-03-SUMMARY.md`.
</output>
