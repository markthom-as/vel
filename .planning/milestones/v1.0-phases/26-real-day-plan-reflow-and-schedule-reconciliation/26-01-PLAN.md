---
phase: 26-real-day-plan-reflow-and-schedule-reconciliation
plan: 01
type: execute
depends_on: []
files_modified:
  - crates/vel-core/src/context.rs
  - crates/vel-core/src/operator_queue.rs
  - crates/vel-api-types/src/lib.rs
  - crates/veld/src/services/reflow.rs
  - docs/product/operator-action-taxonomy.md
  - docs/cognitive-agent-architecture/architecture/
  - crates/veld/tests/
autonomous: true
requirements:
  - REFLOW-REAL-01
  - SCHED-RECON-01
must_haves:
  truths:
    - "Reflow and schedule reconciliation use one canonical typed backend contract instead of status-only heuristics."
    - "Codex-workspace scheduling rules are mapped intentionally into Vel-side semantics before recomputation logic widens."
---

<objective>
Publish the canonical reflow/reconciliation contract and the scheduler-rule mapping seam for real day-plan recovery.
</objective>

<tasks>
- Define or refine the typed reflow proposal/output shape so backend services can represent moved work, unscheduled work, and remaining-day guidance explicitly.
- Introduce the first canonical mapping for scheduler semantics such as block targeting, durations, free-calendar behavior, time windows, and urgent/defer-style local flags.
- Add focused tests proving the new contract parses and remains backend-owned and inspectable.
</tasks>

<verification>
- cargo test -p vel-core context -- --nocapture
- cargo test -p veld reflow -- --nocapture
- npm --prefix clients/web test -- --run src/types.test.ts
</verification>

<output>
Create `26-01-SUMMARY.md`.
</output>
