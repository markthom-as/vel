---
phase: 05-now-inbox-core-and-project-substrate
plan: 08
type: execute
wave: 5
depends_on:
  - 05-03
  - 05-05
files_modified:
  - clients/apple/AGENTS.md
  - clients/apple/VelAPI/Sources/VelAPI/Models.swift
  - clients/apple/VelAPI/Sources/VelAPI/OfflineStore.swift
  - clients/apple/VelAPI/Sources/VelAPI/VelClient.swift
  - clients/apple/Apps/VeliOS/ContentView.swift
  - clients/apple/Apps/VelMac/ContentView.swift
  - clients/apple/Apps/VelWatch/ContentView.swift
autonomous: true
requirements:
  - CONTINUITY-01
  - CONTINUITY-02
must_haves:
  truths:
    - "Apple clients decode the same project, action, and linked-node continuity payloads that web and CLI consume."
    - "Offline cache hydration preserves projects, action items, and linked-node state without adding client-owned policy."
    - "Apple surfaces expose continuity status and thin read-only project/action context without trying to re-rank or re-triage locally."
  artifacts:
    - path: "clients/apple/VelAPI/Sources/VelAPI/Models.swift"
      provides: "Swift wire models for projects, action items, and linked nodes"
      contains: "ProjectRecordData"
    - path: "clients/apple/VelAPI/Sources/VelAPI/OfflineStore.swift"
      provides: "offline hydration and cache accessors for projects/action items/linked nodes"
      contains: "projects"
    - path: "clients/apple/Apps/VeliOS/ContentView.swift"
      provides: "thin today/project continuity rendering"
      contains: "Projects"
    - path: "clients/apple/Apps/VelMac/ContentView.swift"
      provides: "linked-node and project continuity status view"
      contains: "Linked nodes"
  key_links:
    - from: "clients/apple/VelAPI/Sources/VelAPI/VelClient.swift"
      to: "/v1/sync/bootstrap"
      via: "shared bootstrap decoding"
      pattern: "syncBootstrap"
    - from: "clients/apple/VelAPI/Sources/VelAPI/OfflineStore.swift"
      to: "clients/apple/VelAPI/Sources/VelAPI/Models.swift"
      via: "typed cache serialization"
      pattern: "ProjectRecordData|ActionItemData|LinkedNodeData"
    - from: "clients/apple/Apps/VelMac/ContentView.swift"
      to: "clients/apple/VelAPI/Sources/VelAPI/OfflineStore.swift"
      via: "read-only continuity display"
      pattern: "cachedProjects|cachedActionItems|cachedLinkedNodes"
---

<objective>
Bring Apple continuity up to Phase 05 parity by consuming the shared backend state without adding client-owned policy.

Purpose: Preserve the thin-client contract for iOS, macOS, and watchOS while letting Apple surfaces participate in the same linked-node and project/action continuity model as web and CLI.
Output: Extended `VelAPI` models/offline store, bootstrap decoding, and thin Apple surface updates for project/action/linking visibility.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-now-inbox-core-and-project-substrate/05-CONTEXT.md
@clients/apple/AGENTS.md
@clients/apple/README.md
@clients/apple/VelAPI/Sources/VelAPI/Models.swift
@clients/apple/VelAPI/Sources/VelAPI/OfflineStore.swift
@clients/apple/Apps/VeliOS/ContentView.swift
@clients/apple/Apps/VelMac/ContentView.swift
@clients/apple/Apps/VelWatch/ContentView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend VelAPI and the offline store for Phase 05 continuity payloads</name>
  <files>clients/apple/AGENTS.md, clients/apple/VelAPI/Sources/VelAPI/Models.swift, clients/apple/VelAPI/Sources/VelAPI/OfflineStore.swift, clients/apple/VelAPI/Sources/VelAPI/VelClient.swift</files>
  <read_first>clients/apple/AGENTS.md
clients/apple/VelAPI/Sources/VelAPI/Models.swift
clients/apple/VelAPI/Sources/VelAPI/OfflineStore.swift
clients/apple/VelAPI/Sources/VelAPI/VelClient.swift
crates/vel-api-types/src/lib.rs
clients/apple/README.md</read_first>
  <action>Update `Models.swift` with `ProjectFamilyData`, `ProjectRootRefData`, `ProjectRecordData`, `ActionEvidenceRefData`, `ActionItemData`, `LinkScopeData`, and `LinkedNodeData`, and extend `ClusterBootstrapData` and `SyncBootstrapData` with `projects`, `action_items`, and `linked_nodes`. Update `OfflineStore.swift` with cache keys `vel.cached.projects`, `vel.cached.action_items`, and `vel.cached.linked_nodes`, plus typed accessors `cachedProjects()`, `cachedActionItems()`, and `cachedLinkedNodes()`. Extend `hydrate(from bootstrap:)` to persist those three collections. Update `VelClient.swift` only as needed to keep `/v1/sync/bootstrap` decoding aligned with the expanded payload. Add one short note to `clients/apple/AGENTS.md` stating that Phase 05 continuity fields must remain backend-owned and must not be re-ranked in Swift.</action>
  <acceptance_criteria>- `rg -n "struct ProjectRecordData|struct ActionItemData|struct LinkedNodeData" clients/apple/VelAPI/Sources/VelAPI/Models.swift` returns all three structs.
- `rg -n "vel.cached.projects|vel.cached.action_items|vel.cached.linked_nodes" clients/apple/VelAPI/Sources/VelAPI/OfflineStore.swift` returns all three cache keys.
- `rg -n "cachedProjects|cachedActionItems|cachedLinkedNodes" clients/apple/VelAPI/Sources/VelAPI/OfflineStore.swift` returns all three accessors.
- `rg -n "must not be re-ranked|backend-owned" clients/apple/AGENTS.md` returns the new continuity rule.</acceptance_criteria>
  <verify>
    <automated>make check-apple-swift</automated>
  </verify>
  <done>`VelAPI` and the offline cache can carry Phase 05 project/action/linking state without redefining backend policy in Swift.</done>
</task>

<task type="auto">
  <name>Task 2: Surface thin read-only project and linking continuity on iOS, macOS, and watchOS</name>
  <files>clients/apple/Apps/VeliOS/ContentView.swift, clients/apple/Apps/VelMac/ContentView.swift, clients/apple/Apps/VelWatch/ContentView.swift</files>
  <read_first>clients/apple/Apps/VeliOS/ContentView.swift
clients/apple/Apps/VelMac/ContentView.swift
clients/apple/Apps/VelWatch/ContentView.swift
clients/apple/README.md
clients/apple/VelAPI/Sources/VelAPI/OfflineStore.swift
.planning/phases/05-now-inbox-core-and-project-substrate/05-CONTEXT.md</read_first>
  <action>Update `VeliOS/ContentView.swift` so the Today surface shows the top cached action title, its project label when present, and a read-only Projects section listing the first `5` projects grouped by family. Update `VelMac/ContentView.swift` so the sidebar status area shows linked-node count, the first linked node's status/scopes, and a small Projects section with primary repo and notes root for the first `5` cached projects. Update `VelWatch/ContentView.swift` so the status section includes linked-node count and the first action title when available, but do not add any new local ranking logic or edit flows.</action>
  <acceptance_criteria>- `rg -n "Projects" clients/apple/Apps/VeliOS/ContentView.swift clients/apple/Apps/VelMac/ContentView.swift` returns matches in both files.
- `rg -n "Linked nodes|linked node|linkedNodes" clients/apple/Apps/VelMac/ContentView.swift clients/apple/Apps/VelWatch/ContentView.swift` returns matches in both files.
- `rg -n "action title|actionItems|cachedActionItems" clients/apple/Apps/VeliOS/ContentView.swift clients/apple/Apps/VelWatch/ContentView.swift` returns matches in both files.</acceptance_criteria>
  <verify>
    <automated>make check-apple-swift</automated>
  </verify>
  <done>Apple clients visibly participate in the shared continuity model while remaining thin shells over backend-owned project/action/linking state.</done>
</task>

</tasks>

<verification>
Build the shared Swift package with `make check-apple-swift` after both tasks. Confirm the Apple subtree guide explicitly preserves backend ownership of action ranking.
</verification>

<success_criteria>
Apple clients can hydrate and display the same Phase 05 continuity state as the rest of the product without forking project/action/linking policy into SwiftUI.
</success_criteria>

<output>
After completion, create `.planning/phases/05-now-inbox-core-and-project-substrate/05-08-SUMMARY.md`
</output>
