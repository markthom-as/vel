---
phase: 05-now-inbox-core-and-project-substrate
plan: 09
type: execute
wave: 5
depends_on:
  - 05-02
  - 05-05
files_modified:
  - crates/veld/src/services/synthesis.rs
  - crates/vel-cli/src/commands/review.rs
  - docs/user/daily-use.md
  - docs/api/chat.md
  - docs/api/runtime.md
autonomous: true
requirements:
  - REVIEW-01
must_haves:
  truths:
    - "Daily and weekly review outputs reuse the same typed project and action vocabulary that powers `Now` and `Inbox`."
    - "Project-aware review output resolves against the typed project substrate first and only falls back to legacy aliases when necessary."
    - "Operator docs describe `Inbox` as a triage queue and `open_thread` as reuse of the existing conversation routes."
  artifacts:
    - path: "crates/veld/src/services/synthesis.rs"
      provides: "project-aware synthesis against typed projects with legacy alias fallback"
      contains: "get_project_by_slug"
    - path: "crates/vel-cli/src/commands/review.rs"
      provides: "typed review JSON output for today and week flows"
      contains: "project_review_candidates"
    - path: "docs/user/daily-use.md"
      provides: "operator loop guidance for Now, Inbox, and weekly project review"
      contains: "orient in Now"
  key_links:
    - from: "crates/vel-cli/src/commands/review.rs"
      to: "crates/veld/src/services/synthesis.rs"
      via: "review output references project-aware weekly synthesis candidates"
      pattern: "project_review_candidates"
    - from: "docs/api/chat.md"
      to: "docs/api/runtime.md"
      via: "API docs describe Inbox triage semantics and thread reuse consistently"
      pattern: "triage queue|open_thread"
---

<objective>
Make review and synthesis flows project-aware and aligned with the `Now`/`Inbox` operating model.

Purpose: Keep daily and weekly review outputs consistent with the typed project substrate and operator-facing triage semantics introduced earlier in Phase 05.
Output: Project-aware synthesis resolution, typed CLI review JSON, and docs that describe `Now`, `Inbox`, and project review consistently.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-now-inbox-core-and-project-substrate/05-CONTEXT.md
@.planning/phases/05-now-inbox-core-and-project-substrate/05-UI-SPEC.md
@docs/user/daily-use.md
@docs/api/chat.md
@docs/api/runtime.md
@crates/veld/src/services/synthesis.rs
@crates/vel-cli/src/commands/review.rs
@crates/veld/src/services/operator_queue.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Make review synthesis resolve against typed projects first</name>
  <files>crates/veld/src/services/synthesis.rs</files>
  <read_first>crates/veld/src/services/synthesis.rs
crates/veld/src/services/operator_queue.rs
crates/vel-storage/src/repositories/projects_repo.rs
crates/vel-core/src/project.rs</read_first>
  <action>Update `crates/veld/src/services/synthesis.rs` so project synthesis resolves against the typed `projects.slug` table first and only falls back to legacy `commitment.project` alias matches when no typed project exists. Keep the logic explainable and traceable to persisted project or commitment records.</action>
  <acceptance_criteria>- `rg -n "get_project_by_slug|project_alias" crates/veld/src/services/synthesis.rs` returns at least one match.
- `rg -n "projects.slug|slug" crates/veld/src/services/synthesis.rs` returns the typed project lookup path.</acceptance_criteria>
  <verify>
    <automated>cargo test -p veld synthesis -- --nocapture</automated>
  </verify>
  <done>Weekly synthesis candidates resolve against the typed project substrate first, with legacy alias matching only as a compatibility fallback.</done>
</task>

<task type="auto">
  <name>Task 2: Expose typed review outputs and align operator docs</name>
  <files>crates/vel-cli/src/commands/review.rs, docs/user/daily-use.md, docs/api/chat.md, docs/api/runtime.md</files>
  <read_first>crates/vel-cli/src/commands/review.rs
docs/user/daily-use.md
docs/api/chat.md
docs/api/runtime.md
crates/veld/src/services/synthesis.rs
crates/veld/src/services/operator_queue.rs</read_first>
  <action>Update `crates/vel-cli/src/commands/review.rs` so `vel review today --json` emits `open_action_count`, `triage_count`, and `top_action_titles`, and `vel review week --json` emits `project_review_candidates` with `project_id`, `slug`, `family`, and `open_commitment_count`. Update `docs/user/daily-use.md` so the daily loop explicitly says `orient in Now`, `triage Inbox`, and `review Projects weekly`. Update `docs/api/chat.md` and `docs/api/runtime.md` so `Inbox` is documented as a triage queue rather than a message list, and note that `open_thread` reuses the existing conversation route family.</action>
  <acceptance_criteria>- `rg -n "project_review_candidates|top_action_titles|triage_count" crates/vel-cli/src/commands/review.rs` returns all three fields.
- `rg -n "orient in Now|triage Inbox|review Projects weekly" docs/user/daily-use.md` returns all three phrases.
- `rg -n "triage queue|open_thread" docs/api/chat.md docs/api/runtime.md` returns matches in both docs.</acceptance_criteria>
  <verify>
    <automated>cargo test -p vel-cli review -- --nocapture && rg -n "triage queue|open_thread|orient in Now|triage Inbox|review Projects weekly" docs/user/daily-use.md docs/api/chat.md docs/api/runtime.md</automated>
  </verify>
  <done>CLI review JSON and operator docs reinforce the same `Now`/`Inbox`/Projects model that the runtime exposes elsewhere in Phase 05.</done>
</task>

</tasks>

<verification>
Run focused daemon and CLI review/synthesis tests, then grep the updated docs so `Inbox` and the daily loop are described with their triage semantics and thread reuse path.
</verification>

<success_criteria>
Daily and weekly review flows reinforce the same backend-owned action/intervention vocabulary and typed project substrate that the operator-facing surfaces use.
</success_criteria>

<output>
After completion, create `.planning/phases/05-now-inbox-core-and-project-substrate/05-09-SUMMARY.md`
</output>
