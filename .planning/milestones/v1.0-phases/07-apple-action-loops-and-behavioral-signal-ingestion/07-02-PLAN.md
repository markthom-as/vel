---
phase: 07-apple-action-loops-and-behavioral-signal-ingestion
plan: 02
type: execute
wave: 2
depends_on:
  - 07-01
files_modified:
  - crates/veld/src/app.rs
  - crates/veld/src/services/apple_voice.rs
  - crates/veld/src/services/mod.rs
  - crates/veld/src/routes/apple.rs
  - crates/veld/src/routes/mod.rs
  - crates/veld/src/routes/now.rs
  - crates/veld/src/services/now.rs
  - crates/veld/src/services/client_sync.rs
  - crates/veld/tests/apple_voice_loop.rs
autonomous: true
requirements:
  - IOS-01
  - IOS-02
  - IOS-03
  - APPLE-01
must_haves:
  truths:
    - "Apple voice capture persists transcript provenance before returning an answer or low-risk action result."
    - "Current schedule retrieval on Apple surfaces comes from backend-owned `/v1/now`, with any `AppleScheduleSnapshot` derived server-side from the same `Now` output rather than client heuristics."
    - "Voice-triggered nudge and commitment actions use existing safe backend mutation paths and remain replay-safe."
  artifacts:
    - path: "crates/veld/src/services/apple_voice.rs"
      provides: "backend-owned Apple voice orchestration"
      min_lines: 180
    - path: "crates/veld/src/routes/apple.rs"
      provides: "authenticated Apple voice endpoint"
      contains: "apple_voice_turn"
    - path: "crates/veld/tests/apple_voice_loop.rs"
      provides: "integration coverage for voice persistence and response behavior"
      contains: "apple_voice"
  key_links:
    - from: "crates/veld/src/app.rs"
      to: "crates/veld/src/routes/apple.rs"
      via: "operator-authenticated route mount and exposure policy"
      pattern: "/v1/apple|RouteExposureClass::OperatorAuthenticated"
    - from: "crates/veld/src/routes/apple.rs"
      to: "crates/veld/src/services/apple_voice.rs"
      via: "thin authenticated route"
      pattern: "apple_voice_turn"
    - from: "crates/veld/src/services/apple_voice.rs"
      to: "crates/veld/src/services/now.rs"
      via: "schedule/query evidence derivation"
      pattern: "get_now|NowOutput"
    - from: "crates/veld/src/services/apple_voice.rs"
      to: "crates/veld/src/services/client_sync.rs"
      via: "reuse of safe action and continuity seams"
      pattern: "sync|action"
---

<objective>
Move Apple voice/query behavior into Rust so iPhone/watch loops use persisted, explainable backend truth instead of local Swift synthesis.

Purpose: satisfy the fast capture, current schedule, and nudge-response goals without widening client policy ownership.
Output: authenticated backend Apple voice endpoint, persisted transcript-first orchestration, derived schedule answers, and focused integration tests.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-apple-action-loops-and-behavioral-signal-ingestion/07-RESEARCH.md
@.planning/phases/07-apple-action-loops-and-behavioral-signal-ingestion/07-01-PLAN.md
@crates/veld/src/routes/captures.rs
@crates/veld/src/routes/sync.rs
@crates/veld/src/routes/now.rs
@crates/veld/src/services/now.rs
@crates/veld/src/services/client_sync.rs
@clients/apple/Apps/VeliOS/ContentView.swift

<interfaces>
From crates/veld/src/routes/now.rs:
```rust
pub async fn get_now(
    State(state): State<AppState>,
) -> Result<Json<ApiResponse<NowData>>, AppError>;
```

From crates/veld/src/routes/sync.rs:
```rust
pub async fn sync_actions(
    State(state): State<AppState>,
    Json(payload): Json<ClientActionBatchRequest>,
) -> Result<Json<ApiResponse<ClientActionBatchResultData>>, AppError>;
```

From clients/apple/Apps/VeliOS/ContentView.swift:
```swift
case queryContext = "query_context"
case queryNextCommitment = "query_next_commitment"
case queryNudges = "query_nudges"
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing integration coverage for the backend-owned Apple voice loop</name>
  <files>crates/veld/tests/apple_voice_loop.rs</files>
  <behavior>
    - Test 1: posting an Apple voice query persists transcript provenance as a capture and returns a typed response with non-empty reasons/evidence.
    - Test 2: a current-schedule voice query derives its answer from backend `Now` state instead of any client-supplied summary.
    - Test 3: a low-risk nudge or commitment voice action reuses existing mutation paths and returns queued/applied status without inventing a client-side policy result.
  </behavior>
  <action>Create `crates/veld/tests/apple_voice_loop.rs` with failing integration tests covering transcript-first persistence, schedule retrieval from backend state, and safe low-risk action execution. Use authenticated request setup, seeded `Now`/nudge/commitment fixtures, and assert the typed Phase 7 Apple DTO fields defined in `07-01`.</action>
  <verify>
    <automated>cargo test -p veld apple_voice -- --nocapture</automated>
  </verify>
  <done>The repository has red tests that lock the intended backend-owned Apple voice behavior before implementation lands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement the Apple voice route and service on top of existing capture, now, and safe-action seams</name>
  <files>crates/veld/src/app.rs, crates/veld/src/services/apple_voice.rs, crates/veld/src/services/mod.rs, crates/veld/src/routes/apple.rs, crates/veld/src/routes/mod.rs, crates/veld/src/routes/now.rs, crates/veld/src/services/now.rs, crates/veld/src/services/client_sync.rs</files>
  <behavior>
    - Test 1: backend persists transcript capture before returning a voice response.
    - Test 2: schedule and query answers derive from persisted state and include explainable reasons.
    - Test 3: low-risk action intents map to the existing safe mutation path; unsupported or ambiguous intents fail closed as capture-only responses.
  </behavior>
  <action>Create `crates/veld/src/services/apple_voice.rs` as the single orchestration boundary for Apple voice turns. The service must persist transcript capture provenance first, derive query answers from backend-owned `Now` state or sync/bootstrap-owned state, reuse existing nudge/commitment safe mutation paths for low-risk intents, and return typed reasons/evidence in the response. Add a thin authenticated route in `crates/veld/src/routes/apple.rs`; wire it through `routes/mod.rs` and `crates/veld/src/app.rs` in the operator-authenticated route group so the new `/v1/apple/*` endpoints are mounted with the repo's auth-by-default posture. Commit explicitly that Apple schedule retrieval will reuse `GET /v1/now` as the canonical schedule source, with any smaller `AppleScheduleSnapshot` derived server-side from `NowOutput` and returned through typed Apple payloads where needed rather than widening Swift query logic. Explicitly deprecate the Swift-local query synthesis path in plan-facing comments/docs by making the backend endpoint and `/v1/now` the intended authorities for all query intents and schedule answers.</action>
  <verify>
    <automated>cargo test -p veld apple_voice -- --nocapture && cargo test -p veld now_endpoint_returns_consolidated_snapshot -- --nocapture</automated>
  </verify>
  <done>Apple voice turns are backend-owned, explainable, authenticated, and grounded in persisted state rather than Swift-local synthesis.</done>
</task>

</tasks>

<verification>
Run the dedicated Apple voice integration tests plus the existing `Now` endpoint coverage to prove Phase 7 voice, schedule, and nudge-response behavior stays backend-owned.
</verification>

<success_criteria>
Apple voice loops now persist transcript provenance, answer schedule/context questions from backend state, and execute only bounded safe actions through existing mutation seams.
</success_criteria>

<output>
After completion, create `.planning/phases/07-apple-action-loops-and-behavioral-signal-ingestion/07-02-SUMMARY.md`
</output>
