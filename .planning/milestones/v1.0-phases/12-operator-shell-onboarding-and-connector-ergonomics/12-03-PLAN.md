---
phase: 12-operator-shell-onboarding-and-connector-ergonomics
plan: 03
type: execute
wave: 2
depends_on:
  - 12-01
  - 12-02
files_modified:
  - clients/web/src/components/ProjectsView.tsx
  - clients/web/src/components/ProjectsView.test.tsx
  - clients/web/src/components/SettingsPage.tsx
  - clients/web/src/components/SettingsPage.test.tsx
  - clients/web/src/types.ts
  - docs/user/surfaces.md
autonomous: true
requirements:
  - PROJ-UX-01
  - SHELL-02
  - DOCS-01
  - INTEGR-UX-01
must_haves:
  truths:
    - "Projects have a clearer detail/edit path that stays grounded in the typed project substrate rather than turning into a separate planning system."
    - "Settings distinguishes operator-usable connector information from internal or diagnostic-only detail."
    - "Contextual docs/help and template editing/viewing remain tied to the actual shell and settings surfaces."
  artifacts:
    - path: "clients/web/src/components/ProjectsView.tsx"
      provides: "Project detail/edit ergonomics over the existing project record seam."
      contains: "project"
    - path: "clients/web/src/components/SettingsPage.tsx"
      provides: "Improved settings clarity for templates, connector icons, and operator-facing help."
      contains: "Settings"
    - path: "docs/user/surfaces.md"
      provides: "Updated user-facing shell/settings authority for the improved project and connector surfaces."
      contains: "Settings"
  key_links:
    - from: "clients/web/src/components/ProjectsView.tsx"
      to: "/v1/projects"
      via: "typed project detail/edit surface"
      pattern: "project"
    - from: "clients/web/src/components/SettingsPage.tsx"
      to: "docs/user/surfaces.md"
      via: "operator-facing settings/shell documentation"
      pattern: "Settings"
---

<objective>
Improve project and settings ergonomics without widening product scope.

Purpose: Close the obvious thin UX seams around project detail/editing and connector/settings clarity while keeping projects, templates, and integration status grounded in the already-shipped typed substrate.
Output: improved Projects and Settings surfaces, focused tests, and updated surface docs.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-operator-shell-onboarding-and-connector-ergonomics/12-CONTEXT.md
@clients/web/src/components/ProjectsView.tsx
@clients/web/src/components/SettingsPage.tsx
@clients/web/src/types.ts
@docs/user/surfaces.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add project detail/edit ergonomics over the existing typed project substrate</name>
  <files>clients/web/src/components/ProjectsView.tsx, clients/web/src/components/ProjectsView.test.tsx, clients/web/src/types.ts</files>
  <behavior>
    - Test 1: Operators can move from the project list into a clearer project detail state without losing the main daily-shell context.
    - Test 2: Editable fields remain bounded to the current project contract; the UI does not invent new project semantics locally.
    - Test 3: The resulting project surface stays useful for daily work rather than becoming a second dashboard hierarchy.
  </behavior>
  <action>Extend `ProjectsView` so project detail and supported edit affordances are easier to reach and understand. Reuse the existing typed project shape and backend routes; if a missing client decoder field blocks the UX, add it through the shared types first.</action>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/ProjectsView.test.tsx src/types.test.ts</automated>
  </verify>
  <done>The project surface is materially easier to use without exceeding the project substrate already defined in prior phases.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Clarify Settings templates, connector status, and contextual help</name>
  <files>clients/web/src/components/SettingsPage.tsx, clients/web/src/components/SettingsPage.test.tsx, docs/user/surfaces.md</files>
  <behavior>
    - Test 1: Settings renders the new template/help/connector affordances using typed data and existing mutations rather than local policy branches.
    - Test 2: Internal or diagnostic-only integration details are hidden or clearly demoted relative to operator-usable setup information.
    - Test 3: Connector icons/help links and template viewing/editing improve discoverability without fragmenting the settings trust model.
  </behavior>
  <action>Rework the relevant parts of `SettingsPage` so operators can understand connector status, templates, and setup/help paths more quickly. Add connected-service iconography if it materially improves scanability, but keep the implementation aligned with the existing settings shell and trust posture. Update `docs/user/surfaces.md` to match the shipped behavior.</action>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/SettingsPage.test.tsx</automated>
  </verify>
  <done>Settings becomes a clearer operator surface for templates and connectors, with docs aligned to the new affordances.</done>
</task>

</tasks>

<verification>
Run the focused Projects and Settings tests, then manually open a project and a few connector cards in Settings to confirm the surfaces feel simpler and more bounded than before.
</verification>

<success_criteria>
Project detail/edit paths and Settings affordances are clearer, more contextual, and better documented, while still remaining thin shells over the typed project and integration contracts.
</success_criteria>

<output>
After completion, create `.planning/phases/12-operator-shell-onboarding-and-connector-ergonomics/12-03-SUMMARY.md`
</output>
