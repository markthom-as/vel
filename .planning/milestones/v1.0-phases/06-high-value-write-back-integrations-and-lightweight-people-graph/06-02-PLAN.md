---
phase: 06-high-value-write-back-integrations-and-lightweight-people-graph
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - crates/vel-core/src/lib.rs
  - crates/vel-core/src/node_identity.rs
  - crates/vel-core/src/ordering.rs
  - migrations/0039_phase6_conflicts_and_writebacks.sql
  - crates/vel-storage/src/db.rs
  - crates/vel-storage/src/lib.rs
  - crates/vel-storage/src/repositories/conflict_cases_repo.rs
  - crates/vel-storage/src/repositories/writeback_operations_repo.rs
  - crates/vel-storage/src/repositories/upstream_refs_repo.rs
  - crates/veld/src/services/client_sync.rs
  - crates/veld/src/services/operator_queue.rs
  - crates/veld/src/services/writeback.rs
autonomous: true
requirements:
  - RECON-01
  - CONFLICT-01
  - PROV-01
must_haves:
  truths:
    - "Phase 6 has one deterministic ordering primitive and explicit node-origin contract before multi-writer reconciliation widens."
    - "Write-back attempts, upstream refs, and conflict cases persist as first-class records with stable IDs."
    - "Pending conflicts and queued writes can already project into cross-client operator state before any provider-specific slice lands."
  artifacts:
    - path: "crates/vel-core/src/node_identity.rs"
      provides: "NodeIdentity"
      min_lines: 40
    - path: "crates/vel-core/src/ordering.rs"
      provides: "deterministic ordering primitive and comparator"
      min_lines: 80
    - path: "crates/vel-storage/src/repositories/conflict_cases_repo.rs"
      provides: "conflict queue persistence"
      contains: "list_open_conflict_cases"
    - path: "crates/vel-storage/src/repositories/writeback_operations_repo.rs"
      provides: "write-back history persistence"
      contains: "insert_writeback_operation"
    - path: "crates/veld/src/services/writeback.rs"
      provides: "provider-neutral write-back orchestration and conflict opening"
      contains: "queue_writeback_operation"
  key_links:
    - from: "crates/veld/src/services/writeback.rs"
      to: "crates/vel-storage/src/repositories/conflict_cases_repo.rs"
      via: "service opens conflict records on reconciliation failure"
      pattern: "insert_conflict_case|open_conflict_case"
    - from: "crates/veld/src/services/client_sync.rs"
      to: "crates/veld/src/services/writeback.rs"
      via: "sync/bootstrap emits queued write and conflict state"
      pattern: "pending_writebacks|conflicts"
---

<objective>
Install the deterministic reconciliation and persistence foundation that every Phase 6 provider slice needs.

Purpose: make write-back, upstream ownership, and conflict review durable and replayable before Todoist, notes, reminders, GitHub, or email logic starts mutating real state.
Output: `NodeIdentity`, ordering primitive, migration-backed repos for write-back/conflict/upstream refs, and backend services that project pending write/conflict state into operator continuity.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-CONTEXT.md
@.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-RESEARCH.md
@docs/tickets/phase-2/005-hlc-sync-implementation.md
@crates/vel-core/src/integration.rs
@crates/veld/src/services/client_sync.rs
@crates/veld/src/services/operator_queue.rs
@crates/vel-storage/src/repositories/integration_connections_repo.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add NodeIdentity and deterministic ordering contracts in vel-core</name>
  <files>crates/vel-core/src/lib.rs, crates/vel-core/src/node_identity.rs, crates/vel-core/src/ordering.rs</files>
  <read_first>crates/vel-core/src/lib.rs
crates/vel-core/src/integration.rs
docs/tickets/phase-2/005-hlc-sync-implementation.md
.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-CONTEXT.md</read_first>
  <action>Create `crates/vel-core/src/node_identity.rs` with a `NodeIdentity` newtype wrapper over UUID string that implements `Display`, `Serialize`, `Deserialize`, `Clone`, `Eq`, and `Ord`. Create `crates/vel-core/src/ordering.rs` with `OrderingStamp` containing `physical_ts`, `logical_counter`, and `node_id: NodeIdentity`, plus comparator semantics that order by `physical_ts`, then `logical_counter`, then `node_id`. Re-export both modules from `crates/vel-core/src/lib.rs`. Add explicit tests proving equal timestamps with different node IDs still sort deterministically and proving serialized `NodeIdentity` round-trips without losing the UUID string.</action>
  <acceptance_criteria>- `rg -n "pub struct NodeIdentity" crates/vel-core/src/node_identity.rs` returns one match.
- `rg -n "pub struct OrderingStamp" crates/vel-core/src/ordering.rs` returns one match.
- `rg -n "physical_ts|logical_counter|node_id" crates/vel-core/src/ordering.rs` returns all three ordering fields.
- `rg -n "impl Display for NodeIdentity" crates/vel-core/src/node_identity.rs` returns one match.
- `cargo test -p vel-core ordering -- --nocapture` exits 0.</acceptance_criteria>
  <verify>
    <automated>cargo test -p vel-core ordering -- --nocapture</automated>
  </verify>
  <done>Phase 6 has an explicit node-origin and deterministic ordering contract that later sync and provider slices can reuse consistently.</done>
</task>

<task type="auto">
  <name>Task 2: Persist write-back history, conflict queue, and upstream ownership records</name>
  <files>migrations/0039_phase6_conflicts_and_writebacks.sql, crates/vel-storage/src/db.rs, crates/vel-storage/src/lib.rs, crates/vel-storage/src/repositories/conflict_cases_repo.rs, crates/vel-storage/src/repositories/writeback_operations_repo.rs, crates/vel-storage/src/repositories/upstream_refs_repo.rs, crates/veld/src/services/writeback.rs, crates/veld/src/services/client_sync.rs, crates/veld/src/services/operator_queue.rs</files>
  <read_first>migrations/
crates/vel-storage/src/db.rs
crates/vel-storage/src/lib.rs
crates/vel-storage/src/repositories/integration_connections_repo.rs
crates/veld/src/services/client_sync.rs
crates/veld/src/services/operator_queue.rs
crates/vel-core/src/writeback.rs
crates/vel-core/src/conflicts.rs
crates/vel-core/src/node_identity.rs
crates/vel-core/src/ordering.rs</read_first>
  <action>Create `migrations/0039_phase6_conflicts_and_writebacks.sql` with tables `writeback_operations`, `conflict_cases`, and `upstream_object_refs`. `writeback_operations` must include `id TEXT PRIMARY KEY`, `kind TEXT NOT NULL`, `risk TEXT NOT NULL`, `status TEXT NOT NULL`, `family TEXT NOT NULL`, `provider_key TEXT NOT NULL`, `project_id TEXT`, `connection_id TEXT`, `external_id TEXT`, `requested_payload_json TEXT NOT NULL DEFAULT '{}'`, `result_payload_json TEXT NOT NULL DEFAULT '{}'`, `provenance_json TEXT NOT NULL DEFAULT '{}'`, `conflict_case_id TEXT`, `requested_by_node_id TEXT NOT NULL`, `ordering_stamp_json TEXT NOT NULL`, `requested_at INTEGER NOT NULL`, `applied_at INTEGER`, and `updated_at INTEGER NOT NULL`. `conflict_cases` must include `id TEXT PRIMARY KEY`, `kind TEXT NOT NULL`, `status TEXT NOT NULL`, `family TEXT NOT NULL`, `provider_key TEXT NOT NULL`, `project_id TEXT`, `connection_id TEXT`, `external_id TEXT`, `summary TEXT NOT NULL`, `local_payload_json TEXT NOT NULL DEFAULT '{}'`, `upstream_payload_json TEXT NOT NULL DEFAULT '{}'`, `resolution_payload_json TEXT NOT NULL DEFAULT '{}'`, `opened_at INTEGER NOT NULL`, `resolved_at INTEGER`, and `updated_at INTEGER NOT NULL`. `upstream_object_refs` must include `id TEXT PRIMARY KEY`, `family TEXT NOT NULL`, `provider_key TEXT NOT NULL`, `project_id TEXT`, `local_object_kind TEXT NOT NULL`, `local_object_id TEXT NOT NULL`, `external_id TEXT NOT NULL`, `external_parent_id TEXT`, `ordering_stamp_json TEXT NOT NULL`, `last_seen_at INTEGER NOT NULL`, and `metadata_json TEXT NOT NULL DEFAULT '{}'`. Add repository methods for insert/get/list/update paths in the three new storage modules and export them through `db.rs` and `lib.rs`. Create `crates/veld/src/services/writeback.rs` with `queue_writeback_operation`, `mark_writeback_applied`, `mark_writeback_failed`, and `open_conflict_case` orchestration helpers that use the new repos. Extend `crates/veld/src/services/client_sync.rs` and `crates/veld/src/services/operator_queue.rs` so sync/bootstrap and ranked action items can include typed `pending_writebacks` and `conflicts` from backend storage.</action>
  <acceptance_criteria>- `rg -n "CREATE TABLE writeback_operations|CREATE TABLE conflict_cases|CREATE TABLE upstream_object_refs" migrations/0039_phase6_conflicts_and_writebacks.sql` returns all three tables.
- `rg -n "insert_writeback_operation|list_writeback_operations" crates/vel-storage/src/repositories/writeback_operations_repo.rs` returns both methods.
- `rg -n "insert_conflict_case|list_open_conflict_cases" crates/vel-storage/src/repositories/conflict_cases_repo.rs` returns both methods.
- `rg -n "upsert_upstream_object_ref|get_upstream_object_ref" crates/vel-storage/src/repositories/upstream_refs_repo.rs` returns both methods.
- `rg -n "queue_writeback_operation|open_conflict_case" crates/veld/src/services/writeback.rs` returns both helpers.
- `rg -n "pending_writebacks|conflicts" crates/veld/src/services/client_sync.rs crates/veld/src/services/operator_queue.rs` returns matches in both files.</acceptance_criteria>
  <verify>
    <automated>cargo test -p vel-storage integration_connections_repo -- --nocapture && cargo test -p veld client_sync -- --nocapture</automated>
  </verify>
  <done>Write-back attempts, conflict cases, and upstream ownership refs are durable and can already surface into operator continuity before provider-specific writes exist.</done>
</task>

</tasks>

<verification>
Run focused core, storage, and sync/operator tests so deterministic ordering and the new Phase 6 durable records are proven before provider slices depend on them.
</verification>

<success_criteria>
Phase 6 has a replayable reconciliation foundation: one ordering rule, one conflict queue, one write-back history seam, and one typed upstream ownership record model.
</success_criteria>

<output>
After completion, create `.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-02-SUMMARY.md`
</output>
