---
phase: 06-high-value-write-back-integrations-and-lightweight-people-graph
plan: 05
type: execute
wave: 4
depends_on:
  - 06-01
  - 06-02
  - 06-04
files_modified:
  - migrations/0040_phase6_people_and_graph.sql
  - crates/vel-storage/src/db.rs
  - crates/vel-storage/src/lib.rs
  - crates/vel-storage/src/repositories/people_repo.rs
  - crates/vel-storage/src/repositories/semantic_memory_repo.rs
  - crates/veld/src/services/people.rs
  - crates/veld/src/services/retrieval.rs
  - crates/veld/src/services/client_sync.rs
  - crates/veld/src/routes/people.rs
  - docs/cognitive-agent-architecture/cognition/memory-model.md
autonomous: true
requirements:
  - PEOPLE-01
  - PEOPLE-02
  - NOTES-01
  - PROV-01
must_haves:
  truths:
    - "Phase 6 ships a minimal practical people registry with explicit alias and linkage records."
    - "Semantic graph expansion indexes only durable typed entities with provenance-bearing links."
    - "People and graph records participate in cross-client continuity and retrieval without opaque merge heuristics."
  artifacts:
    - path: "crates/vel-storage/src/repositories/people_repo.rs"
      provides: "person and alias persistence"
      contains: "create_person"
    - path: "crates/vel-storage/src/repositories/semantic_memory_repo.rs"
      provides: "indexing for projects, notes, transcripts, threads, and people"
      contains: "source_kind = 'person'"
    - path: "crates/veld/src/services/people.rs"
      provides: "people orchestration and alias/link inspection"
      contains: "list_people"
    - path: "crates/veld/src/routes/people.rs"
      provides: "operator-authenticated people routes"
      contains: "/v1/people"
  key_links:
    - from: "crates/veld/src/services/people.rs"
      to: "crates/vel-storage/src/repositories/people_repo.rs"
      via: "service boundary over typed person/alias repos"
      pattern: "create_person|list_people|upsert_person_alias"
    - from: "crates/veld/src/services/retrieval.rs"
      to: "crates/vel-storage/src/repositories/semantic_memory_repo.rs"
      via: "retrieval widens to people and project/note entities"
      pattern: "person|project|note|thread"
---

<objective>
Add the practical people registry and widen the semantic graph only to the durable entities Phase 6 already owns.

Purpose: make people identity and recall useful in day-to-day operator loops without overpromising opaque auto-merge or speculative graph structure.
Output: people tables/repos/services/routes, alias-driven linkage, and semantic retrieval over projects, notes, transcripts, threads, and people records.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-CONTEXT.md
@.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-RESEARCH.md
@docs/tickets/phase-4/009-semantic-memory-rag.md
@crates/vel-storage/src/repositories/semantic_memory_repo.rs
@crates/veld/src/services/client_sync.rs
@crates/veld/src/services/context_runs.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist minimal people records and alias-driven linkage</name>
  <files>migrations/0040_phase6_people_and_graph.sql, crates/vel-storage/src/db.rs, crates/vel-storage/src/lib.rs, crates/vel-storage/src/repositories/people_repo.rs, crates/veld/src/services/people.rs, crates/veld/src/services/client_sync.rs, crates/veld/src/routes/people.rs</files>
  <read_first>migrations/
crates/vel-storage/src/db.rs
crates/vel-storage/src/lib.rs
crates/vel-core/src/people.rs
crates/veld/src/services/client_sync.rs
crates/veld/src/services/projects.rs
.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-CONTEXT.md</read_first>
  <action>Create `migrations/0040_phase6_people_and_graph.sql` with tables `people` and `person_aliases`. The `people` table must include `id TEXT PRIMARY KEY`, `display_name TEXT NOT NULL`, `given_name TEXT`, `family_name TEXT`, `relationship_context TEXT`, `birthday TEXT`, `last_contacted_at INTEGER`, `metadata_json TEXT NOT NULL DEFAULT '{}'`, `created_at INTEGER NOT NULL`, and `updated_at INTEGER NOT NULL`. The `person_aliases` table must include `id TEXT PRIMARY KEY`, `person_id TEXT NOT NULL`, `platform TEXT NOT NULL`, `handle TEXT NOT NULL`, `display TEXT`, `source_ref_json TEXT NOT NULL DEFAULT '{}'`, `created_at INTEGER NOT NULL`, and a uniqueness constraint on `(platform, handle)`. Add `create_person`, `list_people`, `get_person`, `upsert_person_alias`, and `list_person_aliases` queries in `crates/vel-storage/src/repositories/people_repo.rs` and export them through `db.rs` and `lib.rs`. Create `crates/veld/src/services/people.rs` with `list_people`, `get_person`, and `upsert_person_alias`, and create `crates/veld/src/routes/people.rs` exposing `GET /v1/people`, `GET /v1/people/:id`, and `POST /v1/people/:id/aliases`. Extend `crates/veld/src/services/client_sync.rs` so typed people lists can hydrate thin clients when requested.</action>
  <acceptance_criteria>- `rg -n "CREATE TABLE people|CREATE TABLE person_aliases" migrations/0040_phase6_people_and_graph.sql` returns both tables.
- `rg -n "create_person|list_people|get_person|upsert_person_alias" crates/vel-storage/src/repositories/people_repo.rs` returns all four methods.
- `rg -n "GET /v1/people|/v1/people/:id|/v1/people/:id/aliases" crates/veld/src/routes/people.rs` returns all three route patterns.
- `rg -n "people" crates/veld/src/services/client_sync.rs` returns typed people hydration or projection support.</acceptance_criteria>
  <verify>
    <automated>cargo test -p veld people -- --nocapture</automated>
  </verify>
  <done>Vel has a practical typed people registry with explicit aliases and inspectable source-linked identity data.</done>
</task>

<task type="auto">
  <name>Task 2: Expand semantic retrieval over durable Phase 6 entities with provenance</name>
  <files>crates/vel-storage/src/repositories/semantic_memory_repo.rs, crates/veld/src/services/retrieval.rs, docs/cognitive-agent-architecture/cognition/memory-model.md</files>
  <read_first>crates/vel-storage/src/repositories/semantic_memory_repo.rs
crates/veld/src/services/retrieval.rs
crates/vel-core/src/project.rs
crates/vel-core/src/people.rs
crates/vel-core/src/integration.rs
docs/tickets/phase-4/009-semantic-memory-rag.md
docs/cognitive-agent-architecture/cognition/memory-model.md</read_first>
  <action>Extend `crates/vel-storage/src/repositories/semantic_memory_repo.rs` so it can index and retrieve durable records for the exact source kinds `project`, `note`, `transcript_note`, `thread`, and `person` in addition to the shipped `capture` baseline. For every new record type, persist a provenance payload that includes either the typed local ID, the upstream external ID, or both. Update `crates/veld/src/services/retrieval.rs` so retrieval filters and result mapping can surface those source kinds without collapsing them back into anonymous capture hits. Update `docs/cognitive-agent-architecture/cognition/memory-model.md` so the Phase 6 memory model explicitly names projects, notes, transcripts-under-notes, threads, and people as the new durable graph-linked retrieval entities.</action>
  <acceptance_criteria>- `rg -n "project|note|transcript_note|thread|person" crates/vel-storage/src/repositories/semantic_memory_repo.rs` returns all five Phase 6 source kinds.
- `rg -n "provenance" crates/vel-storage/src/repositories/semantic_memory_repo.rs` returns persisted provenance handling for new record types.
- `rg -n "project|person|thread" crates/veld/src/services/retrieval.rs` returns Phase 6 source-kind mapping logic.
- `rg -n "projects|notes|transcripts|threads|people" docs/cognitive-agent-architecture/cognition/memory-model.md` returns all five entity groups.</acceptance_criteria>
  <verify>
    <automated>cargo test -p vel-storage semantic_memory_repo -- --nocapture && cargo test -p veld retrieval -- --nocapture</automated>
  </verify>
  <done>Semantic retrieval expands only over durable Phase 6 entities with stable provenance rather than speculative or opaque graph edges.</done>
</task>

</tasks>

<verification>
Run focused people and retrieval tests proving alias-driven identity persistence and provenance-bearing semantic results across the new Phase 6 entity set.
</verification>

<success_criteria>
Vel can persist practical people identity and retrieve graph-linked context across projects, notes, transcripts, threads, and people without losing provenance or inventing opaque merge logic.
</success_criteria>

<output>
After completion, create `.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-05-SUMMARY.md`
</output>
