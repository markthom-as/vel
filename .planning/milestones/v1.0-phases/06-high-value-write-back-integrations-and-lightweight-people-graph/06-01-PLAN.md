---
phase: 06-high-value-write-back-integrations-and-lightweight-people-graph
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/vel-core/src/lib.rs
  - crates/vel-core/src/writeback.rs
  - crates/vel-core/src/conflicts.rs
  - crates/vel-core/src/people.rs
  - crates/vel-api-types/src/lib.rs
  - config/contracts-manifest.json
  - config/README.md
  - config/schemas/writeback-operation.schema.json
  - config/examples/writeback-operation.example.json
  - config/schemas/person-record.schema.json
  - config/examples/person-record.example.json
  - docs/cognitive-agent-architecture/integrations/writeback-and-conflict-contracts.md
autonomous: true
requirements:
  - WB-01
  - CONFLICT-01
  - PROV-01
  - PEOPLE-01
must_haves:
  truths:
    - "Phase 6 has one typed vocabulary for write-back operations, conflict cases, and people records before provider slices widen."
    - "Transport DTOs can expose write-back status, conflict state, and people linkage without opaque JSON placeholders."
    - "Schema, example, and owner docs exist for the new durable Phase 6 contract surfaces."
  artifacts:
    - path: "crates/vel-core/src/writeback.rs"
      provides: "WritebackOperationId, WritebackOperationKind, WritebackOperationRecord, WritebackTargetRef"
      min_lines: 90
    - path: "crates/vel-core/src/conflicts.rs"
      provides: "ConflictCaseId, ConflictCaseStatus, ConflictCaseRecord"
      min_lines: 70
    - path: "crates/vel-core/src/people.rs"
      provides: "PersonId, PersonRecord, PersonAlias, PersonLinkRef"
      min_lines: 90
    - path: "crates/vel-api-types/src/lib.rs"
      provides: "write-back, conflict, and people DTOs"
      contains: "pub struct WritebackOperationData"
    - path: "config/schemas/writeback-operation.schema.json"
      provides: "machine-readable write-back contract"
      contains: "\"WritebackOperationKind\""
    - path: "config/schemas/person-record.schema.json"
      provides: "machine-readable people contract"
      contains: "\"PersonRecord\""
  key_links:
    - from: "crates/vel-core/src/lib.rs"
      to: "crates/vel-core/src/writeback.rs"
      via: "pub use re-export"
      pattern: "pub mod writeback|pub use writeback::"
    - from: "crates/vel-api-types/src/lib.rs"
      to: "crates/vel-core/src/conflicts.rs"
      via: "DTO mapping boundary"
      pattern: "ConflictCaseData"
    - from: "config/contracts-manifest.json"
      to: "config/schemas/writeback-operation.schema.json"
      via: "contracts manifest entry"
      pattern: "writeback-operation.schema.json"
---

<objective>
Freeze the Phase 6 contract vocabulary before reconciliation, provider writes, graph work, or operator-surface changes widen around it.

Purpose: stop the repo from implementing Todoist, notes, reminders, GitHub, email, and people work through provider-specific ad hoc payloads.
Output: typed Phase 6 core modules, matching transport DTOs, and schema/example/owner docs for write-back operations, conflict cases, and people records.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-CONTEXT.md
@.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-RESEARCH.md
@docs/cognitive-agent-architecture/integrations/canonical-data-sources-and-connectors.md
@crates/vel-core/src/integration.rs
@crates/vel-core/src/project.rs
@crates/vel-core/src/operator_queue.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Publish canonical Phase 6 write-back, conflict, and people contracts in vel-core</name>
  <files>crates/vel-core/src/lib.rs, crates/vel-core/src/writeback.rs, crates/vel-core/src/conflicts.rs, crates/vel-core/src/people.rs</files>
  <read_first>crates/vel-core/src/lib.rs
crates/vel-core/src/integration.rs
crates/vel-core/src/project.rs
crates/vel-core/src/operator_queue.rs
.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-CONTEXT.md</read_first>
  <action>Create `crates/vel-core/src/writeback.rs` with `WritebackOperationId` using the `wb_` prefix, `WritebackRisk` with exactly `safe`, `confirm_required`, and `blocked`, `WritebackStatus` with exactly `queued`, `in_progress`, `applied`, `conflicted`, `denied`, `failed`, and `cancelled`, `WritebackOperationKind` with exactly `todoist_create_task`, `todoist_update_task`, `todoist_complete_task`, `todoist_reopen_task`, `notes_create_note`, `notes_append_note`, `reminders_create`, `reminders_update`, `reminders_complete`, `github_create_issue`, `github_add_comment`, `github_close_issue`, `github_reopen_issue`, `email_create_draft_reply`, and `email_send_draft`, `WritebackTargetRef` with `family`, `provider_key`, `project_id`, `connection_id`, and `external_id`, and `WritebackOperationRecord` with `id`, `kind`, `risk`, `status`, `target`, `requested_payload`, `result_payload`, `provenance`, `conflict_case_id`, `requested_by_node_id`, `requested_at`, `applied_at`, and `updated_at`. Create `crates/vel-core/src/conflicts.rs` with `ConflictCaseId` using the `conf_` prefix, `ConflictCaseKind` with exactly `upstream_vs_local`, `cross_client`, `stale_write`, and `executor_unavailable`, `ConflictCaseStatus` with exactly `open`, `acknowledged`, `resolved`, `dismissed`, and `expired`, and `ConflictCaseRecord` with `id`, `kind`, `status`, `target`, `summary`, `local_payload`, `upstream_payload`, `resolution_payload`, `opened_at`, `resolved_at`, and `updated_at`. Create `crates/vel-core/src/people.rs` with `PersonId` using the `per_` prefix, `PersonAlias` with `platform`, `handle`, `display`, and `source_ref`, `PersonLinkRef` with `kind`, `id`, and `label`, and `PersonRecord` with `id`, `display_name`, `given_name`, `family_name`, `relationship_context`, `birthday`, `last_contacted_at`, `aliases`, and `links`. Re-export all three modules from `crates/vel-core/src/lib.rs`.</action>
  <acceptance_criteria>- `rg -n "pub enum WritebackOperationKind" crates/vel-core/src/writeback.rs` returns one match.
- `rg -n "todoist_create_task|notes_append_note|github_create_issue|email_create_draft_reply" crates/vel-core/src/writeback.rs` returns all four operation kinds.
- `rg -n "pub enum ConflictCaseStatus" crates/vel-core/src/conflicts.rs` returns one match.
- `rg -n "upstream_vs_local|cross_client|stale_write|executor_unavailable" crates/vel-core/src/conflicts.rs` returns all four conflict kinds.
- `rg -n "pub struct PersonRecord" crates/vel-core/src/people.rs` returns one match.
- `rg -n "pub mod writeback|pub mod conflicts|pub mod people" crates/vel-core/src/lib.rs` returns all three module declarations.</acceptance_criteria>
  <verify>
    <automated>cargo test -p vel-core -- --nocapture</automated>
  </verify>
  <done>Vel has a typed Phase 6 vocabulary for write-back, conflicts, and people before any provider slice starts improvising names or payloads.</done>
</task>

<task type="auto">
  <name>Task 2: Extend vel-api-types and contract assets for Phase 6 durable surfaces</name>
  <files>crates/vel-api-types/src/lib.rs, config/contracts-manifest.json, config/README.md, config/schemas/writeback-operation.schema.json, config/examples/writeback-operation.example.json, config/schemas/person-record.schema.json, config/examples/person-record.example.json, docs/cognitive-agent-architecture/integrations/writeback-and-conflict-contracts.md</files>
  <read_first>crates/vel-api-types/src/lib.rs
crates/vel-core/src/writeback.rs
crates/vel-core/src/conflicts.rs
crates/vel-core/src/people.rs
config/contracts-manifest.json
config/README.md
docs/cognitive-agent-architecture/integrations/canonical-data-sources-and-connectors.md</read_first>
  <action>Add `WritebackTargetRefData`, `WritebackOperationData`, `ConflictCaseData`, `PersonAliasData`, `PersonLinkRefData`, and `PersonRecordData` to `crates/vel-api-types/src/lib.rs`. Extend the operator-facing DTO surfaces that already carry Phase 5 action and linking state so they can also expose `pending_writebacks`, `conflicts`, and `people` as typed arrays rather than `serde_json::Value` placeholders. Create `config/schemas/writeback-operation.schema.json` and `config/examples/writeback-operation.example.json` describing the `WritebackOperationRecord` contract. Create `config/schemas/person-record.schema.json` and `config/examples/person-record.example.json` describing the `PersonRecord` contract. Add both schema/example pairs to `config/contracts-manifest.json` and document them in `config/README.md`. Write `docs/cognitive-agent-architecture/integrations/writeback-and-conflict-contracts.md` that names the Phase 6 owner modules, the exact write-back operation kinds, the conflict kinds/statuses, and the rule that provider slices may only expose the allowed operations listed in `WritebackOperationKind`.</action>
  <acceptance_criteria>- `rg -n "pub struct WritebackOperationData" crates/vel-api-types/src/lib.rs` returns one match.
- `rg -n "pub struct ConflictCaseData" crates/vel-api-types/src/lib.rs` returns one match.
- `rg -n "pub struct PersonRecordData" crates/vel-api-types/src/lib.rs` returns one match.
- `rg -n "pending_writebacks|conflicts|people" crates/vel-api-types/src/lib.rs` returns typed array fields in operator-facing DTOs.
- `rg -n "writeback-operation.schema.json|person-record.schema.json" config/contracts-manifest.json config/README.md` returns matches in both files.
- `rg -n "todoist_create_task|email_create_draft_reply|executor_unavailable|confirm_required" docs/cognitive-agent-architecture/integrations/writeback-and-conflict-contracts.md` returns all four terms.</acceptance_criteria>
  <verify>
    <automated>cargo test -p vel-api-types -- --nocapture && node scripts/verify-repo-truth.mjs</automated>
  </verify>
  <done>Transport, schema, and owner-doc boundaries exist for all Phase 6 durable surfaces before storage and provider implementation begins.</done>
</task>

</tasks>

<verification>
Run focused `vel-core` and `vel-api-types` tests, then run repo-truth verification so the new Phase 6 contract assets are discoverable from the contracts manifest.
</verification>

<success_criteria>
The repository has one typed contract set for write-back operations, conflict cases, and people records, and every later Phase 6 plan can build against that vocabulary without adding opaque JSON or provider-specific contract drift.
</success_criteria>

<output>
After completion, create `.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-01-SUMMARY.md`
</output>
