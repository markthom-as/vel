---
phase: 06-high-value-write-back-integrations-and-lightweight-people-graph
plan: 03
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - crates/vel-core/src/commitment.rs
  - crates/vel-storage/src/repositories/projects_repo.rs
  - crates/veld/src/services/integrations_todoist.rs
  - crates/veld/src/services/integrations.rs
  - crates/veld/src/services/writeback.rs
  - crates/veld/src/routes/integrations.rs
  - crates/veld/src/routes/sync.rs
  - docs/user/integrations/todoist.md
  - docs/api/runtime.md
autonomous: true
requirements:
  - WB-01
  - WB-02
  - TODO-01
  - CONFLICT-01
  - PROV-01
must_haves:
  truths:
    - "Todoist stops treating labels as the durable core model; label parsing lives at the adapter boundary only."
    - "Todoist writes use explicit typed operations and durable write-back/conflict records."
    - "Todoist project/task linkage resolves through typed project upstream IDs and source refs rather than slug-only heuristics."
  artifacts:
    - path: "crates/veld/src/services/integrations_todoist.rs"
      provides: "typed Todoist read/write policy plus label-boundary mapping"
      contains: "todoist_create_task"
    - path: "crates/veld/src/services/writeback.rs"
      provides: "Todoist provider-specific writeback execution entrypoints"
      contains: "execute_todoist_writeback"
    - path: "docs/user/integrations/todoist.md"
      provides: "operator-facing write-back and conflict guidance"
      contains: "draft|conflict|write-back"
  key_links:
    - from: "crates/veld/src/services/integrations_todoist.rs"
      to: "crates/vel-storage/src/repositories/projects_repo.rs"
      via: "Todoist project linkage uses typed upstream IDs"
      pattern: "upstream_ids|todoist"
    - from: "crates/veld/src/routes/integrations.rs"
      to: "crates/veld/src/services/writeback.rs"
      via: "Todoist write routes call the shared writeback service"
      pattern: "todoist_create_task|todoist_update_task|todoist_complete_task"
---

<objective>
Close the Todoist lane as the first real high-value write-back slice.

Purpose: prove the Phase 6 write-back and conflict model against an already shipped credential-backed integration with clear operator value.
Output: typed Todoist mapping and write operations, project/upstream linkage, conflict handling, and user/runtime docs that reflect the new boundary.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-CONTEXT.md
@.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-RESEARCH.md
@crates/veld/src/services/integrations_todoist.rs
@crates/veld/src/services/integrations.rs
@crates/vel-storage/src/repositories/projects_repo.rs
@docs/user/integrations/todoist.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Translate Todoist labels at the adapter boundary and anchor linkage in typed project refs</name>
  <files>crates/vel-core/src/commitment.rs, crates/vel-storage/src/repositories/projects_repo.rs, crates/veld/src/services/integrations_todoist.rs</files>
  <read_first>crates/vel-core/src/commitment.rs
crates/vel-storage/src/repositories/projects_repo.rs
crates/veld/src/services/integrations_todoist.rs
crates/vel-core/src/project.rs
crates/vel-core/src/integration.rs
.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-CONTEXT.md</read_first>
  <action>Extend the Todoist integration mapping so it stores and resolves typed task scheduling/write-back state separately from raw labels. Use the exact typed internal fields `project_id`, `scheduled_for`, `priority`, `waiting_on`, and `review_state`; parse provider labels into those fields inside `crates/veld/src/services/integrations_todoist.rs`, but do not expose raw labels as the long-term domain contract. Use `projects.upstream_ids_json["todoist"]` as the primary project linkage path before any slug fallback. When syncing Todoist tasks, always attach `IntegrationSourceRef`-compatible provenance so later write-back/conflict records can identify the exact external task and project.</action>
  <acceptance_criteria>- `rg -n "project_id|scheduled_for|priority|waiting_on|review_state" crates/veld/src/services/integrations_todoist.rs` returns all five typed fields.
- `rg -n "upstream_ids.*todoist|todoist.*upstream_ids" crates/vel-storage/src/repositories/projects_repo.rs crates/veld/src/services/integrations_todoist.rs` returns matches in both files.
- `rg -n "labels" crates/veld/src/services/integrations_todoist.rs` shows label parsing logic only inside the Todoist service, not new domain types.</acceptance_criteria>
  <verify>
    <automated>cargo test -p veld todoist -- --nocapture</automated>
  </verify>
  <done>Todoist label syntax is isolated to the adapter boundary and typed internal scheduling/write-back state becomes the durable contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add explicit Todoist write-back operations and conflict handling</name>
  <files>crates/veld/src/services/integrations_todoist.rs, crates/veld/src/services/integrations.rs, crates/veld/src/services/writeback.rs, crates/veld/src/routes/integrations.rs, crates/veld/src/routes/sync.rs, docs/user/integrations/todoist.md, docs/api/runtime.md</files>
  <read_first>crates/veld/src/services/integrations_todoist.rs
crates/veld/src/services/integrations.rs
crates/veld/src/services/writeback.rs
crates/veld/src/routes/integrations.rs
crates/veld/src/routes/sync.rs
crates/vel-core/src/writeback.rs
crates/vel-core/src/conflicts.rs
docs/user/integrations/todoist.md</read_first>
  <action>Add provider-specific execution helpers in `crates/veld/src/services/writeback.rs` for the exact Todoist operations `todoist_create_task`, `todoist_update_task`, `todoist_complete_task`, and `todoist_reopen_task`. Extend `crates/veld/src/services/integrations_todoist.rs` so each helper checks the latest upstream task state before applying changes and opens a `stale_write` or `upstream_vs_local` conflict case instead of silently overwriting when local intent no longer matches upstream. Expose operator-authenticated routes in `crates/veld/src/routes/integrations.rs` for those four operations, and keep `/v1/sync/todoist` as the read/sync path in `crates/veld/src/routes/sync.rs`. Update `docs/user/integrations/todoist.md` and `docs/api/runtime.md` so they explicitly document the allowed Todoist write actions, the conflict-review path, and the fact that labels remain compatibility-only metadata.</action>
  <acceptance_criteria>- `rg -n "todoist_create_task|todoist_update_task|todoist_complete_task|todoist_reopen_task" crates/veld/src/services/writeback.rs crates/veld/src/routes/integrations.rs` returns all four operation names in both layers.
- `rg -n "stale_write|upstream_vs_local" crates/veld/src/services/integrations_todoist.rs crates/veld/src/services/writeback.rs` returns both conflict kinds.
- `rg -n "/v1/sync/todoist" crates/veld/src/routes/sync.rs` returns the existing sync route.
- `rg -n "allowed Todoist write|conflict|labels remain compatibility" docs/user/integrations/todoist.md docs/api/runtime.md` returns matching guidance in both docs.</acceptance_criteria>
  <verify>
    <automated>cargo test -p veld todoist -- --nocapture && cargo test -p veld integrations -- --nocapture</automated>
  </verify>
  <done>The Todoist lane supports bounded high-value writes with durable provenance and explicit conflict review instead of implicit last-write-wins behavior.</done>
</task>

</tasks>

<verification>
Run focused Todoist and integrations tests proving typed mapping, allowed write ops, and conflict persistence. Re-grep the docs so operator guidance matches the actual write surface.
</verification>

<success_criteria>
Todoist becomes the first complete Phase 6 write-back lane: explicit operation set, typed internal mapping, durable write history, and inspectable conflict handling.
</success_criteria>

<output>
After completion, create `.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-03-SUMMARY.md`
</output>
