---
phase: 06-high-value-write-back-integrations-and-lightweight-people-graph
plan: 04
type: execute
wave: 3
depends_on:
  - 06-01
  - 06-02
files_modified:
  - crates/veld/src/adapters/notes.rs
  - crates/veld/src/adapters/reminders.rs
  - crates/veld/src/adapters/transcripts.rs
  - crates/veld/src/services/integrations.rs
  - crates/veld/src/services/writeback.rs
  - crates/veld/src/services/client_sync.rs
  - crates/veld/src/routes/integrations.rs
  - crates/veld/src/routes/sync.rs
  - docs/user/integrations/local-sources.md
  - docs/api/runtime.md
autonomous: true
requirements:
  - NOTES-01
  - REMIND-01
  - WB-02
  - WB-03
  - PROV-01
must_haves:
  truths:
    - "Notes writes stay inside configured notes roots or project notes roots and are never allowed to escape those scopes."
    - "Transcripts become a notes source subtype instead of remaining a parallel top-level write/read lane."
    - "Reminder write-back is explicit, typed, and inspectable even when execution is queued for a local executor or linked client."
  artifacts:
    - path: "crates/veld/src/services/writeback.rs"
      provides: "notes and reminders provider-specific writeback execution"
      contains: "notes_create_note|reminders_create"
    - path: "crates/veld/src/adapters/transcripts.rs"
      provides: "transcripts folded under notes provenance or source subtype"
      contains: "notes"
    - path: "docs/user/integrations/local-sources.md"
      provides: "operator guidance for scoped note writes and reminder intents"
      contains: "notes root|reminder intent"
  key_links:
    - from: "crates/veld/src/services/writeback.rs"
      to: "crates/veld/src/adapters/notes.rs"
      via: "notes writeback respects configured root boundaries"
      pattern: "notes_create_note|notes_append_note"
    - from: "crates/veld/src/services/client_sync.rs"
      to: "crates/veld/src/services/writeback.rs"
      via: "pending reminder intents and note writes project into sync/bootstrap state"
      pattern: "pending_writebacks"
---

<objective>
Turn notes, transcripts, and reminders into one coherent local-first write lane with explicit execution and provenance boundaries.

Purpose: give the operator safe project-linked note creation/appends, fold transcripts under notes, and make reminder writes inspectable without forcing client-owned policy.
Output: scoped notes write-back, transcript-under-notes mapping, reminder intent execution/result tracking, and matching runtime/user docs.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-CONTEXT.md
@.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-RESEARCH.md
@crates/veld/src/adapters/notes.rs
@crates/veld/src/adapters/reminders.rs
@crates/veld/src/adapters/transcripts.rs
@crates/veld/src/services/integrations.rs
@crates/veld/src/services/client_sync.rs
@docs/user/integrations/local-sources.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add scoped notes write-back and fold transcripts under notes</name>
  <files>crates/veld/src/adapters/notes.rs, crates/veld/src/adapters/transcripts.rs, crates/veld/src/services/writeback.rs, crates/veld/src/services/integrations.rs, crates/veld/src/routes/integrations.rs, docs/user/integrations/local-sources.md</files>
  <read_first>crates/veld/src/adapters/notes.rs
crates/veld/src/adapters/transcripts.rs
crates/veld/src/services/writeback.rs
crates/veld/src/services/integrations.rs
crates/vel-core/src/project.rs
crates/vel-core/src/writeback.rs
docs/user/integrations/local-sources.md</read_first>
  <action>Add notes writeback helpers in `crates/veld/src/services/writeback.rs` for `notes_create_note` and `notes_append_note`. Restrict writes to exactly these roots: the global configured `notes_path`, any typed `project.primary_notes_root.path`, and any `project.secondary_notes_roots[].path`. Deny and persist a `blocked` write-back result for any path outside those roots. Update `crates/veld/src/adapters/transcripts.rs` so transcript ingestion emits notes-compatible provenance and is treated as a notes source subtype rather than a separate top-level semantic/write surface. Expose operator-authenticated note write routes in `crates/veld/src/routes/integrations.rs` and update `docs/user/integrations/local-sources.md` to document scoped note writes, project notes roots, and transcript-under-notes behavior.</action>
  <acceptance_criteria>- `rg -n "notes_create_note|notes_append_note" crates/veld/src/services/writeback.rs crates/veld/src/routes/integrations.rs` returns both operation names in both layers.
- `rg -n "primary_notes_root|secondary_notes_roots|notes_path" crates/veld/src/services/writeback.rs` returns all three allowed note root sources.
- `rg -n "blocked" crates/veld/src/services/writeback.rs` returns the denied out-of-scope write path.
- `rg -n "notes" crates/veld/src/adapters/transcripts.rs` returns notes-oriented provenance or source-subtype handling.
- `rg -n "project notes roots|transcript" docs/user/integrations/local-sources.md` returns both concepts.</acceptance_criteria>
  <verify>
    <automated>cargo test -p veld notes -- --nocapture</automated>
  </verify>
  <done>Notes writes are safely scoped, transcripts fold under the same notes lane, and the operator can see the exact write boundary instead of a generic filesystem capability.</done>
</task>

<task type="auto">
  <name>Task 2: Model reminders as typed intents with explicit execution and result tracking</name>
  <files>crates/veld/src/adapters/reminders.rs, crates/veld/src/services/writeback.rs, crates/veld/src/services/integrations.rs, crates/veld/src/services/client_sync.rs, crates/veld/src/routes/integrations.rs, crates/veld/src/routes/sync.rs, docs/api/runtime.md</files>
  <read_first>crates/veld/src/adapters/reminders.rs
crates/veld/src/services/writeback.rs
crates/veld/src/services/integrations.rs
crates/veld/src/services/client_sync.rs
crates/veld/src/routes/integrations.rs
crates/veld/src/routes/sync.rs
crates/vel-core/src/writeback.rs
crates/vel-core/src/conflicts.rs
docs/api/runtime.md</read_first>
  <action>Add reminder writeback helpers in `crates/veld/src/services/writeback.rs` for `reminders_create`, `reminders_update`, and `reminders_complete`. Model these as typed write intents that can either execute locally through an approved local executor path or remain `queued`/`executor_unavailable` until a local executor or linked client can apply them. Persist the result of every reminder intent through the shared writeback and conflict records. Extend `crates/veld/src/services/client_sync.rs` so pending reminder intents and results appear in sync/bootstrap for thin clients. Expose operator-authenticated reminder write routes in `crates/veld/src/routes/integrations.rs`, keep `/v1/sync/reminders` as the read/sync path in `crates/veld/src/routes/sync.rs`, and update `docs/api/runtime.md` so reminder writes are documented as intent-based with explicit pending/applied/conflicted states.</action>
  <acceptance_criteria>- `rg -n "reminders_create|reminders_update|reminders_complete" crates/veld/src/services/writeback.rs crates/veld/src/routes/integrations.rs` returns all three operations in both layers.
- `rg -n "executor_unavailable|queued|applied|conflicted" crates/veld/src/services/writeback.rs` returns all four reminder intent states.
- `rg -n "pending_writebacks" crates/veld/src/services/client_sync.rs` returns sync/bootstrap projection of pending intents.
- `rg -n "/v1/sync/reminders" crates/veld/src/routes/sync.rs` returns the existing reminders sync route.
- `rg -n "intent-based|queued|executor_unavailable|conflicted" docs/api/runtime.md` returns reminder write lifecycle language.</acceptance_criteria>
  <verify>
    <automated>cargo test -p veld reminders -- --nocapture && cargo test -p veld sync -- --nocapture</automated>
  </verify>
  <done>Reminder writes become explicit, inspectable operations with queued/applied/conflicted states rather than hidden side effects or thin-client policy forks.</done>
</task>

</tasks>

<verification>
Run focused notes, reminders, and sync tests proving scope enforcement, transcript folding, and reminder intent visibility across backend and sync/bootstrap surfaces.
</verification>

<success_criteria>
Notes, transcripts, and reminders form one local-first Phase 6 lane with explicit write scope, durable result tracking, and thin-client continuity.
</success_criteria>

<output>
After completion, create `.planning/phases/06-high-value-write-back-integrations-and-lightweight-people-graph/06-04-SUMMARY.md`
</output>
