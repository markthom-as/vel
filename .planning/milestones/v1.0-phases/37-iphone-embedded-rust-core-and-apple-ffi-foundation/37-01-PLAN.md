# 37-01 Plan

## Slice

Publish the iPhone embedded-core / FFI contract, feature-gating rules, and daemon-vs-embedded boundary map.

## Work

- define the embedded-capable Apple runtime profile with explicit platform gates and feature gates
- document which flows may run locally on iPhone in Phase 37 and which remain daemon-backed
- add the schema/example/manifest updates for the new embedded-capable contract
- align Apple and cross-surface docs to the same current-truth-versus-target-state language

## Verification

- JSON parse checks for the new schema/example/manifest entries
- targeted doc truth checks across Apple/runtime/architecture entrypoints

## Exit

Phase 37 has a precise contract for the embedded-capable iPhone path instead of only aspirational prose.
