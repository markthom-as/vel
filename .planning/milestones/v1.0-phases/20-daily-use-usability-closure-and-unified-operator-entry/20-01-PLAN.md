---
phase: 20-daily-use-usability-closure-and-unified-operator-entry
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - clients/web/src/components/ThreadView.tsx
  - clients/web/src/components/ThreadView.test.tsx
  - clients/web/src/components/MessageComposer.tsx
  - clients/web/src/components/MessageComposer.test.tsx
  - docs/api/chat.md
  - docs/user/daily-use.md
autonomous: true
requirements:
  - ENTRY-01
  - THREADS-UX-01
must_haves:
  truths:
    - "The empty and first-use thread state teaches assistant-first continuity instead of stale placeholder copy."
    - "The shared composer contract explicitly covers ask/capture/send behavior before Phase 20 widens entry routing."
    - "Focused web tests fail only on real behavior regressions, not on known placeholder drift."
  artifacts:
    - path: "clients/web/src/components/ThreadView.test.tsx"
      provides: "Intentional regression coverage for empty thread, fallback thread selection, and composer presence."
      contains: "No messages yet"
    - path: "clients/web/src/components/MessageComposer.test.tsx"
      provides: "Locked assistant-entry copy and send behavior coverage."
      contains: "Ask"
    - path: "docs/api/chat.md"
      provides: "Authority for the current chat/message surface before the new entry seam lands."
      contains: "POST /api/conversations/:id/messages"
  key_links:
    - from: "clients/web/src/components/ThreadView.tsx"
      to: "clients/web/src/components/MessageComposer.tsx"
      via: "shared empty-state and composer contract"
      pattern: "MessageComposer"
    - from: "clients/web/src/components/MessageComposer.test.tsx"
      to: "docs/user/daily-use.md"
      via: "assistant-first entry wording"
      pattern: "assistant"
---

<objective>
Stabilize the drifting thread/composer UI contract before Phase 20 adds a new entry seam.

Purpose: Prevent later assistant-entry work from hiding real regressions behind stale placeholder assertions or inconsistent copy.
Output: updated `ThreadView` and `MessageComposer` behavior/tests plus aligned operator/API wording for the current entry surface.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-daily-use-usability-closure-and-unified-operator-entry/20-CONTEXT.md
@.planning/phases/20-daily-use-usability-closure-and-unified-operator-entry/20-RESEARCH.md
@.planning/phases/20-daily-use-usability-closure-and-unified-operator-entry/20-VALIDATION.md
@docs/api/chat.md
@docs/user/daily-use.md
@clients/web/src/components/ThreadView.tsx
@clients/web/src/components/ThreadView.test.tsx
@clients/web/src/components/MessageComposer.tsx
@clients/web/src/components/MessageComposer.test.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Repair the ThreadView and composer regression contract</name>
  <files>clients/web/src/components/ThreadView.test.tsx, clients/web/src/components/MessageComposer.test.tsx</files>
  <behavior>
    - Test 1: Empty `Threads` state explains continuity/follow-up and still exposes the shared composer.
    - Test 2: Composer copy reflects one assistant-first entry path for asking, capture, and sending.
    - Test 3: Existing realtime and optimistic-send assertions still pass after the contract update.
  </behavior>
  <action>Update the focused tests first so they intentionally describe the Phase 20 entry posture instead of the stale placeholder contract. Keep the first slice narrow: repair the known `ThreadView` drift, lock the `MessageComposer` copy/placeholder contract, and avoid inventing any new routing or backend behavior in this step.</action>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/ThreadView.test.tsx src/components/MessageComposer.test.tsx</automated>
  </verify>
  <done>`ThreadView` and `MessageComposer` tests describe the intended assistant-first contract and fail only for true UI or data-flow regressions.</done>
</task>

<task type="auto">
  <name>Task 2: Align current thread/composer behavior and docs to the repaired contract</name>
  <files>clients/web/src/components/ThreadView.tsx, clients/web/src/components/MessageComposer.tsx, docs/api/chat.md, docs/user/daily-use.md</files>
  <action>Update `ThreadView` and `MessageComposer` so empty-state, header, and placeholder wording match the repaired tests and the Phase 20 product boundaries. Reflect that current typed entry still flows through conversation messages, that `Threads` owns continuity, and that the next plan will add a new backend entry seam. Do not add voice parity, daily-loop assistant closure, or shell-owned heuristics here.</action>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/ThreadView.test.tsx src/components/MessageComposer.test.tsx && rg -n "Threads|assistant|capture|conversation" docs/api/chat.md docs/user/daily-use.md</automated>
  </verify>
  <done>The live thread/composer UI and nearby docs use the same entry wording and preserve the existing product boundary ahead of backend seam changes.</done>
</task>

</tasks>

<verification>
Confirm the repaired tests capture the intended empty-state and composer wording. Read the updated docs once from the perspective of an operator entering through `Threads` today and confirm the seam is described truthfully as current-state behavior, not future voice/daily-loop scope.
</verification>

<success_criteria>
Phase 20 starts from an explicit, stable thread/composer contract: no stale placeholder drift, no hidden copy mismatch, and no ambiguity about `Threads` owning continuity while broader assistant entry work is still to come.
</success_criteria>

<output>
After completion, create `.planning/phases/20-daily-use-usability-closure-and-unified-operator-entry/20-01-SUMMARY.md`
</output>
