---
phase: 24-approved-assistant-action-application-and-reversible-write-execution
plan: 01
type: execute
depends_on: []
files_modified:
  - crates/vel-core/src/operator_queue.rs
  - crates/vel-api-types/src/lib.rs
  - crates/veld/src/services/chat/messages.rs
  - crates/veld/src/routes/chat.rs
  - clients/web/src/types.ts
  - crates/veld/tests/
autonomous: true
requirements:
  - ASSIST-APPLY-01
must_haves:
  truths:
    - "Approved assistant proposals can transition through a canonical backend application contract instead of stopping at staged-only state."
    - "Proposal outcome state remains explicit and inspectable rather than becoming implicit shell behavior."
---

<objective>
Publish the backend-owned approved-application contract and canonical proposal state transitions.
</objective>

<tasks>
- Introduce typed proposal outcome state that can represent staged, approved, applied, failed, or reversed outcomes without inventing shell-local semantics.
- Reuse canonical action vocabulary, review/writeback lineage, and bounded target references instead of creating a separate assistant-apply model.
- Add focused tests proving approved assistant proposals persist explicit backend-owned state transitions rather than silently mutating state.
</tasks>

<verification>
- cargo test -p veld --test chat_assistant_entry -- --nocapture
- cargo test -p veld operator_queue -- --nocapture
- npm --prefix clients/web test -- --run src/types.test.ts
</verification>

<output>
Create `24-01-SUMMARY.md`.
</output>
