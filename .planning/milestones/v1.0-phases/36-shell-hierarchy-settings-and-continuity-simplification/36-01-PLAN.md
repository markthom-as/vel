# 36-01 Plan

## Slice

Publish and embody the simplified shell hierarchy contract: primary `Now`, contextual `Threads`, advanced `Settings`, and thin icon-rail sidebar.

## Work

- align the shell hierarchy wording and core layout rules
- make the sidebar thin/secondary by default
- keep zero-or-one thread resurfacing explicit in shell guidance
- ensure top-level affordances do not imply thread-first usage

## Verification

- targeted web tests for shell chrome or navigation behavior
- docs `rg` checks for hierarchy/sidebar wording

## Exit

The simplified hierarchy is explicit and embodied enough that later slices can restructure `Settings` and `Threads` without re-arguing the shell model.
