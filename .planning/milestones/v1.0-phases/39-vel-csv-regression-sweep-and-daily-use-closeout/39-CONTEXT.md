# Phase 39 Context

## Title

Vel.csv regression sweep and daily-use closeout

## Why this phase exists

The operator explicitly wants planning and execution to continue using `~/Downloads/Vel.csv`, but not as the sole source of product truth.

By the time Phase 39 begins, the preceding arc should already have:

- repaired `Now`
- corrected the current-day model
- simplified shell hierarchy
- introduced the real iPhone embedded-core path
- proved a local-first voice continuity loop

This final phase exists to turn the remaining `Vel.csv` pressure into a structured regression and closeout sweep rather than leaving the new product shape half-verified.

## Product problem

Without an explicit closeout pass, the repo risks repeating the same pattern that caused the current UI/UX complaints:

- layered features
- partial corrections
- silent regressions
- unresolved friction that never gets pulled back into one acceptance pass

## Phase goal

Use `Vel.csv` as a regression guardrail and acceptance matrix to verify that web and Apple can support a real daily-use loop cleanly after the repaired arc lands.

## Must stay true

- `Vel.csv` validates and regresses; it does not replace product authority
- the acceptance spine is the operator’s “good Vel day” from wake-up to closeout
- richer context should help action rather than add slop
- unresolved gaps after this phase should be explicitly deferred, not silently tolerated

## Acceptance spine

The closeout should support this shape:

- wake-up / alarm flow
- calendar summary
- standup start
- fluid voice ↔ thread continuity
- clear must/should/defer priorities
- day-long `Now` reflection of those priorities
- in-context nudges and metadata prompts
- likely closeout detection
- end-of-day wrap-up and feedback on Vel’s own performance

## Likely touch points

- `~/Downloads/Vel.csv`
- web shell surfaces
- Apple surfaces
- daily-use docs
- verification and closeout docs

## Expected next step

Phase 39 planning should break this into:

1. structured `Vel.csv` regression matrix creation
2. remaining daily-use friction cleanup
3. cross-surface “good Vel day” verification
4. milestone closeout truth and documented deferrals
