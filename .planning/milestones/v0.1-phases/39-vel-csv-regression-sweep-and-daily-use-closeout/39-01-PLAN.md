# 39-01 Plan

## Slice

Turn `~/Downloads/Vel.csv` into a structured regression and acceptance matrix for the Phase 34-38 daily-use arc.

## Work

- extract the relevant `Vel.csv` items into normalized acceptance buckets
- map those buckets to shipped product rules and known surfaces
- create a checked-in matrix that distinguishes validated, regressed, open, and deferred items

## Verification

- evidence that the matrix is traceable to both `Vel.csv` and the locked product rules
- focused truth checks against the shipped docs/surfaces the matrix references

## Exit

`Vel.csv` is usable as a regression guardrail for the rest of Phase 39.
