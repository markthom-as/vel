---
phase: 1.1
slug: preflight-pre-phase-2-hardening
status: draft
nyquist_compliant: false
wave_0_complete: false
created: 2026-03-18
---

# Phase 1.1 — Validation Strategy

> Per-phase validation contract for feedback sampling during execution.

---

## Test Infrastructure

| Property | Value |
|----------|-------|
| **Framework** | cargo test (Rust built-in) + tokio::test async runtime |
| **Config file** | Cargo.toml workspace (no separate test config) |
| **Quick run command** | `cargo test -p veld` |
| **Full suite command** | `cargo test --workspace --all-features` |
| **Estimated runtime** | ~60-120 seconds |

---

## Sampling Rate

- **After every task commit:** Run `cargo test -p veld`
- **After every plan wave:** Run `cargo test --workspace --all-features`
- **Before `/gsd:verify-work`:** Full suite must be green
- **Max feedback latency:** 120 seconds

---

## Per-Task Verification Map

| Task ID | Plan | Wave | Success Criterion | Test Type | Automated Command | File Exists | Status |
|---------|------|------|-------------------|-----------|-------------------|-------------|--------|
| 1.1-P1-01 | P1 | 1 | SC-1: missing settings → Default | unit | `cargo test -p veld services::integrations` | ❌ W0 | ⬜ pending |
| 1.1-P1-02 | P1 | 1 | SC-1: corrupt settings → warn + Default | unit | `cargo test -p veld services::integrations` | ❌ W0 | ⬜ pending |
| 1.1-P2-01 | P2 | 1 | SC-2: WAL enabled for file DB | unit | `cargo test -p vel-storage infra` | ❌ W0 | ⬜ pending |
| 1.1-P2-02 | P2 | 1 | SC-2: WAL guard for memory DB | unit | `cargo test -p vel-storage infra` | ❌ W0 | ⬜ pending |
| 1.1-P3-01 | P3 | 2 | SC-3: middleware compiles in new location | build | `cargo build -p veld` | ✅ | ⬜ pending |
| 1.1-P3-02 | P3 | 2 | SC-3: all auth tests still pass | integration | `cargo test -p veld app::tests` | ✅ | ⬜ pending |

*Status: ⬜ pending · ✅ green · ❌ red · ⚠️ flaky*

---

## Wave 0 Requirements

- [ ] `crates/vel-storage/src/infra.rs` — add `#[tokio::test] async fn wal_mode_enabled_for_file_db` — covers SC-2
- [ ] `crates/vel-storage/src/infra.rs` — add `#[tokio::test] async fn wal_mode_skipped_for_memory_db` — covers SC-2 WAL guard
- [ ] `crates/veld/src/services/integrations_todoist.rs` or `integrations.rs` — add test for missing key path — covers SC-1
- [ ] `crates/veld/src/services/integrations_todoist.rs` or `integrations.rs` — add test for corrupt JSON path — covers SC-1

---

## Manual-Only Verifications

| Behavior | Criterion | Why Manual | Test Instructions |
|----------|-----------|------------|-------------------|
| Daemon starts cleanly with no settings configured | SC-1 | Runtime startup requires full daemon launch | Remove or empty Todoist/Google Calendar settings from vel.toml; run `make dev-api`; verify no panic, check logs for warning messages |
| WAL files appear on disk | SC-2 | Confirms OS-level WAL file creation | After daemon start, check for `<db>.db-wal` and `<db>.db-shm` files next to the SQLite database file |

---

## Validation Sign-Off

- [ ] All tasks have `<automated>` verify or Wave 0 dependencies
- [ ] Sampling continuity: no 3 consecutive tasks without automated verify
- [ ] Wave 0 covers all MISSING references
- [ ] No watch-mode flags
- [ ] Feedback latency < 120s
- [ ] `nyquist_compliant: true` set in frontmatter

**Approval:** pending
