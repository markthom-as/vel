---
phase: 03-deterministic-verification-continuous-alignment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - crates/vel-core/src/run.rs
  - crates/vel-api-types/src/lib.rs
  - crates/veld/src/routes/runs.rs
  - crates/veld/src/app.rs
  - docs/api/runtime.md
  - docs/cognitive-agent-architecture/agents/handoffs.md
autonomous: true
requirements:
  - TRACE-01
  - TRACE-02

must_haves:
  truths:
    - "Run inspection surfaces expose explicit trace linkage without breaking existing run APIs"
    - "Trace and handoff contract language is aligned between code-facing docs and operator-facing runtime docs"
    - "Trace metadata is derived from structured persisted inputs/events, not ad hoc string formatting"
  artifacts:
    - path: "crates/vel-core/src/run.rs"
      provides: "Shared trace and/or handoff domain contract types"
    - path: "crates/vel-api-types/src/lib.rs"
      provides: "Trace-aware run DTO fields for operator surfaces"
    - path: "crates/veld/src/routes/runs.rs"
      provides: "Run list/detail handlers that expose trace linkage"
    - path: "docs/api/runtime.md"
      provides: "Runtime API contract documenting trace-aware run inspection"
    - path: "docs/cognitive-agent-architecture/agents/handoffs.md"
      provides: "Aligned handoff envelope language tied to trace-linked inspection"
  key_links:
    - from: "crates/vel-core/src/run.rs"
      to: "crates/vel-api-types/src/lib.rs"
      via: "trace-linked DTO fields"
      pattern: "trace_id|parent_run_id"
    - from: "crates/veld/src/routes/runs.rs"
      to: "docs/api/runtime.md"
      via: "run inspection response shape"
      pattern: "trace"
---

<objective>
SP1 Lane A/B entry slice — trace contract and run inspection linkage. Introduce explicit trace-aware shared types and expose trace linkage in run operator surfaces so Phase 3 reviewability has a concrete contract before deeper storage, simulation, or eval work.

Output: trace-aware run DTOs, route mapping, aligned docs, and focused tests proving the contract.
</objective>

<context>
@.planning/phases/03-deterministic-verification-continuous-alignment/03-CONTEXT.md
@docs/tickets/phase-3/017-execution-tracing-reviewability.md
@docs/tickets/phase-3/parallel-execution-board.md
@crates/vel-core/src/run.rs
@crates/vel-api-types/src/lib.rs
@crates/veld/src/routes/runs.rs
@docs/api/runtime.md
@docs/cognitive-agent-architecture/agents/handoffs.md
</context>

<tasks>
- Add minimal shared trace/handoff contract types or helpers in `vel-core` that make trace linkage explicit and serializable
- Extend run summary/detail DTOs with trace-linked metadata fields
- Map trace metadata in `routes/runs.rs` from persisted structured input/output/event payloads with compatibility fallbacks for existing runs
- Update API and architecture docs to describe the new run-inspection trace contract
- Add/extend focused tests covering trace-aware run detail responses and contract serialization
</tasks>

<verify>
- `cargo test -p veld get_run_includes_automatic_retry_policy -- --nocapture`
- `cargo test -p veld run -- --nocapture`
</verify>
