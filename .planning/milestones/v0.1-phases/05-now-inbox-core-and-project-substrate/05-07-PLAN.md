---
phase: 05-now-inbox-core-and-project-substrate
plan: 07
type: execute
wave: 6
depends_on:
  - 05-06
files_modified:
  - clients/web/src/App.tsx
  - clients/web/src/components/NowView.tsx
  - clients/web/src/components/InboxView.tsx
  - clients/web/src/components/MainPanel.tsx
  - clients/web/src/components/Sidebar.tsx
  - clients/web/src/components/ProjectsView.tsx
  - clients/web/src/components/SettingsPage.tsx
  - clients/web/src/components/NowView.test.tsx
  - clients/web/src/components/InboxView.test.tsx
  - clients/web/src/components/MainPanel.test.tsx
  - clients/web/src/components/Sidebar.test.tsx
  - clients/web/src/components/ProjectsView.test.tsx
autonomous: true
requirements:
  - NOW-01
  - NOW-02
  - INBOX-01
  - INBOX-02
  - REVIEW-01
  - CONTINUITY-01
  - CONTINUITY-02
  - PROJ-03
must_haves:
  truths:
    - "`Now` remains the default landing surface and shows the ranked action stack before schedule and source panels."
    - "`Inbox` renders dense triage rows with `Acknowledge`, `Snooze 10m`, `Dismiss`, and `Open thread` actions when available."
    - "`Projects` and linking/status UI become real supporting surfaces without displacing `Now` and `Inbox` from the primary navigation order."
  artifacts:
    - path: "clients/web/src/components/NowView.tsx"
      provides: "ranked action stack above schedule and source panels"
      contains: "actionItems"
    - path: "clients/web/src/components/InboxView.tsx"
      provides: "compact triage queue rows with explicit actions"
      contains: "Acknowledge"
    - path: "clients/web/src/components/ProjectsView.tsx"
      provides: "project registry and local-first project draft UI"
      contains: "Create project"
    - path: "clients/web/src/components/SettingsPage.tsx"
      provides: "linked-node status and guided pairing entry point"
      contains: "linkedNodes"
  key_links:
    - from: "clients/web/src/components/InboxView.tsx"
      to: "clients/web/src/App.tsx"
      via: "open-thread action reuses the canonical Threads navigation state"
      pattern: "setMainView\\('threads'\\)|selectedConversationId|getInboxThreadPath"
    - from: "clients/web/src/components/MainPanel.tsx"
      to: "clients/web/src/components/ProjectsView.tsx"
      via: "projects surface wiring"
      pattern: "ProjectsView"
    - from: "clients/web/src/components/Sidebar.tsx"
      to: "clients/web/src/components/MainPanel.tsx"
      via: "primary navigation keeps now, inbox, then projects"
      pattern: "projects"
---

<objective>
Ship the Phase 05 web views on top of the typed data layer without moving policy into React.

Purpose: Keep the web shell truthful and thin while replacing the Projects placeholder and surfacing the ranked `Now`/`Inbox` loop plus guided linking status.
Output: Updated `Now` and `Inbox`, a real `ProjectsView`, linked-node status/pairing UI, and focused view tests.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-now-inbox-core-and-project-substrate/05-CONTEXT.md
@.planning/phases/05-now-inbox-core-and-project-substrate/05-UI-SPEC.md
@clients/web/README.md
@clients/web/src/App.tsx
@clients/web/src/components/NowView.tsx
@clients/web/src/components/InboxView.tsx
@clients/web/src/components/MainPanel.tsx
@clients/web/src/components/Sidebar.tsx
@clients/web/src/types.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild `Now` and `Inbox` around the ranked action and triage model</name>
  <files>clients/web/src/App.tsx, clients/web/src/components/NowView.tsx, clients/web/src/components/InboxView.tsx, clients/web/src/components/NowView.test.tsx, clients/web/src/components/InboxView.test.tsx</files>
  <read_first>clients/web/src/App.tsx
clients/web/src/components/NowView.tsx
clients/web/src/components/InboxView.tsx
clients/web/src/components/NowView.test.tsx
clients/web/src/components/InboxView.test.tsx
clients/web/src/data/chat.ts
clients/web/src/types.ts
.planning/phases/05-now-inbox-core-and-project-substrate/05-UI-SPEC.md</read_first>
  <action>Update `NowView.tsx` so the first major panel under the header is a ranked action stack rendered from `data.action_items`, ahead of schedule and source activity. Each row must show `kind`, `title`, `summary`, surfaced time, optional `project_label`, and at least one evidence label. Keep the existing freshness banner and supporting schedule/source panels below it. Update `InboxView.tsx` from the current generic intervention list into dense triage rows with `title`, `summary`, `kind`, `project_label`, `available_actions`, and explicit buttons for `Acknowledge`, `Snooze 10m`, `Dismiss`, and `Open thread` when those actions are present. Wire the buttons to the new data-layer helpers. Update `App.tsx` so the `Open thread` path can set `selectedConversationId` and switch to the canonical Threads surface; do not introduce a new inbox-specific thread screen. Preserve the dark-zinc shell, 44px touch targets for row actions, and the empty-state copy from `05-UI-SPEC.md`. Extend the two component tests so they assert action-stack ordering, evidence rendering, and the four explicit Inbox button labels.</action>
  <acceptance_criteria>- `rg -n "actionItems|data.action_items" clients/web/src/components/NowView.tsx` returns at least one match.
- `rg -n "Acknowledge|Snooze 10m|Dismiss|Open thread" clients/web/src/components/InboxView.tsx` returns all four button labels.
- `rg -n "Inbox is clear|No actions need triage right now" clients/web/src/components/InboxView.tsx` returns the empty-state copy.
- `rg -n "Acknowledge|Open thread|evidence" clients/web/src/components/InboxView.test.tsx clients/web/src/components/NowView.test.tsx` returns assertions across both tests.</acceptance_criteria>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/NowView.test.tsx src/components/InboxView.test.tsx</automated>
  </verify>
  <done>The web `Now` and `Inbox` views reflect the ranked action/triage contract and expose explicit triage controls without moving policy into React.</done>
</task>

<task type="auto">
  <name>Task 2: Replace the Projects placeholder and surface guided linking/status UI</name>
  <files>clients/web/src/components/MainPanel.tsx, clients/web/src/components/Sidebar.tsx, clients/web/src/components/ProjectsView.tsx, clients/web/src/components/SettingsPage.tsx, clients/web/src/components/MainPanel.test.tsx, clients/web/src/components/Sidebar.test.tsx, clients/web/src/components/ProjectsView.test.tsx</files>
  <read_first>clients/web/src/components/MainPanel.tsx
clients/web/src/components/Sidebar.tsx
clients/web/src/components/ProjectsView.tsx
clients/web/src/components/SettingsPage.tsx
clients/web/src/components/MainPanel.test.tsx
clients/web/src/components/Sidebar.test.tsx
clients/web/src/components/ProjectsView.test.tsx
clients/web/src/data/operator.ts
.planning/phases/05-now-inbox-core-and-project-substrate/05-UI-SPEC.md
clients/web/README.md</read_first>
  <action>Create `clients/web/src/components/ProjectsView.tsx` and wire it through `MainPanel.tsx` in place of the placeholder. The view must render a grouped list by family (`Personal`, `Creative`, `Work`), a detail pane showing primary repo and notes root, and a local-first `Create project` form that includes two explicit confirmation checkboxes for `Create upstream repo later` and `Create notes root later`. Update `SettingsPage.tsx` to show linked-node status cards plus a guided pairing entry flow that starts token issue, displays granted scopes, and links to the CLI fallback copy. Keep `Now` and `Inbox` as the first two nav items in `Sidebar.tsx`; do not promote `Projects` above them. Extend the `MainPanel`, `Sidebar`, and `ProjectsView` tests to verify the placeholder is gone, nav order remains `now` then `inbox` then `projects`, and the linking/status copy is rendered from real data-driven state.</action>
  <acceptance_criteria>- `rg -n "Create project" clients/web/src/components/ProjectsView.tsx` returns one match.
- `rg -n "Personal|Creative|Work" clients/web/src/components/ProjectsView.tsx` returns all three families.
- `rg -n "linkedNodes|Issue pairing token|CLI fallback" clients/web/src/components/SettingsPage.tsx` returns all three strings or identifiers.
- `rg -n "{ view: 'now'|{ view: 'inbox'|{ view: 'projects'" clients/web/src/components/Sidebar.tsx` shows `now`, `inbox`, then `projects` in that order.</acceptance_criteria>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/MainPanel.test.tsx src/components/Sidebar.test.tsx src/components/ProjectsView.test.tsx</automated>
  </verify>
  <done>The web shell has a real supporting Projects surface plus guided linking/status UI, while `Now` and `Inbox` remain the primary operator entry points.</done>
</task>

</tasks>

<verification>
Run the focused Vitest suites for `Now`, `Inbox`, `Projects`, and shell wiring. Confirm the UI honors the Phase 05 copy and hierarchy contract.
</verification>

<success_criteria>
The web client consumes the shared Phase 05 backend state directly, exposes a truthful Projects surface, and keeps `Now` and `Inbox` primary without forking decision logic into the browser.
</success_criteria>

<output>
After completion, create `.planning/phases/05-now-inbox-core-and-project-substrate/05-07-SUMMARY.md`
</output>
