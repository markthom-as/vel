---
phase: 05-now-inbox-core-and-project-substrate
plan: 03
type: execute
wave: 3
depends_on:
  - 05-02
files_modified:
  - migrations/0039_phase5_linking.sql
  - crates/vel-storage/src/db.rs
  - crates/vel-storage/src/lib.rs
  - crates/vel-storage/src/repositories/mod.rs
  - crates/vel-storage/src/repositories/linking_repo.rs
  - crates/veld/src/services/mod.rs
  - crates/veld/src/services/linking.rs
  - crates/veld/src/routes/mod.rs
  - crates/veld/src/routes/linking.rs
  - crates/veld/src/app.rs
autonomous: true
requirements:
  - CONTINUITY-02
must_haves:
  truths:
    - "Operators can issue short-lived scoped pairing tokens and redeem them into linked-node trust state."
    - "Expired, already-redeemed, malformed, or out-of-scope tokens fail closed without mutating linked-node trust state."
  artifacts:
    - path: "migrations/0039_phase5_linking.sql"
      provides: "pairing_tokens and linked_nodes tables"
      contains: "CREATE TABLE pairing_tokens"
    - path: "crates/veld/src/services/linking.rs"
      provides: "pairing issue, redeem, revoke, and status orchestration"
      min_lines: 120
    - path: "crates/veld/src/routes/linking.rs"
      provides: "POST /v1/linking/tokens, POST /v1/linking/redeem, GET /v1/linking/status, POST /v1/linking/revoke/:node_id"
      exports: ["issue_pairing_token", "redeem_pairing_token", "linking_status", "revoke_link"]
  key_links:
    - from: "crates/veld/src/routes/linking.rs"
      to: "crates/veld/src/services/linking.rs"
      via: "thin route handlers"
      pattern: "services::linking"
---

<objective>
Close the backend half of guided linking with scoped pairing tokens, durable linked-node trust state, and authenticated linking routes.

Purpose: Finish the trustworthy backend path for node linking before CLI, web, and Apple surfaces build on it.
Output: Linking persistence plus authenticated pairing/status routes.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-now-inbox-core-and-project-substrate/05-CONTEXT.md
@docs/tickets/phase-2/012-tester-readiness-onboarding.md
@.planning/phases/02-distributed-state-offline-clients-system-of-systems/02-01-SUMMARY.md
@crates/veld/src/services/linking.rs
@crates/veld/src/routes/linking.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Persist scoped pairing tokens and linked-node trust state</name>
  <files>migrations/0039_phase5_linking.sql, crates/vel-storage/src/db.rs, crates/vel-storage/src/lib.rs, crates/vel-storage/src/repositories/mod.rs, crates/vel-storage/src/repositories/linking_repo.rs</files>
  <read_first>migrations/0039_phase5_linking.sql
migrations/0029_cluster_workers.sql
crates/vel-storage/src/db.rs
crates/vel-storage/src/lib.rs
crates/vel-storage/src/repositories/mod.rs
crates/vel-storage/src/repositories/cluster_workers_repo.rs
crates/vel-core/src/linking.rs</read_first>
  <action>Create `migrations/0039_phase5_linking.sql` with `pairing_tokens` and `linked_nodes` tables. `pairing_tokens` must store `token_id TEXT PRIMARY KEY`, `token_code TEXT NOT NULL UNIQUE`, `issued_at INTEGER NOT NULL`, `expires_at INTEGER NOT NULL`, `issued_by_node_id TEXT NOT NULL`, `scopes_json TEXT NOT NULL`, and `redeemed_at INTEGER`. `linked_nodes` must store `node_id TEXT PRIMARY KEY`, `node_display_name TEXT NOT NULL`, `status TEXT NOT NULL`, `scopes_json TEXT NOT NULL`, `linked_at INTEGER`, `last_seen_at INTEGER`, `transport_hint TEXT`, and `revoked_at INTEGER`. Add `crates/vel-storage/src/repositories/linking_repo.rs` with `issue_pairing_token`, `get_pairing_token_by_code`, `mark_pairing_token_redeemed`, `upsert_linked_node`, `list_linked_nodes`, and `revoke_linked_node`. Export the repo functions through `db.rs` and `lib.rs`.</action>
  <acceptance_criteria>- `rg -n "CREATE TABLE pairing_tokens" migrations/0039_phase5_linking.sql` returns one match.
- `rg -n "CREATE TABLE linked_nodes" migrations/0039_phase5_linking.sql` returns one match.
- `rg -n "async fn issue_pairing_token|async fn mark_pairing_token_redeemed|async fn list_linked_nodes" crates/vel-storage/src/repositories/linking_repo.rs` returns all three functions.
- `rg -n "pub async fn issue_pairing_token|pub async fn list_linked_nodes" crates/vel-storage/src/db.rs crates/vel-storage/src/lib.rs` returns exported facade methods.</acceptance_criteria>
  <verify>
    <automated>cargo test -p vel-storage linking -- --nocapture</automated>
  </verify>
  <done>The storage layer can issue, redeem, inspect, and revoke scoped pairing records without relying on long-lived credentials.</done>
</task>

<task type="auto">
  <name>Task 2: Add linking routes and fail-closed pairing orchestration</name>
  <files>crates/veld/src/services/mod.rs, crates/veld/src/services/linking.rs, crates/veld/src/routes/mod.rs, crates/veld/src/routes/linking.rs, crates/veld/src/app.rs</files>
  <read_first>crates/veld/src/services/mod.rs
crates/veld/src/services/linking.rs
crates/veld/src/routes/mod.rs
crates/veld/src/routes/linking.rs
crates/veld/src/app.rs
crates/vel-api-types/src/lib.rs
crates/vel-core/src/linking.rs</read_first>
  <action>Create `crates/veld/src/services/linking.rs` with `issue_pairing_token`, `redeem_pairing_token`, `list_linked_nodes`, and `revoke_linked_node`. Enforce a default token TTL of `900` seconds and reject requested TTL values above `3600` seconds. Token redemption must fail closed when the token is expired, already redeemed, malformed, or requests scopes outside `read_context`, `write_safe_actions`, and `execute_repo_tasks`. Create `crates/veld/src/routes/linking.rs` exposing `POST /v1/linking/tokens`, `POST /v1/linking/redeem`, `GET /v1/linking/status`, and `POST /v1/linking/revoke/:node_id`. Mount the routes as operator-authenticated in `crates/veld/src/app.rs`. Keep this slice focused on backend linking state and route correctness; bootstrap and cluster continuity wiring move to the next plan.</action>
  <acceptance_criteria>- `rg -n '"/v1/linking/tokens"|"/v1/linking/redeem"|"/v1/linking/status"|"/v1/linking/revoke/:node_id"' crates/veld/src/app.rs` returns all four route mounts.
- `rg -n "900|3600" crates/veld/src/services/linking.rs` returns both TTL values.
- `rg -n "execute_repo_tasks" crates/veld/src/services/linking.rs crates/veld/src/routes/linking.rs` returns matches in both files.</acceptance_criteria>
  <verify>
    <automated>cargo test -p veld linking -- --nocapture</automated>
  </verify>
  <done>Operators can issue and redeem scoped pairing tokens through authenticated routes, and invalid tokens fail closed without mutating link trust state.</done>
</task>

</tasks>

<verification>
Run focused storage and daemon tests for linking.
</verification>

<success_criteria>
The repository ships the backend half of trustworthy linking: short-lived scoped tokens, durable trust state, and fail-closed redemption rules.
</success_criteria>

<output>
After completion, create `.planning/phases/05-now-inbox-core-and-project-substrate/05-03-SUMMARY.md`
</output>
