---
phase: 17-shell-embodiment-operator-mode-application-and-surface-simplification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - clients/web/src/App.tsx
  - clients/web/src/components/Sidebar.tsx
  - clients/web/src/components/MainPanel.tsx
  - clients/web/src/components/Sidebar.test.tsx
  - clients/web/src/components/MainPanel.test.tsx
  - clients/web/src/data/operatorSurfaces.ts
  - docs/user/surfaces.md
autonomous: true
requirements:
  - SHELL-MODE-01
  - SHELL-MODE-02
must_haves:
  truths:
    - "The web shell has one explicit taxonomy source for default, secondary, and advanced surfaces."
    - "Top-level navigation stops teaching extra peer surfaces before the operator reaches daily-use work."
    - "Existing view routes remain available, but the first-contact shell reflects the approved product model."
  artifacts:
    - path: clients/web/src/data/operatorSurfaces.ts
      provides: shared surface classification metadata for the web shell
    - path: clients/web/src/components/Sidebar.tsx
      provides: default/support navigation grouped around the approved taxonomy
    - path: clients/web/src/components/MainPanel.tsx
      provides: shell routing consistent with the new navigation posture
  key_links:
    - from: clients/web/src/data/operatorSurfaces.ts
      to: clients/web/src/components/Sidebar.tsx
      via: shared surface metadata import
      pattern: operatorSurfaces
    - from: clients/web/src/components/Sidebar.tsx
      to: clients/web/src/components/MainPanel.tsx
      via: shared view identifiers
      pattern: MainView
---

<objective>
Stabilize the web shell around the approved Phase 14 taxonomy before content-level embodiment widens.

Purpose: Give the rest of Phase 17 one explicit top-level shell model so later slices sharpen behavior instead of re-litigating navigation and disclosure categories.
Output: one shared web shell-classification seam plus updated shell routing and tests.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/17-shell-embodiment-operator-mode-application-and-surface-simplification/17-CONTEXT.md
@.planning/phases/17-shell-embodiment-operator-mode-application-and-surface-simplification/17-RESEARCH.md
@docs/product/operator-surface-taxonomy.md
@docs/product/now-inbox-threads-boundaries.md
@docs/product/operator-mode-policy.md
@clients/web/src/App.tsx
@clients/web/src/components/Sidebar.tsx
@clients/web/src/components/MainPanel.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create shared web shell surface metadata</name>
  <files>clients/web/src/data/operatorSurfaces.ts, clients/web/src/components/MainPanel.tsx, clients/web/src/components/MainPanel.test.tsx</files>
  <behavior>
    - Test 1: the shell exposes only the approved taxonomy as first-class route metadata: `Now`, `Inbox`, `Threads`, `Projects`, `Settings`
    - Test 2: support/detail views such as `Suggestions` and `Stats` remain reachable only through non-primary classification, not as peer primary surfaces
  </behavior>
  <action>Create `clients/web/src/data/operatorSurfaces.ts` as the single source of truth for web surface classification, labels, and disclosure level. Update `MainPanel.tsx` to consume that metadata instead of hard-coding the old peer-surface posture. Keep the underlying views intact, but route them as contextual or advanced destinations instead of teaching them as default peers. Do not add any new backend calls or shell-local product semantics.</action>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/MainPanel.test.tsx</automated>
  </verify>
  <done>`MainPanel` routing reflects the approved taxonomy and the shell has one shared classification source instead of duplicated navigation assumptions.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Rebuild sidebar navigation around default and advanced shell posture</name>
  <files>clients/web/src/App.tsx, clients/web/src/components/Sidebar.tsx, clients/web/src/components/Sidebar.test.tsx, docs/user/surfaces.md</files>
  <behavior>
    - Test 1: the sidebar foregrounds daily-use surfaces first and no longer treats `Suggestions` and `Stats` as ordinary peer destinations
    - Test 2: `Projects` remains visible but is clearly secondary to `Now` and `Inbox`
  </behavior>
  <action>Update `Sidebar.tsx` and any required `App.tsx` wiring to embody the approved shell hierarchy: default daily-use surfaces first, support/archive surfaces next, advanced/setup entry last. Preserve `Threads`, `Projects`, and `Settings`, but remove the current peer-level framing that teaches `Suggestions` and `Stats` as co-equal destinations. Update `docs/user/surfaces.md` in the same slice so shipped operator docs match the embodied shell.</action>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/Sidebar.test.tsx src/components/MainPanel.test.tsx</automated>
  </verify>
  <done>The web shell opens with the approved taxonomy and the user-facing surfaces doc matches the new top-level posture.</done>
</task>

</tasks>

<verification>
Run the targeted web test suite for the shell entrypoints and confirm the shared surface metadata drives both routing and navigation.
</verification>

<success_criteria>
- The web shell now has one explicit source of truth for surface classification.
- `Now`, `Inbox`, `Threads`, `Projects`, and `Settings` are the only first-contact shell categories.
- Extra detail surfaces still exist, but they are no longer taught as peer primary destinations.
</success_criteria>

<output>
After completion, create `.planning/phases/17-shell-embodiment-operator-mode-application-and-surface-simplification/17-01-SUMMARY.md`
</output>
