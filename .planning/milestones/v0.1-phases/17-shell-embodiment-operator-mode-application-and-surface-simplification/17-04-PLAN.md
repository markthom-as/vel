---
phase: 17-shell-embodiment-operator-mode-application-and-surface-simplification
plan: 04
type: execute
wave: 3
depends_on:
  - 17-01
  - 17-02
  - 17-03
files_modified:
  - clients/apple/Apps/VeliOS/ContentView.swift
  - clients/apple/Apps/VelMac/ContentView.swift
  - clients/apple/Apps/VelWatch/ContentView.swift
  - clients/apple/README.md
  - crates/vel-cli/src/main.rs
  - crates/vel-cli/src/commands/today.rs
  - crates/vel-cli/src/commands/doctor.rs
  - crates/vel-cli/src/commands/docs.rs
  - crates/vel-cli/src/commands/threads.rs
autonomous: true
requirements:
  - APPLE-SHELL-01
  - SHELL-MODE-02
must_haves:
  truths:
    - "Apple shells use the same product-mode hierarchy as the web shell instead of older platform-specific labels."
    - "CLI output and help text map cleanly onto daily-use, trust/setup, and thread/archive responsibilities."
    - "Apple remains summary-first and does not widen runtime/internal detail beyond the approved policy."
    - "Cross-surface alignment happens through embodiment only, not new backend work."
  artifacts:
    - path: clients/apple/Apps/VeliOS/ContentView.swift
      provides: iPhone and iPad shell regrouping around the approved taxonomy
    - path: clients/apple/Apps/VelMac/ContentView.swift
      provides: macOS summary-first shell organization
    - path: crates/vel-cli/src/commands/today.rs
      provides: CLI `Now`-lane embodiment
  key_links:
    - from: clients/apple/Apps/VeliOS/ContentView.swift
      to: existing VelAPI typed payloads
      via: summary-first Apple rendering over cached and live daemon state
      pattern: store.refresh|offlineStore
    - from: crates/vel-cli/src/main.rs
      to: crates/vel-cli/src/commands/today.rs
      via: operator-shell command framing
      pattern: Today|Doctor|Docs|Thread
---

<objective>
Align Apple and CLI surfaces to the same embodied shell policy after the web model is explicit.

Purpose: Finish Phase 17 by making all currently supported operator shells reflect the same taxonomy and disclosure rules without requiring new backend semantics or a desktop implementation.
Output: Apple and CLI embodiment updates plus matching shell-facing docs.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-shell-embodiment-operator-mode-application-and-surface-simplification/17-CONTEXT.md
@docs/product/operator-surface-taxonomy.md
@docs/product/operator-mode-policy.md
@clients/apple/Apps/VeliOS/ContentView.swift
@clients/apple/Apps/VelMac/ContentView.swift
@clients/apple/Apps/VelWatch/ContentView.swift
@clients/apple/README.md
@crates/vel-cli/src/main.rs
@crates/vel-cli/src/commands/today.rs
@crates/vel-cli/src/commands/doctor.rs
@crates/vel-cli/src/commands/docs.rs
@crates/vel-cli/src/commands/threads.rs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Regroup Apple shells around summary-first default mode</name>
  <files>clients/apple/Apps/VeliOS/ContentView.swift, clients/apple/Apps/VelMac/ContentView.swift, clients/apple/Apps/VelWatch/ContentView.swift, clients/apple/README.md</files>
  <behavior>
    - Test 1: iPhone, iPad, macOS, and watch surfaces foreground the approved daily-use categories instead of older platform-specific labels
    - Test 2: Apple remains summary-first and does not surface broad runtime/internal detail as default product education
  </behavior>
  <action>Update the Apple shell entry files to align naming, grouping, and disclosure with the approved taxonomy. Reuse existing cached and live typed data. Keep Apple summary-first, keep `Projects` and `Settings` secondary, and keep deeper runtime detail bounded. Refresh `clients/apple/README.md` in the same slice so Apple setup and product framing match the embodied shell. Do not attempt desktop/Tauri work or backend contract changes.</action>
  <verify>
    <automated>make check-apple-swift</automated>
  </verify>
  <done>The shipped Apple shells reflect the approved product hierarchy and the Apple README describes that hierarchy truthfully.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Reframe CLI command help and output around the same shell taxonomy</name>
  <files>crates/vel-cli/src/main.rs, crates/vel-cli/src/commands/today.rs, crates/vel-cli/src/commands/doctor.rs, crates/vel-cli/src/commands/docs.rs, crates/vel-cli/src/commands/threads.rs</files>
  <behavior>
    - Test 1: CLI help and targeted command output map naturally onto `Now`, advanced trust/setup, and `Threads`
    - Test 2: CLI wording no longer teaches raw implementation categories before operator-facing product lanes
  </behavior>
  <action>Adjust CLI shell framing so `vel` presents the daily-use path first and advanced/setup paths explicitly. Use `today` as the `Now`-lane embodiment, `doctor` and `docs` as advanced trust/setup affordances, and `thread` as the continuity/archive lane. Update output wording and top-level command descriptions where needed, but keep the command inventory and underlying API usage stable.</action>
  <verify>
    <automated>cargo test -p vel-cli</automated>
  </verify>
  <done>The CLI now reads like the same operator product, not a separate implementation-oriented shell.</done>
</task>

</tasks>

<verification>
Run Apple package validation and the `vel-cli` crate tests, then confirm Apple and CLI both reflect the same product classification already embodied in web.
</verification>

<success_criteria>
- Apple surfaces are summary-first and taxonomy-aligned.
- CLI wording and structure reinforce the same shell model.
- No new backend semantics or desktop implementation work was required.
</success_criteria>

<output>
After completion, create `.planning/phases/17-shell-embodiment-operator-mode-application-and-surface-simplification/17-04-SUMMARY.md`
</output>
