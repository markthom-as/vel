---
phase: 17-shell-embodiment-operator-mode-application-and-surface-simplification
plan: 02
type: execute
wave: 2
depends_on:
  - 17-01
files_modified:
  - clients/web/src/components/NowView.tsx
  - clients/web/src/components/NowView.test.tsx
  - clients/web/src/components/InboxView.tsx
  - clients/web/src/components/InboxView.test.tsx
  - clients/web/src/components/ThreadView.tsx
  - clients/web/src/components/ThreadView.test.tsx
autonomous: true
requirements:
  - SHELL-MODE-01
  - TRUST-SUMMARY-01
must_haves:
  truths:
    - "`Now` feels minimal and urgent-first instead of acting like a second inbox."
    - "`Inbox` stays the explicit triage queue."
    - "`Threads` remains continuity/search-first while still serving escalation from the other surfaces."
    - "Summary-first trust and freshness remain visible in daily use without reopening backend semantics."
  artifacts:
    - path: clients/web/src/components/NowView.tsx
      provides: default-mode embodiment for typed `Now` state
    - path: clients/web/src/components/InboxView.tsx
      provides: triage-first queue embodiment
    - path: clients/web/src/components/ThreadView.tsx
      provides: archive/search-first thread embodiment
  key_links:
    - from: clients/web/src/components/NowView.tsx
      to: clients/web/src/data/context.ts
      via: existing typed `Now` query consumption
      pattern: loadNow
    - from: clients/web/src/components/InboxView.tsx
      to: clients/web/src/components/ThreadView.tsx
      via: open-thread handoff
      pattern: onOpenThread
---

<objective>
Apply the Phase 14 daily-use policy across the three core web surfaces.

Purpose: Make the shipped web shell actually behave like the approved product model by tightening `Now`, `Inbox`, and `Threads` around their intended jobs without inventing new shell-owned logic.
Output: one daily-use embodiment slice covering `Now`, triage, and thread escalation posture.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/17-shell-embodiment-operator-mode-application-and-surface-simplification/17-CONTEXT.md
@docs/product/now-inbox-threads-boundaries.md
@docs/product/operator-mode-policy.md
@docs/product/onboarding-and-trust-journeys.md
@clients/web/src/components/NowView.tsx
@clients/web/src/components/InboxView.tsx
@clients/web/src/components/ThreadView.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten `Now` into a minimal urgent-first surface</name>
  <files>clients/web/src/components/NowView.tsx, clients/web/src/components/NowView.test.tsx</files>
  <behavior>
    - Test 1: non-urgent queue pressure stays summarized or deep-linked instead of rendering as equal-weight direct cards
    - Test 2: typed `check_in`, trust, freshness, and heavier `reflow` states remain visibly distinct without shell-local recomputation
  </behavior>
  <action>Update `NowView.tsx` to embody the approved product policy over the existing typed `Now` payload: keep urgent direct actions visible, demote non-urgent work into compact summaries or deep links, preserve summary-first trust/readiness, and make heavier `reflow` treatment visually distinct from routine prompts. Consume Phase 16 output as-is; do not add shell-owned urgency or transition logic.</action>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/NowView.test.tsx</automated>
  </verify>
  <done>`Now` reads as the operator's minimal first-glance surface and uses typed backend state rather than shell-local classification rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Sharpen `Inbox` and `Threads` into triage and continuity support surfaces</name>
  <files>clients/web/src/components/InboxView.tsx, clients/web/src/components/InboxView.test.tsx, clients/web/src/components/ThreadView.tsx, clients/web/src/components/ThreadView.test.tsx</files>
  <behavior>
    - Test 1: `Inbox` continues to foreground actionable triage items and project identity
    - Test 2: `Threads` favors continuity/history and latest-thread fallback without reading like a second inbox
  </behavior>
  <action>Refine `InboxView.tsx` and `ThreadView.tsx` so the shell roles become explicit: `Inbox` handles actionable queue work, while `Threads` handles longer continuity, history, and escalation from `Now` or `Inbox`. Preserve existing typed thread handoff and latest-thread fallback. Avoid adding any shell-only routing heuristics that reinterpret backend-owned action state.</action>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/InboxView.test.tsx src/components/ThreadView.test.tsx</automated>
  </verify>
  <done>The daily-use web surfaces now clearly express `Now` for immediate pressure, `Inbox` for triage, and `Threads` for continuity/history.</done>
</task>

</tasks>

<verification>
Run the targeted `Now`, `Inbox`, and `Thread` component suites and confirm the views still consume the same typed backend data while reflecting the new shell posture.
</verification>

<success_criteria>
- `Now` no longer behaves like a second full queue.
- `Inbox` stays the main triage surface.
- `Threads` remains useful and reachable without becoming the default home for daily use.
</success_criteria>

<output>
After completion, create `.planning/phases/17-shell-embodiment-operator-mode-application-and-surface-simplification/17-02-SUMMARY.md`
</output>
