---
phase: 08-coding-centric-supervised-execution-with-gsd-and-local-agents
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - migrations/0042_phase8_execution_contexts.sql
  - crates/vel-storage/src/repositories/mod.rs
  - crates/vel-storage/src/repositories/execution_contexts_repo.rs
  - crates/vel-storage/src/repositories/projects_repo.rs
  - crates/veld/src/services/mod.rs
  - crates/veld/src/services/execution_context.rs
  - crates/veld/src/routes/execution.rs
  - crates/veld/src/routes/mod.rs
  - crates/veld/src/app.rs
  - crates/vel-cli/src/commands/exec.rs
  - crates/vel-cli/src/commands/mod.rs
  - crates/vel-cli/src/main.rs
  - docs/user/daily-use.md
autonomous: true
requirements:
  - EXEC-01
  - GSD-01
  - GSD-02
must_haves:
  truths:
    - "Each project can carry a persisted execution context rooted in its primary repo and notes roots."
    - "Repo-local GSD artifact generation is explicit, bounded, and write-scoped to the project's declared primary repo root."
    - "Vel can preview or export the same execution context through API and CLI without inventing a second project-metadata system."
  artifacts:
    - path: "crates/vel-storage/src/repositories/execution_contexts_repo.rs"
      provides: "durable project execution context persistence"
      min_lines: 160
    - path: "crates/veld/src/services/execution_context.rs"
      provides: "repo-aware execution context orchestration and export"
      contains: "render_gsd_artifacts"
    - path: "crates/vel-cli/src/commands/exec.rs"
      provides: "operator shell for showing and exporting execution context"
      contains: "run_export_context"
  key_links:
    - from: "crates/veld/src/services/execution_context.rs"
      to: "crates/vel-storage/src/repositories/execution_contexts_repo.rs"
      via: "project execution context persistence"
      pattern: "ProjectExecutionContext"
    - from: "crates/veld/src/services/execution_context.rs"
      to: "crates/vel-storage/src/repositories/projects_repo.rs"
      via: "repo root boundary for artifact export"
      pattern: "primary_repo|primary_notes_root"
    - from: "crates/vel-cli/src/commands/exec.rs"
      to: "crates/veld/src/routes/execution.rs"
      via: "CLI export and preview over authenticated execution route"
      pattern: "execution_context|export"
---

<objective>
Persist project execution context and generate bounded repo-local GSD artifacts from it.

Purpose: turn Phase 5 project roots into the durable anchor for coding execution, and start GSD integration through repo-local sidecar artifacts rather than a bespoke planning bus.
Output: execution-context persistence, authenticated preview/export routes, CLI access, and bounded repo-local artifact generation.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-RESEARCH.md
@.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-01-PLAN.md
@crates/vel-core/src/project.rs
@crates/veld/src/services/projects.rs
@crates/vel-storage/src/repositories/projects_repo.rs
@docs/cognitive-agent-architecture/agents/coding-execution-contracts.md

<interfaces>
From crates/vel-core/src/project.rs:
```rust
pub struct ProjectRootRef {
    pub path: String,
    pub label: String,
    pub kind: String,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing storage/service coverage for project execution context and GSD artifact rendering</name>
  <files>crates/vel-storage/src/repositories/execution_contexts_repo.rs, crates/veld/src/services/execution_context.rs</files>
  <action>Add failing tests that lock three behaviors: persisted execution context is keyed by project ID; artifact rendering uses only declared project repo/notes roots; and any export path outside the primary repo root is rejected. Keep the artifact pack sidecar-oriented, for example `.planning/vel/execution-context.md` and `.planning/vel/gsd-handoff.md`, rather than overwriting unrelated repo planning files.</action>
  <verify>
    <automated>cargo test -p veld execution_context -- --nocapture</automated>
  </verify>
  <done>Phase 8 has red tests for durable execution context and bounded repo-local GSD artifact generation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement execution-context persistence, API routes, and CLI export</name>
  <files>migrations/0042_phase8_execution_contexts.sql, crates/vel-storage/src/repositories/mod.rs, crates/vel-storage/src/repositories/execution_contexts_repo.rs, crates/vel-storage/src/repositories/projects_repo.rs, crates/veld/src/services/mod.rs, crates/veld/src/services/execution_context.rs, crates/veld/src/routes/execution.rs, crates/veld/src/routes/mod.rs, crates/veld/src/app.rs, crates/vel-cli/src/commands/exec.rs, crates/vel-cli/src/commands/mod.rs, crates/vel-cli/src/main.rs, docs/user/daily-use.md</files>
  <action>Add durable execution-context persistence linked to Phase 5 projects, expose authenticated preview/export endpoints under a dedicated execution route family, and add CLI commands to inspect and export the repo-local artifact pack. Artifact export must write only inside the project's primary repo root and should produce a small GSD-readable sidecar pack rather than mutating arbitrary repo files. Reuse project roots from the existing project substrate instead of inventing a new repo metadata table.</action>
  <verify>
    <automated>cargo test -p veld execution_context -- --nocapture && cargo test -p vel-cli exec -- --nocapture</automated>
  </verify>
  <done>Projects now carry durable coding-execution context and can emit bounded repo-local GSD artifacts through supervised CLI/API flows.</done>
</task>

</tasks>

<verification>
Run targeted storage/service and CLI checks so execution context and repo-local artifact export are proven before connect/runtime work begins.
</verification>

<success_criteria>
Vel can persist repo-aware execution context per project and generate bounded repo-local GSD sidecars from it without writing outside the project's declared primary repo root.
</success_criteria>

<output>
After completion, create `.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-02-SUMMARY.md`
</output>
