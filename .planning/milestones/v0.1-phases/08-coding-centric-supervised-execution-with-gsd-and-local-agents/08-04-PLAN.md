---
phase: 08-coding-centric-supervised-execution-with-gsd-and-local-agents
plan: 04
type: execute
wave: 3
depends_on:
  - 08-02
  - 08-03
files_modified:
  - migrations/0043_phase8_execution_handoffs.sql
  - crates/vel-storage/src/repositories/mod.rs
  - crates/vel-storage/src/repositories/execution_handoffs_repo.rs
  - crates/veld/src/services/mod.rs
  - crates/veld/src/services/execution_routing.rs
  - crates/veld/src/services/operator_queue.rs
  - crates/veld/src/services/now.rs
  - crates/veld/src/routes/execution.rs
  - crates/veld/tests/execution_routing.rs
  - crates/vel-cli/src/commands/exec.rs
  - clients/web/src/types.ts
  - clients/web/src/data/operator.ts
  - clients/web/src/components/NowView.tsx
  - clients/web/src/components/SettingsPage.tsx
  - docs/user/daily-use.md
autonomous: true
requirements:
  - EXEC-02
  - GSD-02
  - HANDOFF-01
  - HANDOFF-02
  - POLICY-01
must_haves:
  truths:
    - "Human-to-agent and agent-to-agent handoffs are persisted, explicit, trace-linked, and reviewable before execution."
    - "Routing decisions use typed task kind, agent profile, token budget, and writable-scope inputs rather than opaque heuristics."
    - "Execution review appears on existing operator surfaces instead of hiding in backend-only tables."
  artifacts:
    - path: "crates/veld/src/services/execution_routing.rs"
      provides: "typed routing and handoff review orchestration"
      min_lines: 220
    - path: "crates/veld/tests/execution_routing.rs"
      provides: "integration coverage for routing and handoff review behavior"
      contains: "execution_routing"
    - path: "crates/vel-cli/src/commands/exec.rs"
      provides: "CLI review/approve/reject surfaces for execution handoffs"
      contains: "run_review_handoffs"
  key_links:
    - from: "crates/veld/src/services/execution_routing.rs"
      to: "crates/vel-storage/src/repositories/execution_handoffs_repo.rs"
      via: "handoff persistence and review-state tracking"
      pattern: "ExecutionHandoff"
    - from: "crates/veld/src/services/execution_routing.rs"
      to: "crates/veld/src/services/operator_queue.rs"
      via: "handoff review projected into operator attention surfaces"
      pattern: "handoff"
    - from: "clients/web/src/data/operator.ts"
      to: "clients/web/src/components/NowView.tsx"
      via: "typed operator rendering of pending execution review"
      pattern: "handoff|execution"
---

<objective>
Make routing policy and execution handoffs explicit, persisted, and reviewable across operator surfaces.

Purpose: satisfy the product requirement that coding-task handoff is part of the product, not hidden protocol plumbing.
Output: persisted execution handoffs, typed routing policy service, CLI/web review surfaces, and focused integration coverage.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-RESEARCH.md
@.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-02-PLAN.md
@.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-03-PLAN.md
@docs/cognitive-agent-architecture/agents/handoffs.md
@crates/veld/src/services/operator_queue.rs
@crates/veld/src/services/now.rs
@clients/web/src/components/NowView.tsx
@clients/web/src/components/SettingsPage.tsx

<interfaces>
From docs/cognitive-agent-architecture/agents/handoffs.md:
```json
{
  "objective": "string",
  "read_scopes": [],
  "write_scopes": [],
  "allowed_tools": []
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for routing decisions and persisted handoff review state</name>
  <files>crates/veld/tests/execution_routing.rs</files>
  <action>Create failing tests that prove: a handoff cannot launch without explicit objective, scopes, and expected output; routing decisions persist typed task kind/profile/budget reasons; and approve/reject actions move the handoff through explicit review states rather than implicit launch side effects.</action>
  <verify>
    <automated>cargo test -p veld execution_routing -- --nocapture</automated>
  </verify>
  <done>The intended routing and handoff-review semantics are locked with red tests before implementation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement routing policy, handoff persistence, and operator review surfaces</name>
  <files>migrations/0043_phase8_execution_handoffs.sql, crates/vel-storage/src/repositories/mod.rs, crates/vel-storage/src/repositories/execution_handoffs_repo.rs, crates/veld/src/services/mod.rs, crates/veld/src/services/execution_routing.rs, crates/veld/src/services/operator_queue.rs, crates/veld/src/services/now.rs, crates/veld/src/routes/execution.rs, crates/vel-cli/src/commands/exec.rs, clients/web/src/types.ts, clients/web/src/data/operator.ts, clients/web/src/components/NowView.tsx, clients/web/src/components/SettingsPage.tsx, docs/user/daily-use.md</files>
  <action>Implement a typed execution-routing service that records human-to-agent and agent-to-agent handoffs, computes routing decisions from task kind, agent profile, token budget, and writable-scope inputs, and exposes explicit approve/reject/launch-preview behavior through execution routes plus CLI/web operator surfaces. Reuse the existing operator queue and `Now` attention patterns so pending execution review is visible beside other operator obligations. Keep reasons and scope summaries explicit in the surfaced payloads.</action>
  <verify>
    <automated>cargo test -p veld execution_routing -- --nocapture && cargo test -p vel-cli exec -- --nocapture && npm --prefix clients/web test -- --run src/types.test.ts src/data/operator.test.ts src/components/NowView.test.tsx src/components/SettingsPage.test.tsx</automated>
  </verify>
  <done>Execution routing and handoff review are now explicit, persisted, and operator-visible across CLI and web surfaces.</done>
</task>

</tasks>

<verification>
Run the dedicated routing/handoff tests and the existing typed operator-surface checks so reviewability is real before guest-runtime work widens execution breadth.
</verification>

<success_criteria>
Vel can store and review coding-task handoffs with explicit scopes and routing reasons, and those review surfaces are visible in the same operator shell as other high-priority runtime work.
</success_criteria>

<output>
After completion, create `.planning/phases/08-coding-centric-supervised-execution-with-gsd-and-local-agents/08-04-SUMMARY.md`
</output>
