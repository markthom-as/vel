---
phase: 10-daily-loop-morning-overview-and-standup-commitment-engine
plan: 02
type: execute
wave: 1
depends_on:
  - 10-01
files_modified:
  - crates/veld/src/services/daily_loop_inputs.rs
  - crates/veld/src/services/daily_loop.rs
  - crates/veld/src/services/mod.rs
  - crates/veld/src/routes/daily_loop.rs
  - crates/veld/src/routes/mod.rs
  - crates/veld/src/app.rs
  - crates/veld/tests/daily_loop_morning.rs
autonomous: true
requirements:
  - MORNING-01
  - MORNING-02
  - MORNING-03
  - SESSION-01
must_haves:
  truths:
    - "An operator can manually start Morning Overview against next-12h calendar plus Todoist today/overdue inputs."
    - "Morning Overview returns a brief passive snapshot with at most two friction callouts and one to three questions."
    - "Morning Overview captures intent signals only and remains resumable without creating commitments."
  artifacts:
    - path: "crates/veld/src/services/daily_loop_inputs.rs"
      provides: "contract-specific calendar, Todoist, and friction snapshot builder for the daily loop"
      contains: "load_daily_loop_inputs"
    - path: "crates/veld/src/services/daily_loop.rs"
      provides: "morning session orchestration and resume-safe state transitions"
      contains: "start_morning_overview"
    - path: "crates/veld/src/routes/daily_loop.rs"
      provides: "thin authenticated daily-loop route family"
      contains: "start_session"
    - path: "crates/veld/tests/daily_loop_morning.rs"
      provides: "focused integration proof for morning start, prompt budget, and no-commitment behavior"
      contains: "morning"
  key_links:
    - from: "crates/veld/src/services/daily_loop_inputs.rs"
      to: "crates/veld/src/services/now.rs"
      via: "reuses backend schedule/task seams without treating `/v1/now` DTOs as service inputs"
      pattern: "get_now|todoist"
    - from: "crates/veld/src/services/daily_loop.rs"
      to: "crates/vel-storage/src/repositories/daily_sessions_repo.rs"
      via: "persisted resumable phase state"
      pattern: "DailyLoopStatus|question_count"
    - from: "crates/veld/src/routes/daily_loop.rs"
      to: "crates/veld/src/services/daily_loop.rs"
      via: "thin route -> service orchestration"
      pattern: "start_|turn_|active"
---

<objective>
Implement the backend-owned Morning Overview session engine on top of the new Phase 10 contract.

Purpose: replace the old heuristic `/v1/context/morning` behavior for this use case with a resumable session that reuses existing `Now`, calendar, Todoist, and action-pressure seams while keeping the morning flow passive.
Output: contract-specific input builder, morning session orchestration, authenticated route family, and focused backend integration coverage.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-daily-loop-morning-overview-and-standup-commitment-engine/10-CONTEXT.md
@.planning/phases/10-daily-loop-morning-overview-and-standup-commitment-engine/10-RESEARCH.md
@.planning/phases/10-daily-loop-morning-overview-and-standup-commitment-engine/10-01-PLAN.md
@crates/veld/src/services/now.rs
@crates/veld/src/services/operator_queue.rs
@crates/veld/src/services/apple_voice.rs
@crates/veld/src/routes/context.rs
@.planning/phases/07-apple-action-loops-and-behavioral-signal-ingestion/07-02-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing backend coverage for morning start, resume, and passive bounded output</name>
  <files>crates/veld/tests/daily_loop_morning.rs</files>
  <behavior>
    - Test 1: starting a morning session uses only next-12h calendar events plus Todoist today/overdue candidates in the input snapshot.
    - Test 2: the morning response is capped to four summary sentences, two friction callouts, and three questions, and the question budget persists across resume, skip, and silence-advance turns.
    - Test 3: completing, skipping, or silence-advancing morning persists intent signals only and does not create commitment records.
  </behavior>
  <action>Create focused red integration tests around the new daily-loop routes/services. Seed calendar signals, Todoist-backed commitments, and repeated action/suggestion pressure so the tests prove filtering and deduplication rather than hand-authored placeholder text. Assert on persisted session state and commitment counts, not on arbitrary natural-language phrasing, and include the safe silence -> auto-skip path so the loop remains interruptible/skippable/resumable under passive use.</action>
  <verify>
    <automated>cargo test -p veld daily_loop_morning -- --nocapture</automated>
  </verify>
  <done>The morning slice has failing proof for bounded inputs, bounded output, resumability, and no-commitment behavior.</done>
</task>

<task type="auto">
  <name>Task 2: Implement the morning input snapshot builder and session state machine</name>
  <files>crates/veld/src/services/daily_loop_inputs.rs, crates/veld/src/services/daily_loop.rs, crates/veld/src/services/mod.rs</files>
  <action>Create a service-level `daily_loop_inputs` builder that reuses existing backend seams but applies the Phase 10 contract exactly: calendar is bounded to the next 12 hours, Todoist candidates are today plus overdue only, and friction candidates are deduped from `operator_queue`, pending writebacks, conflicts, and repeated suggestion pressure using explicit keys when available. Implement morning-session transitions in `daily_loop.rs`: manual start now, resume existing active session for the day, safe `skip` and silence-driven advancement, calm brief output, at-most-three question budget, and persisted intent-signal capture. Emit inspectable lifecycle transitions through the existing run/event patterns or equivalent persisted state transitions; do not write commitments in this slice.</action>
  <verify>
    <automated>cargo test -p veld daily_loop_morning -- --nocapture</automated>
  </verify>
  <done>The backend can build a contract-specific morning snapshot and drive a resumable passive Morning Overview without creating commitments.</done>
</task>

<task type="auto">
  <name>Task 3: Expose authenticated daily-loop morning routes without overloading the legacy context brief endpoint</name>
  <files>crates/veld/src/routes/daily_loop.rs, crates/veld/src/routes/mod.rs, crates/veld/src/app.rs</files>
  <action>Add thin operator-authenticated routes for `POST /v1/daily-loop/sessions`, `GET /v1/daily-loop/sessions/active`, and `POST /v1/daily-loop/sessions/:id/turn` (or equivalent naming that preserves the same semantics). Route handlers should only parse/auth/map and call the daily-loop service. Do not overload `/v1/context/morning`; keep the old context-brief path intact until later shell plans switch surfaces to the new Phase 10 authority.</action>
  <verify>
    <automated>cargo test -p veld daily_loop_morning -- --nocapture</automated>
  </verify>
  <done>Morning Overview is reachable through a dedicated authenticated daily-loop API that thin shells can reuse for start, resume, and skip turns.</done>
</task>

</tasks>

<verification>
Morning Overview is only complete when the automated test proves bounded filtering, bounded prompt counts, resumability, and zero commitment writes through the authenticated route/service path.
</verification>

<success_criteria>
Vel exposes one real morning-session engine that starts manually now, stays passive, remains resumable, and builds strictly from explainable backend inputs.
</success_criteria>

<output>
After completion, create `.planning/phases/10-daily-loop-morning-overview-and-standup-commitment-engine/10-02-SUMMARY.md`
</output>
