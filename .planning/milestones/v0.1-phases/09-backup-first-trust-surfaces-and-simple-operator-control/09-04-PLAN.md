---
phase: 09-backup-first-trust-surfaces-and-simple-operator-control
plan: 04
type: execute
wave: 4
depends_on:
  - 09-02
  - 09-03
files_modified:
  - clients/web/src/types.ts
  - clients/web/src/data/operator.ts
  - clients/web/src/data/operator.test.ts
  - clients/web/src/components/SettingsPage.tsx
  - clients/web/src/components/SettingsPage.test.tsx
  - docs/user/backup-and-restore.md
  - docs/user/troubleshooting.md
  - docs/api/runtime.md
  - docs/user/daily-use.md
  - crates/vel-cli/src/client.rs
  - crates/veld/tests/backup_flow.rs
  - crates/vel-cli/src/commands/backup.rs
  - .planning/phases/09-backup-first-trust-surfaces-and-simple-operator-control/09-VALIDATION.md
autonomous: true
requirements:
  - BACKUP-01
  - BACKUP-02
  - CTRL-02
must_haves:
  truths:
    - "The operator has a real create/inspect/verify backup workflow, not just backend plumbing or guidance-only commands."
    - "Restore and recovery guidance is present, explicit, and intentionally smaller than the backup creation path."
    - "The operator has a narrow non-destructive way to inspect or dry-run backup trust before manual recovery, and the final web/docs surfaces reflect the same backend-owned trust state."
    - "The operator can finish Phase 09 with a real create/inspect/verify workflow and manual recovery guidance without needing full restore automation."
  artifacts:
    - path: "docs/user/backup-and-restore.md"
      provides: "manual restore and recovery authority with inspect/dry-run steps"
      contains: "manual restore"
    - path: "crates/vel-cli/src/commands/backup.rs"
      provides: "non-destructive CLI create, inspect, verify, and dry-run restore helper"
      contains: "verify"
    - path: "crates/vel-cli/src/client.rs"
      provides: "typed CLI transport methods for backup create, inspect, and verify routes"
      contains: "backup"
    - path: "clients/web/src/components/SettingsPage.tsx"
      provides: "operator-visible backup trust card over the shipped backend status"
      contains: "backup"
    - path: ".planning/phases/09-backup-first-trust-surfaces-and-simple-operator-control/09-VALIDATION.md"
      provides: "Phase 09 validation evidence recorded against the existing Nyquist-compliant mapping"
      contains: "wave_0_complete"
  key_links:
    - from: "docs/user/backup-and-restore.md"
      to: "crates/vel-cli/src/commands/backup.rs"
      via: "manual recovery and verification commands"
      pattern: "backup inspect|backup verify"
    - from: "crates/vel-cli/src/commands/backup.rs"
      to: "crates/vel-cli/src/client.rs"
      via: "CLI backup subcommands call authenticated backend backup routes"
      pattern: "backup"
    - from: "clients/web/src/components/SettingsPage.tsx"
      to: "crates/veld/src/services/chat/settings.rs"
      via: "web renders backend-owned backup trust state"
      pattern: "backup"
    - from: "crates/veld/tests/backup_flow.rs"
      to: "docs/user/backup-and-restore.md"
      via: "restore guidance validated against shipped manifest rules"
      pattern: "manual restore|verify"
---

<objective>
Close Phase 09 with narrow recovery guidance and verification rather than a full restore engine.

Purpose: finish the trust loop by documenting and proving how an operator can inspect, verify, and manually recover from a backup pack without letting restore automation consume the phase.
Output: final web/doc projection of backup trust state, manual restore guidance, explicit non-destructive CLI create/inspect/verify closure, recorded validation evidence, and final focused backup tests.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-backup-first-trust-surfaces-and-simple-operator-control/09-RESEARCH.md
@.planning/phases/09-backup-first-trust-surfaces-and-simple-operator-control/09-VALIDATION.md
@.planning/phases/09-backup-first-trust-surfaces-and-simple-operator-control/09-03-PLAN.md
@docs/user/backup-and-restore.md
@docs/user/setup.md
@docs/user/daily-use.md
@docs/api/runtime.md
@crates/vel-cli/src/client.rs
@crates/vel-cli/src/commands/backup.rs
@crates/veld/tests/backup_flow.rs
@clients/web/src/components/SettingsPage.tsx
@clients/web/src/data/operator.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add final web/doc projection plus narrow recovery guidance and an explicit non-destructive CLI backup workflow</name>
  <files>clients/web/src/types.ts, clients/web/src/data/operator.ts, clients/web/src/data/operator.test.ts, clients/web/src/components/SettingsPage.tsx, clients/web/src/components/SettingsPage.test.tsx, docs/user/backup-and-restore.md, docs/user/troubleshooting.md, docs/api/runtime.md, docs/user/daily-use.md, crates/vel-cli/src/client.rs, crates/vel-cli/src/commands/backup.rs</files>
  <action>Update the web transport types, decoders, and tests so backup trust status decodes from the shipped backend settings/runtime payload. Add a small runtime/settings card that shows last backup status, freshness, destination, coverage/omission summary, and a direct hint to use the shipped `vel backup` create/inspect/verify flow when trust is degraded. Extend `crates/vel-cli/src/client.rs` with typed backup request methods that call the authenticated Phase 09 create/inspect/verify routes, then update `crates/vel-cli/src/commands/backup.rs` to use those methods so the CLI remains a renderer of backend-owned truth. Finish `docs/user/backup-and-restore.md` so it gives exact create, inspect, verify, and manual restore steps for a backup pack, including what files to copy back, what stays omitted, and how to confirm the backup root before doing anything destructive. Extend `vel backup` with the non-destructive create/inspect/verify or dry-run restore validation path needed to make the operator workflow real, but do not implement automated destructive restore. Keep the docs explicit that restore remains manual-first and narrower than backup creation.</action>
  <verify>
    <automated>cargo test -p vel-cli backup -- --nocapture && npm --prefix clients/web test -- --run src/data/operator.test.ts src/components/SettingsPage.test.tsx && rg -n "backup create|backup inspect|backup verify|manual restore|dry-run" docs/user/backup-and-restore.md docs/user/troubleshooting.md docs/api/runtime.md docs/user/daily-use.md</automated>
  </verify>
  <done>The shipped web/docs/CLI surfaces expose the same backup trust state and a clear, non-destructive create/inspect/verify plus manual recovery path without introducing automated restore as the center of the phase.</done>
</task>

<task type="auto">
  <name>Task 2: Record final Phase 09 validation evidence against the existing Nyquist-compliant mapping</name>
  <files>crates/veld/tests/backup_flow.rs, .planning/phases/09-backup-first-trust-surfaces-and-simple-operator-control/09-VALIDATION.md</files>
  <action>Extend `crates/veld/tests/backup_flow.rs` or adjacent focused tests so the final backup path proves manifest inspection, omission reporting, stale/missing backup warning behavior, and any dry-run restore validation added in this plan. Update `09-VALIDATION.md` only to record execution evidence and completion state against the already-correct command map: mark Wave 0 complete once its contract/doc artifacts exist, flip task statuses as checks turn green, and keep the earlier settings-baseline caveat isolated from backup evidence. Do not reshape the validation contract here or widen into unrelated settings or restore automation work just to make the file look complete.</action>
  <verify>
    <automated>cargo test -p veld backup_flow -- --nocapture && rg -n "nyquist_compliant: true|wave_0_complete: true" .planning/phases/09-backup-first-trust-surfaces-and-simple-operator-control/09-VALIDATION.md</automated>
  </verify>
  <done>Phase 09 ends with explicit validation evidence for backup confidence and intentionally narrow recovery guidance.</done>
</task>

</tasks>

<verification>
Run the focused backup integration suite, CLI backup tests, and final web trust-surface tests, then confirm the validation file records Wave 0 closure and the final evidence for the already-defined automated checks that support phase verification.
</verification>

<success_criteria>
Phase 09 closes with backup confidence fully inspectable, restore still manual-first, and validation evidence that proves the shipped trust workflow without a broader recovery engine.
</success_criteria>

<output>
After completion, create `.planning/phases/09-backup-first-trust-surfaces-and-simple-operator-control/09-04-SUMMARY.md`
</output>
