# 27-04 Plan

## Slice

Close Phase 27 with docs, examples, and verification for the canonical scheduler rule model.

## Why this slice last

Once the backend seam is real, the repo needs one clear story about what scheduler semantics Vel owns today, how they map from compatibility tags, and what remains intentionally out of scope.

## Outcomes

- align architecture, product, and user docs with the shipped canonical rule model
- add or refresh examples/templates that show normalized scheduler facets
- verify the current limits honestly: same-day recovery, backend-owned semantics, no broad autonomous planner claim
- leave the next phase ready to build on the normalized commitment substrate

## Likely Touch Points

- `docs/cognitive-agent-architecture/`
- `docs/api/`
- `docs/user/`
- `docs/product/`
- checked-in example fixtures or manifests if added earlier in the phase

## Verification

- targeted doc truth checks with `rg`
- parsing/fixture verification for any checked-in examples
- focused smoke tests that prove the documented scheduler semantics are live
