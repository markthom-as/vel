# 27-01 Plan

## Slice

Define the canonical scheduler facet schema, ingest mapping rules, and owner documentation.

## Why this slice first

Phase 26 proved the need for normalized semantics, but the product still depends on runtime parsing. This slice locks the durable scheduler vocabulary before persistence and behavior changes widen.

## Outcomes

- publish typed canonical scheduler facet/domain contracts
- define the normalization rules from raw labels/text into those facets
- document which `codex-workspace` semantics are preserved now and which remain future work
- ship checked-in examples or templates if the boundary becomes stable enough

## Likely Touch Points

- `crates/vel-core/src/`
- `docs/cognitive-agent-architecture/`
- `docs/product/`
- adapter-boundary docs for Todoist-compatible label handling

## Verification

- focused Rust tests for facet parsing/normalization contracts
- fixture/example validation if new schema-bearing examples are added
- `rg` checks that docs teach canonical facets rather than raw provider tags as product truth
