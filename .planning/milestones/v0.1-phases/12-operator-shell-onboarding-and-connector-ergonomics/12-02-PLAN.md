---
phase: 12-operator-shell-onboarding-and-connector-ergonomics
plan: 02
type: execute
wave: 1
depends_on:
  - 12-01
files_modified:
  - clients/web/src/App.tsx
  - clients/web/src/components/Sidebar.tsx
  - clients/web/src/components/Sidebar.test.tsx
  - clients/web/src/components/MainPanel.tsx
  - clients/web/src/components/MainPanel.test.tsx
  - clients/web/src/components/ThreadView.tsx
  - clients/web/src/components/ThreadView.test.tsx
  - clients/web/src/components/NowView.tsx
  - clients/web/src/components/NowView.test.tsx
autonomous: true
requirements:
  - SHELL-01
  - SHELL-02
  - INTEGR-UX-01
must_haves:
  truths:
    - "The shell makes the primary daily surfaces faster to navigate without introducing a second product hierarchy."
    - "Threads open in the most operator-useful default state instead of forcing manual recovery to the latest useful conversation."
    - "Freshness and connector degradation stay visible but use calmer, more actionable operator language."
  artifacts:
    - path: "clients/web/src/components/Sidebar.tsx"
      provides: "Improved shell navigation structure and affordances."
      contains: "navigationItems"
    - path: "clients/web/src/components/ThreadView.tsx"
      provides: "Latest-thread default or equivalent thread-entry ergonomics."
      contains: "conversation"
    - path: "clients/web/src/components/NowView.tsx"
      provides: "Softer freshness and connector-state UX over the existing backend-owned data."
      contains: "freshness"
  key_links:
    - from: "clients/web/src/App.tsx"
      to: "clients/web/src/components/Sidebar.tsx"
      via: "main shell view selection"
      pattern: "setMainView"
    - from: "clients/web/src/components/MainPanel.tsx"
      to: "clients/web/src/components/ThreadView.tsx"
      via: "thread surface routing"
      pattern: "ThreadView"
    - from: "clients/web/src/components/NowView.tsx"
      to: "clients/web/src/data/operator.ts"
      via: "connector and freshness actions remain backend-owned"
      pattern: "freshness"
---

<objective>
Improve the operator shell and default navigation path around daily use.

Purpose: Reduce navigation friction for repeated use by tightening shell affordances, making threads recover to the latest useful state, and softening freshness/degradation UX without hiding trust problems.
Output: updated shell/navigation behavior, latest-thread defaulting, and calmer freshness/connector rendering with focused web tests.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/phases/12-operator-shell-onboarding-and-connector-ergonomics/12-CONTEXT.md
@clients/web/src/App.tsx
@clients/web/src/components/Sidebar.tsx
@clients/web/src/components/MainPanel.tsx
@clients/web/src/components/ThreadView.tsx
@clients/web/src/components/NowView.tsx
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Tighten shell navigation and thread entry behavior</name>
  <files>clients/web/src/App.tsx, clients/web/src/components/Sidebar.tsx, clients/web/src/components/Sidebar.test.tsx, clients/web/src/components/MainPanel.tsx, clients/web/src/components/MainPanel.test.tsx, clients/web/src/components/ThreadView.tsx, clients/web/src/components/ThreadView.test.tsx</files>
  <behavior>
    - Test 1: The shell preserves the canonical primary surface order while adding the new navigation affordances required by Phase 12.
    - Test 2: Opening Threads without an explicit selection defaults to the latest useful thread instead of a dead-end blank state.
    - Test 3: Shell state changes remain simple and local; no hidden client-owned policy is introduced.
  </behavior>
  <action>Refactor the current app-shell navigation just enough to add the planned ergonomics: icon/collapsible affordances if they fit the existing shell, clearer primary-surface emphasis, and better thread recovery/defaulting. Keep the shell logic lightweight and local; the goal is faster navigation, not a routing rewrite.</action>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/Sidebar.test.tsx src/components/MainPanel.test.tsx src/components/ThreadView.test.tsx</automated>
  </verify>
  <done>The shell keeps `Now + Inbox` primary, thread access is less fragile, and the navigation remains easy to review.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Soften freshness and connector-state UX in the daily shell</name>
  <files>clients/web/src/components/NowView.tsx, clients/web/src/components/NowView.test.tsx</files>
  <behavior>
    - Test 1: Degraded source freshness still surfaces clearly while preserving the underlying data view.
    - Test 2: The shell prefers one obvious next action for stale or failing connectors instead of stacked warning noise.
    - Test 3: Todoist and related connector rendering become more legible without changing backend freshness or trust policy.
  </behavior>
  <action>Adjust `NowView` freshness and connector rendering so stale/error/disconnected states are calmer, more compact, and still actionable. Preserve the existing backend-owned data model and direct sync/open-settings actions. If richer Todoist rendering belongs in this view, keep it tied to the existing typed payload rather than inventing a separate adapter model.</action>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/NowView.test.tsx</automated>
  </verify>
  <done>The daily shell is easier to scan under degraded conditions, and connector trust remains visible without unnecessary alarmism.</done>
</task>

</tasks>

<verification>
Run the focused shell/thread/Now component tests. Then manually walk the shell from `Now` to `Threads` and back while simulating a stale connector state to confirm the improved navigation and calmer trust messaging.
</verification>

<success_criteria>
The web operator shell feels more intentional and easier to recover through: primary surfaces are clearer, threads open in a useful state by default, and freshness/connector issues stay actionable without overwhelming the operator.
</success_criteria>

<output>
After completion, create `.planning/phases/12-operator-shell-onboarding-and-connector-ergonomics/12-02-SUMMARY.md`
</output>
