---
phase: 07-apple-action-loops-and-behavioral-signal-ingestion
plan: 03
type: execute
wave: 3
depends_on:
  - 07-01
  - 07-02
files_modified:
  - crates/veld/src/app.rs
  - crates/veld/src/adapters/health.rs
  - crates/veld/src/services/apple_behavior.rs
  - crates/veld/src/services/mod.rs
  - crates/veld/src/routes/apple.rs
  - crates/veld/src/routes/mod.rs
  - crates/veld/src/services/now.rs
  - crates/veld/tests/apple_behavior_summary.rs
autonomous: true
requirements:
  - HEALTH-01
  - HEALTH-02
  - APPLE-01
must_haves:
  truths:
    - "Phase 7 ingests only bounded Apple behavior signals for steps, stand, and exercise rather than broad health sprawl."
    - "Daily Apple behavior summaries are produced in the backend with freshness and explainable evidence."
    - "Apple clients can render the same summary across surfaces without inventing their own interpretation rules."
  artifacts:
    - path: "crates/veld/src/services/apple_behavior.rs"
      provides: "behavior rollup and freshness derivation"
      min_lines: 160
    - path: "crates/veld/src/adapters/health.rs"
      provides: "bounded HealthKit metric ingestion rules"
      contains: "step_count"
    - path: "crates/veld/tests/apple_behavior_summary.rs"
      provides: "integration coverage for behavior rollups and freshness"
      contains: "apple_behavior"
  key_links:
    - from: "crates/veld/src/app.rs"
      to: "crates/veld/src/routes/apple.rs"
      via: "operator-authenticated behavior-summary route mount"
      pattern: "/v1/apple|RouteExposureClass::OperatorAuthenticated"
    - from: "crates/veld/src/adapters/health.rs"
      to: "crates/veld/src/services/apple_behavior.rs"
      via: "health signal ingestion feeds bounded rollup logic"
      pattern: "health_metric|AppleBehavior"
    - from: "crates/veld/src/services/apple_behavior.rs"
      to: "crates/veld/src/services/now.rs"
      via: "behavior summary projected into Now/apple summaries"
      pattern: "behavior"
    - from: "crates/veld/src/routes/apple.rs"
      to: "crates/veld/src/services/apple_behavior.rs"
      via: "dedicated Apple behavior-summary route"
      pattern: "behavior_summary"
---

<objective>
Narrow Apple behavioral signal ingestion to useful daily rollups with explainable backend summaries.

Purpose: add practical steps/stand/exercise orientation value without turning Apple health access into a broad client-owned interpretation system.
Output: bounded health ingestion rules, backend behavior-summary derivation, optional Apple-facing summary route/projection, and focused tests.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-apple-action-loops-and-behavioral-signal-ingestion/07-RESEARCH.md
@.planning/phases/07-apple-action-loops-and-behavioral-signal-ingestion/07-01-PLAN.md
@crates/veld/src/adapters/health.rs
@crates/veld/src/routes/health.rs
@crates/veld/src/services/now.rs
@clients/apple/VelAPI/Sources/VelAPI/VelLocalSourceExporter.swift

<interfaces>
From clients/apple/VelAPI/Sources/VelAPI/VelLocalSourceExporter.swift:
```swift
public struct VelHealthSnapshot: Codable, Sendable {
    public var source: String
    public var samples: [Sample]
}
```

From crates/veld/src/adapters/health.rs:
```rust
pub async fn ingest(storage: &Storage, config: &AppConfig) -> Result<u32, crate::errors::AppError>;
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add failing tests for bounded Apple behavior ingestion and explainable summary derivation</name>
  <files>crates/veld/tests/apple_behavior_summary.rs</files>
  <behavior>
    - Test 1: `step_count`, `stand_hours`, and `exercise_minutes` samples persist and summarize correctly for the current day.
    - Test 2: unsupported or extra health metrics are ignored for Phase 7 summary purposes.
    - Test 3: the derived behavior summary reports freshness/source timestamps and human-readable reasons tied to persisted signals.
  </behavior>
  <action>Create failing integration tests in `crates/veld/tests/apple_behavior_summary.rs` that seed bounded health signals, verify same-day rollups, assert freshness behavior, and prove out-of-scope metrics do not widen the summary. Keep test fixtures on the current `health_metric` seam so the lane extends the shipped adapter rather than replacing it.</action>
  <verify>
    <automated>cargo test -p veld apple_behavior -- --nocapture</automated>
  </verify>
  <done>The repository has red tests locking Phase 7's bounded behavior-signal scope and explainability rules.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement bounded health ingestion and backend-owned Apple behavior summaries</name>
  <files>crates/veld/src/app.rs, crates/veld/src/adapters/health.rs, crates/veld/src/services/apple_behavior.rs, crates/veld/src/services/mod.rs, crates/veld/src/routes/apple.rs, crates/veld/src/routes/mod.rs, crates/veld/src/services/now.rs</files>
  <behavior>
    - Test 1: only steps, stand, and exercise metrics contribute to the Apple behavior summary.
    - Test 2: the summary includes freshness and evidence, not opaque scores.
    - Test 3: Apple-facing projection comes from backend-owned summary state and can be reused by Now.
  </behavior>
  <action>Implement `crates/veld/src/services/apple_behavior.rs` to roll up daily `step_count`, `stand_hours`, and `exercise_minutes` signals into the typed `AppleBehaviorSummary` contract from `07-01`, including freshness/source timestamps and explainable reasons. Tighten `crates/veld/src/adapters/health.rs` so Phase 7 summary logic explicitly filters to the bounded metric set while leaving broader raw ingestion paths out of scope for this phase. Expose the summary through a thin authenticated dedicated Apple behavior-summary route, and wire that route through `crates/veld/src/app.rs` in the operator-authenticated route group; optionally mirror the same backend-owned summary into `Now` as a read-only projection if useful, but keep the canonical client contract on the dedicated route. Do not add sleep programs, heart analytics, or client-side heuristics.</action>
  <verify>
    <automated>cargo test -p veld apple_behavior -- --nocapture && cargo test -p veld sync_health_ingests_snapshot_and_triggers_evaluation -- --nocapture</automated>
  </verify>
  <done>Phase 7 behavior signals are bounded, explainable, fresh-aware, and produced by backend logic that Apple surfaces can render consistently.</done>
</task>

</tasks>

<verification>
Run the dedicated Apple behavior tests plus the existing health-sync baseline so Phase 7 closes on bounded ingestion and explainable summary projection rather than raw HealthKit sprawl.
</verification>

<success_criteria>
Vel can ingest bounded Apple behavior signals and produce one backend-owned daily summary for steps, stand, and exercise that remains explainable across clients.
</success_criteria>

<output>
After completion, create `.planning/phases/07-apple-action-loops-and-behavioral-signal-ingestion/07-03-SUMMARY.md`
</output>
