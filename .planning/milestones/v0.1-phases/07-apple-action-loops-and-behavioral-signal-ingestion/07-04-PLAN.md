---
phase: 07-apple-action-loops-and-behavioral-signal-ingestion
plan: 04
type: execute
wave: 4
depends_on:
  - 07-02
  - 07-03
files_modified:
  - clients/apple/VelAPI/Sources/VelAPI/VelClient.swift
  - clients/apple/VelAPI/Sources/VelAPI/OfflineStore.swift
  - clients/apple/Apps/VeliOS/ContentView.swift
  - clients/apple/Apps/VelWatch/VelWatchApp.swift
  - clients/apple/Apps/VelWatch/ContentView.swift
  - clients/apple/README.md
  - docs/user/daily-use.md
  - docs/api/runtime.md
autonomous: true
requirements:
  - IOS-01
  - IOS-02
  - IOS-03
  - HEALTH-02
  - APPLE-01
must_haves:
  truths:
    - "iPhone and watch surfaces use the backend-owned Apple voice and Apple behavior-summary endpoints plus the shared queue/bootstrap seams."
    - "Apple schedule retrieval uses an explicit `VelClient.now()` path over backend-owned `/v1/now`, with watch/iPhone consumers rendering that server-derived schedule state instead of synthesizing it locally."
    - "Swift-side local query synthesis is removed or explicitly deprecated in favor of backend-owned voice responses."
    - "Apple client auth for new `/v1/apple/*` routes is explicit in shared transport rather than relying on unauthenticated defaults."
    - "Apple docs describe the real permissions, offline behavior, schedule retrieval, nudge response, and behavior summary semantics."
  artifacts:
    - path: "clients/apple/VelAPI/Sources/VelAPI/VelClient.swift"
      provides: "typed client methods and auth-header plumbing for `/v1/now`, Apple voice, and Apple behavior-summary endpoints"
      contains: "now()"
    - path: "clients/apple/VelAPI/Sources/VelAPI/OfflineStore.swift"
      provides: "cache and queue support for new Apple loop payloads"
      contains: "cachedAppleBehaviorSummary"
    - path: "clients/apple/Apps/VelWatch/VelWatchApp.swift"
      provides: "watch fetch/refresh state updated for backend-owned Apple schedule and behavior summaries"
      contains: "VelWatchStore"
    - path: "clients/apple/Apps/VeliOS/ContentView.swift"
      provides: "backend-driven iPhone voice UI without local query synthesis"
      contains: "appleVoiceTurn"
    - path: "clients/apple/Apps/VelWatch/ContentView.swift"
      provides: "watch quick-loop rendering over shared queue/bootstrap truth"
      contains: "Apple"
  key_links:
    - from: "clients/apple/Apps/VeliOS/ContentView.swift"
      to: "clients/apple/VelAPI/Sources/VelAPI/VelClient.swift"
      via: "voice tab submits typed backend requests"
      pattern: "appleVoiceTurn"
    - from: "clients/apple/Apps/VelWatch/VelWatchApp.swift"
      to: "clients/apple/VelAPI/Sources/VelAPI/VelClient.swift"
      via: "watch store fetches authenticated `/v1/now` and Apple summaries for refresh state"
      pattern: "VelWatchStore|now()|apple"
    - from: "clients/apple/Apps/VelWatch/ContentView.swift"
      to: "clients/apple/VelAPI/Sources/VelAPI/OfflineStore.swift"
      via: "watch renders cached backend summaries and pending queue state"
      pattern: "cached"
    - from: "clients/apple/README.md"
      to: "docs/api/runtime.md"
      via: "docs truth alignment"
      pattern: "voice|behavior|schedule"
---

<objective>
Wire the Phase 7 backend contracts into Apple surfaces and documentation without widening Swift policy ownership.

Purpose: finish the iPhone/watch experience by reusing shared transport, cache, and queue seams while removing the current local query-synthesis drift.
Output: typed Apple client methods, shared cache updates, iPhone/watch UI changes, and aligned Apple/runtime/user docs.
</objective>

<execution_context>
@/home/jove/.codex/get-shit-done/workflows/execute-plan.md
@/home/jove/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/07-apple-action-loops-and-behavioral-signal-ingestion/07-RESEARCH.md
@.planning/phases/07-apple-action-loops-and-behavioral-signal-ingestion/07-02-PLAN.md
@.planning/phases/07-apple-action-loops-and-behavioral-signal-ingestion/07-03-PLAN.md
@clients/apple/AGENTS.md
@clients/apple/README.md
@clients/apple/VelAPI/Sources/VelAPI/VelClient.swift
@clients/apple/VelAPI/Sources/VelAPI/OfflineStore.swift
@clients/apple/Apps/VeliOS/ContentView.swift
@clients/apple/Apps/VelWatch/ContentView.swift

<interfaces>
From clients/apple/VelAPI/Sources/VelAPI/OfflineStore.swift:
```swift
public func hydrate(from bootstrap: SyncBootstrapData)
public func queuedActionRequests() -> [SyncActionRequestData]
public func drainQueuedActions(using client: VelClient) async -> Int
```

From clients/apple/VelAPI/Sources/VelAPI/VelClient.swift:
```swift
public func syncBootstrap() async throws -> SyncBootstrapData
public func syncActions(_ request: SyncActionsRequestData) async throws -> SyncActionsResultData
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add shared Apple transport and cache wiring for backend-owned voice and behavior payloads</name>
  <files>clients/apple/VelAPI/Sources/VelAPI/VelClient.swift, clients/apple/VelAPI/Sources/VelAPI/OfflineStore.swift</files>
  <action>Add typed client methods for the Phase 7 backend surfaces in `VelClient.swift`, including an explicit `now()` method over `GET /v1/now` for Apple schedule retrieval, the Apple voice-turn endpoint from `07-02`, and the dedicated Apple behavior-summary endpoint from `07-03`, and add explicit auth-header support for operator-authenticated `/v1/*` calls using the shared Apple transport configuration seam rather than unauthenticated defaults. Extend `OfflineStore.swift` with cached Apple schedule/behavior summary storage only where shared caching materially improves quick-loop rendering. Reuse the existing queue/bootstrap seam for nudge done/snooze, commitment, and capture flows; do not add a second Apple-only mutation path. If any voice-turn offline fallback remains, constrain it to queueing/provenance and cached rendering, not local answer synthesis.</action>
  <verify>
    <automated>make check-apple-swift</automated>
  </verify>
  <done>The shared Apple package knows how to call and cache the backend-owned Phase 7 endpoints without widening local policy ownership.</done>
</task>

<task type="auto">
  <name>Task 2: Replace iPhone/watch local query behavior with backend-owned loops and align docs</name>
  <files>clients/apple/Apps/VeliOS/ContentView.swift, clients/apple/Apps/VelWatch/VelWatchApp.swift, clients/apple/Apps/VelWatch/ContentView.swift, clients/apple/README.md, docs/user/daily-use.md, docs/api/runtime.md</files>
  <action>Update `clients/apple/Apps/VeliOS/ContentView.swift` so the Voice tab submits typed requests to the backend Apple voice endpoint and renders the typed response, and any explicit current-schedule retrieval uses `VelClient.now()` rather than cached local synthesis. Remove or explicitly deprecate the current local query synthesis helpers that answer from cached state; keep only local transcript capture, editing, rendering, queueing, and playback responsibilities. Update `clients/apple/Apps/VelWatch/VelWatchApp.swift` and `clients/apple/Apps/VelWatch/ContentView.swift` together so watch quick actions and compact schedule/behavior displays are driven by authenticated `VelClient.now()` plus the Apple behavior-summary endpoint and the existing safe queue path, with refresh/published state changes living in `VelWatchStore` instead of being forced into the view alone. Refresh `clients/apple/README.md`, `docs/user/daily-use.md`, and `docs/api/runtime.md` so they describe the real Phase 7 permissions, offline behavior, current schedule retrieval via `/v1/now`, backend-owned voice semantics, Apple auth configuration for the new routes, and bounded behavior-signal summaries.</action>
  <verify>
    <automated>make check-apple-swift</automated>
  </verify>
  <done>Apple surfaces and docs now align on backend-owned voice, schedule, nudge, and behavior-summary semantics, with Swift acting only as transport/render/queue shell.</done>
</task>

</tasks>

<verification>
Build the shared Apple package after the client wiring changes and manually exercise the iPhone/watch quick loops during execution before closing the phase summary.
</verification>

<success_criteria>
The iPhone and watch clients use backend-owned Phase 7 contracts for fast capture, schedule retrieval, nudge response, and behavior summaries, and the previous Swift-local query synthesis path is no longer the intended authority.
</success_criteria>

<output>
After completion, create `.planning/phases/07-apple-action-loops-and-behavioral-signal-ingestion/07-04-SUMMARY.md`
</output>
