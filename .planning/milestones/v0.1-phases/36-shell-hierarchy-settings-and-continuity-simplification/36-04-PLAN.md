# 36-04 Plan

## Slice

Close the shell simplification phase with `Vel.csv`-backed verification and daily-use docs alignment.

## Work

- verify the shipped shell against the relevant `Vel.csv` friction lines
- align user/product docs to the simplified hierarchy
- record remaining shell slop honestly if anything still does not fit this phase

## Verification

- targeted docs `rg` checks
- focused reruns of `NowView`, `ThreadView`, and `SettingsPage` tests

## Exit

Phase 36 closes with one coherent daily-use shell story and explicit evidence against the relevant `Vel.csv` pressure.
