# Phase 52 Plan 01

## Goal

Implement the full `Now/UI MVP Conformance Closure` correction memo in one execution chunk with no deferred product items.

## Scope

- rebuild shell navigation into top-nav + collapsible context/documentation sidebar
- refactor `Now` structure, task grouping, nudge presentation, and floating input
- correct `Inbox` shared-object behavior and remove helper-heavy presentation
- rebuild `Threads` into a left-list + main-content layout
- simplify `Settings` into compact left-rail groups with docs removed
- update parity-sensitive client/shared seams where needed

## Workstreams

### 1. Shell And Navigation

- replace the left sidebar shell pattern in [AppShell.tsx](/home/jove/code/vel/clients/web/src/components/AppShell.tsx)
- replace [Sidebar.tsx](/home/jove/code/vel/clients/web/src/components/Sidebar.tsx) with a compact top-nav implementation
- add top-level `Documentation` route/access behavior
- make the right sidebar collapsible with default-collapsed posture and responsive info-nav affordance

### 2. Now Surface

- reshape [NowView.tsx](/home/jove/code/vel/clients/web/src/components/NowView.tsx) into:
  - containerless top micro-rows
  - minimal timing
  - single-line active description
  - styled nudge info boxes with project tags
  - grouped task container with `NOW`, `TODAY`, `AT RISK`, `NEXT`
  - floating bottom-center composer with text + voice only
- remove `More context and controls` from the main surface and relocate relevant content to the sidebar
- standardize small ALL-CAPS action/button styling through reusable components

### 3. Inbox Truth And Presentation

- inspect the `Now` and `Inbox` data/query seams
- correct shared-object invariants at the data/query layer where `Now` actionable items are missing from `Inbox`
- simplify [InboxView.tsx](/home/jove/code/vel/clients/web/src/components/InboxView.tsx) into compact triage UI with minimal prose

### 4. Threads And Settings

- rebuild [ThreadView.tsx](/home/jove/code/vel/clients/web/src/components/ThreadView.tsx) into a left thread list plus main panel
- prioritize unread status and message preview in list rows
- simplify [SettingsPage.tsx](/home/jove/code/vel/clients/web/src/components/SettingsPage.tsx) into compact grouped left-rail sections
- move documentation access out of settings

### 5. Shared Types, Tests, And Parity-Sensitive Seams

- adjust shared types/query helpers only where required by the new surface or inbox invariant
- add or update focused tests around:
  - hidden empty-state rules
  - task grouping order
  - docs/top-nav behavior
  - thread list row priority
  - settings grouping shell
  - inbox/now shared-object behavior
- update parity-sensitive seams with source-level alignment where app-target execution is unavailable

## Baseline Checks Before Editing

- run the narrowest relevant web tests for `Now`, `Inbox`, `Threads`, `Settings`, and shell navigation first
- inspect current type/data contracts feeding `Now` and `Inbox` before changing UI assumptions

## Exit Criteria

- every memo item requested for implementation is present in this phase’s code changes
- no implementation item is left for a later phase
- any remaining gap is verification-only or environment-only and explicitly recorded
