# Phase 91 Plan

## Objective

Execute the second acceptance-driven UI revision pass after the first remediation checkpoint.

Primary acceptance ledger:
- `91-ACCEPTANCE-CHECKLIST.md`
- use that checklist, not ad hoc pass summaries, to determine what is still outstanding

## Scope

### In scope

- capture the new acceptance blockers in planning
- revise `Now`, `Nudges`, shared composer/action entry, `Threads`, and `System`
- fix interaction failures that leave the shared composer in a stranded queued state
- improve surface fidelity where the current runtime still diverges from the archived/layout targets
 - improve surface fidelity where the current runtime still diverges from the accepted checklist targets

### Out of scope

- milestone closeout
- new surface invention
- backend redesign not required by the stated UI failures
- transport/API gaps now inventoried for follow-on handling in Phase 92 rather than hidden inside frontend workaround code

## Initial Revision Buckets

1. `Now`
   - uppercase section titling and badge iconography
   - multi-active-task handling
   - task drag/reorder across sections with state changes
   - unify active-task row with the standard task component
   - restore checkbox/tag alignment, opacity, and scale behavior

2. `Nudges`
   - show total count in heading
   - remove page-specific result limits
   - apply full type-color treatment to border/background/icon/ring
   - tighten icon/ring centering and gutter placement
   - ensure action chiplets are visible
   - keep text/tags inside the pill without indentation from the gutter icon
   - add defer/snooze affordance and deferred-count header treatment
   - reduce action chiplet size and make them icon-led

3. Shared composer / action entry
   - fix stranded queued follow-up state
   - make queued follow-up actionable and dismissible
   - implement intent marking / fallback interaction
   - increase control height to match the rest of the language
   - verify microphone / recording behavior
   - add right-side floating attachment affordance

4. `Threads`
   - remove collapse behavior
   - uppercase sidebar heading
   - align filter-tag sizing with the rest of the system
   - restore active shimmer/outline treatment
   - replace sidebar created-time tag with project tag
   - improve sidebar scrolling/fade behavior
   - refine archive button, header separators, tag alignment, and bubble shape
   - add unread marker treatment in the left gutter
   - move current-thread metadata into a floating header line
   - tighten bubble width and dock alignment by speaker
   - add assistant copy affordance and participant fallback avatars

5. `System`
   - remove remaining dark background treatment
   - remove `On this page`
   - match separator structure more closely to `Threads`
   - keep single-page anchor model
   - remove side-by-side main content layouts
   - remove visible containers/borders/padding where they still dominate
   - ensure lists show complete content and stats lead sections
   - collapse all content into one anchored vertical document
   - colocate integration/project/account config directly under their owning heading
   - add sidebar filtering for anchored sections

## Clarifications Locked

- `Now` drag targets are explicitly:
  - `ACTIVE`
  - `NEXT UP`
  - `IF TIME ALLOWS`
  - `COMPLETED`
- dropping a task into a section should directly change its state
- if multiple tasks are active, they should all remain visually primary in a stacked active section
- thread sidebar project tags should be omitted when no obvious project exists
- ambiguous composer intent fanout should offer:
  - `Task`
  - `Question`
  - `Thread`
  - `Capture`
- choosing an intent should also persist/tag that intent at the data level
- queued follow-up should prefer `Now`-local affordances first
- opening the related thread should always be available as an affordance
- if microphone input is unavailable or broken, the UI should raise a system/config nudge
- `System` anchors are the left-rail second-level items; no separate `On this page` widget should remain
- `System` should use separators and spacing rather than visible subsection containers/cards
- if more than one task is active, all active tasks should render as visually primary stacked rows
- thread rows without an obvious project should omit the project tag entirely
- composer intent fallback options are:
  - `Task`
  - `Question`
  - `Thread`
  - `Capture`
- selecting a composer fallback intent should also persist/tag that intent at the data level when supported
- queued follow-up handling should surface `Now`-local affordances first and keep `Open thread` available
- unsupported voice input should surface as a system/config nudge rather than disappearing silently
- the `Now` header stack is:
  - `CLIENT_NAME | LOCATION`
  - `Now`
  - current date/time string including timezone
  - `CURRENT_EVENT | CONTEXT`
- items in `Next up` without chronology should render after calendar events in descending priority order
- `Stretch Goals` is renamed `If time allows`
- checking a task completes it immediately and moves it into a collapsed completed section at the bottom
- the shared input/composer remains in its current body location on `Now`
- unclear intent should show as a small popout/fanout from the right side after submit only
- no intent fanout appears when there is no pending payload
- nudges use urgency ordering, `+N more` overflow, and one shared pill shape with type-driven color/icon treatment
- the `Threads` left rail is no longer collapsible in this revision
- thread list freshness should use relative time in rows; creation date remains in the opened header
- review affordances that need user input should appear as visible in-thread messages with action chiplets
- `System` remains a single-scroll anchor document with the two-level left rail as the anchor map
- `System` editing is inline with immediate application; destructive integration actions stay visually separated
- navbar left content now carries `CLIENT | LOCATION` with mono date/time/timezone beneath it
- navbar center uses icon-and-count-only badges for completed tasks, nudges, and sync status
- task/nudge badges and tags should keep icons and text vertically centered
- each surface title should carry the same icon as its navbar link
- thread and system filter inputs should carry a right-aligned search icon
- `Now` no longer repeats the client/location header line; that state lives in the navbar
- `Now` section badges should represent section-local counts
- active tasks should use a brighter brand state with shimmer/glow treatment
- `Now` should keep mono datetime, icon-led section badges, a backlog marker for `IF TIME ALLOWS`, and stronger active-task glow
- thread sidebar rows should use an unread gutter marker without separator lines between rows
- thread sidebar title should show `THREADS (N)` for non-archived rows
- current-thread metadata should read `CURRENT THREAD | N MESSAGES | PARTICIPANTS...`, with participant circles as fallback avatars
- thread sidebar active rows should use a stronger brand-outline pill state, with non-active rows slightly faded
- thread headers should keep participant chips on the top line and the editable title below, with autosave when backend support exists
- message bubbles should stay narrower than full width, dock by speaker, and use seamless same-color tails
- message meta rows should keep all-caps speaker/time and de-emphasized message tags
- system sidebar title should match thread-sidebar typography and support text filtering over anchor content
- `System` remains one main vertical document; integrations, logs, activity, and config should stay colocated with their owning item rather than split into separate detail shells
- `System` sidebar should stay docked while scrolling and update its active anchor state
- actual system config values should be exposed directly in the main document
- nudge pills should reuse the task-pill radius/family, keep icon rings in the left gutter without indenting content, and show compact icon-led actions plus relative age
- archived `Now` is no longer a fidelity target on its own; only the explicitly accepted component/layout behaviors remain in scope

## Remaining Truth Gaps Routed To Phase 92

- durable multi-active task lanes and lane ordering are still backend-limited even after the local `Now` lane pass
- thread sidebar project tags remain blocked on thread-list metadata that is not in the current DTO
- many `System` fields are now visibly editable in the UI, but durable persistence still requires a typed settings mutation seam

## Current Execution Notes

- shared pill centering was tightened again in the core pill/tag primitives rather than via more surface-local overrides
- `Threads` and `System` search inputs now share one `SearchField` primitive
- the nudge gutter shell continues to run through the shared `FloatingPill` primitive
- `System` overview/activity/capability sections were flattened further onto shared `SystemDocument` rows to reduce local panel drift
- navbar center badges were tightened further toward the slim faded icon/count treatment
- thread header/meta alignment was compacted again and the active row now preserves the shared `activeBrand` treatment instead of cancelling it locally
- composer now surfaces a stronger in-field recorded/transcribing status chip with elapsed time while listening
- thread filtering now includes an archived mode rather than stopping at all/unread/active
- nudge defer/delay affordances now use the reschedule glyph instead of the generic clock
- local browser voice-failure nudges now carry timestamps so they behave like the rest of the nudge lane
- shared `SystemDocument` rows and status chips were compacted further to keep `System` moving toward a denser single-document feel
- thread filter pills were brought closer to the rest of the compact tag system
- `System` subsection headings now use the smaller document-label treatment instead of larger section-title styling
- `Now` task rows now route title + right-aligned metadata through the shared title/meta band instead of splitting them into separate local clusters
- navbar wordmark spacing, queued assistant-feedback actions, floating nudge geometry, and bubble-tail geometry were all tightened again in shared/UI-shell code rather than left as bespoke local recipes
- `Now` section headers now carry clearer icon-led labels, the thread header archive control uses the shared chip treatment, and the top of `System` was flattened a bit more
