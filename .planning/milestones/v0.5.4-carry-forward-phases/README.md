# Historical Carry-Forward Bucket: v0.5.4

This directory holds historical carry-forward packets `90` through `92`.

They remain useful planning context but are not part of the active milestone and should stay out of `.planning/phases/`.
