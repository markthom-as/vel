# Archived Phase Bucket: v0.5.3

This directory holds archived `0.5.3` phase packets `78` through `83`.

These packets were moved out of `.planning/phases/` to keep active milestone discovery limited to the current line.
