---
phase: 47-rust-owned-now-core-models-and-transport-seam
plan: 02
type: execute
wave: 1
depends_on:
  - 47-01
files_modified:
  - crates/veld/src/services/now.rs
  - crates/veld/src/routes/now.rs
  - crates/veld/src/app.rs
autonomous: true
requirements:
  - NOW-02
  - TASK-01
must_haves:
  truths:
    - "The Now service emits canonical header, status-row, context-line, nudge-stack, task-lane, and thread-escalation fields from Rust-owned logic."
    - "Current task, recent completed, overflow, and shared nudge ordering no longer depend on shell-local interpretation."
    - "The existing /v1/now route exposes the new canonical seam directly."
  artifacts:
    - path: "crates/veld/src/services/now.rs"
      provides: "Rust-owned canonical Now assembly over the existing service seam."
      contains: "Now"
    - path: "crates/veld/src/routes/now.rs"
      provides: "Route mapping for the canonical Now transport seam."
      contains: "Now"
    - path: "crates/veld/src/app.rs"
      provides: "Execution-backed route tests for the canonical Now seam."
      contains: "now"
---

<objective>
Assemble the canonical Now read model in the existing Rust service and expose it through `/v1/now`.

Purpose: Make the portable Now DTO seam real instead of leaving it as an unused transport shell.
Output: backend-owned service output and route mapping for the canonical header, status row, one-liner, nudge stack, task lane, and escalation markers.
</objective>

<context>
@.planning/phases/47-rust-owned-now-core-models-and-transport-seam/47-CONTEXT.md
@.planning/phases/47-rust-owned-now-core-models-and-transport-seam/47-01-PLAN.md
@docs/product/now-surface-canonical-contract.md
@crates/veld/src/services/now.rs
@crates/veld/src/routes/now.rs
@crates/veld/src/app.rs
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the canonical Now read model in Rust and expose it through /v1/now</name>
  <files>crates/veld/src/services/now.rs, crates/veld/src/routes/now.rs, crates/veld/src/app.rs</files>
  <read_first>crates/veld/src/services/now.rs
crates/veld/src/routes/now.rs
crates/veld/src/app.rs
docs/product/now-surface-canonical-contract.md</read_first>
  <action>Extend the existing `Now` service output and route mapping so Rust assembles the canonical header bucket counts, status-row inputs, context line, ranked nudge stack, canonical task subset, and compact escalation/open-target state. Reuse existing overview, reflow, check-in, review, freshness, and continuation inputs where possible, but stop forcing later client phases to infer missing product meaning from legacy field groupings. Add or update focused backend route tests in `crates/veld/src/app.rs` for the new shape.</action>
  <acceptance_criteria>- `rg -n "header|status|context|nudge|task|dock|open_target|escalat" crates/veld/src/services/now.rs crates/veld/src/routes/now.rs` returns the new canonical seam.
- `rg -n "now_endpoint" crates/veld/src/app.rs` returns tests that assert canonical Now fields, not only legacy overview fields.</acceptance_criteria>
  <verify>
    <automated>cargo test -p veld app::tests::now_endpoint_returns_consolidated_snapshot -- --nocapture</automated>
  </verify>
  <done>The backend Now route exposes the canonical shared seam directly from Rust-owned logic.</done>
</task>

</tasks>

<verification>
Confirm `/v1/now` now carries the canonical shared execution contract directly from the Rust service seam.
</verification>

<success_criteria>
The canonical Now contract is no longer only theoretical; the backend emits it through the existing Now route.
</success_criteria>

<output>
After completion, create `.planning/phases/47-rust-owned-now-core-models-and-transport-seam/47-02-SUMMARY.md`
</output>
