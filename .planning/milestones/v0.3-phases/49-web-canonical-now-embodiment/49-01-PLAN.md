---
phase: 49-web-canonical-now-embodiment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - clients/web/src/components/NowView.tsx
  - clients/web/src/components/NowView.test.tsx
  - /home/jove/code/vel/.planning/milestones/v0.3-phases/49-web-canonical-now-embodiment/49-01-SUMMARY.md
autonomous: true
requirements:
  - NOW-03
  - PARITY-01
must_haves:
  truths:
    - "The web `Now` surface becomes compact and execution-first instead of a card-heavy summary dashboard."
    - "Header, status row, context line, and mesh trust posture render from shared Rust-owned transport."
---

<objective>
Restructure the web `Now` surface to the canonical compact layout using the existing shared transport blocks before deeper interaction work lands.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Rebuild the web `Now` frame around the canonical compact structure</name>
  <files>clients/web/src/components/NowView.tsx, clients/web/src/components/NowView.test.tsx</files>
  <action>Replace the current card-heavy `Now` layout with the canonical compact frame: header with filter buckets, status row, context line, compact mesh trust visibility, and a bottom-docked input shell. Keep it strictly driven by typed backend data.</action>
  <acceptance_criteria>- `NowView` renders the shared header title and bucket counts without local fallback ranking.
- Status row and context line render in the compact top stack.
- Compact mesh trust posture is visible without moving repair workflows into `Now`.
- Tests assert the new structure without depending on stale explanatory copy.</acceptance_criteria>
  <verify>
    <automated>npm --prefix clients/web test -- --run src/components/NowView.test.tsx</automated>
  </verify>
  <done>The web shell has the correct compact top-level structure for the canonical `Now` contract.</done>
</task>

</tasks>

<output>
After completion, create `/home/jove/code/vel/.planning/milestones/v0.3-phases/49-web-canonical-now-embodiment/49-01-SUMMARY.md`
</output>
