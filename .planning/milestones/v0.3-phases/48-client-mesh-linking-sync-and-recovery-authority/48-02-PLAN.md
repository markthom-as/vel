---
phase: 48-client-mesh-linking-sync-and-recovery-authority
plan: 02
type: execute
wave: 1
depends_on:
  - 48-01
files_modified:
  - crates/vel-api-types/src/lib.rs
  - crates/veld/src/services/now.rs
  - crates/veld/src/routes/now.rs
  - clients/web/src/types.ts
  - clients/web/src/types.test.ts
  - clients/apple/VelAPI/Sources/VelAPI/Models.swift
  - .planning/phases/48-client-mesh-linking-sync-and-recovery-authority/48-02-SUMMARY.md
autonomous: true
requirements:
  - MESH-01
  - SYNC-01
  - SYNC-02
must_haves:
  truths:
    - "Shared DTOs and `/v1/now` transport expose compact mesh, sync, queued-write, and repair-route truth."
    - "Client boundaries decode the same typed authority summary without shell-local reinterpretation."
---

<objective>
Add shared typed mesh/sync authority blocks to the canonical `Now` transport seam and map them through backend and client boundaries.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add shared mesh and sync transport blocks</name>
  <files>crates/vel-api-types/src/lib.rs, crates/veld/src/services/now.rs, crates/veld/src/routes/now.rs, clients/web/src/types.ts, clients/web/src/types.test.ts, clients/apple/VelAPI/Sources/VelAPI/Models.swift</files>
  <action>Introduce typed transport for compact mesh authority, sync posture, queued-write counts, last-sync visibility, and repair-route targets. Assemble that truth in the Rust `Now` service and expose it through typed web and Apple boundaries in the same slice.</action>
  <acceptance_criteria>- `/v1/now` transport includes a typed compact mesh/sync summary.
- Web and Apple typed boundaries accept the new fields.
- The seam stays compact and suitable for both header/status use and urgent warning-bar routing.</acceptance_criteria>
  <verify>
    <automated>cargo check -p veld && npm --prefix clients/web test -- --run src/types.test.ts</automated>
  </verify>
  <done>The shared `Now` seam now carries Rust-owned compact mesh and sync truth.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/48-client-mesh-linking-sync-and-recovery-authority/48-02-SUMMARY.md`
</output>
