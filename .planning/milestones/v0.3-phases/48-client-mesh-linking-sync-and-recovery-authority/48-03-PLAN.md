---
phase: 48-client-mesh-linking-sync-and-recovery-authority
plan: 03
type: execute
wave: 1
depends_on:
  - 48-01
  - 48-02
files_modified:
  - crates/vel-api-types/src/lib.rs
  - crates/veld/src/services/now.rs
  - crates/veld/src/routes/now.rs
  - config/README.md
  - docs/templates/
  - .planning/phases/48-client-mesh-linking-sync-and-recovery-authority/48-03-SUMMARY.md
autonomous: true
requirements:
  - CORE-04
  - MESH-02
  - MESH-04
must_haves:
  truths:
    - "Governed config for `Now` title/count/watch behavior is typed and Rust-owned."
    - "Repair-route and approval/config posture are explicit, parseable support lanes rather than shell constants."
---

<objective>
Define the governed-config and repair-route support lane needed for rapid cross-platform `Now` iteration without shell-owned policy drift.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Ship versioned governed-config support for Now and repair routes</name>
  <files>crates/vel-api-types/src/lib.rs, crates/veld/src/services/now.rs, crates/veld/src/routes/now.rs, config/README.md, docs/templates/</files>
  <action>Introduce typed governed-config support for title/display/count/watch behavior and explicit repair-route targets. If the boundary is stable in this slice, ship the code-adjacent docs/template/example needed by repo rules so the config support is parseable and reviewable.</action>
  <acceptance_criteria>- Rust-owned config support exists for title/count/watch behavior needed by the canonical `Now` contract.
- Repair-route targets are typed rather than implicit strings in client code.
- Config/contract docs reflect the shipped boundary.</acceptance_criteria>
  <verify>
    <automated>rg -n "title|count|watch|repair" config/README.md crates/vel-api-types/src/lib.rs crates/veld/src/services/now.rs crates/veld/src/routes/now.rs</automated>
  </verify>
  <done>Phase 48 leaves governed `Now` support as shared Rust-owned policy, not shell drift.</done>
</task>

</tasks>

<output>
After completion, create `.planning/phases/48-client-mesh-linking-sync-and-recovery-authority/48-03-SUMMARY.md`
</output>
