# Phase 70 Validation

- [ ] canonical `Threads` read flow exists
- [ ] manual invocation is object-scoped only
- [ ] manual invocation requires exactly one bound canonical object with a supported binding
- [ ] `/system` is a single top-level route
- [ ] `/system` ships the fixed first-pass section set
- [ ] `Modules` / `Integrations` / `Accounts` / `Scopes` expose only named allow-listed canonical actions
- [ ] no additional, inferred, composite, or ambiguous configuration actions are rendered
- [ ] no builders, editors, or workflow simulation land
