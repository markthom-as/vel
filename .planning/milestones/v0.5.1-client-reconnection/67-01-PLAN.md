# 67-01 Plan

## Objective

Enumerate every current client/backend touchpoint shaped by pre-`0.5` assumptions and classify its fate.

## Work

1. Audit current web routes, fetchers, DTO assumptions, and view-level normalization seams.
2. Build the named kill list for legacy read/config routes and compatibility shims.
3. Mark each touchpoint as `rewrite`, `quarantine`, or `delete`.

## Acceptance

- no unnamed temporary shims remain
- kill list exists before implementation spreads
- all route/contract decisions are explicit and reviewable
