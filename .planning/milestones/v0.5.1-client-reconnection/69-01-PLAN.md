# 69-01 Plan

## Objective

Rebind `Now` to canonical task/calendar/availability truth and keep it as a thin execution surface.

## Work

1. Replace stale `Now` data sources with canonical read models only.
2. Reconnect direct triage mutations through canonical `WriteIntent`.
3. Absorb `Inbox` behavior into `Now` triage/query flows without reviving a separate `Inbox` surface.

## Acceptance

- `Now` renders canonical operational truth only
- all `Now` mutations are canonical writes
- no client-side ranking, inferred urgency, or hidden sorting logic remains
