# Phase 69 Context

## Goal

Make `Now` the truthful temporal and operational surface over canonical backend state.

## Boundary

This phase reconnects `Now`; it does not rebuild the product hierarchy beyond the frozen three-surface model.

## Required Outputs

- canonical `Now` read flow
- canonical triage mutation flow
- `Inbox` absorption into `Now`
- explicit ban on client-side prioritization or semantic inference
