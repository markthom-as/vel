# Future Seed Bucket: Post-v0.5.7

This directory holds the future-only `999.1` milestone seed for multi-transport cluster mesh, routing, and capability sync.

It is intentionally parked here instead of `.planning/phases/` because it is backlog planning, not active execution scope.
