# Build workflow context binding

## Scope
- Implement resolver strategies for project/task/nudge/thread
- Build typed context packet assembly
- Persist resolved bindings in run records

## Acceptance criteria
- workflow can bind to explicit thread/project/task IDs
- runtime can resolve nearest_active/resume_or_create strategies
- UI/CLI output shows bound context clearly
