---
phase: 60-module-loader-registry-and-core-module-bootstrap
plan: 01
type: execute
wave: 0
depends_on:
  - 59-05
files_modified:
  - crates/vel-core/src/registry_ids.rs
  - crates/vel-core/src/module_registry.rs
  - crates/vel-storage/src/module_registry_store.rs
  - crates/veld/src/services/registry_loader.rs
autonomous: true
requirements:
  - MODULE-50-01
must_haves:
  truths:
    - "Phase 60 starts by building one canonical registry/loader contract rather than separate core and integration loading paths."
    - "Registry objects keep stable semantic IDs and canonical persisted state independent of manifest file paths."
    - "The loader path materializes manifests into canonical registry entities rather than treating manifests as ambient runtime truth."
    - "Reconciliation results are explicit named states rather than implicit loader folklore."
  artifacts:
    - path: "crates/vel-core/src/registry_ids.rs"
      provides: "Stable typed registry ID helpers and semantic namespace posture."
      contains: "module.integration.todoist"
    - path: "crates/vel-core/src/module_registry.rs"
      provides: "Canonical module registry object types and loader/reconciler contracts."
      contains: "RegistryLoader"
    - path: "crates/vel-storage/src/module_registry_store.rs"
      provides: "Dedicated registry persistence seam."
      contains: "RegistryStore"
    - path: "crates/veld/src/services/registry_loader.rs"
      provides: "Runtime loader service over manifest source, registry store, and reconciler."
      contains: "ManifestSource"
---

<objective>
Implement the canonical registry/loader contract, typed registry IDs, and dedicated module-registry persistence seam.

Purpose: give Phase 60 one lawful registry substrate so core and integration modules can be loaded through the same governed path instead of through ad hoc bootstrap code.
Output: registry ID helpers, canonical registry object types, registry store seam, and runtime loader service.
</objective>

<context>
@.planning/milestones/v0.5-core-rewrite/60-CONTEXT.md
@.planning/milestones/v0.5-core-rewrite/57-03-PLAN.md
@.planning/milestones/v0.5-core-rewrite/59-CONTEXT.md
@docs/cognitive-agent-architecture/architecture/0.5-module-skill-tool-and-workflow-registry.md
@docs/cognitive-agent-architecture/architecture/0.5-rust-backend-implementation-constraints.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the canonical registry loader contract</name>
  <files>crates/vel-core/src/registry_ids.rs, crates/vel-core/src/module_registry.rs, crates/vel-storage/src/module_registry_store.rs, crates/veld/src/services/registry_loader.rs</files>
  <read_first>.planning/milestones/v0.5-core-rewrite/60-CONTEXT.md
.planning/milestones/v0.5-core-rewrite/57-03-PLAN.md
.planning/milestones/v0.5-core-rewrite/59-CONTEXT.md
docs/cognitive-agent-architecture/architecture/0.5-module-skill-tool-and-workflow-registry.md
docs/cognitive-agent-architecture/architecture/0.5-rust-backend-implementation-constraints.md</read_first>
  <action>Add typed registry ID helpers for canonical module/skill/tool namespaces, define canonical registry object and reconciliation contracts, add a dedicated registry store seam in `vel-storage`, and implement a runtime `RegistryLoader` service over `ManifestSource` and `RegistryReconciler`. Keep manifests as inputs to canonical materialization rather than direct runtime truth. Define explicit reconciliation states such as `new`, `unchanged`, `updated`, `drifted`, `forked_local`, `superseded`, `disabled`, and `invalid`.</action>
  <acceptance_criteria>- `rg -n "module.integration.todoist|module.integration.google-calendar|skill.core|tool.object.get" crates/vel-core/src/registry_ids.rs crates/vel-core/src/module_registry.rs` returns canonical registry ID posture.
- `rg -n "RegistryLoader|ManifestSource|RegistryReconciler" crates/vel-core/src/module_registry.rs crates/veld/src/services/registry_loader.rs` returns the required loader roles.
- `rg -n "RegistryStore|module registry|persisted overlay|reconcile|new|unchanged|updated|drifted|forked_local|superseded|disabled|invalid" crates/vel-storage/src/module_registry_store.rs crates/vel-core/src/module_registry.rs` returns dedicated registry persistence and reconciliation concepts.
- Narrow compile checks or tests prove the loader contracts compile without provider adapters enabled.</acceptance_criteria>
  <verify>
    <automated>rg -n "module.integration.todoist|module.integration.google-calendar|skill.core|tool.object.get|RegistryLoader|ManifestSource|RegistryReconciler|RegistryStore|persisted overlay|reconcile|new|unchanged|updated|drifted|forked_local|superseded|disabled|invalid" crates/vel-core/src/registry_ids.rs crates/vel-core/src/module_registry.rs crates/vel-storage/src/module_registry_store.rs crates/veld/src/services/registry_loader.rs</automated>
  </verify>
  <done>Phase 60 now has one canonical registry/loader contract instead of implicit manifest execution and split registry truth.</done>
</task>

</tasks>

<verification>
Confirm that registry identity and registry persistence are canonical and stable, and that loader behavior is expressed through explicit interfaces rather than bootstrap folklore.
</verification>

<success_criteria>
Phase 60 starts with one governed registry/loader substrate: canonical IDs, canonical registry entities, explicit loader/reconciler roles, and dedicated registry storage.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.5-core-rewrite/60-01-SUMMARY.md`
</output>
