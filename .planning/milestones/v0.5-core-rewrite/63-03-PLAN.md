---
phase: 63-todoist-multi-account-adapter-and-canonical-task-cut-in
plan: 03
type: execute
wave: 2
depends_on:
  - 63-02
files_modified:
  - crates/vel-adapters-todoist/src/ownership_sync.rs
  - crates/vel-adapters-todoist/src/tombstones.rs
  - crates/veld/src/services/todoist_write_bridge.rs
  - crates/veld/tests/phase63_sync_and_conflicts.rs
autonomous: true
requirements:
  - SYNC-50-01
  - TODOIST-50-01
must_haves:
  truths:
    - "Todoist sync obeys field ownership and source-owned precedence rather than adapter-local guesswork."
    - "Upstream deletes become local tombstones with reconciliation state by default."
    - "Outward Todoist writes remain conservative, config-gated, and mediated through `WriteIntent`."
    - "Local Vel-originated writes also emit `TaskEvent` history with explicit provenance so behavior analytics remain continuous across provider and local actions."
  artifacts:
    - path: "crates/vel-adapters-todoist/src/ownership_sync.rs"
      provides: "Todoist ownership and sync reconciliation rules."
      contains: "source-owned"
    - path: "crates/vel-adapters-todoist/src/tombstones.rs"
      provides: "Todoist delete/tombstone handling."
      contains: "tombstone"
    - path: "crates/veld/src/services/todoist_write_bridge.rs"
      provides: "Todoist outward-write bridge through the membrane and `WriteIntent`."
      contains: "WriteIntent"
    - path: "crates/veld/tests/phase63_sync_and_conflicts.rs"
      provides: "Tests for ownership-aware sync, tombstones, and conservative outward writes."
      contains: "PolicyDenied"
---

<objective>
Implement ownership-aware Todoist sync, tombstone handling, and conservative outward-write bridging.

Purpose: prove bidirectional Todoist behavior without bypassing the canonical ownership, policy, and `WriteIntent` law.
Output: ownership-sync rules, tombstone handling, outward-write bridge, and sync/conflict tests.
</objective>

<context>
@.planning/milestones/v0.5-core-rewrite/63-CONTEXT.md
@.planning/milestones/v0.5-core-rewrite/59-CONTEXT.md
@.planning/milestones/v0.5-core-rewrite/59-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build ownership-aware sync and conservative outward writes</name>
  <files>crates/vel-adapters-todoist/src/ownership_sync.rs, crates/vel-adapters-todoist/src/tombstones.rs, crates/veld/src/services/todoist_write_bridge.rs, crates/veld/tests/phase63_sync_and_conflicts.rs</files>
  <read_first>.planning/milestones/v0.5-core-rewrite/63-CONTEXT.md
.planning/milestones/v0.5-core-rewrite/59-CONTEXT.md
.planning/milestones/v0.5-core-rewrite/59-VERIFICATION.md</read_first>
  <action>Add Todoist ownership/reconciliation rules over the Phase 59 membrane model, add delete/tombstone handling, implement a Todoist outward-write bridge that goes through `WriteIntent` and policy mediation, ensure local and provider-originated task changes can emit normalized `TaskEvent` history with explicit provenance, and add tests proving source-owned precedence, tombstones, and conservative write behavior under read-only and denied paths.</action>
  <acceptance_criteria>- `rg -n "source-owned|shared|Vel-only|conflict|reconcile" crates/vel-adapters-todoist/src/ownership_sync.rs` returns ownership-aware sync concepts.
- `rg -n "tombstone|deleted_upstream|pending_reconcile|restored" crates/vel-adapters-todoist/src/tombstones.rs` returns delete/tombstone concepts.
- `rg -n "WriteIntent|read_only|PolicyDenied|ask_if_external_write" crates/veld/src/services/todoist_write_bridge.rs crates/veld/tests/phase63_sync_and_conflicts.rs` returns conservative outward-write and refusal-path concepts.
- Tests prove source-owned fields win, upstream deletes become tombstones, outward writes stay mediated and config-gated, and local/provider task history remains continuous through normalized `TaskEvent` provenance.</acceptance_criteria>
  <verify>
    <automated>rg -n "source-owned|shared|Vel-only|conflict|reconcile|tombstone|deleted_upstream|pending_reconcile|restored|WriteIntent|read_only|PolicyDenied|ask_if_external_write" crates/vel-adapters-todoist/src/ownership_sync.rs crates/vel-adapters-todoist/src/tombstones.rs crates/veld/src/services/todoist_write_bridge.rs crates/veld/tests/phase63_sync_and_conflicts.rs</automated>
  </verify>
  <done>Phase 63 now proves Todoist sync and outward writes obey canonical ownership, tombstone, and `WriteIntent` law rather than adapter shortcuts.</done>
</task>

</tasks>

<verification>
Confirm that Todoist bidirectional behavior remains conservative and constitutional: source-owned precedence, tombstones by default, and outward writes through policy plus `WriteIntent`.
</verification>

<success_criteria>
Phase 63 makes Todoist bidirectional behavior lawful and ownership-aware without creating bespoke adapter shortcuts.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.5-core-rewrite/63-03-SUMMARY.md`
</output>
