---
phase: 61-workflow-and-skill-primitives-over-canonical-objects
plan: 03
type: execute
wave: 2
depends_on:
  - 61-02
files_modified:
  - crates/vel-core/src/workflow_runs.rs
  - crates/vel-core/src/approvals.rs
  - crates/veld/src/services/workflow_runner.rs
  - crates/veld/tests/phase61_manual_invocation.rs
autonomous: true
requirements:
  - WF-50-01
  - ACTION-50-01
  - POLICY-50-01
must_haves:
  truths:
    - "Run records remain runtime/control evidence rather than canonical content objects."
    - "Manual invocation is the proving posture for `0.5` workflow runtime."
    - "Approval and dry-run paths are first-class workflow runtime behavior."
    - "Dry-run evaluates and records lawfully but does not mutate canonical content or perform irreversible external mutation."
  artifacts:
    - path: "crates/vel-core/src/workflow_runs.rs"
      provides: "Run-record types and lifecycle states for workflow execution."
      contains: "RunRecord"
    - path: "crates/vel-core/src/approvals.rs"
      provides: "Approval request/state types for workflow steps."
      contains: "ApprovalRequired"
    - path: "crates/veld/src/services/workflow_runner.rs"
      provides: "Manual workflow invocation and step execution over the membrane."
      contains: "WorkflowRunner"
    - path: "crates/veld/tests/phase61_manual_invocation.rs"
      provides: "Tests for manual invocation, dry-run, approval pause, and run-record emission."
      contains: "dry_run"
---

<objective>
Implement manual workflow invocation, run records, and approval-aware execution.

Purpose: prove a minimal backend workflow runtime without widening into triggers or speculative orchestration.
Output: workflow run/approval types, manual runner service, and invocation tests.
</objective>

<context>
@.planning/milestones/v0.5-core-rewrite/61-CONTEXT.md
@.planning/milestones/v0.5-core-rewrite/59-VERIFICATION.md
@docs/cognitive-agent-architecture/architecture/0.5-workflow-runtime-primitives.md
@docs/cognitive-agent-architecture/architecture/0.5-ownership-conflict-and-write-intent.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build manual workflow invocation and run-record behavior</name>
  <files>crates/vel-core/src/workflow_runs.rs, crates/vel-core/src/approvals.rs, crates/veld/src/services/workflow_runner.rs, crates/veld/tests/phase61_manual_invocation.rs</files>
  <read_first>.planning/milestones/v0.5-core-rewrite/61-CONTEXT.md
.planning/milestones/v0.5-core-rewrite/59-VERIFICATION.md
docs/cognitive-agent-architecture/architecture/0.5-workflow-runtime-primitives.md
docs/cognitive-agent-architecture/architecture/0.5-ownership-conflict-and-write-intent.md</read_first>
  <action>Add workflow run-record and approval types, implement a manual workflow runner over the membrane that can execute `action` and `skill` steps, pause on `approval`, and respect dry-run behavior, and add tests proving run records and approval states are emitted as runtime/control evidence rather than content objects. Make record boundaries explicit: `RunRecord` tracks workflow lifecycle, `AuditEntry` tracks governed actions and policy-significant events, `ApprovalRecord` tracks approval lifecycle, and `WriteIntent` tracks outward mutation proposals. Define dry-run so that policy may be checked, runtime evidence may be emitted, and planning/evaluation may occur, but canonical content and irreversible external state remain unmodified.</action>
  <acceptance_criteria>- `rg -n "RunRecord|created|ready|running|awaiting_approval|dry_run_complete|completed|failed|refused|cancelled" crates/vel-core/src/workflow_runs.rs` returns run lifecycle/state-machine concepts.
- `rg -n "ApprovalRequired|approved|rejected|pending" crates/vel-core/src/approvals.rs crates/veld/src/services/workflow_runner.rs` returns approval concepts.
- `rg -n "AuditEntry|ApprovalRecord|WriteIntent|run record|dry_run|no canonical mutation|no external mutation" crates/veld/src/services/workflow_runner.rs crates/veld/tests/phase61_manual_invocation.rs` returns runtime evidence and dry-run boundary concepts.
- Tests prove manual invocation emits run records, supports dry-run, and pauses lawfully for approval without requiring trigger infrastructure.</acceptance_criteria>
  <verify>
    <automated>rg -n "RunRecord|created|ready|running|awaiting_approval|dry_run_complete|completed|failed|refused|cancelled|ApprovalRequired|approved|rejected|pending|AuditEntry|ApprovalRecord|WriteIntent|manual|dry_run|no canonical mutation|no external mutation|WorkflowRunner|run record" crates/vel-core/src/workflow_runs.rs crates/vel-core/src/approvals.rs crates/veld/src/services/workflow_runner.rs crates/veld/tests/phase61_manual_invocation.rs</automated>
  </verify>
  <done>Phase 61 now proves minimal manual workflow execution with runtime evidence and approval seams, without pretending triggers already exist.</done>
</task>

</tasks>

<verification>
Confirm that workflow execution remains manual and approval-aware, and that run records stay in runtime/control space rather than becoming canonical content.
</verification>

<success_criteria>
Phase 61 lands a minimal backend workflow runtime: manual invocation, run records, approval seams, and dry-run behavior over canonical objects.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.5-core-rewrite/61-03-SUMMARY.md`
</output>
