---
phase: 62-calendar-core-model-and-canonical-availability-semantics
plan: 03
type: execute
wave: 2
depends_on:
  - 62-02
files_modified:
  - crates/vel-core/src/availability.rs
  - crates/veld/src/services/availability_projection.rs
  - crates/veld/src/services/calendar_explain.rs
  - crates/veld/tests/phase62_availability.rs
autonomous: true
requirements:
  - CAL-50-01
  - SYNC-50-01
must_haves:
  truths:
    - "Availability remains a governed read model, not a canonical content object."
    - "Availability is explainable and rebuildable from canonical calendar state and policy/config inputs."
    - "This phase ends with native availability semantics without drifting into scheduling UX or provider sync behavior."
  artifacts:
    - path: "crates/vel-core/src/availability.rs"
      provides: "Availability read-model types and policy/config inputs."
      contains: "AvailabilityWindow"
    - path: "crates/veld/src/services/availability_projection.rs"
      provides: "Availability projection/materialization service."
      contains: "Projection"
    - path: "crates/veld/src/services/calendar_explain.rs"
      provides: "Explainability service for availability and calendar interpretation."
      contains: "explain"
    - path: "crates/veld/tests/phase62_availability.rs"
      provides: "Tests for availability derivation, rebuildability, and explainability."
      contains: "Availability"
---

<objective>
Implement governed availability as a calendar read model.

Purpose: make availability native and explainable without canonizing it as an authored content object.
Output: availability types, projection/materialization service, explain service, and availability tests.
</objective>

<context>
@.planning/milestones/v0.5-core-rewrite/62-CONTEXT.md
@.planning/milestones/v0.5-core-rewrite/57-CONTEXT.md
@.planning/milestones/v0.5-core-rewrite/61-VERIFICATION.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build availability as a governed read model</name>
  <files>crates/vel-core/src/availability.rs, crates/veld/src/services/availability_projection.rs, crates/veld/src/services/calendar_explain.rs, crates/veld/tests/phase62_availability.rs</files>
  <read_first>.planning/milestones/v0.5-core-rewrite/62-CONTEXT.md
.planning/milestones/v0.5-core-rewrite/57-CONTEXT.md
.planning/milestones/v0.5-core-rewrite/61-VERIFICATION.md</read_first>
  <action>Add availability read-model types over canonical calendar state plus policy/config inputs, implement an availability projection/materialization service and explainability service, and add tests proving availability is derived, rebuildable, and not treated as authored canonical content.</action>
  <acceptance_criteria>- `rg -n "AvailabilityWindow|busy|free|tentative|policy|config" crates/vel-core/src/availability.rs` returns availability read-model concepts.
- `rg -n "Projection|rebuild|materialize|explain" crates/veld/src/services/availability_projection.rs crates/veld/src/services/calendar_explain.rs` returns projection and explainability concepts.
- Tests prove availability derives from canonical state, rebuilds without loss of authority, and remains outside canonical content storage.</acceptance_criteria>
  <verify>
    <automated>rg -n "AvailabilityWindow|busy|free|tentative|policy|config|Projection|rebuild|materialize|explain" crates/vel-core/src/availability.rs crates/veld/src/services/availability_projection.rs crates/veld/src/services/calendar_explain.rs crates/veld/tests/phase62_availability.rs</automated>
  </verify>
  <done>Phase 62 now has native, explainable availability semantics as a governed read model rather than a content-object mistake.</done>
</task>

</tasks>

<verification>
Confirm that availability is explainable, rebuildable, and still clearly a read model over canonical calendar state rather than an authored durable object.
</verification>

<success_criteria>
Phase 62 completes the native calendar domain with governed availability semantics that later adapters and workflows can consume honestly.
</success_criteria>

<output>
After completion, create `.planning/milestones/v0.5-core-rewrite/62-03-SUMMARY.md`
</output>
