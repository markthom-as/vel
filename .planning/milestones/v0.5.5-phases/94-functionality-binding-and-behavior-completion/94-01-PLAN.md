# Phase 94 Plan

## Objective

Finish the accepted behavior layer now that the backend truth seams exist.

## Implementation slices

1. Bind composer attachment/recording state to the accepted interaction model using the new boundary seams.
   Status: attachment entry is wired, queued chips render immediately, recording/transcribing state is surfaced in the composer, and explicit-intent fallback now re-submits the original assistant-entry payload with the chosen intent instead of leaving fanout selection as dead UI state.
2. Complete nudge behavior: standardized actions, expansion, truncation handling, and error routing.
   Status: expansion/truncation behavior is live, intervention-backed acknowledge/defer actions now use real mutations, unsupported actions are disabled instead of pretending, and error routing can surface through the nudge path.
3. Finish `Now` behavior that depended on durable lane truth, including title/description/tags/progress details.
   Status: lane persistence is truthful now, richer task fields are exposed through the widened contract, and the remaining `Now` work is now polish-level iconography/spacing/visual rhythm rather than missing runtime behavior.
4. Finish `Threads` behavior that depended on row metadata and accepted filter/default-selection rules.
   Status: thread row metadata is wired, fallback thread selection now updates shell state truthfully, archive uses the real conversation patch seam, and the remaining thread work is presentation polish.
5. Finish `System` navigation depth and truthful field/config exposure over persisted settings.
   Status: sticky third-level child navigation now expands around the active subsection and scroll target, and the surface now exposes real persisted `web_settings` fields inline. Broader provider/integration config breadth remains a follow-on concern, but the accepted settings seam is no longer UI-local fiction.

## Verification

- targeted web tests for changed behaviors
- focused Rust tests only when a new route/service seam changes again
- browser/manual checks for interactive correctness where the acceptance item is behavior-oriented

## Success Criteria

1. Remaining TODO items in the functionality bucket are either implemented or explicitly identified as Phase 95 polish.
2. No accepted behavior still depends on fake client-local truth where Phase 93 already exposed a real seam.
3. Phase 95 can focus on visual/browser fidelity rather than missing runtime behavior.

## Result

Phase 94 is complete. The remaining unchecked `TODO.md` work is now either:

- browser-level polish for Phase 95, or
- broader follow-on configuration breadth that is no longer required to make the accepted current UI truthful.
