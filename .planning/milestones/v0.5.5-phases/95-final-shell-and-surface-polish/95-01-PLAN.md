# Phase 95 Plan

## Objective

Finish the accepted shell and surface polish once behavior truth is in place.

## Implementation slices

1. Unify final pill, tag, chip, and badge geometry across navbar, nudges, filters, and row metadata.
   Status: in progress. Shared pill frame, dense tag radius, filter-toggle centering, floating nudge shell geometry, and navbar badge sizing/centering were tightened in the first polish slice.
2. Close navbar and composer polish, including non-`Now` context slots, docs/info affordance styling, badge temperament, and final attachment/recording presentation.
   Status: in progress. Non-`Now` context slots remain in place, the docs/info affordance now points through a real bundled repo-doc asset, and navbar badge temperament/spacing were refined; composer polish still remains.
3. Close `Now` and `Nudges` browser-fidelity issues, especially progress/header rhythm, active-task emphasis, gutter icon geometry, and title/count/icon polish.
   Status: in progress. Nudge gutter geometry and action-chip density were refined, and `Now` now de-duplicates project tags, trims task typography slightly, and reduces active-section header drift; more browser-level fidelity remains.
4. Close `Threads` browser-fidelity issues, especially active-row emphasis, participant glyph centering, archive control form, message meta alignment, and bubble tails.
   Status: not started.
5. Close `System` density/readability polish, including stats/info column treatment, header removal, action alignment, and final single-document compactness.
   Status: not started.

## Verification

- browser/manual review for each touched surface
- targeted frontend tests only as regression hints
- web build after each cohesive polish slice

## Success Criteria

1. Remaining unchecked `TODO.md` items are polish-only and are either closed in this phase or explicitly deferred before closeout.
2. The shell and all three surfaces are visually coherent without bespoke drift in shared chip/tag geometry.
3. Phase 96 can focus on browser proof and honest acceptance audit rather than additional implementation discovery.
