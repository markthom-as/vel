# Phase 96 Plan

## Objective

Generate browser evidence, run the final acceptance audit against `TODO.md`, and close or hand off the milestone honestly.

## Implementation slices

1. Create or update a browser-proof script that exercises the current `Now`, `Threads`, and `System` surfaces against local fixtures.
   Status: not started.
2. Run browser proof and store screenshots, logs, and evidence notes under the phase packet.
   Status: not started.
3. Audit every unchecked `TODO.md` item as implemented, partial, or deferred with a stated reason.
   Status: not started.
4. If the evidence supports it, close `0.5.5`; otherwise, produce explicit carry-forward planning instead of pretending the line is done.
   Status: not started.

## Verification

- direct browser proof via the repo-local Playwright harness
- evidence artifacts checked into the phase packet
- final roadmap/state updates only after the audit result is clear

## Success Criteria

1. Browser evidence exists for the accepted web surfaces and major states.
2. `TODO.md` is audited directly rather than inferred from chat memory.
3. Milestone closeout, if it happens, is justified by evidence rather than build/test status alone.
