# Phase 76 Validation

- `/system` remains a single top-level surface
- detail states are subordinate, not a new route family
- `Domain`, `Capabilities`, and `Configuration` remain visibly grouped instead of flattening into one list
- action rendering does not exceed the allow-list
