# 72-01 Plan

## Objective

Turn the post-`0.5.1` UI direction into active milestone authority docs and freeze the embodiment target.

## Work

1. Publish the operator-surface doctrine as durable architecture authority.
2. Publish the `v0.5.2` roadmap packet and active requirement buckets.
3. Activate the milestone in top-level planning state without inventing implementation details yet.
4. Freeze the exact embodiment rules for `Now`, `Threads`, `System`, performance, verification, and backend-endpoint control.
5. Lock the rule that `Now` layout/design must be defined together before implementation and may not materially change without explicit approval.

## Acceptance

- operator-surface doctrine exists as a standalone doc
- `STATE.md`, `ROADMAP.md`, `PROJECT.md`, `REQUIREMENTS.md`, and `MASTER_PLAN.md` point at `v0.5.2` as active
- no ambiguity remains about three-surface scope, web-first embodiment, or frozen backend truth
- no implementation-critical UI behavior is left to be “discovered” during execution
- `Now` layout churn is blocked until a specific approved layout/design contract exists
