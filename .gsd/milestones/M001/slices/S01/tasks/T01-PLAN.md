# T01: Readiness audit

**Slice:** S01 — **Milestone:** M001

## Description

Determine whether this repo can safely migrate from the current local `get-shit-done` install to `GSD 2` without breaking practical workflow use.

Planned work:
1. identify current repo and Codex workflow dependencies on `get-shit-done`
2. compare those dependencies against the available `GSD 2` surface
3. produce a concrete readiness verdict with cutover constraints
